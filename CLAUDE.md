# CLAUDE.md

This file is auto-loaded by Claude Code at the start of every session.
It holds pipeline conventions, tool rules, and project-specific
decisions that all builders must follow.

## Published copy needs the user's approval first

- **NO CHANGE TO `website/`, `README.md` OR THE APP STORE LISTING COPY (`appstore/LISTING.md` and what is written into App Store Connect) IS MADE WITHOUT THE USER READING THE NEW LANGUAGE AND APPROVING IT FIRST. Corrections and one-word fixes included. This applies to every agent working in this repository (user direction, 2026-09-21, reaffirmed 2026-09-22 when the App Store copy was aligned to the same register).** Show the exact before and after, as a page the user can open (a tailnet URL for the website, the README rendered, the App Store text in full), wait for an explicit yes, and only then write it. A feature build may PROPOSE at most one sentence for its tab's section and never appends, rewrites or re-sequences on its own; a version-stamp step moves only the version pill and footer. The register, the mechanics and the reasons are in `.claude/rules/docs-and-website.md`; this bullet exists so the rule is read at the start of every session rather than only when those files are touched. The same rule is restated in `AGENTS.md` for agents that do not read this file.

## Versioning

- Version is tracked in `frontend/package.json` (semver, patch increments for small features/fixes, minor for larger features)
- **Bump BOTH `frontend/package.json` AND `src-tauri/tauri.conf.json` to the same version.** `tauri.conf.json` is the source of the desktop *bundle* version (macOS `CFBundleShortVersionString`, Windows installer version, and the in-app updater's version check) and does NOT read from `package.json`. Forgetting it ships a mislabeled bundle and breaks the updater; `release.sh`'s version guard catches the macOS side, but the Windows installer is built by CI from `tauri.conf.json` at the tagged commit, so the tag must point at a commit where both are bumped. (Patch-only by default — never minor/major unless explicitly told.)
- `CHANGELOG.md` at the repo root must be updated with every version bump
- **A version bump is a FOUR-file set, and the fourth is the website: `frontend/package.json`, `src-tauri/tauri.conf.json`, `CHANGELOG.md`, and `website/index.html` (the `version-pill` visible text AND its `aria-label`, plus the `footer-version` line).** All four are asserted by one guard, `it('the website version pill and footer follow the app version')` in `frontend/src/lib/icloudKeysPublishedClaims.test.ts` -- a repo-wide release-parity check housed in a feature-named suite, which is why it is named here rather than left to be found. The website leg is stated in `.claude/rules/docs-and-website.md`, and that file is **path-gated** on `website/**` / `README.md` / `docs/**`, so a release-prep session touching only the two manifests and the changelog never loads it: that path-gating is the mechanism by which 1.0.14's prep (`e249051`) bumped three of the four, left the pill at `v1.0.13`, and shipped with the suite RED on `main` for five commits. Touching the file is not what repairs it -- `f02b063` edited `website/index.html` and still left the stale version; only the version-stamp step does. **Known weakness of the guard:** it is a `toContain` substring check over the whole file, so it goes green as soon as ONE correct version string exists anywhere -- a corrected pill with a stale footer still passes (ROADMAP).
- **Always bump the version and update the changelog when adding a feature or fix**, even if the user does not ask
- **A dev-only / toolchain change that does NOT affect the shipped bundle — a dev-dependency `npm audit fix`, a lockfile refresh, test-only tooling — does NOT require a version bump, changelog entry, tag, or release.** It produces a byte-identical app, so commit it straight to `main`; it reaches the release machine on the next `git pull`. The "always bump" rule above is about user-facing features and fixes. (Precedents in `DECISIONS.md`: the `dev-dependency-cleanup` `undici` patch, and the Node-25 release-tooling fix — both "no version bump.")
- **After pushing a version bump, run `./release.sh`** to build, notarize, and publish the macOS desktop app. This script creates the GitHub release (or uploads to an existing one), signs the updater bundle, and generates `latest.json` for in-app update detection. Credentials (Apple API key, notarization) stay local — never stored in GitHub.
- **Do not use `gh release create` directly** for desktop app releases — always use `release.sh` so the signed binary and `latest.json` are included. The in-app update check depends on `latest.json` being present in the release assets.
- **Tauri v2 macOS updater mechanism:** `downloadAndInstall` performs synchronous in-place bundle replacement — no shell script, no sleep, no `open -a`. The `.app` bundle is fully replaced on disk before the Promise resolves. After `downloadAndInstall` returns, call `relaunch()` (not `exit(0)`). `relaunch()` spawns `current_exe` (now the new binary) and exits cleanly. `exit(0)` just terminates the process with no relaunch, leaving the user with no running app.
- **Release rhythm (summary):** bump version → commit → push `main` → push the `vX.Y.Z` tag (starts Windows CI) → wait for CI green → run `./release.sh` → ship the iOS TestFlight build of the SAME version → submit that same build for App Store review. **A release goes to ALL available platforms, every time** (user direction, v0.5.78; the App Store became a standing leg at 1.0.0) — a release is not done until every leg has shipped and been submitted. **VERIFY the App Store leg against App Store Connect; never infer it from the repo (v1.0.17).** 1.0.15 and 1.0.16 each shipped a `VALID` TestFlight build and neither ever got an App Store *version record*, so the store sat on 1.0.14 for two releases — and nothing in this repo would have said so: a TestFlight build is not a submission, the changelog and tags look identical either way, and the miss was only found by querying ASC during the next ship. A ROADMAP item naming the debt was **not** enough; it was item 1 in Up Next while 1.0.16 shipped past it. **AND WRITING THIS RULE HERE WAS NOT ENOUGH EITHER: IT HAPPENED A THIRD TIME, AT 1.0.18 (v1.0.20).** Queried at the 1.0.20 ship, version records exist for 1.0.19, 1.0.17, 1.0.14, 1.0.13 and 1.0.4, while TestFlight holds VALID builds for 1.0.15.1, 1.0.16.1, 1.0.17.1, **1.0.18.1** and 1.0.19.1 -- so 1.0.18 is the third release to ship a VALID build and never receive a store version record. Nothing was lost to users (1.0.17 carried 1.0.15 and 1.0.16 to the store, 1.0.19 carried 1.0.18), and each skip was found the same way and only that way: **by querying ASC at the next ship.** Record the pattern rather than the intention -- a roadmap item and a rule in this file have each now failed to stop it, so do not add a third reminder; do the query. So at every ship, list the version records for the last few releases (`GET /v1/apps/{id}/appStoreVersions`) and confirm each one either shipped or is deliberately deferred **in writing**; a deferral is a decision with a reason, never a silence. **THE QUERY CANNOT DISTINGUISH A DELIBERATE ROLLUP FROM A SILENT SKIP, WHICH IS WHY THE WRITTEN RECORD IS LOAD-BEARING RATHER THAN CEREMONIAL.** When a release is folded into the next one -- the in-review record withdrawn, retargeted at the newer version, repointed at the newer build and resubmitted -- the older version ends with a `VALID` TestFlight build and no version record of its own, which is the same evidence signature a skip leaves. ASC offers nothing else to separate them: the withdrawn record no longer names the version it used to carry, so from the outside a rollup and an omission are identical. **This is measured, not hypothetical: at the 1.0.23 ship the iOS build agent applied the rule above correctly and flagged 1.0.20 as a probable FOURTH silent skip -- because the repo's own record gave it no way to tell.** Five rollups exist and all were deliberate user decisions at the deploy gate: 1.0.20 into 1.0.21 (2026-09-06), 1.0.22 into 1.0.23 (2026-09-07 -- record `bd0af66a` withdrawn, retargeted, repointed at build 1.0.23.1, resubmitted as `ac7619d0`), 1.0.26 into 1.0.27 (recorded in DECISIONS.md at v1.0.27), 1.0.29 into 1.0.30 (2026-09-13 -- record `930ed55a` withdrawn while WAITING_FOR_REVIEW, retargeted, repointed at build 1.0.30.1, resubmitted as `c2b858fc`), and 1.0.33 into 1.0.34 (2026-09-22 -- record `f380012a` withdrawn from submission `44f7cb18` while WAITING_FOR_REVIEW, retargeted, repointed at build 1.0.34.1, resubmitted as `4fe5686a`). **This list read "two rollups" until the 1.0.30 closeout and had never counted 1.0.26, which is the failure the list exists to prevent arriving through the list itself: when you roll one up, add it HERE in the same ship.** The three genuine skips remain 1.0.15, 1.0.16 and 1.0.18. **1.0.25 is a fifth case and a THIRD kind -- deliberately DEFERRED, neither rolled up nor skipped:** at its deploy gate 1.0.24 was `IN_REVIEW` on record `6900bf93` and the user chose to leave that review undisturbed, so 1.0.25 ended with VALID build 1.0.25.1 and no record of its own, and the deferral was resolved at 1.0.26, whose review notes carry the 1.0.25 fixes. **It was missing from this list until the 2026-09-15 apple-platform-readiness closeout, which is the SECOND time this list has been found incomplete** -- a live ASC query that run showed version records jumping 1.0.24 straight to 1.0.27 while TestFlight held a VALID 1.0.25.1 build, which is the exact evidence signature a skip leaves, and only DECISIONS.md separated them. This list, not DECISIONS.md, is what a ship reads at a glance, so a version that ends without a record of its own is added HERE in the same ship whatever the reason -- rollup, deferral, or a build Apple refused. So a rollup is written down at the ship exactly as a deferral is, naming both versions and the resubmitted record id; **a version with no record and no sentence is a skip**, because that is the only reading the query supports. When releases have accumulated, the notes cover all of them. **TWO ENTRIES FROM THE 1.0.33 SHIP (2026-09-22), both here so the list stays complete.** **1.0.32 is RESOLVED**: it holds its own record `34a97cc3-c028-4894-907b-72cd7a988bab`, READY_FOR_SALE on build 1.0.32.1, submitted at `d3c71d1` after its own closeout had already written that leg down as deliberately outstanding. That disposition was true when written and went stale when the submission happened, which is the decay this rule exists for -- and it is also the rule WORKING, because only the live query at the next ship found it, exactly as the instruction above says it would. **1.0.33 was submitted and then ROLLED UP into 1.0.34 the same day (listed above), so it holds VALID build 1.0.33.1 (delivery `b5b71100`) and no version record of its own by rollup, not by skip:** its record `f380012a` was created on 1.0.33.1 after the device check and submitted as `44f7cb18`, and was still `WAITING_FOR_REVIEW` when 1.0.34 shipped. The reconciliation at the 1.0.34 ship found no unaccounted gap between 1.0.13 and 1.0.34. **TWO ENTRIES FROM THE 1.0.35 SHIP (2026-09-23).** **1.0.34 is RESOLVED**: its own record `f380012a` is READY_FOR_SALE on build 1.0.34.1 (submission `4fe5686a` COMPLETE). **1.0.35 was DEFERRED, then SUBMITTED on its own record the same day:** after the user's real-device install of build 1.0.35.1 (delivery `88d5d94d`) and with the four recaptured store screenshots that had shown the species picker's pre-fix bottom edge (`4f7fe39`), record `cfa11d7b` was created on build 1.0.35.1 and submitted as `36c333a9` (`WAITING_FOR_REVIEW`). The full runbook lives in the `snowraven-release` skill (below).
- **A RELEASED VERSION STRING'S PRE-RELEASE TRAIN IS CLOSED, SO THERE IS NO TESTFLIGHT-ONLY PATH AGAINST IT (2026-09-15, `b472ae8`, unreleased).** Once a version record reaches `READY_FOR_SALE`, Apple closes that version's *train* to **all** new builds -- TestFlight included, not only store submissions. An iOS-only fix therefore cannot ride a fresh build number against the shipped version: `altool --validate-app` refuses it with **90186** (`Invalid Pre-Release Train. The train version '1.0.30' is closed for new build submissions`) and **90062**. The apple-platform-readiness deploy gate reasoned that a released version merely could not take a new *store* build and concluded a TestFlight-only build would still go through; that is what made an authorized route look available when it did not exist. **So for an iOS-only fix the routes are exactly two:** a full version bump, which pulls the entire all-platforms rhythm above with it, or letting the fix ride whatever ships next. Validation is what made this cost a minute instead of a wasted upload, which is why `--validate-app` before `--upload-app` stays unconditional. The narrow shortcut is closed by this repo's own guard: bumping only the iOS bundle past `frontend/package.json` trips `iosSceneManifest.test.ts` -> `it('the stamped iOS version never LEADS the app version')`.
- **A GREEN SUITE, TWO GREEN CI JOBS AND A SIMULATOR SCREENSHOT ARE NOT EVIDENCE THAT AN iOS BUILD LAUNCHES, SO A DEVICE INSTALL PRECEDES THE APP STORE SUBMISSION RATHER THAN FOLLOWING IT (v1.0.31).** The 1.0.31 iOS build crashed on launch, every time, on the exact commit where 6,269 frontend tests, 636 backend tests, typecheck, lint and both CI jobs were green -- and it was submitted for review having never run on hardware. The user found it. **The class is RELEASE-ONLY and therefore invisible to every debug and simulator check**: tao 0.35.3's `configuration_for_connecting_scene_session` returned `Retained::as_ptr(&config)` on a local dropped at function exit, and the release profile (`lto = true`, `opt-level = "s"`) is what lets the optimizer drop it immediately after the borrow, which upstream states is what makes the crash reliable. The iOS 27.0 simulator screenshots that settled the UIScene work were an honest check that could not have caught this, so **a simulator or debug screenshot does not clear a release-build launch path** -- and neither does `altool --validate-app`, which checks icons and entitlements, not whether the app runs. So: install the exact uploaded build on a real device, open it, and only then submit. The fix is vendored at `src-tauri/vendor/tao` (tauri-apps/tao#1245, one line) with its removal condition in `VENDORED.md`; the post-mortem is in DECISIONS.md.
- **Cross-platform Rust deps must live in `[dependencies]`, not `[target.'cfg(target_os = "macos")'.dependencies]`.** `tzf-rs` (used by the cross-platform `get_timezone` command) was mis-scoped there and broke the Windows build with "unresolved import." The macOS-only block is only for genuinely macOS-only crates (objc2*, the CoreLocation stack). A crate real on both Apple platforms goes in the `cfg(any(target_os = "macos", target_os = "ios"))` block, and an Apple-vendor crate needed only on iOS (`objc2-ui-kit`) in its own `cfg(target_os = "ios")` block, never the android|ios mobile block: the cfg says where a crate is real, not which build happens to pull it (v1.0.11).
- **iOS app icons must be fully OPAQUE — NO alpha channel — in BOTH `src-tauri/icons/ios/` AND the committed `src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/`** (App Store Connect rejects the upload otherwise, altool error 90717; desktop icons MAY keep alpha). Flattening detail and precedent are in the release skill.
- **VERIFY A LAUNCH-LIFECYCLE OR SCENE-MANIFEST CHANGE WITH A SCREENSHOT, NEVER A PROCESS CHECK (2026-09-15, `b472ae8`).** The iOS 27 UIScene adoption was settled by launching three builds rather than by reading code, and the middle one is the trap: with the manifest present but `UIApplicationSupportsMultipleScenes` **false**, tao 0.35.3's `multiple_scenes_enabled()` sends it down the non-scene startup path while UIKit runs the app in scene mode, so the process stays **alive**, WebKit loads and *finishes* the page, the window is never attached to a scene, and the screen is **blank**. It does not crash. A liveness check, an exit code and "did it crash" all pass that build, and it was reported as working before anyone looked at the screen. `true` is the only configuration that renders -- an accepted iPadOS side effect, below -- and the manifest must name a real `TaoSceneDelegate` configuration rather than the empty `UISceneConfigurations` dict from Tauri's own docs, which traps under the iOS 27 SDK as well (tauri-apps/tauri #15719). The manifest lives in three files (`gen/apple/snowraven_iOS/Info.plist`, `gen/apple/project.yml`, and `src-tauri/Info.ios.plist`, the only one outside `gen/` and so the only one that survives a re-run of `tauri ios init`), pinned identical by `iosSceneManifest.test.ts`. **TWO CORRECTIONS FROM THE 1.0.31 CRASH (v1.0.31), because the false half was repeated in all three files plus the guard's own header.** (1) The claim that tao 0.35.3 "does NOT implement `application:configurationForConnectingSceneSession:options:`" is **wrong**: it implements that callback and registers it at `view.rs:752` whenever `multiple_scenes_enabled()` reads true -- which is exactly what this manifest makes true. The static manifest is still right, for a different reason than the one written down: tauri-apps/tauri #15719 shows the iOS 27 validator reads the STATIC manifest and ignores tao's runtime registration, so the empty `UISceneConfigurations` dict traps and the configuration must name `TaoSceneDelegate`. `iosSceneManifest.test.ts` is correct as written and must NOT be weakened. (2) **The manifest was the TRIGGER of the 1.0.31 iOS launch crash, never the fault** -- only a build with it enabled calls that tao method, and tao's implementation of it returned a freed `UISceneConfiguration` until the vendored one-line patch. A newer tao therefore retires the vendored patch, not the manifest.
- **A guard that must run in CI parses Apple plists in PURE JS, strips a config file's comments before scanning it, and asserts "never leads" rather than equality across files stamped at different times (2026-09-15, `b472ae8`).** The frontend CI job runs on `ubuntu-latest`, where `plutil` and `PlistBuddy` do not exist, so shelling out to them makes a guard macOS-only and it silently does not run where it matters; `iosSceneManifest.test.ts` parses the plists itself, fails closed on any shape it does not understand, and carries guard-the-guard rows. It strips YAML comments from `project.yml` before scanning, because that file's own comment explains the absence of the version keys and names both of them, so a raw text scan would fail a correct file. And it asserts that the ship-stamped iOS plist never *leads* the prep-bumped `frontend/package.json`, deliberately not equality: the release rhythm bumps `package.json` first and stamps the plist several commits later, so `main` legitimately sits with the plist *behind* the app version for the length of every ship, and an equality guard would turn the suite red every release -- the exact failure the four-file parity guard already caused once. Leading can only ever be a mistake.
- **For ANY release, packaging, updater-artifact, TestFlight, or App Store work, invoke the `snowraven-release` skill** (`.claude/skills/snowraven-release/SKILL.md`). It holds the complete mechanics and post-mortem guards moved out of this file: `release.sh` preflights and the pinned-Node story, universal-binary and `latest.json` rules, headless DMG styling and notarization/keychain, the Windows CI artifact-selection guard, and the full iOS build + upload recipe with its env-name mappings. Do not ship from this file alone.

## Pipeline Overview

This project uses the Weft framework. Run /new-feature to start a new feature.

- **A feature with a visible surface gets a live look at the BUILT app before the deploy gate, and what that look finds goes back to The Designer rather than shipping as a post-ship fix (v1.0.29).** The Planner had passed the Tester and the Auditor when the user's look over the tailnet preview found a drag that re-snapped after every programmatic scroll and a desktop view that wanted prominence -- two things jsdom, a stylesheet scan and a fixture cannot measure. Both changed the design, so the run re-entered Stage 4 with the approved parts not re-litigated, and the Engineer, Tester and Auditor ran again over the revised spec. The pause is cheap and the alternative is a post-ship fix of something every user would meet.

- **A DECISION NOT TO ACT ON A MEASURED DIFFERENCE IS WRITTEN DOWN AS A DECISION -- the evidence, what would reverse it, and the exact fix with its location (2026-09-15, `b472ae8`).** A verification item that ends with a real measured difference and no code looks, from the outside, exactly like a site that was never driven, which is the state that item was genuinely in one round earlier. **A silent non-action and an oversight leave the same evidence, so the written sentence is the only thing that separates them.** This repo already enforces the identical shape one layer up -- the App Store rule above, where a rollup and a skip are indistinguishable without the sentence naming both versions -- which is what makes it a pattern worth stating rather than a one-off. The shipped instance is the `.sr-combobox-list` scroll-anchoring difference: measured, deliberately not fixed, and recorded with its reversal condition and one-line fix in DECISIONS.md and ROADMAP.md.

- **This repo's Weft pipeline tracks SnowRaven only.** Never record issues, follow-ups, or status for outside projects (e.g. snowraven-mini, which has its own repo and its own Weft session) in pipeline state, handoffs, ROADMAP.md, or the record files (DECISIONS.md, PRODUCT_CONTEXT.md, CHANGELOG.md). If work here surfaces something about another project, tell the user in chat and stop there — it does not enter this repo's records.

## Conventions

### Ports
- Backend runs on port **1620** (not 8000)
- Frontend dev server runs on port **5173** (Vite default)
- Vite proxies `/weather`, `/tide`, `/nominatim`, and the other backend route prefixes to `http://localhost:1620` (see `frontend/vite.config.ts`). **When you add a backend route, add its path prefix to that proxy.** Otherwise the web-dev path hits Vite's SPA fallback (HTML) instead of the backend and the call silently fails under `npm run dev` — this bit the new `/tide/at` route in 0.5.34 (and, latently, the existing checklist tide, which is normally exercised only via the desktop app or the FastAPI-served build).

### Environment
- API keys live in `backend/.env` — never commit this file

- Required keys: `EBIRD_API_KEY`, `OPENWEATHER_API_KEY`

- OpenWeather key must be subscribed to "One Call by Call" at openweathermap.org

### Running locally
```
# Backend
cd backend && uvicorn main:app --reload --port 1620

# Frontend
cd frontend && npm run dev
```

### Running tests
```
cd backend && .venv/bin/python -m pytest tests/ -v
```
The backend interpreter is the project venv at `backend/.venv`; a bare `python` is not on PATH on the dev machine, so `python: command not found` is a PATH miss, not a backend failure (v1.0.18).

### Standing rules and lazy-loaded convention files

The detailed conventions live in `.claude/rules/*.md`. Each auto-loads when you work on files matching its `paths` frontmatter; if your change is related but starts from other files, READ the relevant file first. Never work from these summaries alone, and never weaken a rule without reading its full entry — most rules are backed by guard tests, so an off-path violation still fails the suite.

**AND THE CONVERSE IS AN OBLIGATION OF EVERY BUILD: WHEN A CHANGE ADDS, MOVES OR TWINS A FILE A RULE GOVERNS, EXTEND THAT RULE'S `paths` IN THE SAME CHANGE (v1.0.32).** A rule gated off the file it governs stays correct and silently stops loading, which is indistinguishable from the rule being absent -- the mechanism this file already records costing a stale version pill and five red commits on `main`. One bundle produced **six** extensions: `security.md` gained `backend/formatters/**`, `weatherFormatter*.ts`, `forecastSlice*.ts`, `tide*.ts`, `wallClock*.ts`, `transport.ts`, `lib/tauri/**` and `src-tauri/src/lib.rs`, and `weather-tide.md` gained `tide.ts`, `tidePlan.ts`, `tideInstant*.ts`, both `lib/tauri` weather/tide services and `backend/services/tide.py`. **Two shapes recur and both are worth checking for by name.** (1) **A twinned rule is a claim about a PAIR, and until v1.0.32 only the PYTHON half of each pair auto-loaded** -- `backend/routers/tide.py` loaded `security.md` while its desktop twin `lib/tauri/tideService.ts`, where `Date` silently rolls an impossible calendar into a real instant exactly where Python raises, loaded neither rule file. Gate the twin on the same line already drawn on the other side. (2) **A rule that names a canonical implementation must have that implementation inside its own gate**, so RELOCATING the code a rule points at is a two-part edit -- the prose *and* the `paths` list -- or the build silently removes coverage from the file it just made canonical. **The claim is mechanically checkable and is not checked:** seventeen files cited in `security.md`'s own rule bodies match no glob in its frontmatter (ROADMAP), and the durable form is a `cacheInventory.test.ts`-shaped guard that extracts path-shaped citations from rule bodies and fails on one that matches nothing.

- **UI construction** — `.claude/rules/ui.md` (colors/theming, responsive layout, accessibility, display-copy conventions). Always true: every color via `var(--sr-*)` tokens in both themes, no hardcoded hex/RGB in components; layout made responsive by lifting to classes, never inline styles; WCAG 2.1 AA holds at 320px and 200% in-app text scale; the in-app brand mark is the shared `RavenGlyph` component, never a lucide bird or a re-inlined copy of its path; **every app-owned button and href link renders through the canonical native `Button` or `Link` primitive, which supplies the literal `tabIndex={0}` default** -- WebKit's default tab mode, which the shipped Mac, iPhone and iPad apps run, skips both entirely without it; raw intrinsic controls stay inside those primitive implementations, ordinary call sites inherit the default, intentional non-default semantics stay explicit in the authoritative counted roster, and `lib/tabOrderCoverage.test.ts` asserts the whole contract over every shipped `.tsx` on every build (v1.0.16, ownership moved in v1.0.24); and **a DOM identifier -- an `id`, any ARIA IDREF, anything a selector must later find -- is keyed on an INDEX, never on user file content (v1.0.21)**, because an id built from a data value can carry whitespace, which cannot resolve as an IDREF and silently switches off the very announcement it exists for.
- **Maps** — `.claude/rules/maps.md` (MapLibre layers, overlays, sprites, popups, FABs, tile providers). Always true: all maps render through the shared `<SnowMap>`; maplibre and the county geometry stay OFF the entry chunk (`entryChunk.test.ts` guards it); tile-provider changes are a `PRIVACY_POLICY.md` change.
- **Bird names** — `.claude/rules/bird-names.md`. Always true: every user-facing bird name renders through `<BirdName>`; countability goes through `isNonCountableForm`; ML catalog links build on `ML_CATALOG_BASE`.
- **Testing and verification methodology** — `.claude/rules/testing.md`. Read before writing or judging any frontend test, CSS fix, performance claim, or guard test. Always true: `vitest` + `eslint` are NOT the pre-push gate — run `npm run build` (or at minimum `npm run typecheck`) before pushing; and **never run `npm audit --offline`**, which prints "found 0 vulnerabilities" without consulting the registry (v1.0.16).
  - **REPLACING A PREDICATE OWES ITS SYMMETRIC DIFFERENCE IN BOTH DIRECTIONS, AND THE SECOND DIRECTION IS WHERE THE DEFECT LIVES (v1.0.32).** `.claude/rules/testing.md` already states this for selectors and predicates in the abstract; what one five-build bundle added is that a one-directional derivation is not a partial answer, it is **a reliable defect generator**. Four separate QA and security rounds each caught a fix that had introduced a NEW instance of the very bug it was closing, and every one was the unargued half: a truthiness test swapped to `is not None` was argued for `{}`/`[]` and not for `0`/`false`/`""`, and opened a fresh three-shape divergence in the function it had just rewritten; a validator moved to a fan-in point was argued for the fields it reads and not the fields it newly validates, turning one bad hour out of 48 into total refusal of the checklist copy block. The artifact is a table -- **one row per predicate, one column per direction** (`pipeline/weather-at-malformed-parity/decisions.md` §12) -- and it costs minutes. **Fill each row from a MEASUREMENT, not from what the change was reaching for**, and mutate the old predicate back to fill it in; that table got one row backwards precisely because its fix was taken as a rider. **A FIXTURE CAN ONLY EVER REPORT ON ITS OWN ROWS, so diff the two predicates over a GENERATED CORPUS:** a 32-row roster said "nothing moved", which was true of those 32 strings and false of the predicate -- 80,504 generated strings, run in seconds, found 67 newly-refused shapes in the reference guard, from nothing cleverer than every single-position edit of one conforming string over a wide alphabet. **Where one half of a twin is a library call, enumerate what that library accepts BEYOND the shape you had in mind** (`strptime` takes whitespace runs, Unicode digits and one-digit fields; `Number()` and `split(' ')` take none of them), and remember that **no truthiness spelling is a twin of the other's** -- presence is `is not None` / `!= null` on both sides, validity belongs to the shared validator. **THE SEAM COROLLARY, measured twice in one bundle: when a fix adds a call to a seam, write the row for the seam's own FAILURE RETURN first** -- `""`, `None`, an unknown name -- **and if the twin already carries a default for that case, that default IS the contract** and its absence on the other side is the divergence, whether or not any existing row supplies the value that exposes it. Both times the fixture varied the axis the build was ABOUT and supplied a valid value on the other axis in every row, so it was structurally incapable of seeing the new defect. A seam has **two** failure modes and a fixture that varies the return value has covered one: put a `mockRejectedValue` beside the `mockResolvedValue` rows, and expect them to want **different answers** -- an unusable return means the lookup ran and found nothing, which a default can stand in for; no answer means you do not know, which a default silently fabricates.
  - **AN AGREEING WRONG NUMBER IS INVISIBLE TO CROSS-TRANSPORT PARITY CHECKING BY CONSTRUCTION, so the evidence must be against GROUND TRUTH rather than against the other runtime (v1.0.32).** Three instances in one bundle, and no parity fixture could have found any of them: `temp: true` rendered `Temperature: 1°F` on both runtimes, because `round(True)` is `1` in Python and `Math.round(true)` is `1` in JavaScript (which is why `is_finite_figure` / `isFiniteFigure` exclude `bool` explicitly rather than resting on `isinstance(v, int)` / `typeof v === 'number'` -- do not tidy that away); `Date.UTC` maps years 0-99 onto 1900-1999, so one `dt` resolved to 1901 on desktop and year 1 on the backend and both then answered `out-of-range`, agreeing; and an unplaceable NOAA timestamp anchored at the 1970 epoch produced the same wrong water level on both sides for 20 of 23 shapes, byte for byte, into text a user pastes into a permanent public eBird checklist. **A shared coercion needs its own argument, not a parity fixture.** The answer is not a hand-typed expected column either, which just encodes the model that wrote the bug. **Derive the assertion STRUCTURALLY: the output built from an input carrying the bad element must EQUAL the output the same builder produces from that input with the element REMOVED** -- each runtime derives its own expectation from its own builder, cross-transport agreement is what is left over when both are right, it needs no oracle, it cannot go stale, and it generated 39 discriminating rows per runtime from one roster. Where that is not available, check against something outside both runtimes (an independent oracle sharing no `re`, no `datetime` and no `Date` with either twin agreed on 32,597 shapes at 0 disagreements). Reach for a structural equality whenever the claim is "this input should behave like that input".
  - **FIVE GUARD-QUALITY RULES PAID FOR ACROSS THE v1.0.33 BUNDLE, all of the same class: a guard that looks like coverage and is not.** (1) **A `testTimeout` CANNOT INTERRUPT A SYNCHRONOUS LOOP** -- the timer that would fire sits behind the same blocked thread -- so a guard for a HANG walks a **capped replica** of the loop before it calls the real one, and the stated timeout stays only as the backstop for anything asynchronous. Measured, not reasoned: a first draft stating 5,000 ms ran a vitest worker at 96% CPU for minutes with nothing failing, and two independent wall-clock-bounded runs of 75 s and 35 s never finished on a mutation that was supposed to go red. A defect a suite cannot express is one the suite must be built around. (2) **A guard that scans for a code shape the codebase also names in a COMMENT strips comments for that row, and proves the strip with a guard-the-guard row** -- `singleWebviewInvariant.test.ts`'s keeper row forbids `RunEvent::` while the note directly above it says `RunEvent::SceneRequested`, so a raw scan cannot tell the mention from a handler, and a stripper silently reduced to a no-op leaves the row passing vacuously. (3) **A guard's FAILURE MESSAGE must not claim coverage its assertions lack**, or it will be trusted for the difference: that same keeper row's message names "any second window on any platform" and reaches only the scene path. Narrow the message, or add the rows that make it true, preferring the second when they are cheap. (4) **A whole-file pointer or phrase assertion is measured against HEAD before it is trusted** -- count the phrase's pre-existing occurrences per file, and scope the scan to the note wherever that count is non-zero. One of five "points at CLAUDE.md" rows shipped vacuous because its file already carried an unrelated `Per CLAUDE.md` line. CLAUDE.md already records this class for the website version pill; this is a second instance in a different guard, which makes it a pattern. (5) **Compare two declarations of one number to each other, never restate the literal** -- a second literal stays green through exactly the drift it exists to catch. **And one about where a defect ARRIVES:** a shared register can carry a declaration a feature-scoped guard's forbid-list is structurally blind to (the v1.0.33 `white-space: nowrap` regression came in on `.sr-pill` while the Breeding Codes guard forbade it only on rules selected through `.sr-ctl-row`), so when a shared register lands on a surface that already carries a measured layout repair, **pin the PROPERTY at the surface that needs it, not the selector family it happened to arrive through**, and assert the resolved CASCADE rather than a declaration's presence -- a presence check passed three of that fix's four mutations.
  - **A guard test never reads a gitignored per-run file unconditionally (v1.0.12).** `pipeline/.gitignore` keeps `handoff.md`, `how-to-see.md` and the stage run-records out of the repo, so a test that reads one passes locally and fails CI on a fresh clone (the 1.0.12 stamp commit's frontend job); read it only where it exists, or assert on a tracked file. **And judge a timing-ratio suite (`speciesUtilsMemoBound` and its siblings) only from a run with nothing else compiling on the machine:** a parallel `cargo test` build plus the backend suite pushed a 3x floor to 2.79 in an untouched file, and the same suite alone was green. **A RATIO GUARD IS STILL BOUNDED BY AN ABSOLUTE TIMEOUT NOBODY REASONED ABOUT, AND THAT IS THE HALF THAT GOES RED (v1.0.32).** `frontend/vite.config.ts` sets no `testTimeout`, so every test inherits vitest's **5,000 ms default**; `weatherStatsShared.test.ts`'s hostile-input growth check is built to consume its whole run with no short-circuit and landed at 4,696 ms on the tag-commit runner -- about 6% of headroom -- then tipped over on the next runner and left `main` RED (ROADMAP). Its authors reasoned carefully about ratio noise and never about the wall-clock budget. **So a deliberately-expensive guard states its own `testTimeout` explicitly**, which weakens nothing when the assertion is a ratio, and a timing test whose margin is single-digit percent against any limit is not yet measured.
  - **A NULL RESULT IS PER-SCROLLPORT AND PER-ENGINE, never inherited by analogy (2026-09-15, `b472ae8`).** Two call sites with an identical `scrollIntoView({ block: 'nearest' })` shape measured differently under Safari 27 scroll anchoring, because what anchoring adjusts is the **scrollport and its content mutation**, not the call. A site named in a brief is driven, or is reported as undriven -- "same shape as one I did drive" is not coverage. **And "the app has shipped under Chromium's anchoring for years" does not transfer to WebKit:** that was the whole item's load-bearing reassurance, and the one site that diverged is precisely one Chromium does not adjust at all, so long exposure to one engine is evidence about that engine only -- which matters in a repo whose Mac, iPhone and iPad apps all run WebKit. Two harness rules ride with it: a probe that must attribute a difference to a mechanism needs a **scoped-off** leg (`.sr-combobox-list { overflow-anchor: none }`), not only an everything-off leg, because the scoped leg is what names the element and hands the next person the fix; and a probe whose app persists state through a backend needs a **fresh context per reading and a rotated config order**, or leg 2 starts where leg 1 finished.
  - **An async test waits for the exact downstream observable its next assertion consumes (v1.0.25)** — the portaled child or requested control, not an always-mounted loading shell; the effect-owned announcement, not the render that schedules the effect. Do not replace the missing readiness condition with a sleep, timeout increase, or weaker assertion.
- **Media embeds** — `.claude/rules/media-embeds.md` (ML embed iframes, the embed-eligibility gate, non-destructive fallbacks).
- **Security standing checks** — `.claude/rules/security.md`. Apply to ANY change touching hrefs/links, **any scan over untrusted text -- regexes and ordinary primitives alike**, routes, outbound requests, or lookup tables keyed by external strings; a security review must read the file. The linearity rule's trigger was widened at v1.0.21 because it had been written entirely in terms of regexes, and that framing is what let a quadratic `Array.includes` past a posture that had swept eight regex sites. **A NUMERIC-SPAN CLAMP DECLARES ITS ANCHOR'S RANGE TOO, because a loop terminates on the MAGNITUDE of its variable and not only on the width of its range (v1.0.33).** `t += step` stops advancing once `step < ulp(t) / 2`, so any bound written `end - start <= MAX` owes a companion sentence about where `start` may be: v1.0.33 clamped the Planner's window span and still had to add `PLAN_TS_ABS_MAX` to bound the loop ANCHOR, without which both sun loops hang inside the range the new clamp admits. **And such a constant carries its UNIT CONVERSION rather than a borrowed figure** -- the brief specified 8.64e15 and the shipped value is 8.64e12, because the ECMAScript Date range is +/- 8.64e15 MILLISECONDS and this document's instants are epoch SECONDS; the unconverted figure was not harmless headroom, it admitted a band where a second, different loop hangs. Relatedly, a hostile-input fixture that varies one axis and holds the other at a realistic value is structurally incapable of seeing a defect on the second, which is `.claude/rules/testing.md`'s seam corollary in a new domain: when a build declares a bound, sweep one level DOWN the call graph for iteration counts driven by something other than the quantity just bounded. **A build that ADDS a scan over user file content DECLARES it in the schema up front, with its linearity argument attached (v1.0.23)** -- `schema.md` §7 naming `hasAnyWeatherBlock` as an exception to its own PRD's "no new scan" claim is what turned that review into a verification rather than a hunt, and the failure the widened trigger exists to catch is not a scan that fails review, it is a scan nobody thought to look at.
- **Weather/tide comment blocks** — `.claude/rules/weather-tide.md` (`stripWeatherTideBlocks` contract, byte-golden moon-phase parity across both runtimes).
- **Backend conventions** — `.claude/rules/backend.md` (datadir single-sourcing, the import-time env gotcha, exact dependency pins, the shared pooled http client).
- **Docs and website** — `.claude/rules/docs-and-website.md`. Always true: `docs/HELP.md` is the single source of truth for in-app help; every feature or behavior change updates `docs/HELP.md`, `README.md`, and `website/` in the SAME change; `PRIVACY_POLICY.md` and `ACCESSIBILITY.md` are published statements that must stay true; no em dashes (U+2014) in user-facing copy or the published prose surfaces; **a prose repair is swept at PARAGRAPH scope, never sentence scope**, and a published claim states the PROPERTY, never a count (v1.0.16 -- seven paragraph-scope misses and six QA rounds on one file). **THAT RULE EXTENDS TO THE PIPELINE RECORDS, BECAUSE THEY ARE PROMOTED INTO `DECISIONS.md` AND THEN BELIEVED (v1.0.32).** One bundle produced seven count slips across four builds -- a heading saying "two twin divergences" over a body, a fixture and every downstream document saying three; "eight added attributes" where the diff adds twelve; "28 tide disagreements"; a red-first of 3 that was 2; "both in SOURCE" where there were four -- and none was a misunderstanding: every one had been true when written and went stale when a row moved. **The durable fix is not more care at the keyboard, it is preferring a form that does not decay:** a per-guard mutation figure over a combined red-first count, a property over a total, a denominator carried beside any count that is kept. Where counts are genuinely wanted, **re-derive every one of them after the last row lands** rather than patching the cell someone happened to catch; **a claim that reached the published prose FROM the source is swept starting at the SOURCE** -- `frontend/src/` and `.claude/rules/` are in scope, not just `docs/`, `README.md` and `website/` (v1.0.17: a struck design premise survived in a doc comment in the very file the fix was about, and a dead capture-width justification survived in the rule file that promoted it); **never name a user-facing surface from a component name**, `TAB_LABELS` in `lib/tabLayout.ts` is authoritative (`life-list` is **Multimedia**). **AND NO CHANGE TO `website/` OR `README.md` IS WRITTEN WITHOUT THE USER READING THE COPY AND APPROVING IT FIRST -- corrections and one-word fixes included (user direction, 2026-09-21).** Show before/after and publish both as tailnet pages for review, the README rendered; structure first, then copy. This OVERRIDES the same-change rule above, which otherwise reads as licence for a feature build to append to these two surfaces on its own: a feature build may PROPOSE at most one sentence in its tab's section, approved before it lands. **And the register is fixed:** one or two sentences per tab saying what that part does and what a birder gets -- no operating detail, no reassurance, no "you already have", no offline mention, privacy as a posture with the policy linked, "weather" lowercase in prose. **The mechanism is why both rules exist:** the per-feature append with nothing ever re-reading the document whole took the README from a user-directed 891 words at v1.0.10 to 2,043 at `d3c71d1`, and two trimming passes (46% and 36% off) failed to restore the register because trimming an appended document preserves its shape. So a new feature's README/website copy is a PROPOSAL, not an append (v1.0.32+, `website-readme-copy-pass`).

### Production build
```
./start.sh
```
Builds frontend into `frontend/dist/`, starts uvicorn on port 1620.
FastAPI serves the built frontend as static files automatically.

### Desktop app seams

Permanent architectural seams route all platform-sensitive operations. Use them in new code — do not bypass them.

- **Transport:** `transport.get()` / `transport.post()` from `frontend/src/lib/transport.ts` — for all outbound HTTP calls. In Tauri mode, `TauriTransport` routes each API path directly to a TypeScript service (weather, map, taxonomy, nominatim, stats, version). In web/Pi mode, `WebTransport` delegates to the Python backend.
- **A failed file-status lookup is UNKNOWN, never EMPTY (v1.0.25).** Both Web and Tauri storage seams preserve the difference between an absent metadata record and an unreadable or non-OK status response; every consumer routes the latter to its existing load-error state rather than setup guidance that claims no file is saved.
- **Caching layers — pick by lifetime.** `CACHED_GET_PATHS` in `transport.ts` is the short-TTL (~90s) repeat-call cache; `replayStore` is the live-first offline replay of a last-loaded result. A **long-TTL persistent network cache** ("hold live data for days") follows `lib/countyCompletenessCache.ts` instead: a storage-seam document with an in-memory mirror, TTL-gated reads, per-entry shape validation on load (malformed entries dropped, never thrown on), a strict 250-entry cap plus a `JSON.stringify(data).length` payload-code-unit budget with `order[]` FIFO eviction, an in-flight dedupe Map, errors never cached, and offline stale-reads served for display. Keys, metadata, ordering and the document envelope are outside that budget, and one sole oversized newest payload is allowed; do not call it a byte cap. `replayStore` has the same semantics with a 300-entry cap, **with two stated exceptions worth knowing before copying it**: its `ensureLoaded` is the one durable store with no per-entry validation on load, and its adjacent `bytes` guard checks `Number.isFinite` only, so it admits negatives (both bounded by the 300-entry cap, both on the roadmap). Since v1.0.14 it also owns an ordered writer (below). `storage.ts` is the persistence seam, not a cache owner; `persistedStyle.ts` has a finite shipped caller graph but its string-accepting API is not structurally bounded. A path served by a durable cache stays OUT of `CACHED_GET_PATHS` — one caching layer per call. `lib/hotspotActivityCache.ts` (6h TTL, FIFO at 2,000) is the second instance of the `countyCompletenessCache` pattern and adds one rule worth copying (v0.5.92): a durable cache computes its DERIVED fields at ONE chokepoint — the cache module's own `dedupedFetch` — so every persisted entry is fixed-shape by a single write path rather than by every caller's discipline. **Sharpened at the third instance, `lib/checklistProjectsCache.ts` (v1.0.5): that chokepoint must also VALIDATE, not merely compute.** Run the candidate entry through the store's own per-entry validator — the same predicate the load path already uses — before merging, so "no out-of-bounds value can exist in this document" is a property of the cache MODULE rather than of whichever producer happens to sit upstream. It matters most where the decision to ship without a payload budget is justified by those very bounds: the projects store shipped with `isValidEntry` present in the module and called on load only, while its write path built entries from a defensive re-read whose contract was strictly weaker than the transport seam's normalizer (no pattern or length bound on the id string, no upper bound or length cap on the id array). Bounds enforced at the seam and again on load are two places the next store's author may simply not wire up; the validator at the write chokepoint is the one that travels with the pattern.
- **A BUNDLED BUILD-TIME ASSET AND A PERSISTED RUNTIME CACHE ARE DIFFERENT TRUST BOUNDARIES, and the per-entry validation rule above applies only to the second (v0.5.89).** The distinguishing question is whether an attacker can change the document without already being able to change the code that reads it. For a Vite-inlined JSON import the answer is no — malformed JSON fails the build, not the user's session — so the correct defenses are **build-time** (a generator that fails closed on implausible output) and **CI-time** (an equivalence test against an independent re-derivation, plus a size bound on the entry chunk), not runtime validation. `ebird-countability.json` is the reference: `build-countability.mjs` hard-fails on short lists, overlapping lists, and a name collision in the inversion; `countableForms.test.ts` asserts it member by member and pins its `version` to the taxonomy snapshot; `entryChunk.test.ts` bounds it. Worth stating because the cache rule is written forcefully enough to be applied where it costs bytes on first paint and buys nothing. **If such an asset ever becomes runtime-loaded, this note inverts and the cache rule applies in full.**
- **A durable cache may hold TWO stores with OPPOSITE retention policies, and the choice follows what an eviction COSTS (v0.5.87).** The escapee document is the reference: its checklist ledger is **FIFO**, because evicting an entry costs one redundant request and loses no answer; its species index uses **admission control** (fill to the limit, then stop admitting, never evict), because evicting there would destroy a paid-for network answer and, at capacity+1, would do so on every pass forever. This is the v0.5.86 rule applied rather than contradicted — capacity+1 is a measurement rule, not a universal policy. Gate admission on the CONTAINER's own size, never a separate counter, and prove it: a test re-merges one species fifty times to show admission capacity is not silently consumed, and both stores are measured at capacity+1 asserting **work done**, not elapsed time.
- **A denormalized, PUBLISHED classification field is a legitimate member of a persistent cache document when a passive reader structurally cannot re-derive it — provided the raw evidence stays the source of truth and the reader CONFIRMS rather than trusts it (v0.5.87).** `ProvenanceSnapshot.excludedNames` is the reference: the Calendar holds no name-to-code join and may not fetch one, so it cannot classify by code; Statistics publishes the escapee-only names, the raw tokens remain in the document, and Statistics always re-derives from them. The passive reader then **confirms** the published list against the persisted checklist ledger using its own observations, so a newly loaded export that adds a carrier re-opens that species offline exactly as it does on Statistics. **Without the confirmation step this is a stale-cache trap**; with it, the offline reader tracks a new export correctly. Stated limit: for a species the passive reader cannot re-open, a published name is only as current as the last Statistics visit.
- **A durable store keyed on USER-FILE CONTENT registers with the same teardown that deletes the file, and the rule that travels is the REGISTRY, not the discipline (v1.0.14).** Four stores each missed this because there were three clear paths, so wiring a new store meant remembering all three. One table and one entry point (`lib/clearDerived.ts` / `purgeDerivedOnClear`), called from `Settings.tsx handleDeleteFile` and both `icloudSync.ts` clear paths, with `cacheInventory.test.ts` pairing each store's exported purge to a row that calls it -- and `clearDerived.ts` carries no static imports at all, reaching every store through `import()`, because `Settings.tsx` is on App.tsx's static graph and `countyCompletenessCache.ts` must stay off the entry chunk. Five rules ride with it. **The purge is a PRODUCTION export, never the `_reset*ForTests` seam** -- those detach the in-memory mirror and leave the document on disk, so shipping one as the clear path looks correct in a test and clears nothing. **A store is registered on where its KEYS come from, not its VALUES**: `county-completeness-v1` holds only eBird's public payloads and still belongs, because a key exists only for a county the user has birded (accepted cost: those lookups re-fetch once after a clear and re-upload); by the same rule `hotspot-activity-v1` is deliberately OUT, and a future store's row is argued on its key set. **Purge on CLEAR only, never on REPLACE** -- the teardown is a dependency separate from the controller's `invalidate`, which the synced arrival also uses, so the pull path physically cannot reach it and `PRIVACY_POLICY.md`'s "asks only about checklists that have not been answered yet" stays true. **The purge must SUPERSEDE the store's in-flight work, not merely empty it**: cancel the debounced flush timer, stop a parked `ensureLoaded` from adopting the document it just read, and refuse a write from work that began before the purge -- a purge missing any one un-does itself a few hundred milliseconds later. **And that third guard's capture belongs at the FETCH CHOKEPOINT, before the request.** Identity (`store !== _store`) suffices only where the writer captured its store before the await; a PUBLIC entry point that calls `ensureLoaded()` for itself finds the fresh post-purge mirror perfectly valid and writes the deleted export's data into it, so it needs a monotone purge generation -- and a generation read inside the write path is entered only after the answer lands, by which time the counter has moved, so it is inert. Where the chokepoint is inside the store (`dedupedFetchChecklist`) the capture is internal; where it is outside (`transport.getReplayable`) the store exports `purgeGeneration()` and the chokepoint hands it back as a REQUIRED argument. **A best-effort teardown returns what it could not do** -- keep the `allSettled` loop so one store's failure does not abandon the rest, but resolve with the failures, and name what happened (not "delete failed" when the file is gone, not "try again" when the row is now empty); a background path with no user waiting may decline to surface it as a stated decision at the call site, never as a discarded value.
- **Nominatim has one bounded retention and provider-etiquette contract on both transports (v0.5.86).** Tauri and FastAPI each retain at most 4,096 rounded-coordinate county results with fill-and-stop admission: overflow calls still return their result but do not evict the admitted working set. On desktop, forward search and reverse county lookup share one request-start queue enforcing at least one second between starts, and concurrent reverse calls for the same rounded key share one in-flight Promise. Keep the JS/Python rounded-key semantics in parity, including negative half-step cases.
- **Outbound eBird fetching at volume follows ONE pacing contract on both transports (v0.5.92): spaced request starts + one key-global Retry-After-honoring cooldown + bounded per-item retries — the eBird analogue of the Nominatim queue, at eBird scale rather than Nominatim's mandated 1 s.** The mechanics are pure in `lib/rateLimit.ts`: 150 ms global start spacing (the concurrency pool stays a latency-absorbing ceiling; spacing is the governor); a 429 opens ONE shared cooldown gating every request start (the KEY is over the limit, not a slot), honoring a seconds-form `Retry-After` capped at 60 s, else bounded exponential 2 s → 30 s with jitter; a burst of simultaneous 429s counts as one wave, a post-cooldown success resets the ladder, and the cooldown deliberately survives a pass restart. Bounded retries per item (2), then the honest unanswered state with Retry; a 429 is NEVER cached. The upstream `Retry-After` is parsed, bounded, and re-serialized identically on both transports (`rateLimit.ts` ↔ `_parse_retry_after_seconds` in `backend/routers/map.py`) — never reflected raw — and both transports surface a 429 AS a 429 on EVERY eBird-backed `/map/*` route (one shared mapper per transport: `_raise_ebird_http_error` ↔ `throwEbirdHttpError`; single-sourcing prevents drift, so each route/function still keeps its own 429 test). Two test rules ride with it: anchor timing-policy measurements on CLIENT observation of request starts with a settled main thread, never network-side fulfill timestamps; and `rateLimit.ts` deliberately rides the entry chunk (dependency-free by extraction, policed by `entryChunk.test.ts`). **Scope since v0.5.93: every eBird-backed Map Explorer lookup is governed.** The cooldown/spacing STATE lives in the shared module-scoped gate `lib/ebirdGate.ts` (also entry-chunk-safe); the transport chokepoint routes `/map/hotspots`, `/map/recent-obs`, `/map/hotspot-region`, and `/map/county-species` through `gatedEbirdCall` (serialized spaced starts + cooldown wait-out + the same bounded retries), wired BELOW `CACHED_GET_PATHS` so a cache hit never waits. **One enforcement point per request:** `/map/hotspot-activity` is deliberately NOT transport-gated — the activity controller enforces the identical contract for it over the SAME shared state, so a 429 anywhere slows everywhere. A future eBird-backed lookup joins `EBIRD_GATED_PATHS` (transport.ts) or owns its enforcement over the shared state — never neither, never both. Accepted, stated cost: a transport-cache miss that an inner cache would serve (backend recent-obs single-flight; desktop raw-fetch dedupe) still waits for its start slot, bounded at the spacing floor. Still outside the gate by scope: the non-map eBird families (weather/tide checklist lookups, taxonomy, escapee provenance). **A pass-scale pacing layer rides ON TOP of the gate, in the pass's own controller — never inside gate policy (v1.0.8).** A sweep that must escalate across 429 waves owns that policy itself (the projects sweep: `sweepSpacingMs` in `rateLimit.ts` + the pause in `useChecklistProjects`); the gate contributes observation-only MONOTONIC fields (`waveCount` in `ebirdGate.ts` — never reset by a success, never consulted by gate policy) that a pass reads by DIFFERENCING over its own window, so single-shot lookups never inherit a sweep's escalation or pause. A layered schedule sleeps its FULL widened interval between items: the gate's floor elapses during the sleep, so the two never add, and "widened minus the floor" quietly under-delivers. Slower than the floor is always contract-compliant, so the layer needs no gate-policy change to exist.
- **Storage:** `storage.getApiKey()` / `storage.getSetting()` / etc. from `frontend/src/lib/storage.ts` — for all key, setting, and file access. Do not call `/settings/*` endpoints directly from new components. **A web/Pi stored-file read owns one inactivity watchdog across response headers and body bytes (v1.0.28):** headers and non-empty chunks are progress; zero-byte chunks are not. Race the deadline explicitly before abort/cancel cleanup so even a transport that ignores `AbortSignal` settles, and ensure any cache sharing that promise releases it on failure so retry does not require a restart.
- **Data-file change signal:** `lib/filesChanged.ts` + `useFilesEpoch()` (v1.0.11). **EVERY path that saves, replaces or removes a stored data file bumps the epoch through the seam, with no exception for the handler that did it** -- v1.0.14 dropped the earlier carve-out for the Settings tab's own handlers, whose reasoning ("the tab that did it already knows") is wrong in this app specifically: tabs are hidden with `display:none` rather than unmounted, so the other seven stay mounted with loaders keyed on the epoch, and that carve-out is exactly what left `Settings.tsx handleDeleteFile` as the one mutation path that never fired the signal while both synced paths did. A tab that loads a stored file puts the epoch in its load effect's deps rather than loading on mount only, so the new or removed file reaches it without a relaunch. A new tab that reads a stored file subscribes the same way. **Second instance, `lib/keysChanged.ts` + `useKeysEpoch()` for the api-keys document (v1.0.12):** the same shape is the house signal for any stored document another path can change under a mounted reader. App derives any prop-threaded version (`keysVersion`) from the epoch rather than counting saves itself, and a reader that checked the document on mount only (`BirdingStats`' `hasEbirdKey`) puts the epoch in its effect deps; a third stored document that gains an off-tab writer gets its own epoch module of this shape, entry-safe.
- **Promise boundaries: in an async function, `try { return v } catch` catches NEITHER a rejection of `v` NOR a throw from its `then` getter, so any "this never rejects" claim over a dependency that could return a promise needs `return await` (v1.0.16).** An async function's `return v` performs promise resolution of `v` **after** the try block has exited, so a thenable escapes through **two** doors on one enclosed-looking statement: the `then` call, and the `Get(v, "then")` getter lookup performed during resolution. Both were measured rejecting on the shipped shape of `lib/weatherBacklogLoad.ts` and fulfilling under `return await`; the cost on a synchronous array is one microtask tick. **A synchronous-today return type is not protection** -- the change that breaks the claim is a refactor nobody will connect to it, and this repo has form for exactly that move (the observations parse went off-thread for the same class of reason). On the day the dependency goes async, the `try` silently stops catching, a call site relying on the claim (here a `.then` with no `.catch`, with no global handler anywhere in `src`) becomes a live defect that parks the surface on its spinner for the session, and nothing goes red. **`return await` closes rejection; it does NOT close non-settlement** -- a thenable that never settles leaves the promise pending forever, no `try` closes that anywhere, and the answer is a timeout owned by whoever introduces a dependency that can hang. **Name that boundary at the definition site**: a totality claim with an unnamed exception is worse than an honest partial one, and "there are no stated exceptions" was wrong by two shapes before it was measured. `@typescript-eslint/return-await` cannot help -- `frontend/eslint.config.js` sets no `project` / `projectService`, so no type-aware rule runs at all.

- **A worker may HOLD its input rather than receive it per request, and the settle contract then separates TEARDOWN from SETTLE (v1.0.20).** `createStatsSession` in `lib/statsOffThread.ts` is the third instance of the rule above and the first that is not one-shot. **Take the trade only on measurement.** Re-posting the observations per request costs a `structuredClone` round trip of 34.6 ms, ~17 ms of it unbreakable main-thread serialization, to move 47.9 ms of compute off -- a third of the win handed back, scaling with the device exactly as the compute does, so it is worth least on the phones and the Pi such a change exists for. Holding the copy makes every later request 0.003 ms out and ~4 ms back. **The price is a second copy in worker heap (19.2 MB measured, against 19.3 MB on the main thread) -- larger than the 13.2 MB of raw CSV `observationsCache` was already made to stop retaining -- so it is BOUNDED, never merely accepted:** `STATS_WORKER_IDLE_MS` (30 s) tears the worker down when nothing is using it and the next request respawns and re-pays the hand-over, which is a smaller win after a pause and never a loss. The bound exists because this app never unmounts a tab (`display:none`), so one visit would otherwise hold the copy for the session. Four things change at this shape. A worker outlives a request, so **each request owns an idempotent settle while the worker's teardown is separate** (fatal paths, idle timer, `dispose`), and **termination assertions become two-sided: zero on the happy path, exactly one per fatal path.** A `{ ok: false }` reply **rejects the request without killing the worker**, since a compute that threw says nothing about the worker's health and the alternative discards a 19 MB hand-over over one answer. **The PAYLOAD is validated, not just the timing** -- a reply whose id matched but whose bundle was absent resolved `undefined`, and a falsy render gate then parks the surface on its spinner permanently, which is the settle contract's own failure shape one layer up; validate behind a `Record<keyof T, true>` field table so a new field is a build error, test `undefined`/`null` rather than truthiness (a `!b[field]` check rejects a legitimately-zero empty bundle and causes the exact hang), and iterate the TABLE's keys so the index stays allowlist-driven. **And a budget re-keyed onto a different unit from its precedent needs an absolute ceiling:** the 50 MB upload cap bounds a bytes-keyed budget for free and does not bound a rows-keyed one (at two-byte rows, 50 MB is ~25M rows and ~10 hours), so `STATS_BUDGET_CEILING_MS` is 240 s -- chosen because the parse budget's effective ceiling at the same cap is 230 s, which makes it an invariant (anything that survived its parse has at least as long to compute) rather than a preference. **When two budgets guard successive stages of one pipeline, state their relationship, not just their values.** Forgery was cleared in one line only because both ends use the per-worker `onmessage`; a worker reaching for `addEventListener('message')` looks identical at the call site and is a genuine hole, so name the property in the module doc.

- **A DERIVATION THAT JOINS THE WORKER BUNDLE EXPRESSES ABSENCE INSIDE ITS OBJECT, NEVER AS A `null` FIELD (v1.0.22).** This is the other half of the payload-validation rule above, owed by the member rather than by the validator, and the failure it prevents is silent and permanent. `isStatsBundle` rejects any `BUNDLE_FIELDS` key that is `undefined` **or** `null` -- correctly, since a `!b[field]` check would reject a legitimately-zero empty bundle -- so the instinctive "return `null` when there is nothing to show" fails validation on **every reply for every user in the empty case**: the promise settles, the reply is rejected as unusable, and the tab computes the whole chain on the thread that paints for the rest of the session, with every figure still correct and nothing going red. The weather derivation states emptiness inside the object (`foundCount === 0`) for exactly this reason, and both `lib/statsBundle.ts` and `lib/weatherStats.ts` document it at their definition sites. A new bundle member owes the same shape, and its empty case is a test row rather than an assumption -- **an empty-input reply is the case a fixture-driven suite is least likely to exercise and the only one every new user meets.**

- **The v1.0.14 failure signal INVERTS when the input is already resident on the calling thread, and the inversion is checked rather than assumed (v1.0.20).** `parseOffThread` resolves `null` and refuses a main-thread retry; the stats session REJECTS and its caller runs the identical chain synchronously. Both halves of the precedent were tested against the new case and neither transfers. A failed parse must not be retried here because that re-runs the identical multi-megabyte allocation that just failed, on the thread that paints, with no other way to get the answer -- **but a failed compute's input is the array we cloned FROM, so the fallback allocates nothing the worker allocated** (measured: the bundle retains 1.3 MB against 19.3 MB already in the heap), and **the fallback is not a new risk, it is the previously shipped behaviour**, which ran that chain on that thread on every render. Refusing it would replace a working surface with a blank one. **So pick the signal by reading the consumers AND by asking whether the precedent's REASON applies, not by which precedent is nearer.** State the totality claim with its bound at the definition site: this closes rejection, it does not close non-settlement, which is closed one level down by the watchdog because that is the only place it can be.

- **An IDLE teardown respawns; every FATAL path does not, and that asymmetry is named at the definition site rather than left to be inferred from two flags (v1.0.20).** An idle teardown says nothing went wrong. **A fatal event is a verdict about this device and this input, and every one of them is a verdict that would repeat** -- a worker OOM-killed on a 19 MB hand-over is killed again by the same hand-over, a `postMessage` that could not clone the input out cannot clone it a moment later, a constructor that refused is a platform saying no, and silence past the budget means the worker is gone or the device took a thousand times the measured cost. Retrying per request would re-run the allocation that just killed a thread, pay the spawn and the serialize for it, and fall back anyway. **The cost of being wrong is bounded and is why this is the right side to err on: one transient death that would not have recurred leaves the surface computing on the main thread for the rest of the input's life, which is exactly the previously shipped behaviour.** Keep `dead` and "no worker" as two flags; collapsing them loses the distinction.

- **A derived value held only IN MEMORY, keyed on a user data file, is outside `clearDerived.ts` BY DESIGN, and the boundary is stated here so the next one is argued rather than re-litigated (v1.0.20).** Every row in that registry is a durable DOCUMENT on disk and every row ends in a `storage.deleteSetting`, which `cacheInventory.test.ts` pairs to an exported production purge -- so an in-memory memo can never hold a row, and adding one would turn that guard red for a store that does not exist. The house answers for an in-memory derivation are a `WeakRef` on the source (`lib/speciesIndex.ts`) and, for a worker holding a copy, **an explicit statement at the definition site of the chain that terminates it when the file is deleted, plus a test that asserts the terminate** -- an implicit coupling through three hops is exactly the discipline the registry rule exists to replace. Two residuals are known and accepted: a `WeakRef`'d memo still holds its derived value strongly after the source is collectable (bounded, undisplayable, not zero), and a worker's copy survives the main thread's clear by one async round trip plus two React commits, single-digit to low-double-digit milliseconds, never persisted.

- **Every stored-file import goes through ONE refusal registry, because the rule is a registry and not a discipline (v1.0.20).** `lib/uploadGuard.ts` exports `refuseByFilename` and `refuseByContent` as a pair so an import path added later gets all of them or visibly none; `lib/detectExportType.ts` classifies a CSV from its header line alone and is deliberately LOOSER than both parsers (proved by running them), so a refusal can never turn away a file the app could have used; `lib/firstLine.ts` bounds the header read at `MAX_HEADER_CHARS`. A refusal stores nothing, clears no cache, fires no epoch, and leaves the slot's existing file exactly as it was. **The bound is a RETENTION bound, not a CPU one** -- unbounded, a 200 MB input with no line break retained 200 MB and one line followed by a newline retained 6.4 GB through the cons-string copy, which is an OOM kill rather than a slow load. Two seam facts ride with it: `WebStorage.writeFile` and `deleteFile` now check `res.ok` (with `404` treated as done on delete, since raising it would put "try again" over a row that is already empty), because the web/Pi transport is the only one that has ever enforced the backend's 50 MB cap and was the only one that could not report it; and iCloud pull paths are deliberately NOT guarded, because a synced arrival is not a user upload.

- **A worker-backed promise owes a SETTLE CONTRACT, not two handlers (v1.0.14).** `parseOffThread` in `observationsCache.ts` is the reference: one idempotent `settle` covering all five exits -- a reply, `onerror`, `onmessageerror` for a reply that cannot be cloned back, a `postMessage` that throws on the way out, and silence past a budget -- clearing the watchdog, detaching the handlers, terminating the worker and settling exactly once, so a late event after the watchdog can neither re-settle nor double-terminate. **The silence budget scales with the input and its factor is MEASURED, never guessed:** parse throughput was 57.8 to 75.0 Mchar/s flat across a 20x size range (so linear in the input), and the shipped `30 s + 4 s/MB` is ~250x slower than measured, with the 30 s floor covering worker spawn, module evaluation and the request's structured clone. **A memoized promise inherits its producer's worst settle path** -- the pending promise sat in `inflight`, whose `.finally` therefore never ran, so every later caller for the whole session joined the dead promise. **And the failure signal must match the branch the consumers ALREADY take:** a failed parse resolves `null` rather than throwing, because all eight tab loaders map a *thrown* load to `setup-required` ("upload a backup" over a backup that is plainly loaded) while each already had an honest branch on a falsy result. A new load path picks its signal by reading its consumers, not by which is more idiomatic. The failure path also never re-runs the parse that just failed on the main thread; the one surviving synchronous parse is where `new Worker` throws because Workers do not exist.
- **A feature whose native half must stay off the entry chunk ships as an entry-safe store plus a dynamic-imported controller** (v1.0.11; `lib/icloud/icloudState.ts` / `icloudSync.ts` is the reference). Components read the store and call an `actions` slot the controller installs after boot, so the entry graph carries only the store, the platform gate and the copy; the controller, the native wrapper and its types emit as lazy chunks, and `entryChunk.test.ts` polices both halves.
- **A lazy chart's GEOMETRY lives in a `lib/` module the host can import without the chart library (v1.0.29).** `lib/planChartGeometry.ts` owns the lane heights, the pixels-per-hour derivation and every density rule as pure functions swept at their boundaries; `PlanResult` sizes the `Suspense` fallback from `planChartHeight(...)`, so the box is already its final height when the lazy `PlanChart` chunk lands and nothing shifts, and `entryChunk.test.ts` keeps Recharts off the tab's static graph. The component that only APPLIES the geometry is the one that imports the library.
- **Platform detection:** `isTauri()` from `frontend/src/lib/platform.ts` — single source of truth for branching on Tauri vs. web. Do not check `window.__TAURI_INTERNALS__` elsewhere.
- **Clipboard:** `copyText()` from `frontend/src/lib/clipboard.ts` — for all clipboard writes. In Tauri mode it uses the native `@tauri-apps/plugin-clipboard-manager` (no user-gesture requirement, so auto-copy after an `await` works); on web it uses `navigator.clipboard` with an `execCommand` fallback. **Do not call `navigator.clipboard` directly** — an async clipboard write that runs after an `await` (outside a click) throws `NotAllowedError` in WKWebView/WebView2 and is silently lost on desktop. The capability grants `clipboard-manager:allow-write-text` only (write, not read).
- **Opening an external URL from code:** `openExternalUrl(url)` from `frontend/src/lib/openExternal.ts` — **never `window.open()`**. `window.open()` is silently dropped in the Tauri desktop WebView (WKWebView never opens the system browser), so an external open works on web but does nothing on desktop with no error (the Weather Backlog's "Copy weather & go" hit exactly this, v0.5.67). The seam invokes `plugin:opener|open_url` directly with an immutable URL string in Tauri and synthesizes a transient `<a target="_blank" rel="noopener noreferrer">` only on web/Pi. For a **user click**, prefer the shared link components (`OutboundLink` / `ChecklistLink` / `HotspotLink`); reach for `openExternalUrl` only when the open must happen from code (e.g. after an `await`). **A native action in a reorderable or paged list owns its validated primitive through dispatch (v1.0.28):** pass the row-derived URL to `openExternalLink` instead of relying on a global click listener to rediscover identity from the live DOM. Keep the same id-shape guard + `encodeURIComponent` on any id in the URL as everywhere else.

### Desktop storage (Tauri)

`TauriStorage` in `storage.ts` uses **`tauri-plugin-fs` with `BaseDirectory.AppLocalData` for everything** — API keys, settings, file metadata, and CSV files. All data lives under `AppLocalData/data/`:

| File | Contents |
|---|---|
| `data/api-keys.json` | API keys (`ebird`, `openweather`) and, since v1.0.12, a per-key `meta` (change time and origin, or a cleared marker) validated on every read, so a pre-1.0.12 document reads as untimed and is never defaulted to now |
| `data/settings.json` | App settings (map center, zoom, etc.); the `icloud-sync` object is the one place sync state lives (preference, device id, the key-sync switch, a pending key removal, last-known shared state per file and per key slot), and anything new about sync joins that object as one link per save, never a new document |
| `data/metadata.json` | Uploaded file metadata |
| `data/ebird-backup.csv` | eBird data file |
| `data/ml-export.csv` | ML data file |

Every write calls `mkdir(DATA_DIR, { recursive: true })` before writing to ensure the directory exists.

**Shared-document writes serialize on a per-document promise chain (v1.0.9).** Every read-modify-write on a shared JSON document above (settings, api-keys, metadata) runs as a link on that document's chain in `TauriStorage` (`docChains`, keyed by the closed set of path constants) — a new writer joins the chain, never bypasses it; a link never awaits another chained op, and a failed link rejects only its own caller. Unserialized read-modify-write is the lost-update clobber that silently erased the projects ledger (post-mortem in DECISIONS.md v1.0.9); a future store that rewrites a whole shared document from a base read owes the same mechanism and an interleaving test shaped like `storageWriteSerialization.test.ts`.

**That serialization is per-JS-CONTEXT, and is sufficient ONLY while the app runs exactly one webview (v1.0.9 entry extended 2026-09-15, `b472ae8`).** `docChains`, `replayStore.writeThrough` and the two purge generations -- which live in `replayStore.ts` and `exoticProvenanceCache.ts`, with `clearDerived.ts` as the entry point that moves them through `import()` rather than a third counter of its own (corrected v1.0.33: the wording here used to put a generation in `clearDerived`, where none exists) -- are each module-scoped JavaScript state, so a protection built from them is only as strong as that guarantee. The fact that keeps it true lives in a different language, a different file and a different layer from all of them, **and it is a CHOICE rather than a handler, which is why looking for it by name finds nothing**: `RunEvent::SceneRequested` appears NOWHERE under `src-tauri/src` (corrected v1.0.33: the wording here used to say it was forwarded and dropped in `lib.rs`, which sends a reader hunting for a handler that was never there). That file simply ends at `.run(tauri::generate_context!())`, and `Builder::run` is what installs Tauri's OWN no-op callback -- `self.build(context)?.run(|_, _| {})` at `app.rs:2441` in the locked tauri 2.11.2, with `RuntimeRunEvent::SceneRequested` mapped into it at `app.rs:2630` -- so a second iPadOS scene never constructs a second webview. Changing that one call to `.build(...).run(|app, event| ...)` is what dissolves the keeper, which is why the note sits at that line rather than at a handler. All three capabilities are scoped `windows: ["main"]` as the backstop (a second window would carry a different label and receive zero permissions, so it could not reach the shared documents at all). The dependency became load-bearing the moment `UIApplicationSupportsMultipleScenes: true` declared the app multi-window-capable. **Reversal condition: if a second window or webview is ever created on any platform, all three stop being sufficient and the shared documents need cross-context exclusion -- a native-side lock or a single owning context, never a promise chain.** Two foreseeable changes would each dissolve it, and both would likely remove the capability backstop in the same breath: real iPad multi-window support, and the deferred Tauri 3 upgrade (the tao clause this sentence used to carry -- that tao 0.37 "implements `application:configurationForConnectingSceneSession:options:` itself" -- was wrong, and is corrected at the scene-manifest bullet above: 0.35.3 implements it too). The person making either would be adding a window, not editing `storage.ts`, which is why the invariant is stated here; repeating it at each definition site is DONE (v1.0.33): five notes carry the marker phrase `SINGLE-WEBVIEW INVARIANT` at `storage.ts`, `replayStore.ts`, `clearDerived.ts`, `exoticProvenanceCache.ts` and the keeper in `lib.rs`, each pointing back HERE for the reversal condition instead of restating it, with `singleWebviewInvariant.test.ts` asserting the marker at all five and the keeper's own shape. The guard's residual reach -- it covers the scene path only -- is in ROADMAP.

**`data/replay.json` has an equivalent write chain of its own and deliberately does NOT join `docChains` (v1.0.14).** It is its own file with a single writing MODULE, so it has no read-modify-write to clobber -- but one module is not one write at a time: two concurrent `setReplayStore` calls completed in whatever order the filesystem returned, and a purge's smaller document was the likelier to land first and be overwritten by a pre-purge flush already inside the seam, which neither a cancelled timer nor an identity check reaches. `replayStore` therefore has its own ordered writer (`writeThrough`, the same shape and same two rules as `TauriStorage.chain`: the next write is not CALLED until the previous one settles). **The reason it needs no read-modify-write wrapping is structural and is recorded here because a future change would break it silently:** a write is reachable only from a flush that requires a completed `ensureLoaded`, or from the purge, which awaits it. **Add a re-load-after-purge and that reason is gone**, at which point this document owes the full `docChains` treatment.

**Do not use `localStorage`** — it is ephemeral in Tauri's WKWebView and cleared on every relaunch.  
**Do not use the system Keychain** (`keyring` crate / `invoke('get_api_key', ...)`) — it requires entitlements not configured in this app and fails silently.

**Persist all UI settings through the `storage` seam, never `localStorage` directly.** Anything that must survive a desktop relaunch (e.g. the tab layout) goes through `storage.getSetting`/`setSetting`. The web/Pi path may still read `localStorage` synchronously inside the seam for a flash-free first paint, but the seam — not `localStorage` — is the source of truth on desktop. (See the tab-layout post-mortem in DECISIONS.md.)

**A SHIPPED DEFAULT THAT USERS MAY HAVE A SAVED COPY OF MIGRATES AT READ TIME, AND ONLY FROM THE DEFAULT IT IMMEDIATELY REPLACED (v1.0.19).** When `DEFAULT_TAB_ORDER` changed, a saved order equal to the *previous* default was read as carrying no preference and returned as the new default; any order that differs in any way is a CUSTOM layout and is kept verbatim. Four parts make that safe and all four are load-bearing: keep the previous default as a NAMED CONSTANT so the check is one equality and not a heuristic; run the comparison AFTER the existing unknown-id drop and missing-tab append, so a stale saved copy is normalized before it is compared; preserve the hidden set through the swap; and write NOTHING back, so hydration stays a pure read and the user's document is untouched until they next reorder. **Exactly one generation migrates** -- an older default (here the pre-Calendar 0.5.42 order) is deliberately left alone, because chaining generations turns the one equality back into a heuristic. This narrows v0.5.41/v0.5.42's "saved layouts preserved" to **"saved CUSTOM layouts preserved"**, which is the formulation to use from now on. The migration is worth doing at all, rather than letting fresh installs have it, because these documents are device-local (iCloud syncs the two data files only, never settings), so a default that reaches only fresh installs never reaches the device the change was for.

**NATIVE-side state written by NATIVE code sits outside the `storage` seam, and that is not a violation of the rule above (v1.0.13).** The seam governs the *webview's* data under `AppLocalData/data/` and exists because WKWebView wipes `localStorage`. Window geometry is the standing example: `tauri-plugin-window-state` writes `.window-state.json` into `app_config_dir()` with `std::fs`, because geometry must be applied before the first frame and the seam is read after boot (a seam-based version would open at the configured default and visibly jump, and would need a new JS window-management grant to move the window at all). Such a file never passes through `TauriStorage`, never joins `docChains`, adds nothing to `settings.json`, and is never synced -- it is device-local by nature, so the iCloud scope statement and `PRIVACY_POLICY.md` are unaffected. Do not "fix" `src-tauri/src/window_geometry.rs` into the seam. Two things ride with it, both in `.claude/rules/security.md`: such a document stays out of the webview's granted `fs` scope only while its name begins with a DOT (on macOS `app_config_dir()` and `app_local_data_dir()` are the same directory), and every native read of a file outside the sealed bundle owes the `record_text_at` regular-file-plus-size-bound treatment.

**Two macOS facts for any native window work, both measured rather than inferred (v1.0.13).** `available_monitors()` returns an **EMPTY** list on macOS 26 -- tao 0.35 backs it with `CGGetActiveDisplayList`, which now reports a count of zero, reproduced standalone with no Tauri involved; `primary_monitor()` and `current_monitor()` still work, and `NSScreen` is the supported enumeration. Anything that walks that list on macOS silently does nothing, which is how the window-state plugin's position restore was dead on arrival (`window_geometry.rs` reapplies position itself for exactly this reason). And `set_size`/`set_position`/`maximize` are **asynchronous** on macOS, so at `setup()` the live window still reports the config defaults: decide from the SAVED record, never from `is_maximized()`. Separately, prefer a shared compile-time constant to a runtime lookup for a compile-time value -- `window_geometry::STATE_FILENAME` replaced an `AppHandleExt::filename` call, which is a `Manager::state` lookup that panics when unmanaged, and under this repo's `panic = "abort"` release profile that aborts during `setup()` with no window ever shown. `let _ = fallible()` swallows `Err`, not panics, so a "this cannot keep the app from starting" claim must account for panicking calls too.

Desktop development (requires Rust + `@tauri-apps/cli`):
```
npm run desktop:dev    # Tauri dev mode
npm run desktop:build  # production .app / .exe bundle
```
