# Appendix A — Confirmed findings (107)

Every finding below was independently re-verified against the code by an adversarial reviewer instructed to refute it. Severity reflects the verifier-corrected level. IDs are stable for tracking remediation.

## CRITICAL

### F001 — axe select-name: six filter <select> elements have no accessible name
- **Where:** `frontend/src/components/MapExplorer.tsx:853` · **WCAG:** 4.1.2 Name, Role, Value A · **Found by:** audit:axe
- **Issue:** axe-core rule `select-name` (impact: critical), 6 instances across 4 tabs at runtime. Screen-reader users hear only "combo box" with no indication of what each filter does. Locations: MapExplorer.tsx:853 (species filter), :872 (county filter), :896 (media filter); SpeciesDetail.tsx:604 (county filter); LifeList.tsx:616 (county filter, the Multimedia tab); BreedingCodeList.tsx:376 (county filter). The visual label is a decorative MapPin icon with pointerEvents:'none', so nothing names the control. Checklists.tsx:629/:644 does this correctly with aria-label="Protocol"/"County" — that tab had zero select-name violations, proving the in-house fix pattern.
- **Fix:** undefined
- **Verification:** All six selects verified bare: MapExplorer 853/872/896 (SidebarLabel is an unassociated div), SpeciesDetail 604, LifeList 616, BreedingCodeList 376 (decorative MapPin only); grep confirms no aria-label/title on any of them, while Checklists.tsx 629/644 has the in-house aria-label fix. Accessible name computes to empty, so SR users hear only the current value with no control purpose — axe select-name at critical impact is fairly rated for six primary filter controls across four tabs. Minor imprecision only: for the three MapExplorer selects the visual label is the SidebarLabel div, not a MapPin icon.

## SERIOUS

### F002 — axe aria-hidden-focus: focusable recharts SVG inside aria-hidden observer-pie wrapper
- **Where:** `frontend/src/components/BirdingStats.tsx:1286` · **WCAG:** 4.1.2 Name, Role, Value A (also 2.4.3 Focus Order A) · **Found by:** audit:axe
- **Issue:** axe-core rule `aria-hidden-focus` (impact: serious), 1 instance on the Statistics tab. The decorative observers donut is wrapped in <div aria-hidden="true">, but the recharts <PieChart> renders its SVG with tabIndex=0 (recharts accessibilityLayer default), so keyboard users Tab into an element that screen readers cannot perceive — a focus ghost with no announcement.
- **Fix:** undefined
- **Verification:** recharts 3.8.1 defaults accessibilityLayer to true (PieChart.d.ts) and RootSurface.js then renders the SVG with tabIndex=0 and role=application; the PieChart at BirdingStats.tsx:1287 passes no override and sits directly inside <div aria-hidden="true"> (line 1286). That is exactly axe's aria-hidden-focus (serious): a tab stop screen readers cannot perceive. It is the only aria-hidden chart wrapper in the file, matching the claimed 1 instance; the accessibilityLayer={false} fix is valid for this recharts version.

### F003 — Breeding-tier colored text on 8–15% tier tints fails in both themes (1.97–4.12:1)
- **Where:** `frontend/src/components/BreedingCodeList.tsx:45` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** Active breeding pills set color: var(--sr-tier-N) over rgba(var(--sr-tier-N-rgb), 0.08–0.15) composited on the card surface. Computed over --sr-surface: LIGHT — tier-1 text on tier-1@0.15 = 2.32:1 (any active tier-1 code pill, and the 'Possible' filter pill in BirdingStats); DARK — tier-4 text 1.97:1 (the active 'Confirmed' category pill), tier-3 text 2.94:1, tier-2 text 4.12:1, tier-2-on-tier-1-tint 3.53:1. Same defect at BreedingCodeList.tsx:58-60 (category pills), SpeciesDetail.tsx:786-788 (breeding category pill), BirdingStats.tsx:1637-1638 (Confirmed/Probable/Possible filter pills). Related same-root-cause case: the white badge count on the tier-2 circle (BreedingCodeTable.tsx:219) is 3.96:1 in dark (tier-2 lightened to #A855F7) while passing in light (5.38:1).
- **Fix:** undefined
- **Verification:** Re-ran all contrast math independently (alpha-compositing tints over --sr-surface with globals.css token values): every claimed ratio reproduces exactly (light tier-1 2.32:1; dark tier-4 1.97, tier-3 2.94, tier-2 4.12, tier-2-on-tier-1 3.53; badge 3.96 dark / 5.38 light), and pill text is 12px/500 so the 4.5:1 normal-text threshold applies. The pairs genuinely co-occur in codePillStyle (line 45) and categoryPillStyle (lines 58-60) active states, plus the cited BirdingStats:1626/1637-1638, SpeciesDetail:786-788, and BreedingCodeTable:219 locations, all verified in current code. ACCESSIBILITY.md does not disclose this — it affirmatively claims filter pills meet 4.5:1 in both themes, so 'serious' is the right severity; the suggested --sr-tier-N-fg fix matches the existing --sr-tier-1-text both-themes token precedent and I verified dark #1A0030 on dark tier-2 #A855F7 = 4.88:1 passes.

### F004 — Breeding-code badge contrast fails — in the exact category ACCESSIBILITY.md cites as exemplary (down to 1.71:1 in dark theme)
- **Where:** `frontend/src/components/ChecklistComparer.tsx:35` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:claims
- **Issue:** The statement says interface colors meet AA, 'for example, the top breeding-code badge uses dark text on its light tier color.' That is true only for the Breeding Codes table's tier-1 badge (BreedingCodeTable.tsx:219, #3B0764 on #C084FC = 5.68:1 light / 7.31:1 dark). Elsewhere the same badge concept fails: (1) ChecklistComparer's BreedingBadge uses `color: var(--sr-on-accent)` on `background: TIER_COLORS[def.tier]` — light theme: white on tier-1 #C084FC = 2.64:1; dark theme: #052E16 on tier-2 #A855F7 = 3.77:1, tier-3 #7C3AED = 2.62:1, tier-4 #6B21A8 = 1.71:1 (the badge text is essentially illegible in dark mode). (2) BreedingCodeTable.tsx:218-219 badge in dark theme: white on tier-2 #A855F7 = 3.96:1 (0.6875rem bold = small text, needs 4.5). (3) SpeciesDetail.tsx:788 breeding pill renders its label in TIER_COLORS[tier]; for tier 1 in light theme that is #C084FC on a near-white 8%-tinted surface ≈ 2.6:1. The published example claim is materially false outside the one table.
- **Fix:** undefined
- **Verifier refinement:** Per the tokens-in-both-themes convention, define per-tier text tokens rather than branching inline: keep --sr-tier-1-text (exists in both themes), add --sr-tier-2-text (#FFFFFF in :root = 5.38:1 on #9333EA; #1A0030 in dark = 4.88:1 on #A855F7) and --sr-tier-3-text/--sr-tier-4-text (#FFFFFF both themes, 5.70–15.0:1). Export a TIER_TEXT_COLORS map alongside TIER_COLORS in lib/breedingCodes.ts and use it in ChecklistComparer's BreedingBadge AND BreedingCodeTable.tsx:219 (whose hardcoded '#fff' also violates the no-raw-hex rule). For the SpeciesDetail.tsx:789 pill, set the tier-1 label color to var(--sr-tier-1-text) instead of the raw tier color.
- **Verification:** Recomputed all ratios with WCAG luminance: ChecklistComparer's BreedingBadge fails in both themes (light tier-1 white-on-#C084FC = 2.64:1; dark #052E16 on tier-2/3/4 = 3.77/2.62/1.71) at 0.625rem bold small text, and the BreedingCodeTable dark tier-2 (3.96) and SpeciesDetail tier-1 pill (2.46 on the composited 8% surface) sub-claims also verify. ACCESSIBILITY.md line 31 holds the breeding badge up as the AA example and its only disclosed exception is chart-bar labels, so this is not disclosed. Serious is correct: 1.71:1 on confirmed-breeding codes in dark mode is effectively illegible and contradicts the published statement.

### F005 — Contrast claim is overstated: informational text rendered in the --sr-text-disabled token sits at 2.3–2.6:1 in both themes
- **Where:** `frontend/src/components/Checklists.tsx:177` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:claims
- **Issue:** ACCESSIBILITY.md claims body text and primary interface text meet 4.5:1 in both themes, with exactly one disclosed exception (percentage labels inside chart bars). In fact, real informational text — not disabled controls — is rendered in --sr-text-disabled, which computes to 2.56:1 light (#A1A1AA on #FFFFFF) / 2.45:1 on the page bg, and 2.29:1 dark (#52525B on #18181B). Locations: Checklists.tsx:177 and :668 (the result-count labels that are also the polite live regions), MediaCommentsSection.tsx:100 and NamedBirdsTable.tsx:77 (count labels), SpeciesDetail.tsx:461 ('{n} species' count) and :584 (scientific names inside the species-combobox options), ChecklistComparer.tsx:534 (italic placeholder labels), and the uppercase mini-headers in SetupRequired.tsx:56, ListComparer.tsx:203, HelpDocs.tsx:354. None of these are covered by the statement's disclosed chart-label exception, so the published claim is false as written, and low-vision users genuinely cannot read these labels.
- **Fix:** undefined
- **Verification:** Re-ran WCAG luminance math: --sr-text-disabled is 2.56:1 on white / 2.46:1 on --sr-surface-faint (the line 177/668 labels' actual backdrop) light, 2.29:1 / 2.20:1 dark — all well below 4.5:1 for 11-12px informational text. All ten cited locations exist and use the token for real content (count labels that are also the only result-count display, combobox sci names, mini-headers), and ACCESSIBILITY.md's sole disclosed exception (chart-bar percentage labels) covers none of them, so the published AA claim is false as written. Fix is correct and convention-compliant: --sr-text-muted passes in both themes (4.83/4.63 light, 6.91/6.63 dark) and is already defined in both.

### F006 — Help overlay: fixed 200px TOC sidebar never collapses — content column collapses to ~30px at 320px width
- **Where:** `frontend/src/components/HelpDocs.tsx:348` · **WCAG:** 1.4.10 Reflow AA · **Found by:** audit:resize
- **Issue:** The HelpDocs body lays the TOC and content side by side with inline styles only — no media query and no wrap. The TOC is `width: 200, flexShrink: 0`, the row adds `padding: '0 24px'` and `gap: 40`, so at a 320 CSS px viewport (e.g. 1280px window at 400% zoom, or a phone) the help content column gets roughly 320 − 48 − 200 − 40 ≈ 32px. Markdown text wraps at one or two characters per line (long words overflow), making the in-app documentation effectively unreadable exactly for the low-vision users most likely to need it. Every other layout in the app handles this (globals.css has a 640px media query; TabNav measures and collapses), so this is the one true reflow failure found. It also contradicts ACCESSIBILITY.md line 39 ("The layout reflows for narrow windows and at large sizes … without clipping content").
- **Fix:** undefined
- **Verifier refinement:** The suggested .sr-help-body class + rule in the existing @media (max-width: 640px) block is right, but because the layout is inline styles the override needs !important (the existing block already does this pattern for .sr-header's inline padding) — e.g. add classes to the row and nav and rule `.sr-help-row { flex-direction: column !important; gap: 0 !important; } .sr-help-toc { width: 100% !important; position: static !important; max-height: none !important; }`. The existing 640px breakpoint is sufficient for SC 1.4.10 (failure threshold is 320 CSS px); no new ~700px breakpoint needed.
- **Verification:** HelpDocs.tsx lines 341-351 lay out the TOC (inline width:200, flexShrink:0) and content (flex:1, minWidth:0) with gap 40 and 0 24px padding, all inline styles with no className, and globals.css's only width media block (640px) has no help-overlay rules — so at 320 CSS px the content column is ~32px as claimed. ACCESSIBILITY.md line 39 affirmatively claims reflow works (exempting only tables/maps), so this is contradicted rather than disclosed. Severity serious is fair: the in-app documentation becomes unreadable at 400% zoom for exactly the low-vision users it serves.

### F007 — Filter form controls have no programmatic label across most data tabs
- **Where:** `frontend/src/components/LifeList.tsx:616` · **WCAG:** 1.3.1 Info and Relationships / 4.1.2 Name, Role, Value (also 2.4.6) AA · **Found by:** audit:forms
- **Issue:** The county <select> and From/To date inputs are repeated across tabs with no aria-label, no <label for>, and no association to any visible text — a screen-reader user hears an unnamed combobox and two unnamed, indistinguishable date fields. Locations: LifeList.tsx:616 (county select), :644 and :658 (date from/to); BreedingCodeList.tsx:376, :408, :424 (same trio); SpeciesDetail.tsx:604, :629, :643 (same trio). Related instances where a VISIBLE label exists but is not programmatically associated: MapExplorer.tsx:853 (species select), :872 (county select), :896 (media select) and :862/:864 (date text inputs) — their 'Species'/'County'/'Media'/'Date Range' headings are SidebarLabel <div>s (map/MapSidebarUI.tsx:35-43), so the selects have no accessible name and the two free-text date inputs are distinguishable only by the placeholder 'YYYY-MM-DD' (format instruction also vanishes once filled — 3.3.2); Settings.tsx:1214, :1226, :1238 (Latitude/Longitude/Radius number inputs whose labels are plain <div>s at :1213/:1225/:1237); ChecklistComparer.tsx:350-353 ('Checklist A/B' <label> elements with no htmlFor, so the inputs' name falls back to the example-URL placeholder). Checklists.tsx:629-663 does this correctly (aria-label="Protocol"/"County"/"From date"/"To date"), proving the house pattern exists.
- **Fix:** undefined
- **Verifier refinement:** The fix is correct and convention-consistent (mirror Checklists.tsx's aria-label="County"/"From date"/"To date"). One caveat: do not unconditionally convert SidebarLabel to <label htmlFor> — it also captions the Breeding Code SegControl, a button group with no labelable element. Either add aria-label per control in MapExplorer (including format-bearing names like 'Date from (YYYY-MM-DD)' on the free-text date inputs), or give SidebarLabel an optional htmlFor prop that renders <label> only when an id is passed.
- **Verification:** Verified every cited location in current code: LifeList/BreedingCodeList/SpeciesDetail county selects and date inputs have no aria-label/label/title; MapExplorer's SidebarLabel is a bare <div> (MapSidebarUI.tsx:35-43) so its selects are unnamed and the two text date inputs share the identical 'YYYY-MM-DD' placeholder; Settings lat/lon/radius captions are plain <div>s; ChecklistComparer's <label> has no htmlFor and doesn't wrap the input. Checklists.tsx really does carry the aria-label house pattern the fix cites, and ACCESSIBILITY.md discloses only chart-label contrast as a known exception, not this. Serious is the right severity — repeated unnamed form controls across most data tabs is a genuine 4.1.2/1.3.1 SR barrier, though the controls remain operable.

### F008 — Selected state not programmatically exposed on segmented controls and toggle pills
- **Where:** `frontend/src/components/map/MapSidebarUI.tsx:15` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The shared SegControl renders plain <button>s whose active option is conveyed only by background/border/font-weight — no aria-pressed, aria-checked, or radiogroup semantics. It is used for the Map Explorer radius (MapExplorer.tsx:700), breeding filter (:881), Pins/Heatmap display mode (:912), and All/Week (:1285). The same style-only-state defect appears in SpeciesDetail.tsx:1189 (Pins/Heatmap map-mode buttons), BirdingStats.tsx:541 (accumulation granularity), :1628 (breeding filter pills), :1717 (media view mode), ListComparer.tsx:215 (My List / Upload source buttons), and MapExplorer.tsx:1030 (hotspot kind-legend toggles, whose only state cue is opacity 0.4). A screen-reader user cannot tell which option is active. This directly contradicts ACCESSIBILITY.md: "Filter pills announce whether they are pressed." The codebase already has the correct pattern (SortSeg in Checklists.tsx:74, SnowMap.tsx:144, SpeciesDetail graph groups :962/:974).
- **Fix:** undefined
- **Verifier refinement:** Give SegControl an ariaLabel prop rendered as role="group" aria-label on its wrapper div plus aria-pressed={value === opt.value} on each button, mirroring SortSeg in Checklists.tsx (the SidebarLabel above it is not programmatically associated, so the group needs its own label). Apply the same aria-pressed to the inline groups in SpeciesDetail, BirdingStats, and ListComparer; for the kind-legend visibility toggles use aria-pressed={!isHidden} so pressed means shown.
- **Verification:** SegControl (MapSidebarUI.tsx:15-29) renders plain buttons with style-only active state, and every cited usage checks out: MapExplorer 700/881/912/1285, SpeciesDetail ~1189, BirdingStats 541/1628/1717, ListComparer 215, and the hotspot kind-legend toggles at MapExplorer 1030 whose only state cue is opacity 0.4. The correct pattern does exist in SortSeg (Checklists.tsx, role=group + aria-pressed) and SnowMap.tsx:144, and ACCESSIBILITY.md's 'filter pills announce whether they are pressed' claim is contradicted rather than disclosed as an exception. Serious is fair: a screen-reader user cannot determine the active option on roughly ten primary filter/view controls.

### F009 — Media Targets keyboard path is partial and ACCESSIBILITY.md overstates it
- **Where:** `frontend/src/components/map/TargetMarkers.tsx:69` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:maps
- **Issue:** Media Target chips are DOM <Marker>s with a click-only <div> (no tabindex/role), so the chips themselves are not operable by keyboard. The only keyboard route to a target popup is the 'Nearest Targets' list (MapExplorer.tsx:1292-1338), which is hard-capped at 10 items sorted by distance from the SEARCH CENTER (nearest10 useMemo, MapExplorer.tsx:453-461) — it is not scoped to the map viewport, does not update on pan/zoom, and shows no over-cap hint. Any target beyond the nearest 10 (easy with a 25-50 mi radius) has no keyboard path at all. ACCESSIBILITY.md line 13 claims "The Media Targets mode's 'Nearest Targets' list works the same way" and that "These lists are scoped to the current map view and update as you pan or zoom" — that is true for sightings/hotspots (cap 200, BoundsTracker-driven) but false for targets, so the published statement is currently inaccurate.
- **Fix:** undefined
- **Verifier refinement:** The suggested InViewMarkerList fix is correct and matches existing conventions, with two precisions: (1) the locId grouping currently lives in TargetMarkers.tsx's locationGroups useMemo, so either lift it to MapExplorer (shared with displayedTargetPins) or recompute it there before feeding markersInView, and wire onActivate to the existing setSelectedTargetLocId + setPanTarget pair (same owner the chip click uses); (2) per CLAUDE.md, update ACCESSIBILITY.md in the same change — either to truthfully describe the viewport-scoped list once built, or immediately to state that the targets list is the nearest 10 from the search center.
- **Verification:** Verified TargetMarkers.tsx:69-75: chips are click-only divs with no tabindex/role, and MapExplorer.tsx:453-461 confirms nearest10 is distance-from-search-center, slice(0,10), with no mapBounds dependency — so targets beyond the nearest 10 have no keyboard route at all, unlike sightings/hotspots which use markersInView (cap 200, viewport-driven) into InViewMarkerList. ACCESSIBILITY.md line 13's claim that the targets list 'works the same way' and is 'scoped to the current map view' is factually wrong for this mode. Genuine 2.1.1 Level A gap in a primary feature plus an inaccurate published statement; serious stands.

### F010 — Inline error messages are silent to assistive tech (no role=alert/aria-live, no aria-describedby)
- **Where:** `frontend/src/components/MapExplorer.tsx:105` · **WCAG:** 4.1.3 Status Messages / 3.3.1 Error Identification AA · **Found by:** audit:forms
- **Issue:** Several async form-submission error paths render a plain styled <div>/<span>/<p>, so a screen-reader user who activates the control hears nothing and must hunt the page to discover the failure. Locations: MapExplorer.tsx:105 (place-name search failure), :687 (geolocation error), :1006-1011 (Find Hotspots error), :1227-1232 (Find Recent Sightings error); Settings.tsx:300-309 (malformed/failed CSV upload error in FileRow), :476-485 (API-key save error in KeyRow), :1210 (map-location detect error), :1281-1283 (map defaults save error — additionally the text 'Check your values and try again.' does not identify which of the three fields is invalid); ListComparer.tsx:276-280 (my-list load error). None are associated to their input via aria-describedby either. The codebase has correct reference implementations: App.tsx:668+717-736 (role=alert plus aria-describedby on the input), ChecklistComparer.tsx:372, DropZone.tsx:132, WeatherTidePanel.tsx:184.
- **Fix:** undefined
- **Verification:** Every cited location verified as a plain styled div/span/p (MapExplorer 105/687/1006/1227, Settings 300-309/476-485/1210/1281-1283 including the field-anonymous 'Check your values' message, ListComparer 276-280); grep shows zero role=alert or aria-live in those files. The cited correct reference implementations all exist (App.tsx role=alert + aria-describedby, ChecklistComparer 372, DropZone 132, WeatherTidePanel 184), so this is inconsistent application of an established house pattern. Serious is fair for 4.1.3/3.3.1: async failures on key flows (location search, CSV upload, API-key save) are completely silent to SR users.

### F011 — Mobile filter sidebar: Escape focus-restore is dead code, trap snapshot goes stale, other close paths drop focus — published a11y claim is false
- **Where:** `frontend/src/components/MapExplorer.tsx:202` · **WCAG:** 2.4.3 Focus Order A (and 2.1.2 No Keyboard Trap A integrity) · **Found by:** audit:focus
- **Issue:** Three defects in the one focus trap the app ships: (1) The Escape handler runs `setSidebarOpen(false); filtersButtonRef.current?.focus()` (lines 200-203), but the Filters button is inside the FAB cluster rendered only when the sidebar is CLOSED (`{!sidebarOpen && (<div className="sr-map-fab-cluster">` at line 1446, ref at 1461). While the sidebar is open the ref is null, so the restore is a no-op; the focused element then gets display:none (`.sr-map-sidebar-hidden`) and the browser dumps focus to <body> — the keyboard user restarts from the top of the document. ACCESSIBILITY.md line 51 explicitly claims 'Escape closes it and returns focus to the button that opened it' — that claim is currently false. (2) The focusable list is snapshotted once at open (lines 192-196: `sidebar.querySelectorAll(...)`, then `first`/`last` captured); the sidebar contains a Filters accordion and async-loaded hotspot/target lists, so after content changes Tab escapes the trap into obscured background content (the page behind is not inert). (3) The mobile 'Close filters' button (line 1414) and the backdrop click (line 1400) close via `setSidebarOpen(false)` with no focus restore — same display:none → body drop.
- **Fix:** undefined
- **Verification:** All three defects verified: (1) the FAB cluster with filtersButtonRef renders only when !sidebarOpen (line 1446, ref 1461), so the Escape handler's focus() at line 202 runs while ref.current is null — dead code — and globals.css confirms .sr-map-sidebar-hidden is display:none on mobile, dropping focus to body; ACCESSIBILITY.md line 51's claim is therefore false. (2) Focusables are snapshotted once (lines 192-196) with [sidebarOpen]-only deps while the sidebar content is dynamic. (3) The Close button (1414) and backdrop (1400) close with no restore. Serious is appropriate: every keyboard close path on mobile strands focus, and the published a11y statement affirms the broken behavior.

### F012 — --sr-text-disabled used for real content text (2.29–2.56:1)
- **Where:** `frontend/src/components/Settings.tsx:240` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** The disabled-text token (#A1A1AA light = 2.56:1 on surface, 2.45:1 on bg; #52525B dark = 2.29:1 on surface, 1.93:1 on surface-subtle) is widely used for meaningful, non-disabled content: Settings file sublabels (Settings.tsx:240) and full explanatory paragraphs (Settings.tsx:1143, 1174), file dates (:235), tab-layout 'HIDDEN' labels (:739), Species Detail counters and zero values (SpeciesDetail.tsx:461, 584, 690, 830, 926), comparer '—' and italic placeholders that convey 'no value' (ChecklistComparer.tsx:100, 534), the List Comparer section label (ListComparer.tsx:203), and the LifeList 'no filter' chip (LifeList.tsx:606). The WCAG inactive-control exemption does not apply to any of these — they are informative text.
- **Fix:** undefined
- **Verifier refinement:** Swapping to --sr-text-muted is right for most sites, but light-theme muted (#71717A) on --sr-surface-subtle (#F4F4F5) is only 4.40:1 — just under AA. The 'No file saved' / 'No key saved' chips (Settings.tsx:250, 388) sit on surface-subtle, so for those either use --sr-text (or a new AA-checked token added to BOTH :root and [data-theme="dark"] in globals.css) or lighten the chip background; all other cited Settings sites are on --sr-surface or --sr-bg where muted passes (4.83:1 / 4.62:1 light, 6.91:1+ dark).
- **Verification:** Re-ran the WCAG math independently: every reported ratio is exact (light #A1A1AA on #FFFFFF = 2.56:1, on #F9FAFB = 2.45:1; dark #52525B on #18181B = 2.29:1, on #27272A = 1.93:1; muted = 4.83/6.91 on surface). The cited Settings.tsx sites (235, 240, 250, 388, 739, 1143, 1174) are informative content — file dates, sublabels, status chips, explanatory paragraphs — none inside a disabled control, so the 1.4.3 inactive-control exemption does not apply. ACCESSIBILITY.md's contrast section claims AA for body text and discloses only chart-bar labels, not this; serious is the right severity for sub-2.6:1 body-size content text.

### F013 — Settings tab reorder is drag-and-drop only — no keyboard alternative
- **Where:** `frontend/src/components/Settings.tsx:712` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:keyboard
- **Issue:** Tab Layout rows are reordered exclusively via HTML5 drag-and-drop on the <li draggable> (onDragStart/onDragOver/onDrop, Settings.tsx:712-717). The drag handle is a non-focusable <div aria-label="Drag to reorder…"> (line 721-723) — it isn't even in the tab order, and there are no move-up/move-down buttons. The component self-documents the failure with an sr-only note at line 699 ("Keyboard reordering is not supported in this version"), which is honest disclosure but still a WCAG 2.1.1 failure: the reorder function is entirely unavailable to keyboard (and screen-reader) users. The per-row show/hide eye toggles ARE proper buttons, so only ordering is blocked.
- **Fix:** undefined
- **Verification:** Verified: reorder is exclusively HTML5 DnD on the <li draggable> (Settings.tsx:710-718); the handle div (721-730) is unfocusable, there are no move-up/down buttons, and the file's only onKeyDown is the API-key Enter handler — so ordering is unreachable by keyboard, a hard 2.1.1 Level A failure. The sr-only note at line 699 self-documents it; ACCESSIBILITY.md's keyboard section does not disclose this exception (its only caveat is map markers, which have a keyboard alternative). Severity serious is correct; the suggested fix (move buttons or APG keyboard-drag with aria-live) is sound and fits the existing onReorder/storage-seam plumbing.

### F014 — Species Detail / Named Birds map pins (DOM markers) have no keyboard access path
- **Where:** `frontend/src/components/SightingsMap.tsx:55` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:keyboard
- **Issue:** The shared SightingsMap renders each sighting pin as a react-map-gl <Marker> whose content is a plain <div> with a click handler routed through Marker onClick — no tabIndex, no role, no keydown handler — so a keyboard user can never open the per-coordinate popup. On Species Detail ("Sighting Locations" map) that popup is the ONLY place exposing the per-sighting checklist links for sightings without comments (Top Locations rows at SpeciesDetail.tsx:1122-1137 show only name+count; the Comments section only covers commented sightings), so those eBird links are pointer-only functionality. Same pattern, lower impact (keyboard equivalents exist nearby): TargetMarkers.tsx:69-74 chip markers (mitigated by the sidebar "Nearest Targets" locate buttons opening the same popup) and BirdingStats.tsx:863-872 geographic-stats rank pins (popup content fully duplicated in the adjacent "Top locations" text lists). Named Birds card maps are also unreachable but each row already lists every sighting's checklist link (NamedBirdRow.tsx:90-100). Note ACCESSIBILITY.md's keyboard-alternative claim covers only the Map Explorer GL markers; these DOM-marker maps have no equivalent of its in-view lists.
- **Fix:** undefined
- **Verifier refinement:** The button-child fix needs no custom keydown handler: react-map-gl attaches a native click listener to the marker wrapper, so Enter/Space on a real <button aria-label={`${locName ?? coord}, ${n} sightings`}> child dispatches a click that bubbles to it. One refinement: maplibre's Marker.addTo() already stamps the wrapper div with role='button' + generic aria-label='Map marker', so via the Marker ref call marker.getElement().setAttribute('role','presentation') and removeAttribute('aria-label') to avoid a nested-button announcement (or instead set the descriptive aria-label + tabindex on the wrapper itself through the ref and skip the inner button). Apply the same treatment to TargetMarkers.tsx and the BirdingStats rank pins; the Species Detail alternative (links in Top Locations rows) should reuse the existing NamedBirdRow.tsx aria-label pattern for checklist anchors.
- **Verification:** Verified SightingsMap.tsx:54-58 (and TargetMarkers.tsx, BirdingStats.tsx:862-872): pins are plain div Marker children with click wired through react-map-gl's native click listener; maplibre only adds tabindex/keypress via setPopup(), which this code never uses, so no pin is focusable or keyboard-activatable. The Species Detail impact claim checks out — Top Locations rows (SpeciesDetail.tsx:1115-1138) show name+count only and the Comments section filters to commented sightings (line 372), so checklist links for uncommented sightings are popup-only, i.e. pointer-only. ACCESSIBILITY.md's keyboard-alternative claim is explicitly Map Explorer-scoped and its only disclosed exception is chart-label contrast, so this Level A lockout is undisclosed; serious is the right severity.

### F015 — Filter selects and date inputs have no accessible name on four tabs
- **Where:** `frontend/src/components/SpeciesDetail.tsx:604` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The county <select> and the From/To <input type="date"> controls have no aria-label, no <label>, and no title — a screen reader announces an unnamed combo box / date field. Same defect repeats: LifeList.tsx:616 (select), :645, :659 (dates); BreedingCodeList.tsx:376 (select), :409, :425 (dates); MapExplorer.tsx:853 (Species select), :872 (County select), :896 (Media select) — the visual SidebarLabel is a styled <div> with no htmlFor association — and MapExplorer.tsx:862/:864 (two date text inputs distinguished only by placeholder "YYYY-MM-DD"). Checklists.tsx:629/:644/:655/:663 does this correctly (aria-label on each), proving the intended pattern exists in-repo.
- **Fix:** undefined
- **Verification:** Every cited location verified: unnamed selects at SpeciesDetail.tsx:604, LifeList.tsx:616, BreedingCodeList.tsx:376, and MapExplorer.tsx:853/:872/:896 where SidebarLabel (map/MapSidebarUI.tsx:35) is a plain styled <div> with no htmlFor; MapExplorer.tsx:862/:864 are text inputs distinguished only by identical 'YYYY-MM-DD' placeholders. Minor nit: HTML-AAM makes the placeholder a fallback accessible name on the two text inputs, but both compute to the same string so the fields remain indistinguishable, and the selects have no name at all. Serious is fair and the aria-label fix matches the existing Checklists.tsx pattern.

### F016 — axe label: From/To date inputs on three tabs have no accessible name
- **Where:** `frontend/src/components/SpeciesDetail.tsx:630` · **WCAG:** 4.1.2 Name, Role, Value A (also 3.3.2 Labels or Instructions A) · **Found by:** audit:axe
- **Issue:** axe-core rule `label` (impact: critical), 6 instances across 3 tabs at runtime. The date-range filter inputs are announced as unlabeled date fields, and the only visual relationship cue is a decorative Calendar icon and a "→" span. Locations: SpeciesDetail.tsx:630 and :644, LifeList.tsx:645 and :659 (Multimedia tab), BreedingCodeList.tsx:409 and :425. The identical control in Checklists.tsx:655/:663 carries aria-label="From date"/"To date" and passed — the fix pattern already exists in the codebase.
- **Fix:** undefined
- **Verification:** Verified: both date inputs at SpeciesDetail.tsx:629-654 and the LifeList.tsx/BreedingCodeList.tsx duplicates have no aria-label, <label>, or title, while Checklists.tsx:655/:663 carries the exact labels claimed. Real 4.1.2/3.3.2 failure — a SR user gets two identical unnamed date fields and cannot tell From from To. Downgraded critical→serious: 'critical' is axe's rule-impact taxonomy, but contextually these are secondary filter controls that stay operable, the same defect class finding 5 rates serious; the suggested fix is correct and matches the in-repo convention.

### F017 — axe color-contrast: 139 runtime instances in light theme, traceable to ~4 token-level causes
- **Where:** `frontend/src/globals.css:29` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:axe
- **Issue:** axe-core rule `color-contrast` (impact: serious), 139 violating nodes across all 10 tabs at default (light) theme, deduped by failing color pair: (a) --sr-accent #2D8653 (globals.css:29) used as small text fails by a hair on every light surface — 4.32:1 on #f9fafb (active tab label in TabNav, all 10 tabs), 4.03:1 on #e8f5ee accent-bg (selected segmented/sort buttons, 24x), 4.11–4.33:1 on #f4f4f5/#fafafa (links, 9x); (b) --sr-text-muted #71717A (globals.css:18) on --sr-surface-subtle #f4f4f5 = 4.39:1 — the 66 "Jump to section" nav pills (BirdingStats.tsx:450) and similar chips; (c) --sr-text-disabled #A1A1AA (globals.css:19) used for INFORMATIVE text at 2.33–2.56:1 — result-count announcements ("87 comments" aria-live spans in Checklists/LifeList), empty date-input text (color: var(--sr-text-disabled) at SpeciesDetail.tsx:638 etc.), "267 species" counts — worst offenders; (d) chart/inline labels: hardcoded white 10px % labels on --sr-graph-photo #3B82F6 bars = 3.67:1 (BirdingStats.tsx:1153, 5x) and on --sr-graph-video #8B5CF6 = 4.23:1; --sr-tier-1 #C084FC 1.5rem-bold breeding "Possible" count on white = 2.64:1, below even the 3:1 large-text threshold (BirdingStats.tsx:1613); --sr-accent-border-strong text "Loaded from Settings" on accent-bg = 2.52:1 (ListComparer); Settings date-format sublabels 3.16–3.33:1.
- **Fix:** undefined
- **Verification:** Independently recomputed every claimed pair and all match within 0.03 (accent 4.32/4.03/4.11/4.33 as small text incl. the 0.875rem/500 TabNav active label; muted 4.40 on the verified jump pills; disabled 2.33-2.56 confirmed on informative text at Checklists.tsx:177/668 aria-live counts, SpeciesDetail.tsx:638, Settings sublabels; white-on-#3B82F6 3.68 at BirdingStats.tsx:1153; tier-1 2.64 at :1613; accent-border-strong 2.53 at ListComparer.tsx:258). ACCESSIBILITY.md discloses only the chart-bar percentage-label subpart and otherwise claims AA for text/buttons/links in both themes, which the accent/muted/disabled items contradict, so not treated as disclosed. Proposed fixes verified: #277448 yields 5.09-5.46 and #65656e yields 5.25 on the cited surfaces; token-level :root changes fit conventions (dark theme has its own values).

### F018 — Dark theme: white chip/badge text on lightened recency-tier fills fails badly (1.52–1.92:1)
- **Where:** `frontend/src/lib/mapExplorerFormat.ts:61` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** tierColors() hardcodes text:'white' for the fresh and mid recency tiers, but the dark-theme tokens lighten those fills (--sr-map-target-fresh #34D399, --sr-map-target-mid #6EE7B7). Computed: white on #34D399 = 1.92:1, white on #6EE7B7 = 1.52:1 (dark); even light-theme mid (#5EA07C) is only 3.09:1. These colors carry the species-name label on every Media Targets map chip (TargetMarkers.tsx:72, 11px/600) and the '≤7 days' tier badge in the target popup (TargetMarkers.tsx:100, 10px/600) — the primary content of the Map Explorer's Media Targets mode is near-unreadable in dark mode. The 'old' tier already does this right (--sr-map-target-old-text adapts per theme, 4.86:1 light / 10.83:1 dark).
- **Fix:** undefined
- **Verifier refinement:** The suggested per-tier text tokens are correct and convention-conforming (define in both :root and [data-theme="dark"]); make explicit that --sr-map-target-mid-text must be dark in the LIGHT theme too (white on light-mid #5EA07C is only 3.09:1, failing 4.5:1 at 11px), while --sr-map-target-fresh-text can stay white in light (#2D8653 = 4.52:1) and go dark (e.g. #052E16) in dark.
- **Verification:** Re-ran WCAG math: white on dark-theme #34D399 = 1.92:1 and #6EE7B7 = 1.52:1 (light-theme mid #5EA07C = 3.09:1), all failing 4.5:1 for the 11px/600 chip text (TargetMarkers.tsx:72) and 10px/600 tier badge (line 100) that carry the Media Targets mode's primary content. tierColors() at mapExplorerFormat.ts:61-62 hardcodes 'white' while the old tier already uses a per-theme --sr-map-target-old-text token, confirming the asymmetry. ACCESSIBILITY.md's only disclosed contrast exception is chart-bar labels and it claims map popups meet AA, so this is undisclosed; serious is the right severity.

## MODERATE

### F019 — ACCESSIBILITY.md contrast claim is broader than the computed reality
- **Where:** `ACCESSIBILITY.md:31` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** The published statement says text, buttons, links, map popups and form controls 'meet the WCAG 2.1 AA contrast standard (4.5:1) in both the light and dark themes', with chart-bar labels as the only known exception. Computed counterexamples outside that exception: light-theme active pills/links on tinted backgrounds 4.03–4.41:1 (accent-on-accent-bg, error-on-error-bg, active tab on page bg), --sr-text-disabled used on content at 2.29–2.56:1, dark-theme target chips at 1.52–1.92:1, dark-theme breeding pills at 1.97–2.94:1, and form-control borders at 1.19–1.27:1 (non-text). The repo treats this file as a published statement whose claims must be true; today it overstates. (Verified-true parts: the tier-1 badge example, the recency-dot hidden labels, the text-footer/sci-name token fixes, focus-ring contrast, and the dark-mode popup theming.)
- **Fix:** undefined
- **Verifier refinement:** The two-option fix is sound, with one correction: the dark-theme target-chip failure is not a token edit — lib/mapExplorerFormat.ts hardcodes text: 'white' (itself against the no-hardcoded-colors convention). Fix it like the existing old-tier pattern: add --sr-map-target-fresh-text / --sr-map-target-mid-text tokens to BOTH :root and [data-theme="dark"] in globals.css and reference them, mirroring --sr-map-target-old-text. The breeding-pill fix likewise needs the dark-theme tier text colors lightened (or pill text switched to --sr-text) rather than tint-alpha tweaks, since the 0.08 tint barely moves the blended background. Any token change must land in both theme blocks per convention; ACCESSIBILITY.md edits alone need no version bump.
- **Verification:** Recomputed all cited ratios independently and each one holds with co-occurring real UI: light accent-on-accent-bg 4.03 / error-on-error-bg 4.41 (LifeList.tsx:107-108 active pills) / active tab on bg 4.32 (TabNav.tsx:94); --sr-text-disabled 2.29-2.57 on non-disabled content (Settings.tsx:240/376 sublabels); dark target chips 1.52/1.92 (white text hardcoded in lib/mapExplorerFormat.ts:61-62); dark breeding pills 1.97-2.94 after alpha-blending the 0.08 tints (BreedingCodeList.tsx:43-60); borders 1.19-1.27. ACCESSIBILITY.md:31 claims AA in both themes with only the chart-bar-label exception, so the published statement is overstated — and CLAUDE.md explicitly requires this file's claims to stay true. The only stated exception does not cover any of these, so alreadyDisclosed is false.

### F020 — Footer update-checker outcomes auto-dismiss on fixed 4–8s timers with no way to extend — including the web path's actionable 'update available' instruction
- **Where:** `frontend/src/App.tsx:505` · **WCAG:** 2.2.1 Timing Adjustable (A) · **Found by:** gap:WCAG 2.2.1 Timing Adjust
- **Issue:** Every outcome of the user-initiated 'Check For Updates' action is erased by a content-set time limit with no mechanism to turn off, adjust, or extend it (none of the 2.2.1 exceptions — real-time, essential, 20-hour — applies). Worst case: on the WEB path, the 'available' state renders 'v{latest} available — run ./update.sh' (App.tsx:1172-1177) — actionable content the user must read, remember, and carry to a terminal — and auto-reverts to idle after 8000ms (App.tsx:505). The Tauri path proves persistence is the intended design: its 'available' state (App.tsx:492-493) has NO timer and keeps its Install button indefinitely, so the web timer is an arbitrary asymmetry. Additionally, 'up-to-date' ('Up to date (v{current})' — the answer to the user's question, shown nowhere else) and 'error' ('Could not check for updates') auto-revert after 4000ms (App.tsx:491, 496, 502, 510; the post-install failure at 524), after which the footer returns to the bare 'Check For Updates' button — indistinguishable from never having clicked. A screen-magnifier user panning the viewport, a user reading at 200% text size, or a user with a cognitive disability can entirely miss a 4-second message, and because the timer re-arms on every check, re-clicking the button is not an extension mechanism — the reading window can never exceed 8 seconds. This compounds verified finding i=35 (the 'available' and 'error' states are also never announced to AT): for a screen-reader user the error outcome is both silent AND gone within 4 seconds. Applying the brief-success-confirmation reasoning explicitly: only 'up-to-date' resembles a success confirmation, and even it carries content (the version number) with no persistent equivalent; 'available' and 'error' are informational/actionable outcomes, not confirmations, so the commonly-accepted toast exemption does not cover them.
- **Evidence:** App.tsx:505 `updateTimerRef.current = setTimeout(() => setUpdateStatus({ kind: 'idle' }), 8000)` on the web 'available' branch, whose render (App.tsx:1173-1175) is 'v{updateStatus.latest} available — run <code>./update.sh</code>'; the Tauri 'available' branch at 492-493 sets state with no timer; 'up-to-date'/'error' timers at 491, 496, 502, 510, 524 all revert to {kind:'idle'} after 4000ms, unmounting the message span (renders at 1153-1154, 1190-1192).
- **Fix:** Remove the auto-revert timers for the 'available' (web) and 'error' states — let them persist until the user acts, exactly as the Tauri 'available' state already does (render the 'Check For Updates' button alongside or convert the message to include a dismiss/recheck affordance). For 'up-to-date', either let it persist with the recheck button returning next to it, or keep an auto-revert only if the duration is generous (20s+) — though persisting is simpler and costs nothing in a footer. This pairs naturally with the fix for verified finding i=35: one persistent aria-live span whose text changes per state, with the timer logic deleted rather than announced.
- **Verifier refinement:** Delete the timers for 'available' (web) and 'error' and let them persist like the Tauri 'available' branch; re-render the 'Check For Updates' button alongside the persistent message (also resolving the idle-button unmount focus drop noted under i=28). Fold into i=35's fix: one always-mounted aria-live span whose text changes per state, timer logic removed rather than announced.
- **Verification:** All six timers verified at App.tsx:491/496/502/510/524 (4000ms) and :505 (8000ms on the web 'available' branch, whose render at 1173-1176 contains the actionable './update.sh' instruction); the Tauri 'available' branch (492-493) persists indefinitely, proving the asymmetry. No 2.2.1 exception applies and re-clicking is not an Extend mechanism (each window stays ≤8s, no warning, <20s); the 'up-to-date' answer and the error message vanish in 4s leaving the footer indistinguishable from never having clicked. Distinct root cause (content-set time limits, 2.2.1) from confirmed i=35 (live-region semantics, 4.1.3) and absent from both findings files, so not a duplicate; moderate is fair given it compounds i=35 on the same user-initiated flow.

### F021 — No h1, most views have no headings, and heading levels are inconsistent across tabs
- **Where:** `frontend/src/App.tsx:570` · **WCAG:** 1.3.1 Info and Relationships A · **Found by:** audit:aria
- **Issue:** The app title "SnowRaven" is styled <span>s in a <div> (App.tsx:570-580) — no h1 and no banner landmark. Settings, Life List, Species Detail, and Map Explorer contain no heading elements at all (section titles like "Appearance"/"Map Overlays" are styled divs), so SR heading navigation lands on nothing in four major views. Where headings do exist they are inconsistent: ListComparer.tsx:184 and ChecklistComparer.tsx:340 use h1 inside tab content while Checklists.tsx:522, BirdingStats.tsx:424 and NamedBirds.tsx:128 use h2; SetupRequired.tsx:31 (the empty state of 8 tabs) styles its title as a div. Additionally the main nav loses its landmark: role="tablist" on the <nav> (TabNav.tsx:105-108) overrides the navigation role, and in dropdown mode there is no nav element at all.
- **Fix:** undefined
- **Verifier refinement:** The suggested heading plan is sound (h1 title in <header>, h2 per view incl. SetupRequired, h3 section cards — statsPrimitives.tsx already uses h3 for stats cards, so follow that pattern), but it omits the landmark half of its own description: also move role="tablist" off the <nav> in TabNav.tsx onto an inner div so the navigation landmark survives, and wrap the dropdown-mode trigger+listbox in the same <nav aria-label="Main navigation">.
- **Verification:** All claims check out: the title is styled spans in a div (App.tsx:570-580, no h1/header element); grep shows zero heading elements in Settings, LifeList, SpeciesDetail, MapExplorer and SetupRequired (title is a styled div, SetupRequired.tsx:31); ListComparer:184/ChecklistComparer:340 use h1 in tab content while Checklists:522/BirdingStats:424/NamedBirds:128 use h2; TabNav.tsx:105-108 puts role="tablist" on the <nav> (killing the navigation landmark) and the dropdown variant renders no nav at all. Heading navigation is a primary SR strategy and four major views expose nothing, so this is genuinely user-impacting at 1.3.1 Level A; moderate is the right tier since tablist/main/contentinfo landmarks still give a navigation path.

### F022 — Entire Weather tabpanel is an aria-live aria-atomic region
- **Where:** `frontend/src/App.tsx:591` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** audit:aria
- **Issue:** The whole weather panel carries aria-live="polite" aria-atomic="true" (App.tsx:591-592). With aria-atomic, ANY mutation — the button flipping to "Looking up…", Copy → "Copied!", the tide box changing state — re-announces the entire panel content (label, help text, full weather <pre>, tide section). The nested role="alert" (:720) and role="status" elements (:837-845) inside the live region then announce the same changes a second time. Screen-reader users get long duplicate read-outs after every interaction on the default tab.
- **Fix:** undefined
- **Verification:** App.tsx:587-592 carries aria-live="polite" aria-atomic="true" on the entire weather tabpanel, with nested role="alert" (:720) and role="status" (:837-845) regions inside it — every state flip (Looking up…, Copied!, tide states, result insertion) re-announces the full panel atomically plus a duplicate from the scoped child region. This is the default tab, so the verbosity hits every screen-reader session; moderate is correct (annoyance/verbosity, not blocked function). ACCESSIBILITY.md describes the live region as a feature ('announced politely'), not as a known exception, so not disclosed. The suggested fix (drop the panel-level live attrs, keep the scoped children, add a small persistent polite region for the copy confirmation) is correct and matches the existing scoped role=status/alert pattern.

### F023 — Map fullscreen overlay: no Escape exit and focus tabs into content obscured behind the overlay
- **Where:** `frontend/src/App.tsx:1031` · **WCAG:** 2.4.3 Focus Order A (also 2.4.11 Focus Not Obscured Minimum, WCAG 2.2 AA) · **Found by:** audit:focus
- **Issue:** Fullscreen turns the Map Explorer tabpanel into `position: fixed; inset: 0; zIndex: 1200` (App.tsx:1031-1034) with body scroll locked (App.tsx:390-395), but no keydown handler exits it (grep confirms no Escape handler in App.tsx; the only Escape handlers in the app are the sidebar trap, HelpDocs, the combobox, TabNav, WelcomeScreen). The page chrome (tab bar, footer links, other panels' controls) stays in the DOM and in the tab order BEHIND the overlay, so a keyboard user who tabs past the map's controls watches focus vanish under the map — the focus indicator is rendered but fully obscured. Exit is possible only by finding the Exit-fullscreen FAB again. Escape is the universally expected exit for a viewport-takeover, and the obscured-focus walk is disorienting.
- **Fix:** undefined
- **Verifier refinement:** Put the Escape handling inside MapExplorer rather than App: MapExplorer owns onToggleFullscreen AND the sidebarOpen filters trap (MapExplorer.tsx:187-221), which already listens for Escape on document — an independent App-level listener would close the filters panel and exit fullscreen on the same keypress. Exit fullscreen on Escape only when sidebarOpen is false (innermost layer first), returning focus to the fullscreen FAB. For the obscured-focus half, React 19 (in use, ^19.2.5) supports the boolean inert prop — set inert on the header div, TabNav, footer <p>, and sibling tabpanels while mapFullscreen && activeTab === 'map-explorer'.
- **Verification:** Fullscreen overlay styles (App.tsx:1031-1033) and body scroll lock (:390-395) exist exactly as cited; grep confirms no Escape handler or inert/focus-trap for fullscreen anywhere (MapExplorer.tsx:200's Escape handler is the mobile filters-panel trap only). The footer links/buttons and tab bar stay focusable behind the fixed z-1200 panel, so the obscured-focus walk is real; exit is FAB-only. Fairly flagged under 2.4.11 (WCAG 2.2 AA) with a real keyboard-UX gap; moderate is right since a keyboard exit does exist.

### F024 — Update-check outcomes are not reliably announced ('available' and 'error' not at all)
- **Where:** `frontend/src/App.tsx:1156` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** audit:aria
- **Issue:** The footer update checker swaps conditionally-rendered spans per state. 'checking' (:1148), 'up-to-date' (:1154) and 'downloading' (:1180) have aria-live="polite" but each span MOUNTS already containing its text — newly-inserted live regions with initial content are inconsistently announced by AT. The two states that matter most, 'available' (:1156-1178, with its Install update button) and 'error' (:1190), have no live semantics at all, so a screen-reader user who activates "Check For Updates" may hear nothing whatsoever.
- **Fix:** undefined
- **Verification:** Verified all five state branches: 'checking' (:1148), 'up-to-date' (:1154), 'downloading' (:1180) mount aria-live spans already containing their text, and 'available' (:1156-1178) and 'error' (:1190) have no live semantics at all — a screen-reader user activating Check For Updates can hear nothing for the two outcomes that matter (the idle button also unmounts, so there's no visual-focus fallback either). Moderate is fair: explicit user action with potentially zero feedback, on a feature central to desktop updating. The suggested fix (one persistent always-mounted aria-live span whose text changes across states) is the standard correct pattern; keep the Install-update button as a sibling outside the live span so its mount doesn't depend on live-region timing.

### F025 — Desktop updater: download progress has no progressbar semantics and mutates a polite live region on every network chunk; the flow ends in an unannounced self-restart ('ready-to-restart' is dead UI set after relaunch() has begun)
- **Where:** `frontend/src/App.tsx:1179` · **WCAG:** 4.1.2 Name, Role, Value A / 4.1.3 Status Messages AA (3.2.2 advisory) · **Found by:** gap:Tauri desktop shell acce
- **Issue:** Desktop-only updater states beyond verified-finding i=35's scope (which covered only the unreliable mount-with-text announcement of checking/up-to-date/downloading and the missing announcements for 'available'/'error' — not re-reported here): (1) No progressbar semantics: the 'downloading' state (App.tsx:1179-1186) conveys progress purely as text ("Downloading… N%") in an aria-live="polite" span, with no role="progressbar", aria-valuenow, aria-valuemin/max, or aria-busy — AT cannot query the operation's progress as a value. (2) Unthrottled live-region churn: handleInstallUpdate's callback (App.tsx:518-520) calls setUpdateStatus on EVERY 'Progress' chunk event from update.downloadAndInstall (lib/tauri/updateManager.ts:36-42), so the live region's text content mutates dozens-to-hundreds of times across a multi-megabyte universal-bundle download; polite regions queue each text change, so VoiceOver/NVDA either chatter stale percentages for the entire download or coalesce unpredictably. (3) The terminal state is imperceivable: updateManager.downloadAndInstall() awaits relaunch() — which terminates the process — BEFORE returning (updateManager.ts:49-50), so setUpdateStatus({kind:'ready-to-restart'}) at App.tsx:521 runs only as the process is already exiting; the "Update installed — restarting…" span (App.tsx:1187-1189) is statically dead UI, and even if it paints for a frame it carries no live semantics. The resulting SR experience: "Downloading… 97%", then the application silently quits and relaunches with no completion message and no prior notice — the trigger is labeled only "Install update" (App.tsx:1169), nothing warns of the immediate automatic restart. Additional note, NOT counted as a new finding: the 'idle' "Check For Updates" button (App.tsx:1129-1146) and the "Install update" button unmount on activation, dropping keyboard focus to <body> — two further locations of the unmount-on-activation pattern already recorded as verified-finding i=28, whose location list omits the footer. needs-runtime-verification: whether the 'ready-to-restart' frame ever paints before process exit, and the actual VoiceOver/NVDA behavior on the per-chunk live updates.
- **Evidence:** App.tsx:1180-1185 `<span aria-live="polite">…{updateStatus.progress !== null ? `Downloading… ${Math.round(updateStatus.progress * 100)}%` : 'Downloading update…'}` (no progressbar role); App.tsx:518-520 setUpdateStatus inside the per-chunk onProgress; updateManager.ts:36-43 onProgress fires per 'Progress' event, :49-50 `const { relaunch } = await import('@tauri-apps/plugin-process'); await relaunch();` before the function returns; App.tsx:1187-1189 plain span with no live attributes.
- **Fix:** Give the downloading indicator role="progressbar" with aria-valuemin={0} aria-valuemax={100} aria-valuenow={Math.round(progress*100)} (and aria-label="Downloading update"), and throttle state/announcement updates (e.g. update only when the rounded percent crosses a 5-10% step, or announce milestones at 25/50/75/100 via the single persistent polite live region that finding i=35 already recommends for the footer). Announce intent up front: on Install activation, push "Downloading update — SnowRaven will restart automatically when finished" into that live region, and/or rename the button "Install update and restart". If a perceivable completion state is wanted, split relaunch() out of downloadAndInstall(): set 'ready-to-restart', announce it assertively, then call relaunch() after a short delay.
- **Verifier refinement:** Fix is sound and convention-compatible: add role="progressbar" with aria-valuemin/max/now, throttle updates to ~5% steps, announce the impending automatic restart up front (or rename the button 'Install update and restart'), and optionally split relaunch() out of downloadAndInstall() so App can set/announce 'ready-to-restart' before relaunching — CLAUDE.md only requires relaunch() (not exit(0)) after downloadAndInstall returns, which this preserves. Route announcements through the single persistent live span from i=35's fix.
- **Verification:** All three sub-claims verified in code: the 'downloading' span (App.tsx:1180-1185) is a text-only aria-live region with no progressbar role/values; setUpdateStatus fires on every 'Progress' chunk (App.tsx:518-520, updateManager.ts:36-42) so the polite region's text mutates per network chunk; and updateManager.ts:49-50 awaits relaunch() — which exits the process — before returning, making the 'ready-to-restart' state at App.tsx:521 and its non-live span (1187-1189) statically dead, so the app silently quits at ~100% with the trigger labeled only 'Install update'. Genuinely distinct from confirmed i=35 (announcement reliability of mounted spans, not value semantics, churn, or the restart) and from i=70 (keystroke churn at a different location); not in either findings file. Moderate is right: a screen-reader user gets chatter then an unexplained app disappearance on a desktop-critical flow.

### F026 — Atlas block popups are pointer-only — no keyboard route to open them
- **Where:** `frontend/src/components/AtlasLayer.tsx:111` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:maps
- **Issue:** Atlas block info popups open exclusively via a MapLibre map.on('click','sr-atlas-fill') handler on the GL fill layer (AtlasLayer.tsx:109-132). There is no sidebar list, search, or other focusable path to a block, so the popup's content — block name, the eBird atlas portal link, and 'Highest breeding code … N of your breeding records in this block' — is unreachable without a pointer. The breeding data shown in the popup is not surfaced anywhere else in the app (the sidebar legend only explains tier colors). The popup JSX itself is fine (escaped, keeps the default labeled close button), but keyboard users can never open it.
- **Fix:** undefined
- **Verification:** Verified the cited handler (AtlasLayer.tsx:109-132) is the sole path to setSel, and grep confirms BlockBreeding/breedingByBlock data is surfaced nowhere else in the app, so the popup content is genuinely pointer-only. The suggested fix matches existing conventions (InViewMarkerList in map/MapSidebarUI.tsx, blocksInBounds viewport scoping, cap+hint pattern) and correctly notes only shaded blocks need listing. Not disclosed in ACCESSIBILITY.md; moderate severity appropriate. (Duplicate of finding 20.)

### F027 — Atlas block popup is only openable by pointer click on the GL fill layer
- **Where:** `frontend/src/components/AtlasLayer.tsx:123` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:keyboard
- **Issue:** The California atlas block popup (block name, code, and the link to the eBird Atlas portal for that block) opens solely via map.on('click', 'sr-atlas-fill', …) — a canvas hit-test with no DOM focus path. Unlike the sighting pins, hotspots, and targets, there is no keyboard-accessible in-view list for atlas blocks, so the block-details/atlas-portal-link functionality is unavailable without a pointer. The atlas TOGGLE switches themselves are fine (real role="switch" buttons).
- **Fix:** undefined
- **Verification:** AtlasLayer.tsx:109-132 opens the block popup only via map.on('click','sr-atlas-fill') canvas hit-testing; I verified InViewMarkerList is used solely for sightings (MapExplorer.tsx:943) and hotspots (:1059) and that breedingByBlock is consumed nowhere but this popup, so no keyboard route to the block name, breeding summary, or eBird atlas portal link exists. ACCESSIBILITY.md's map-markers paragraph documents the sightings/hotspots/targets keyboard lists but never mentions atlas blocks, so this is an undisclosed gap. Real 2.1.1 failure; moderate is fair for a supplementary overlay feature. (Duplicate of finding 58.)

### F028 — Map chrome text inherits MapLibre's fixed `font: 12px/20px Helvetica` — does not scale with the in-app Text Size control
- **Where:** `frontend/src/components/AtlasLayer.tsx:213` · **WCAG:** 1.4.4 Resize Text AA · **Found by:** audit:resize
- **Issue:** maplibre-gl.css (imported at SnowMap.tsx:7) sets `.maplibregl-map{font:12px/20px Helvetica Neue,…}`. The app's `.maplibregl-popup-content` override in globals.css:568 themes colors but sets no font-size or family, so any map-rendered text without an explicit rem size stays at a hard 12px Helvetica regardless of the Text Size setting. Affected: (1) the AtlasLayer block popup title — `<div style={{ fontWeight: 600 }}>{sel.name}</div>` at AtlasLayer.tsx:213 and the linked variant at :208-211 have no fontSize; (2) the attribution credits on every map (SnowMap.tsx:121 `attributionControl={{ compact: true }}` — provider links render at fixed 12px when expanded); (3) the AtlasLayer popup's default close button (it is the only popup that doesn't pass `closeButton={false}`, so users get maplibre's small fixed-size '×' — also a WCAG 2.2 2.5.8 Target Size concern). This matters more here than in a typical web app: ACCESSIBILITY.md:37 states the Text Size control is "especially useful in the desktop app, which has no browser zoom of its own", so on desktop this text is genuinely un-resizable. All other popups (SightingMarkers, HotspotMarkers, TargetMarkers, SightingsMap, the BirdingStats geo popup) set explicit rem sizes and are fine.
- **Fix:** undefined
- **Verifier refinement:** The CSS parts are right: extend the existing globals.css override with `.maplibregl-popup-content { font-size: 0.8125rem; font-family: var(--font-sans); }` and add `.maplibregl-ctrl-attrib { font-size: 0.6875rem; }`, and give the AtlasLayer block-name elements an explicit rem fontSize (e.g. 0.8125rem). But do NOT pass closeButton={false} on the AtlasLayer popup: it also sets closeOnClick={false}, so the default close button is its only dismiss affordance and its only keyboard-operable control — removing it would strand the popup open and worsen 2.1.1. Instead keep the button and size it via the existing .maplibregl-popup-close-button override (e.g. add `font-size: 1rem; padding: 2px 8px;`), which also addresses the 2.5.8 target-size concern.
- **Verification:** Every claim checks out: maplibre-gl.css sets .maplibregl-map{font:12px/20px Helvetica Neue,...}, the globals.css:568 popup override adds no font-size/family, the AtlasLayer block name (:208-213) has no fontSize, .maplibregl-ctrl-attrib inherits the fixed 12px, and the Text Size control scales only rem text via html{font-size:calc(100% * var(--sr-text-scale))} (globals.css:378). On desktop — which ACCESSIBILITY.md:37 itself says has no browser zoom — this text is un-resizable, a genuine 1.4.4 failure there (web is saved by browser zoom); at 200% the popup title would render smaller than its own muted secondary text. AtlasLayer is indeed the only popup of six without closeButton={false}, but that part of the fix is wrong (see fixNote).

### F029 — In-page jump links scroll the view but never move keyboard focus
- **Where:** `frontend/src/components/BirdingStats.tsx:449` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:keyboard
- **Issue:** The Statistics "Jump to section" nav (9 anchors) calls e.preventDefault() + smoothScrollIntoView, and LifeList's "media comments" anchor does the same (LifeList.tsx:505). preventDefault suppresses the browser's native sequential-focus relocation for fragment navigation, and the scroll targets are plain divs without tabindex="-1" (e.g. MediaCommentsSection.tsx:40), so after "jumping" a keyboard user's focus is still on the link at the top — the next Tab continues from the nav and they must traverse every control between anyway. The jump feature is effectively visual-only for keyboard users.
- **Fix:** undefined
- **Verifier refinement:** Put tabIndex={-1} on the SectionCard root div (statsPrimitives.tsx) so all nine Statistics targets inherit it, add the same to the #media-comments div in MediaCommentsSection.tsx, and add a jumpTo(el) wrapper in lib/scroll.ts that calls smoothScrollIntoView(el) then el.focus({ preventScroll: true }); switch the three preventDefault call sites (BirdingStats.tsx:449, LifeList.tsx:505) to it. Add :focus-visible-only outline styling (or outline: none on [tabindex="-1"]) so the programmatic focus doesn't paint the green focus ring on the whole card for mouse users.
- **Verification:** BirdingStats.tsx:449 (and LifeList.tsx:505) call e.preventDefault() + smoothScrollIntoView, and lib/scroll.ts only scrolls — it never moves focus; the targets are plain divs (SectionCard in statsPrimitives.tsx, MediaCommentsSection.tsx:40) with no tabindex. preventDefault suppresses the browser's native fragment-navigation focus relocation, so after a jump the next Tab resumes from the nav chip — the feature is visual-only for keyboard users. The suggested fix (tabindex=-1 on targets + focus({preventScroll:true}) folded into the shared scroll helper) is correct and convention-conforming.

### F030 — Chart hover tooltips are not hoverable (1.4.13): cursor-following wrapper with pointer-events none on all five recharts Tooltip mounts
- **Where:** `frontend/src/components/BirdingStats.tsx:575` · **WCAG:** 1.4.13 Content on Hover or Focus AA (failure F95 — content on hover not hoverable) · **Found by:** gap:WCAG 1.4.13 Content on H
- **Issue:** All five recharts <Tooltip> mounts — BirdingStats.tsx:575 and :601 (life-list accumulation area charts), :1276 (checklists-by-observer bar chart), :1768 (media-over-time line chart), and MediaStatsSections.tsx:320 (media-by-hour stacked bar chart) — pass only contentStyle/formatter/labelFormatter, so recharts 3.8.1 defaults apply (trigger 'hover', no fixed position, no wrapperStyle). The library renders the tooltip in a wrapper hardcoded to pointerEvents:'none' that follows the cursor at a ~10px offset (node_modules/recharts/es6/component/TooltipBoundingBox.js:77), so the pointer can never be moved over the tooltip content: approaching it repositions the box and swaps its data to a different point, and at the plot edge the move exits the hover area and the tooltip vanishes. That fails 1.4.13's hoverable condition, which exists for screen-magnification users who must bring revealed content under the lens to read it — the media chart's five-line tooltip in particular can extend past a 400% lens and cannot be panned to without the content changing. The other two 1.4.13 conditions PASS in this recharts version (verified in installed source, see passes), which bounds the severity, as does the trigger-adjacent placement; and because every chart wrapper is role="img" with an aria-label summary, this is a sighted-pointer-user issue only (per-point values are otherwise unavailable as visible text anywhere in the UI). The tooltip is also clamped inside the chart viewBox (allowEscapeViewBox default false), so it can overlay bars, axis ticks, and the media chart's in-container legend — acceptable under 1.4.13 only because the Escape dismissal works. ACCESSIBILITY.md makes no claim about hover content, so this is an undisclosed gap rather than a false statement.
- **Evidence:** BirdingStats.tsx:575-582/:601-605/:1276-1280/:1768-1773 and MediaStatsSections.tsx:320-323 pass no content/trigger/position/wrapperStyle props; recharts 3.8.1 TooltipBoundingBox.js:77 sets `pointerEvents: 'none'` in positionStyle and translates the box from the live cursor coordinate via getTooltipTranslate; defaultTooltipProps in Tooltip.js:36-58 sets `trigger: 'hover'`.
- **Fix:** Pick one per-chart approach and apply it to all five mounts: (a) anchor the tooltip with a fixed `position={{ x, y }}` (e.g. a chart corner) plus `wrapperStyle={{ pointerEvents: 'auto' }}` — wrapperStyle is spread after the hardcoded style so it wins — making the box stationary and physically hoverable; or (b) switch to `trigger="click"` (supported in recharts 3), which makes the content activation-triggered and exempt from 1.4.13 entirely, matching the posture the app already takes for map popups; or (c) surface the per-point values in a visible-text alternative (a small data-table disclosure under each chart). Mention chart tooltips in ACCESSIBILITY.md once fixed.
- **Verifier refinement:** Option (a) is verified workable in the installed source: outerStyle = {...positionStyle, visibility..., ...props.wrapperStyle}, so wrapperStyle={{ pointerEvents: 'auto' }} wins, but it only satisfies 1.4.13 combined with a fixed position prop (otherwise the box still tracks the cursor). Option (b) trigger="click" is the lowest-risk route and matches the app's map-popup posture.
- **Verification:** All five Tooltip mounts (BirdingStats.tsx:575/601/1276/1768, MediaStatsSections.tsx:320) pass only contentStyle/formatter/labelFormatter, and the installed recharts 3.8.1 hardcodes pointerEvents:'none' plus cursor-following translate in TooltipBoundingBox.js and trigger:'hover' in defaultTooltipProps — so the hover content can never be hovered, an F95 failure of 1.4.13. The bounding claims also check out: Escape dismissal exists in TooltipBoundingBox, every chart wrapper is role=img with an aria-label, and wrapperStyle is spread after the hardcoded positionStyle so fix option (a) works. No existing finding covers 1.4.13/chart tooltips and ACCESSIBILITY.md says nothing about hover content.

### F031 — Hardcoded milestone-chip and rank-icon colors below AA (2.76–4.16:1), bypassing the token system
- **Where:** `frontend/src/components/BirdingStats.tsx:672` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** The Firsts & Milestones chips hardcode hex colors (also violating the project's tokens-only rule): the tier-1 date link #5EA07C on #E8F5EE = 2.76:1 (10px text), tier-1 number #2D8653 on #E8F5EE = 4.03:1, tier-2 date #3E7A56 on #D8EDE4 = 4.16:1 (tier-3/4 pass: 4.92–6.37:1). These values are identical in both themes since the chips don't adapt. Separately, RankIcon (statsPrimitives.tsx:97-98) prints white 10px-bold rank numbers on hardcoded #3B82F6 = 3.68:1 (the #2D8653 circle squeaks by at 4.52:1).
- **Fix:** undefined
- **Verifier refinement:** Tokenize the milestone palette into --sr-milestone-* tokens defined in BOTH :root and [data-theme="dark"] (globals.css), darkening only the failing date colors (tier-1 date >= #43755C on #E8F5EE = 4.76:1, tier-2 date similarly); the tier-1 number (20px bold = large text) already passes at 4.03:1 >= 3:1 and needs no change. Do NOT use var(--sr-chart-blue-dark) for the RankIcon square as suggested — it resolves to #3B82F6 in the dark theme, which is 3.68:1 under the white 10px-bold rank text and still fails; instead add a dedicated token (e.g. --sr-rank-square: #1D4ED8 in both themes, 6.70:1 under white) and use var(--sr-accent... ) tokens for the circle.
- **Verification:** Hardcoded hexes verified at BirdingStats.tsx:671-677 and statsPrimitives.tsx:95-98 (violating the CLAUDE.md tokens-only rule), and my recomputation matches: tier-1 date 2.76:1 and tier-2 date 4.16:1 at 10px (need 4.5), RankIcon white-on-#3B82F6 3.68:1 at 10px bold (need 4.5). One overstatement: the tier-1 threshold number is 1.25rem/700 = 20px bold = WCAG large text, so its 4.03:1 PASSES at the 3:1 large-text threshold — the real failures are the two date links and the RankIcon square. Still a fair moderate: real sub-AA text plus a standing-convention violation.

### F032 — Disclosed chart-bar-label exception verified — but it understates the dark-theme failures (down to 1.29:1) and a working fix already exists in-code
- **Where:** `frontend/src/components/BirdingStats.tsx:1125` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast · **Already disclosed in ACCESSIBILITY.md**
- **Issue:** ACCESSIBILITY.md's known exception ('a few dense data-visualization labels (percentage figures printed inside saturated chart bars) do not yet meet AA in every case') is accurate in direction but materially understates scope. Computed: LIGHT — white on --sr-graph-audio 2.15:1 (protocol bar), white on --sr-chart-slate 2.56:1, white on --sr-graph-photo 3.68:1, white on --sr-graph-video 4.23:1. DARK — --sr-text on --sr-chart-blue-light 1.29:1 (comment/species-notes bars, BirdingStats.tsx:1384/1405), white on graph-audio 1.44:1, white on --sr-accent 1.92:1 (hardcoded '#fff' at protocol bar :1181), white on graph-photo 2.54:1, white on chart-slate 2.56:1, --sr-text on chart-slate 2.33:1 (:1361), white on chart-blue-dark 3.68:1 (:1125). In dark mode it is most in-bar labels, not 'a few'. Notably, line 1358 already uses var(--sr-on-accent) which passes in BOTH themes (4.52/7.75) — the failures are the literal '#fff' and var(--sr-text) usages that don't adapt.
- **Fix:** undefined
- **Verification:** Independently recomputed every cited pair with my own WCAG relative-luminance implementation; all match (dark --sr-text on --sr-chart-blue-light = 1.29:1 at lines 1384/1405, white on dark graph-audio 1.44, white on dark accent 1.92 via the hardcoded '#fff' at 1181, dark text-on-slate 2.33 at 1361, white on dark chart-blue-dark 3.68 at 1125), and line 1358's --sr-on-accent does pass both themes (4.52/7.75) proving the fix pattern exists in-code. Labels are 11-12px weight-600, so the 4.5:1 normal-text threshold applies — in dark mode most in-bar labels fail, some catastrophically, which materially exceeds the 'a few cases' wording in ACCESSIBILITY.md line 31. Confirmed at moderate: the percentages are supplemental (raw counts appear in the adjacent header text), but 1.29:1 is effectively invisible text.

### F033 — Repeated "↗" checklist links named only by the arrow glyph
- **Where:** `frontend/src/components/BirdingStats.tsx:1332` · **WCAG:** 2.4.4 Link Purpose (In Context) A / 2.4.6 Headings and Labels AA · **Found by:** audit:sr-content
- **Issue:** Three Statistics sections render external eBird checklist links whose only content is the character "↗" with title="Open checklist": the effort superlative cards (line 1332), single-checklist-bird chips (line 1564), and one-and-done bird chips (line 1589). The accessible name is computed from content, so every one of these (potentially dozens in the chip lists) is announced as "north east arrow, link" or skipped entirely depending on SR symbol verbosity — identical and non-descriptive. title only provides a description, not the name, when content exists.
- **Fix:** undefined
- **Verification:** All three sites verified (BirdingStats.tsx:1332, 1564, 1589): anchor content is solely the ↗ character with title="Open checklist", so per accname computation the name is the glyph (title only supplies the description when content exists) — dozens of identical, non-descriptive links in the chip lists, indistinguishable in a screen-reader links list. The cited in-repo precedent is real: NamedBirdRow.tsx:94 already uses aria-label={`Open eBird checklist ${s.submissionId} (opens in a new tab)`} with an aria-hidden icon. Moderate is fair (2.4.4 in-context is arguable for the chip-adjacent bird name, but the repeated-identical-name failure mode is genuine) and the suggested fix matches the codebase pattern exactly.

### F034 — ChecklistBadges convey present/absent state via aria-label on generic spans; group unnamed
- **Where:** `frontend/src/components/ChecklistBadges.tsx:22` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** Each Badge is a role-less <span aria-label={presentTitle|absentTitle}>. ARIA prohibits naming on the generic role, and screen readers inconsistently honor aria-label on plain spans in browse mode — so SR users may hear only the visible "Photo Audio Video Breeding Weather Tide" text with the present/absent state (the badge's entire point) dropped. The wrapping role="group" (:53) also has no accessible name. The tests assert the aria-labels exist but can't catch the exposure problem.
- **Fix:** undefined
- **Verification:** Verified at ChecklistBadges.tsx:22-24 the Badge is a role-less span whose aria-label (prohibited on role=generic, inconsistently exposed by SRs) is the sole programmatic carrier of present/absent state — both states announce identically when it is dropped — and the role="group" at line 53 is indeed unnamed. Not disclosed in ACCESSIBILITY.md (only the chart-bar contrast exception is). The suggested fix matches the codebase's own established pattern (Checklists.tsx:276 Badge uses role="img" aria-label; Checklists.tsx:69 names its role="group"), so it stands as-is.

### F035 — Media count badges in checklist comparer use aria-label on a generic <span> — info silently lost to screen readers
- **Where:** `frontend/src/components/ChecklistComparer.tsx:57` · **WCAG:** 1.1.1 Non-text Content A · **Found by:** audit:sr-content
- **Issue:** MediaIcons renders per-species photo/audio/video presence as lucide SVG icons inside a plain <span aria-label={`${n} ${label}`}>. ARIA prohibits naming on role=generic, so most browser/SR pairs ignore the aria-label, and the SVGs themselves have no text alternative — the media information (which types exist and counts) is conveyed to sighted users only. The same file's Checklists tab equivalent (Checklists.tsx:276 Badge) does this correctly with role="img", and ChecklistBadges.tsx is correct too — this one component missed the pattern.
- **Fix:** undefined
- **Verifier refinement:** The suggested role="img" fix is correct and matches the in-repo Badge pattern; while there, reuse the already-built pluralized text for the name so SR output matches the tooltip: <span role="img" aria-label={`${n} ${label.toLowerCase()}${n > 1 ? 's' : ''}`}> (the current aria-label would announce '2 Photo').
- **Verification:** ChecklistComparer.tsx:57 puts aria-label on a bare span (role generic, where ARIA prohibits naming, so exposure is unreliable) wrapping lucide SVGs that carry no text alternative; no other text path in SideCell conveys media presence or counts. The repo's own correct pattern exists at Checklists.tsx:275 (Badge with role="img" aria-label), confirming this component missed it. Moderate is right: real informational content (a headline comparer data point) with no reliable SR exposure, but not a control or blocker.

### F036 — One-shot buttons unmount on activation, dropping keyboard focus to <body>
- **Where:** `frontend/src/components/Checklists.tsx:205` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:focus
- **Issue:** Repeated pattern: a focused control disappears when activated, resetting focus to the document body so the keyboard user's next Tab restarts from the top of the page. Locations: 'Show all N' one-shot buttons rendered behind `{!showAll && ...}` — Checklists.tsx:205 (comments) and :718 (checklist rows), MediaCommentsSection.tsx:159, SpeciesDetail.tsx:1347 (species comments); and Settings KeyRow Save/Cancel — Settings.tsx:457 (Cancel) and the post-save `setEditing(false)` at :973 unmount the editing row that contains the focused button. (The toggle-style variants that stay mounted — SpeciesDetail locations/co-occurrence, BirdingStats counties — are fine and are the model to copy.)
- **Fix:** undefined
- **Verifier refinement:** Prefer the stay-mounted toggle ('Show fewer'), matching the existing in-repo model (SpeciesDetail locations/co-occurrence, BirdingStats counties). If moving focus instead, note the revealed rows are plain divs: give the first newly revealed row container tabIndex={-1} and call .focus() on it after the state flip (e.g. via a ref + effect keyed on showAll); for Settings KeyRow, focus the Edit button that replaces the editor.
- **Verification:** ShowAllButton at Checklists.tsx:205 and :718 renders behind {!showAll && ...} and its onClick flips that state, unmounting the focused button; the file contains no useRef or .focus() call, so focus falls to <body> and the next Tab restarts from the page top. The same pattern is real at MediaCommentsSection.tsx:159, SpeciesDetail.tsx:1347, and Settings KeyRow (Cancel :457, post-save setEditing(false) :973). The toggle variants cited as the model do exist in-repo, so the fix direction matches existing conventions.

### F037 — DropZone error state's border and icon are nearly invisible (1.74–1.90:1)
- **Where:** `frontend/src/components/DropZone.tsx:40` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** In the error state the drop zone's border and upload icon switch to --sr-error-muted (#FCA5A5 light): 1.74:1 against the --sr-error-bg fill and 1.90:1 against the surrounding surface, so the state-change boundary/icon fails non-text contrast (dark: icon vs surface 2.74:1; vs error-bg 3.02:1 passes). The role=alert error text is present, but it itself is the marginal 4.41:1 light error-on-error-bg pair reported separately.
- **Fix:** undefined
- **Verification:** DropZone.tsx lines 39-43 and 108 set the error-state border and icon to --sr-error-muted on --sr-error-bg; my own WCAG luminance math reproduces every reported ratio (light 1.74:1 vs error-bg, 1.90:1 vs white surface; dark 2.74:1 vs surface, 3.02:1 vs error-bg), confirming a 1.4.11 failure in light theme and a marginal dark theme. The role=alert text partially conveys the state, so moderate is the right severity. The suggested var(--sr-error) fix is verified in both themes (4.41/4.83:1 light, 7.07/6.40:1 dark) and follows the --sr-* token convention; note it must be applied at both line 40 (border) and line 108 (icon).

### F038 — DropZone inline `outline: 'none'` defeats the global keyboard focus ring
- **Where:** `frontend/src/components/DropZone.tsx:69` · **WCAG:** 2.4.7 Focus Visible AA · **Found by:** audit:keyboard
- **Issue:** The DropZone (role="button", tabIndex=0 — keyboard activation itself works) sets outline: 'none' in its inline style. Inline styles override the stylesheet's [role="button"]:focus-visible { outline: 3px solid var(--sr-accent) } rule (globals.css:546-551, no !important), leaving only the rule's faint 12%-alpha box-shadow glow as the focus indicator; the border/background highlight is driven by mouse-only isHovered state. Keyboard users on the List Comparer get an almost invisible cue on the primary upload control. (Reported here because it directly degrades keyboard operation of an otherwise-correct widget; full focus-visible sweep belongs to that dimension.)
- **Fix:** undefined
- **Verification:** Line 69's inline outline:'none' overrides globals.css:542-551's [role="button"]:focus-visible / [tabindex]:focus-visible ring (no !important), leaving only the rule's rgba(45,134,83,0.12) box-shadow (~1.05:1 over the light surface, effectively invisible), and the component has no focus-driven styling of its own — isHovered is mouse-only. This directly contradicts ACCESSIBILITY.md's claim that every focused control shows a clear green outline, so it is not a disclosed exception. An effectively absent focus indicator on the List Comparer's two upload controls is a real 2.4.7 AA failure, understated at minor; the suggested fix (delete the inline outline:'none') is correct and safe since :focus-visible does not fire on mouse interaction.

### F039 — Help overlay closes without restoring focus to the control that opened it
- **Where:** `frontend/src/components/HelpDocs.tsx:257` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:focus
- **Issue:** HelpDocs correctly focuses its Close button on open and traps Tab (re-querying per keydown — the best trap in the codebase), but on close (Escape at line 257 or the Close button) the overlay unmounts with focus inside it and nothing restores focus: App.tsx:1205 is just `onClose={() => setHelpOpen(false)}`. Focus drops to <body>, so a keyboard user who opened Help from the footer Help button, Settings' 'Open help', or the Welcome screen must Tab from the top of the document to get back to where they were. Standard dialog behavior is to return focus to the invoking element.
- **Fix:** undefined
- **Verifier refinement:** The capture-activeElement-in-HelpDocs approach works because the lazy mount uses `fallback={null}` (focus is still on the opener at first effect time), but the capture effect must be declared BEFORE the existing `closeRef.current?.focus()` effect at line 251, with the restore in that effect's cleanup: `useEffect(() => { const opener = document.activeElement as HTMLElement | null; return () => opener?.focus() }, [])`. This covers Escape, the Close button, and all three openers with no prop plumbing.
- **Verification:** Verified App.tsx:1205 is bare `onClose={() => setHelpOpen(false)}`, HelpDocs has no unmount cleanup, and no activeElement capture/restore exists anywhere in src/ — focus is inside the unmounting overlay (Close button focused on mount) and drops to <body> on both Escape (line 257) and the Close button. All three openers (footer App.tsx:1113, Settings.tsx:1078, WelcomeScreen.tsx:100) are affected. ACCESSIBILITY.md's Focus Management section only covers the map filter panel (which does restore focus), so not disclosed; moderate is the right severity.

### F040 — Closing the Help overlay drops focus to <body> instead of returning to the Help button
- **Where:** `frontend/src/components/HelpDocs.tsx:318` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:keyboard
- **Issue:** HelpDocs correctly focuses its Close button on open and traps Tab, but onClose (Close button or Escape) simply unmounts the overlay (App.tsx:1203-1205 just sets helpOpen false) — focus falls to document.body and the keyboard user restarts from the top of the page. The Map Explorer filter sidebar (filtersButtonRef refocus) and the TabNav dropdown (close(true)) both restore focus correctly; HelpDocs is the inconsistent one. It can be opened from three places (footer, Welcome screen, Settings), so the opener should be remembered.
- **Fix:** undefined
- **Verifier refinement:** Same fix as index 27: a mount-time effect in HelpDocs declared before the closeRef-focus effect, capturing document.activeElement and refocusing it in the cleanup — covers all three openers without prop plumbing.
- **Verification:** This is the same defect as index 27 (exact duplicate, different cited line): App.tsx:1203-1205 just sets helpOpen false, no capture/restore code exists, and focus verifiably falls to <body>; the contrast with the map filter sidebar's correct refocus is accurate. Confirmed, but minor understates it — a keyboard user re-Tabs from the document top after every Help close, and the identical finding at index 27 is rated moderate, so aligned to moderate. Should be merged with index 27 as one finding.

### F041 — Sortable column headers are focusable <th> with no interactive role
- **Where:** `frontend/src/components/LifeListTable.tsx:188` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** Sortable headers are <th tabIndex={0} onClick onKeyDown> with aria-sort (LifeListTable.tsx:188-250, BreedingCodeTable.tsx:120-160). aria-sort updates correctly, but the element exposes role columnheader only — nothing announces it as an activatable control, so a screen-reader user tabbing onto "Entries" or "NB" hears a plain column header and has no cue that Enter/Space re-sorts. The BreedingCodeTable code headers additionally expose only the terse code (e.g. "NB") as the name, with the expansion in title only.
- **Fix:** undefined
- **Verifier refinement:** The button-in-th fix is right (APG sortable-table pattern) but incomplete: also remove tabIndex/onClick/onKeyDown from the <th> and put them on the inner <button type="button"> so there is one tab stop, keep aria-sort on the th, and give the button visible text plus the sort affordance (e.g. aria-label="Sort by Entries" only where the visible label is an icon/terse code). Do NOT instead add role="button" to the th — that would destroy the columnheader role and orphan aria-sort.
- **Verification:** Verified LifeListTable.tsx:188-250: four <th scope="col" tabIndex={0} onClick onKeyDown aria-sort> headers expose only role columnheader, with no role/button conveying activatability, and the headers are the only column-sort mechanism (LifeList.tsx's toggle only switches nameSortMode). BreedingCodeTable.tsx:122-147 shares the pattern as claimed. ACCESSIBILITY.md describes sortable headers as working, not this gap, so not a disclosed exception; moderate severity is fair since keyboard operation and aria-sort do work — the failure is SR discoverability.

### F042 — 'Personal Location' popup badge fails in light theme (2.90:1)
- **Where:** `frontend/src/components/map/HotspotMarkers.tsx:156` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** The hotspot popup's 'Personal Location' badge sets --sr-map-personal (#C9842A) text on --sr-is-target-bg (#FFF7ED): 2.90:1 in light theme at 11px/600 (dark passes, 8.38:1). The pin color token was designed as a map fill, not a text color.
- **Fix:** undefined
- **Verifier refinement:** Swap color: 'var(--sr-map-personal)' for 'var(--sr-is-target-text)' unconditionally — no light-theme branch needed, since the token is theme-scoped (4.88:1 light, 8.30:1 dark, both pass) and --sr-is-target-bg + --sr-is-target-text is already the codebase's badge pairing (LifeList.tsx:529).
- **Verification:** Independently recomputed: #C9842A (--sr-map-personal) on #FFF7ED (--sr-is-target-bg) = 2.90:1 in light theme at 11px/600 — small text, fails the 4.5:1 AA minimum exactly as reported; dark theme (#E0A060 on #1C0F00) = 8.38:1, passes. HotspotMarkers.tsx:156 matches the finding, and the badge is the popup's only textual indicator that the pin is a personal location, so it is meaningful text — moderate stands. Not disclosed: ACCESSIBILITY.md affirmatively claims map popups meet AA and lists only chart-bar percentage labels as the known exception.

### F043 — Light theme: muted text on tinted surfaces falls just under AA (4.27–4.40:1)
- **Where:** `frontend/src/components/map/MapSidebarUI.tsx:24` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** --sr-text-muted #71717A passes on white (4.83:1) but not on the tinted surfaces it is commonly paired with in light theme: on --sr-surface-subtle = 4.40:1 (inactive segmented-control labels in MapSidebarUI SegControl, the basemap switcher buttons in globals.css:246, the missing-media badges in the target popup TargetMarkers.tsx:92, numerous chips); on --sr-quote-bg = 4.27:1 (Named Birds comment block); on --sr-accent-bg = 4.31:1 (secondary text inside selected rows, e.g. the location line of a selected Nearest Targets row, MapExplorer.tsx:1306+1317). Dark theme passes all three (5.27–5.82:1).
- **Fix:** undefined
- **Verification:** Independent WCAG math confirms the ratios: #71717A is 4.397:1 on #F4F4F5, 4.268:1 on #EFF1F3, and 4.310:1 on #E8F5EE (dark theme passes 5.27-5.82), and the surface-subtle and accent-bg pairings genuinely co-occur at small text sizes (SegControl inactive labels MapSidebarUI.tsx:13+24, basemap switcher globals.css:234/246, TargetMarkers.tsx:92 badges, selected-row secondary text MapSidebarUI.tsx:96+109 and MapExplorer.tsx:1306+1317); these are enabled controls, so the inactive-component exemption does not apply. One cited pairing is wrong: the quote-bg comment blocks (NamedBirdRow.tsx:110, Checklists.tsx:350-351) use full-contrast --sr-text, not muted, so that instance never occurs — but the finding stands on the other two surfaces. The proposed #6B6B74 verifies as passing everywhere (4.66-4.80 on the tinted surfaces, 5.28 on white), and changing only the :root value is convention-clean since the token already exists in both themes; not covered by ACCESSIBILITY.md's chart-bar-label exception.

### F044 — Map popups cannot be dismissed with the keyboard (no close button, no Escape)
- **Where:** `frontend/src/components/map/SightingMarkers.tsx:111` · **WCAG:** 2.1.1 Keyboard A · **Found by:** audit:keyboard
- **Issue:** Every app-rendered map popup passes closeButton={false}, and MapLibre's Popup has no built-in Escape handling (verified: no keydown/Escape in maplibre-gl/src/ui/popup.ts), so the only dismissal is a pointer click on empty map. The Map Explorer in-view list rows open popups by keyboard (good) but never close them: onActivate always sets the locId (MapExplorer.tsx:504-512) — re-activating a row with aria-pressed="true" does not toggle it off, so the pressed state is also a one-way lie. Affected popups: SightingMarkers.tsx:111, HotspotMarkers.tsx:141, TargetMarkers.tsx:79, SightingsMap.tsx:61, BirdingStats.tsx:875. A stuck-open popup overlays the map and inserts extra tab stops (eBird links inside it). The AtlasLayer popup (AtlasLayer.tsx:205) is the one that keeps the default close button — proving the styled close button is available.
- **Fix:** undefined
- **Verification:** All five cited popups pass closeButton={false} at the exact lines, maplibre-gl's bundle contains zero Escape handling, and MapExplorer's only Escape handler is the mobile-sidebar focus trap — so popups opened via the keyboard-accessible in-view lists (which one-way set locId, never toggling aria-pressed rows off) have no keyboard dismissal at all. The HotspotMarkers popup adds real eBird-link tab stops, making this user-impacting under WCAG 2.1.1. ACCESSIBILITY.md describes the keyboard open path but does not disclose the missing dismissal; the suggested fix is accurate and convention-aligned (AtlasLayer.tsx:205 keeps the default close button, a real <button aria-label="Close popup"> already themed via .maplibregl-popup-close-button with var(--sr-text-muted) in globals.css:576).

### F045 — Missing-media type icons in Media Targets popup and marker chips have no text alternative anywhere
- **Where:** `frontend/src/components/map/TargetMarkers.tsx:93` · **WCAG:** 1.1.1 Non-text Content A · **Found by:** audit:sr-content
- **Issue:** The whole point of a media target is which media types (photo/audio/video) the user is missing, but that information is conveyed exclusively by unnamed inline SVGs: in the target popup (TargetMarkers.tsx:91-97, bare <svg> per missing type, no aria-hidden, no label), in the on-map chip labels (TargetMarkers.tsx:62, MEDIA_ICONS SVG strings injected via dangerouslySetInnerHTML), and the keyboard-accessible "Nearest Targets" sidebar rows (MapExplorer.tsx:~1315-1318) show only name/location/distance — they omit missing types entirely. A screen-reader user can find a target but never learn which media it is missing. This also weakens the ACCESSIBILITY.md claim that icon-only information "carries a hidden text label so it is never lost to a screen reader."
- **Fix:** undefined
- **Verifier refinement:** The suggested fix is right; extend it slightly: also add aria-hidden="true" into the three MEDIA_ICONS SVG strings in lib/mapExplorerFormat.ts so browse-mode screen readers don't hit unnamed images on the on-map chips, and for the sidebar rows reuse the existing sr-only span pattern already used for the recency dot at MapExplorer.tsx:1311-1313 (e.g. <span className="sr-only">Missing photo, audio</span>).
- **Verification:** Verified all three cited surfaces: popup spans (TargetMarkers.tsx:91-97) hold bare unnamed SVGs with no role/aria-label/sr-only text, chip labels inject raw MEDIA_ICONS SVG strings via dangerouslySetInnerHTML (line 62, mapExplorerFormat.ts:30-34) with no ARIA, and the Nearest Targets sidebar rows (MapExplorer.tsx:1315-1319) render only name/location/distance — missingTypes never appears as text on any keyboard/SR path. The type-filter pills filter the set but don't convey per-target info, so the feature's core information is icon-only, contradicting ACCESSIBILITY.md line 19's hidden-label claim. Moderate is the right severity: SR users can reach targets but lose the missing-media detail.

### F046 — Map Explorer mobile filter focus trap snapshots focusables once — focus can escape as content changes
- **Where:** `frontend/src/components/MapExplorer.tsx:192` · **WCAG:** 2.1.2 No Keyboard Trap A · **Found by:** audit:keyboard
- **Issue:** The mobile filter sidebar trap queries focusable elements once when the overlay opens and captures first/last in the closure (lines 192-196). The sidebar's content is highly dynamic — the Filters accordion expands, the hotspot legend and in-view marker lists appear after a fetch and update on every pan/zoom, Show-all reveals rows. Once elements exist after the stale `last`, Tab from the actual last element is not intercepted and focus walks out of the modal into the backdrop-covered page; elements past the stale `last` are also skipped by the wrap. Escape + focus return (lines 200-203) work correctly. HelpDocs already implements the correct pattern (re-query inside the keydown handler, HelpDocs.tsx:261-263).
- **Fix:** undefined
- **Verification:** Verified: lines 192-196 snapshot focusables once with effect deps [sidebarOpen], while the sidebar holds the collapsible Filters accordion, async hotspot/target lists, and viewport-scoped in-view marker lists that change on every pan/zoom — so the captured last goes stale and Tab from the real last element walks out of the non-inert overlay. HelpDocs.tsx 255-275 re-queries inside the keydown handler exactly as the fix says, so the fix matches house convention. One factual error in the description — 'Escape + focus return work correctly' is wrong (the restore is dead code, see finding 4) — but it doesn't affect this finding's core claim; moderate is right (it partially duplicates finding 4's defect #2, and 2.4.3 is arguably the better SC tag than 2.1.2).

### F047 — 'Nearest Targets' list is not scoped to the map view and does not update on pan/zoom, contrary to the statement
- **Where:** `frontend/src/components/MapExplorer.tsx:453` · **WCAG:** Published-claim accuracy (re: 2.1.1 keyboard alternative description) · **Found by:** audit:claims
- **Issue:** ACCESSIBILITY.md says the Media Targets mode's 'Nearest Targets' list 'works the same way' as the in-view lists and then asserts 'These lists are scoped to the current map view and update as you pan or zoom (very dense views are capped, with a "zoom in to narrow the list" hint).' The keyboard path itself is real (each row has a focusable, aria-labelled locate button at lines 1320-1333 that opens the same popup and pans). But `nearest10` (lines 453-461) is computed as the 10 closest pins to the SEARCH CENTER lat/lng inputs — it ignores `mapBounds`, never reacts to panning or zooming, and has no over-cap hint. Only the 'Sightings in view' and 'Hotspots in view' lists (markersInView + mapBounds, cap 200, MapSidebarUI.tsx:118-121) match the published description. A screen-reader user following the statement would expect the targets keyboard list to track the viewport; it doesn't.
- **Fix:** undefined
- **Verifier refinement:** If scoping to the viewport, reuse the existing pattern rather than patching nearest10: derive markersInView(displayedTargetPins, mapBounds) and render through the shared InViewMarkerList (MapSidebarUI.tsx) like sightingsInView/hotspotsInView, which gives the cap + 'zoom in to narrow the list' hint for free. Otherwise the one-line ACCESSIBILITY.md correction ('the targets list shows the 10 nearest the search center') is the cheaper honest fix.
- **Verification:** nearest10 (MapExplorer.tsx:453-461) is sorted by distance from the lat/lng search-center inputs with deps [displayedTargetPins, lat, lng] — it never reads mapBounds, never reacts to pan/zoom, and has no over-cap hint — while ACCESSIBILITY.md line 13 explicitly includes the Nearest Targets list in 'These lists are scoped to the current map view and update as you pan or zoom'. The gap is functional, not just a claims bug: target chips are DOM divs with onClick but no tabIndex (TargetMarkers.tsx), so targets beyond the 10 nearest to the center have no keyboard path at all while mouse users can click any chip. Moderate is fair (false published assistive-behavior claim plus a real 2.1.1 gap).

### F048 — Placeholder-only (or format-only) accessible names on text inputs; Map Explorer From/To dates are indistinguishable
- **Where:** `frontend/src/components/MapExplorer.tsx:862` · **WCAG:** 2.4.6 Headings and Labels AA / 1.3.1 Info and Relationships A · **Found by:** audit:sr-content
- **Issue:** Several inputs have no label or aria-label, so the accessible name falls back to the placeholder. Worst case: the Map Explorer date-range inputs (MapExplorer.tsx:862,864) both have placeholder "YYYY-MM-DD" — a SR user hears the format string twice with nothing distinguishing From vs To (the visible "Date Range" SidebarLabel is not programmatically associated). Same pattern: MapExplorer.tsx:84 (place search), 689/691 (Latitude/Longitude), 1182 (target species search); Settings.tsx:1216/1228/1240 map-default inputs, whose visible "Latitude"/"Longitude"/"Radius (mi)" labels are unassociated <div>s so the computed name is "e.g. 37.8275"; SpeciesDetail.tsx:1268 and MediaCommentsSection.tsx:70 comment filters (placeholder is at least descriptive there, but disappears once text is typed for sighted users). Contrast: Checklists.tsx:655/663 date inputs do it right with aria-label="From date"/"To date".
- **Fix:** undefined
- **Verification:** Verified every cited input: MapExplorer 862/864 are both placeholder="YYYY-MM-DD" with the 'Date Range' SidebarLabel an unassociated div, so From and To compute identical accessible names; 84, 689/691, 1182, Settings 1216/1228/1240 (visible Latitude/Longitude/Radius labels are plain divs), SpeciesDetail 1268, and MediaCommentsSection 70 all have placeholder-only names. Checklists.tsx 655/663 confirmed as the in-house aria-label="From date"/"To date" pattern the fix mirrors. Moderate is right — placeholders provide a partial name fallback, but the indistinguishable date pair is a genuine 2.4.6/1.3.1 failure.

### F049 — Map sidebar filter inputs rely on placeholders for their accessible names; date inputs are named 'YYYY-MM-DD'
- **Where:** `frontend/src/components/MapExplorer.tsx:862` · **WCAG:** 4.1.2 Name, Role, Value A (also 3.3.2 Labels or Instructions A) · **Found by:** audit:maps
- **Issue:** The Map Explorer sidebar inputs have no <label>, aria-label, or aria-labelledby — the SidebarLabel headings are unassociated styled divs. Accessible names therefore fall back to placeholders: the two Date Range inputs (MapExplorer.tsx:862-865) both compute to the meaningless, identical name "YYYY-MM-DD" (a screen-reader user cannot tell From from To, or that they are date filters at all); Latitude/Longitude (:689-692), "Search by place name" (:86), and "Search species…" (:1182) are intelligible but vanish once a value is typed. The adjacent Search button (:90-103) has only a title attribute for its name.
- **Fix:** undefined
- **Verification:** Verified at MapExplorer.tsx:862-865 the two date inputs are type=text with identical YYYY-MM-DD placeholders and no label/aria-label/aria-labelledby, and SidebarLabel (MapSidebarUI.tsx) is an unassociated styled div; lat/lng (:689-692), place search (:84), species search (:1182) are placeholder-named and the search button (:93) is title-only. One nuance: per HTML-AAM a placeholder remains the computed accessible name even after a value is typed (the 'vanish' is visual-only, a 3.3.2 issue), but the identical meaningless names on the two date fields are a genuine 4.1.2/3.3.2 failure that gates the map filters. Severity moderate is appropriate and the aria-label fix matches existing codebase patterns (e.g. the heatmap slider's aria-label at :930).

### F050 — Map Explorer async error messages are silent to assistive tech
- **Where:** `frontend/src/components/MapExplorer.tsx:1006` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** audit:aria
- **Issue:** The hotspot search error (:1006-1011) and media-targets search error (:1227-1232) render as plain styled divs after an async failure — no role="alert" or aria-live, so a screen-reader user who pressed "Find Hotspots"/"Find Recent Sightings" gets no feedback that the request failed (the role="status" loading chip just disappears). Other error paths in the app do this correctly (ChecklistComparer.tsx:372, App.tsx:720, DropZone.tsx:132).
- **Fix:** undefined
- **Verification:** Verified: hotspotsError (1006-1011) and targetsError (1227-1232) render as plain divs with an aria-less AlertCircle icon, no role/aria-live anywhere in MapExplorer per grep, and the role=status loading chip (1437) simply unmounts on failure — so an SR user gets no failure feedback. Cited reference implementations (ChecklistComparer 372, App 720, DropZone 132) all exist, and role=alert on these two divs is the correct house-pattern fix. Note this is a strict subset of finding 11 (same two MapExplorer locations); confirmed independently at moderate, but the two should be deduplicated when applying fixes.

### F051 — Placeholder is the only label on search/filter, coordinate, and API-key text inputs
- **Where:** `frontend/src/components/MediaCommentsSection.tsx:66` · **WCAG:** 3.3.2 Labels or Instructions (also 2.4.6) AA · **Found by:** audit:forms
- **Issue:** These text inputs have no label or aria-label; their only identification is the placeholder, which disappears as soon as the user types (and browsers' placeholder→accessible-name fallback is a last resort, not a reliable label). Locations: MediaCommentsSection.tsx:66-70 ('Filter media comments…'); Checklists.tsx:161-165 (shared CommentSearchBox, placeholders 'Filter checklist comments…' / 'Filter species comments…' at :544/:555); SpeciesDetail.tsx:1264-1268 ('Filter comments…'); MapExplorer.tsx:82-84 ('Search by place name'), :689/:691 (Latitude/Longitude number inputs — after entry a sighted user sees two bare numbers with nothing saying which is which), :1182-1184 ('Search species…'); Settings.tsx:424-428 (API key entry, 'Paste your API key' — the row label at :352 is an unassociated <div>). Impact is worst for low-vision and cognitive users mid-entry, and for the lat/lng pair where the only differentiator vanishes.
- **Fix:** undefined
- **Verifier refinement:** The suggested aria-labels are right and match the file-local convention (Checklists.tsx already puts aria-label on its Protocol/County selects and From/To date inputs; MapExplorer/Settings use aria-label heavily elsewhere). One refinement: CommentSearchBox is a shared component, so add a single aria-label derived from its existing title/placeholder prop (e.g. aria-label={placeholder.replace(/…$/, '')}) — one change covers both Checklists instances. For Settings, derive it from the row's label prop (aria-label={`${label} API key value`} or similar) rather than a hardcoded string, since the component renders multiple key rows.
- **Verification:** Read every cited input: MediaCommentsSection.tsx:66-79, the shared CommentSearchBox in Checklists.tsx:161-175 (both instantiations), SpeciesDetail.tsx:1264-1277, MapExplorer.tsx:82-89/:689-692/:1182-1185, and Settings.tsx:424-440 — none has an aria-label, aria-labelledby, or associated <label>; the placeholder is the only identification, and the Settings row label is an unassociated <div>. ACCESSIBILITY.md's only disclosed exception is chart-bar contrast, so this is undisclosed. Moderate is correct: placeholder fallback usually yields *some* accessible name, but it vanishes on input (worst for the bare lat/lng number pair, which affects sighted users too) — a fair 3.3.2 AA failure, not serious, not pedantry.

### F052 — List Comparer summary bars and result panels don't reflow at 320px — labels truncate to identical text
- **Where:** `frontend/src/components/ResultsView.tsx:111` · **WCAG:** 1.4.10 Reflow AA · **Found by:** audit:resize
- **Issue:** Both comparer results screens use rigid column counts with no narrow-width collapse. The five-cell summary (`gridTemplateColumns: 'repeat(5, 1fr)'` — ResultsView.tsx:111 and ChecklistComparer.tsx:281) gives each cell ~58px at 320px; the 0.625rem uppercase labels are `whiteSpace: nowrap; overflow: hidden; textOverflow: ellipsis` (ResultsView.tsx:165-173, ChecklistComparer.tsx:478), so "Checklist A" and "Checklist B" (and "<name> total" pairs) ellipsize to identical strings — the numbers become indistinguishable at that width. Below them, ResultsView.tsx:129 `repeat(3, 1fr)` species panels and ChecklistComparer.tsx:310 `1fr 1fr` A-only/B-only panels never stack, leaving ~90-140px columns where species names wrap to 4+ lines. Nothing scrolls two-dimensionally and the panel headers repeat the counts, so it is degraded rather than lost — but the summary labels' ambiguity is a concrete information loss at 320px.
- **Fix:** undefined
- **Verifier refinement:** The stats-bar half of the fix is sound (rem-based `repeat(auto-fit, minmax(7rem, 1fr))` inline also scales with the in-app Text Size control). The panel half won't work as written: the grids' `gridTemplateColumns` are inline styles, and a media-query class rule cannot override an inline declaration — move the grid definition into a class (as SpeciesDetail.tsx:802 uses `className="sr-two-col"` with no inline grid). ResultsView's panel grid is 3 columns, so `.sr-two-col` isn't directly reusable: either add an `.sr-compare-panels { grid-template-columns: repeat(3, 1fr) }` base rule with a `1fr` override in the existing `@media (max-width: 640px)` block in globals.css (the ChecklistComparer's 1fr-1fr grid CAN reuse `.sr-two-col` directly), or skip media queries entirely with inline `repeat(auto-fit, minmax(14rem, 1fr))`. Also note ACCESSIBILITY.md line 39 currently claims narrow-window reflow 'without clipping content' — it should be corrected or the fix shipped to keep that published statement true.
- **Verification:** Verified all cited lines: ResultsView.tsx:111 `repeat(5, 1fr)` with nowrap/ellipsis 0.625rem labels (165-176), :129 `repeat(3, 1fr)` panels, and the ChecklistComparer twins (281, 310, ~479) — all inline styles with zero responsive handling (no matchMedia, no media-query class) anywhere in ResultsView/ListComparer/ChecklistComparer. At 320px each stat cell is ~55-64px with 40px horizontal padding, leaving ~15-24px for the label (1-3 chars + ellipsis): 'Checklist A'/'Checklist B' and filename-derived labels (eBird exports share the 'MyEBirdData' prefix) truncate to identical strings — concrete information loss under SC 1.4.10, though the both/A-only/B-only counts are duplicated in the panel headers so only the two totals become truly ambiguous. Moderate is the right severity: real AA failure at 320px/400% zoom, but degraded rather than blocking.

### F053 — role="radio" groups in Settings have no arrow-key behavior or roving tabindex
- **Where:** `frontend/src/components/Settings.tsx:119` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** Theme (:119-128), Text size (:519-525), and Date format (:586-591) use role="radiogroup"/role="radio" with aria-checked, but every radio is a tabIndex={0} button and arrow keys do nothing. The radio role contract tells SR users "radio button, 1 of 3 — use arrow keys"; pressing arrows is a no-op, and Tab walks through each radio as a separate stop. Operable but misleading — the role advertises an interaction model the widget doesn't implement.
- **Fix:** undefined
- **Verification:** Verified all three groups (Settings.tsx:119, 519, 586): role=radiogroup containing role=radio <button>s, every one tabIndex={0}, no roving tabindex, and no arrow-key handler anywhere in the file (only the key-input Enter handler at 439) — so the announced radio interaction model ('use arrow keys') is a no-op and unchecked radios stay in the Tab order. Everything remains operable via Tab+Enter, so moderate (misleading, not blocking) is the right severity. The fix is validated against the codebase: SortSeg exists (Checklists.tsx:67) and aria-pressed buttons are the established house pattern, so either implementing the APG radiogroup pattern or downgrading to aria-pressed is convention-correct.

### F054 — Tab Layout reordering is mouse/touch-only with no keyboard alternative — the statement's keyboard section discloses no exceptions
- **Where:** `frontend/src/components/Settings.tsx:721` · **WCAG:** 2.1.1 Keyboard A (also WCAG 2.2 2.5.7 Dragging Movements AA) · **Found by:** audit:claims
- **Issue:** Settings → Tab Layout reorders tabs exclusively via HTML5 drag-and-drop on the <li> rows; the 'drag handle' is a non-focusable plain <div> carrying an aria-label (which is also invalid ARIA on a generic div — it won't be announced), and there is no keyboard mechanism (no move-up/move-down buttons, no arrow-key handling). The app itself admits this in an sr-only note at Settings.tsx:699 ('Keyboard reordering is not supported in this version'), and ACCESSIBILITY.md's 'Nearly all of SnowRaven is reachable and operable with the keyboard alone' hedge technically covers it — but the Keyboard Navigation section enumerates zero exceptions, so a reader has no way to learn this feature is keyboard-inaccessible. Show/hide toggles are keyboard-accessible; ordering is not achievable at all without a pointer, which is a hard 2.1.1 failure for that function (and a 2.5.7 Dragging Movements failure under WCAG 2.2).
- **Fix:** undefined
- **Verification:** Both halves check out: (a) reordering is pointer-only — DnD on the <li>, unfocusable handle div with an invalid aria-label, no keyboard mechanism in the file — and (b) ACCESSIBILITY.md's Keyboard Navigation section enumerates only the map-marker caveat and never names this exception, so the 'Nearly all' hedge gives readers no way to learn ordering is keyboard-inaccessible. The sr-only in-app note (line 699) is disclosure to SR users but not in the published statement, so alreadyDisclosed stays false. Moderate is fair for the claims-dimension framing (the underlying 2.1.1 failure is index 3's serious finding); the fix — keyboard reorder, or naming the exception in ACCESSIBILITY.md until then — is correct.

### F055 — DOM map markers (Species Detail / Named Birds pins, target chips, Statistics rank pins) are unnamed, role-less click targets invisible to AT
- **Where:** `frontend/src/components/SightingsMap.tsx:55` · **WCAG:** 1.1.1 Non-text Content A / 4.1.2 Name, Role, Value A · **Found by:** audit:sr-content
- **Issue:** ACCESSIBILITY.md documents sidebar-list alternatives for the Map Explorer's GPU canvas markers, but the DOM <Marker> maps have no equivalent treatment: SightingsMap.tsx:55-58 renders each pin as a clickable <div> containing an innerHTML SVG with no role, name, or tabindex; TargetMarkers.tsx:69-74 does the same for target chips; BirdingStats.tsx:863-872 for the Statistics RankIcon pins (statsPrimitives.tsx:92-101, an unnamed SVG). Screen-reader and keyboard users cannot perceive or activate any of these markers or reach their popups. Impact is moderated because equivalent data exists in adjacent text on most surfaces (Named Birds rows list every sighting date/location/checklist; Species Detail has Top Locations and per-sighting lists; Statistics has top-location tables), but the popup-only details (e.g. per-coordinate date groupings) have no named path.
- **Fix:** undefined
- **Verifier refinement:** Correction to the finding's mechanism: the markers are not role-less/invisible — maplibre already applies role='button' + a generic identical aria-label ('Map marker') to the wrapper, so the defect is a non-descriptive name plus no focusability. Fix by giving each pin a descriptive per-pin name: either set it on the wrapper via the Marker ref (marker.getElement().setAttribute('aria-label', `${locName ?? coord}, ${n} sightings`) plus tabindex='0' — maplibre preserves a pre-set label), or render a real <button aria-label=...> child and demote the wrapper to role='presentation' via the ref to avoid nested-button announcements. The finding's hand-rolled role='button'/tabIndex/Enter-Space div is workable but creates button-inside-button nesting and re-implements what a native <button> gives for free.
- **Verification:** The literal 'no role, name' claim is partly wrong — maplibre 5.24's Marker.addTo() (src/ui/marker.ts:340-348) stamps every marker wrapper with role='button' and default aria-label='Map marker' — but the substantive 1.1.1 failure stands: all pins announce identically with no location/count, are not focusable (tabindex comes only from the unused setPopup path), and the popup-only per-coordinate details have no named path. The cited mitigations are real (BirdingStats pins duplicate the adjacent 'Top locations by checklists/species' lists; NamedBirdRow lists every checklist link; TargetMarkers has the Nearest Targets sidebar), so moderate is the right severity. ACCESSIBILITY.md's marker claim is Map Explorer-only and does not disclose this.

### F056 — Rotate/pitch gestures left enabled on all four maps with the compass reset hidden — multipoint-only on touch, no single-pointer alternative or reset, and the app never uses bearing/pitch
- **Where:** `frontend/src/components/SnowMap.tsx:125` · **WCAG:** 2.5.1 Pointer Gestures A · **Found by:** gap:WCAG 2.5.1 Pointer Gestu
- **Issue:** SnowMap's single MapGL mount (SnowMap.tsx:117) passes no gesture overrides, so maplibre-gl 5.24.0 defaults leave dragRotate, touchZoomRotate (rotation), touchPitch, and pitchWithRotate all enabled — empirically confirmed (handler state dragRotateEnabled/touchPitchEnabled/touchZoomRotateEnabled all true in both embed configs; Shift+ArrowRight rotated bearing to 15°, Shift+ArrowUp pitched to 10°). On touch devices, rotation is a two-finger twist and pitch a two-finger vertical drag — multipoint gestures with no single-pointer alternative: the NavigationControl compass (the standard single-tap bearing/pitch reset) is explicitly hidden (`showCompass={false}`, SnowMap.tsx:125), and no app code ever calls resetNorth/setBearing/setPitch. A touch user who triggers these accidentally is stranded in a rotated/tilted view: the app's fitBounds calls reset bearing only — pitch survives fitBounds (verified empirically: pitch 40° unchanged after fitBounds) — and they are data-driven fits, not user-invocable resets. Desktop right-drag/Ctrl-drag and keyboard Shift+arrows exist but are unavailable to touch single-pointer users. The app itself never uses bearing or pitch, so this is unused functionality left armed on all four map surfaces (Map Explorer, Species Detail, Statistics, SightingsMap/Named Birds). Note: this is distinct from every gesture/keyboard finding already recorded in verified-findings.json (none cover rotation/pitch or the hidden compass).
- **Evidence:** SnowMap.tsx:125 `<NavigationControl position="top-left" showCompass={false} />`; grep of frontend/src finds no dragRotate/touchPitch/touchZoomRotate/pitchWithRotate/keyboard props on any mount; maplibre-gl src/ui/map.ts:472-499 defaults `dragRotate: true, touchZoomRotate: true, touchPitch: true, pitchWithRotate: true`; live check: bearing 15°/pitch 10° reached via gestures' keyboard twins, and fitBounds left pitch at 40° (bearing-only reset); no resetNorth/setBearing/setPitch anywhere in app code
- **Fix:** Disable the unused gestures once in SnowMap (covers all four surfaces): pass `dragRotate={false}` `touchPitch={false}` `pitchWithRotate={false}` on the MapGL mount, and call `e.target.touchZoomRotate.disableRotation()` in the map's load handler (this keeps pinch ZOOM working — do not pass `touchZoomRotate={false}`, which would disable pinch zoom too). Do NOT call `map.keyboard.disableRotation()` — in maplibre 5.24 it disables keyboard PAN as well as rotate. Alternative if rotation is ever wanted: restore the compass (`showCompass` default) with `visualizePitch: true` so one tap resets bearing and pitch, providing the single-pointer alternative/reset.
- **Verifier refinement:** One correction to the suggested fix: maplibre 5.24's keyboard.disableRotation() does NOT disable keyboard pan — it only zeroes the Shift+arrow bearing/pitch deltas (bundle: `_rotationDisabled&&(o=0,a=0)`; plain arrows still pan), so it is safe to call as well. The rest is correct: pass dragRotate={false} touchPitch={false} pitchWithRotate={false} on the MapGL mount and call e.target.touchZoomRotate.disableRotation() in onLoad (keeps pinch zoom); or restore the compass with visualizePitch for a single-tap reset.
- **Verification:** Verified: SnowMap.tsx:117-125 passes no gesture overrides (maplibre-gl 5.24 bundle defaults dragRotate/touchZoomRotate/touchPitch/pitchWithRotate all true), hides the compass (showCompass={false}), and grep of frontend/src finds no resetNorth/setBearing/setPitch anywhere — so rotation/pitch are multipoint-only on touch with no single-pointer alternative or reset, on all four SnowMap-backed surfaces. A touch user who accidentally twists/tilts has no recovery control, which is a fair 2.5.1 failure plus a real stranding hazard; no existing finding in either file covers rotation/pitch or the hidden compass. Moderate is appropriate.

### F057 — Species combobox Escape blurs the input, stranding focus on <body>
- **Where:** `frontend/src/components/SpeciesDetail.tsx:487` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:focus
- **Issue:** The Escape branch of the combobox keydown closes the listbox and then deliberately calls `.blur()` on the input: `(e.target as HTMLInputElement).blur()`. Per the ARIA combobox pattern, Escape should close the popup and KEEP focus in the input (a second Escape may clear it). Blurring sends focus to <body>, so a keyboard user dismissing the list loses their place entirely and must Tab back from the top of the page. The blur also exists to suppress the onFocus auto-open (line 481-485 reopens on focus), which compounds the problem: refocusing the input to continue reopens the dropdown whether wanted or not.
- **Fix:** undefined
- **Verifier refinement:** Just delete the .blur() call in the Escape branch — nothing else is needed. onFocus only fires on a focus GAIN, and removing the blur means focus never leaves the input, so the auto-open cannot retrigger; selectorDisplayValue (line 400: dropdownOpen ? selectorQuery : selectedSpecies ?? '') already reverts the input text to the selected species the moment dropdownOpen goes false.
- **Verification:** Verified at SpeciesDetail.tsx:487 — the Escape branch calls (e.target as HTMLInputElement).blur(), and a grep confirms no other code restores focus, so focus lands on <body> and the keyboard user must Tab back from the top; ARIA combobox pattern requires keeping focus in the input. Moderate is fair (workaround exists, but it violates the deliberate-focus-management posture ACCESSIBILITY.md advertises). The suggested 'just closed via Escape' flag is unnecessary, so I supplied a simpler fix.

### F058 — Fixed-pixel text geometry in Statistics overlaps or clips at 200% text scale (pattern, multiple locations)
- **Where:** `frontend/src/components/statsPrimitives.tsx:62` · **WCAG:** 1.4.4 Resize Text AA (clipping cases also 1.4.12 Text Spacing AA) · **Found by:** audit:resize
- **Issue:** One repeating pattern: rem-sized text placed in fixed-pixel boxes that don't grow with the text. (1) BarRow (statsPrimitives.tsx:59-75): label column `width: labelWidth, flexShrink: 0` and value column `width: pctOf ? 68 : 40`, no overflow handling — at 200% scale, labels ("Jan" at labelWidth=28: BirdingStats.tsx:754,794,829; "SnowRaven weather" at 120: BirdingStats.tsx:1425-1429; MediaStatsSections.tsx:180-182,294) and values like "1,234 (56%)" are ~2× wider than their boxes and overlap the adjacent bar graphic. (2) Stacked-bar % labels inside fixed-height `overflow: hidden` segments — BirdingStats.tsx:1122-1133 and 1173-1185 (height 32, fits 200%) but the protocol sub-bars at 1151-1158 are `height: 20` with 0.625rem labels: at 200% the 20px glyphs (30px line box) clip vertically; same construction in the media bars at 1356, 1380, 1401. (3) Hard-ellipsized name columns `width: 100`/`width: 96` + nowrap+hidden (BirdingStats.tsx:953, 984, 1028, 1057) — county/species names truncate to a few characters at 200% with only a mouse-only `title` as recovery. (4) Donut center captions absolutely centered in fixed 120/132px charts — BirdingStats.tsx:764-769, 804-807 and MediaStatsSections.tsx:58-61 — at 200% the caption (e.g. "September" ≈140px wide, or a 1.25rem count) spills across the donut ring on both sides. Screen-reader users are unaffected (the role="img" aria-labels carry the data); this degrades the visual rendering for sighted low-vision users at 150-200%.
- **Fix:** undefined
- **Verifier refinement:** Direction is right, but: BarRow's labelWidth prop is typed `number`, so convert inside the component (width: `${labelWidth / 16}rem`, value column `pctOf ? '4.25rem' : '2.5rem'`) so no call sites change; `height: 20` → `minHeight: 20` on the sub-bars is correct as suggested; recharts PieChart width/height are px numbers so the donut can't be 'sized in rem' — instead give the centered caption maxWidth: '100%' with padding and ellipsis/wrap (or compute the chart px size from the live root font-size); and for the county columns at BirdingStats.tsx:953/984 either add the missing title attribute or replace the hard ellipsis with a rem minWidth that lets names wrap.
- **Verification:** All four cited sub-patterns exist in current code (BarRow px label/value columns at statsPrimitives.tsx:59-75 with labelWidth 28/48/120/150 at the cited consumer lines; height-20 overflow-hidden sub-bars at BirdingStats.tsx:1151-1158; 100/96px ellipsized county/state columns at 953/984/1028/1057, counties lacking even a title; fixed 120/132px donut charts with absolutely-centered captions). The app's Text Size control doubles the root font-size via --sr-text-scale while these px boxes stay fixed, so single-word labels overlap the bars, sub-bar labels clip at the 20px clip edge, names truncate to a few characters, and donut captions spill across the ring at 200% — and ACCESSIBILITY.md affirmatively claims 1.4.4 is met 'without clipping content', so this contradicts the published statement rather than being disclosed. Two specifics are overstated (the '1,234 (56%)' values wrap onto a second line rather than overlapping since rows use minHeight, and the height-32 media bars at 1356/1380/1401 fit at 200%), but the pattern and moderate severity stand.

### F059 — Light theme: accent-colored text marginally fails on tinted/page backgrounds (4.03–4.32:1)
- **Where:** `frontend/src/components/TabNav.tsx:94` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** Light --sr-accent #2D8653 passes on white surfaces (4.52:1) but fails where it is used on anything tinted: on --sr-accent-bg = 4.03:1 — the app-wide active-state pattern (ghost buttons, active filter pills, the tab dropdown's selected option TabNav.tsx:309-314, SpeciesPanel count badge :44-45; background:'var(--sr-accent-bg)' + accent text appears in 14 files); on --sr-bg = 4.32:1 — the active tab label (tab bar sits directly on the page background, App.tsx:583) and the footer 'Up to date' status (App.tsx:1154); on --sr-surface-subtle = 4.11:1. Dark theme passes everywhere (7.75–10.35:1).
- **Fix:** undefined
- **Verification:** Independent WCAG relative-luminance math reproduces every reported ratio exactly: light #2D8653 is 4.03:1 on --sr-accent-bg #E8F5EE, 4.32:1 on --sr-bg #F9FAFB, 4.11:1 on --sr-surface-subtle #F4F4F5 (4.52:1 on white passes; dark theme passes at 7.75-10.35). The pairs genuinely co-occur — dropdown selected option (TabNav 309-314), SpeciesPanel count badge (11px accent text on accent-bg), active tab label over the body's --sr-bg background, footer 'Up to date' span — all normal-size text requiring 4.5:1, and 24 files reference --sr-accent-bg. ACCESSIBILITY.md claims AA for primary interface colors with only chart-bar labels excepted, so this is a real undisclosed AA failure; the suggested --sr-accent-strong fix is convention-compliant (token defined in both themes; verified 7.12:1 on accent-bg, 7.64:1 on bg) and the alternative #27764A verifies at 4.96/5.32/5.56.

### F060 — Primary navigation and header have no landmark exposure; HelpDocs TOC nav is unlabeled
- **Where:** `frontend/src/components/TabNav.tsx:105` · **WCAG:** 1.3.1 Info and Relationships (A) · **Found by:** audit:structure
- **Issue:** Three related landmark-structure gaps (merged): (1) Desktop tab bar: <nav aria-label="Main navigation" role="tablist"> (TabNav.tsx:105-108) — the explicit role="tablist" overrides <nav>'s implicit navigation role, so no navigation landmark exists anywhere on the page; screen-reader landmark navigation (rotor/landmark list) shows no way to find the primary navigation. (2) Collapsed mode: TabDropdown renders in a plain <div> (TabNav.tsx:228) with no nav landmark at all, so narrow/mobile users also get no navigation landmark. (3) The app header with branding and tagline is a plain styled div (App.tsx:570), not <header>/banner, leaving it outside any landmark. By contrast <main> (App.tsx:586) and the footer role="contentinfo" (App.tsx:1100) are correct. Additionally the HelpDocs TOC <nav> (HelpDocs.tsx:347) has no accessible name, so it announces as an anonymous navigation region inside the documentation dialog.
- **Evidence:** TabNav.tsx:105-108 `<nav aria-label="Main navigation" ... role="tablist">`; TabNav.tsx:228 dropdown wrapper is `<div style={{ padding: '12px 16px', position: 'relative' }} ref={wrapRef}>`; App.tsx:570 `<div className="sr-header" ...>`; HelpDocs.tsx:347 `<nav style={{...}}>` with no aria-label.
- **Fix:** Wrap the tablist in a labeled nav instead of putting role="tablist" on the nav itself: <nav aria-label="Main"><div role="tablist">…</div></nav>; wrap TabDropdown's container in the same <nav aria-label="Main">; change the App header div to <header> (banner); add aria-label="Documentation contents" to the HelpDocs TOC nav.
- **Verifier refinement:** Fix is correct (nav wrapping a div[role=tablist] is the canonical pattern). Use the same accessible name (e.g. aria-label="Main navigation") on both the desktop and collapsed-mode <nav> wrappers so the landmark announces identically across modes; keep the sr-header className when converting the header div to <header>.
- **Verification:** All four citations verified: role="tablist" on the <nav> (TabNav.tsx:105-108) overrides its implicit navigation role, so desktop mode exposes no navigation landmark; collapsed mode's wrapper (TabNav.tsx:228) is a bare div with none either; the branding header (App.tsx:570) is a plain div outside any landmark; HelpDocs.tsx:347 is an unlabeled <nav>. Screen-reader landmark navigation therefore cannot locate the primary navigation in either mode — a genuine 1.3.1-adjacent structural gap, distinct from confirmed #33 (which is about dangling tabpanel aria-labelledby, not landmark exposure). Moderate is fair given main and contentinfo exist but the primary nav is unfindable by landmark.

### F061 — Tab key in the collapsed nav dropdown unmounts the focused option before the browser moves focus
- **Where:** `frontend/src/components/TabNav.tsx:209` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:focus
- **Issue:** In TabDropdown, the menu's Tab case calls `close(false)` without preventDefault, intending the browser to move focus onward naturally. But focus lives on a menu option (real DOM focus, line 176/182), and React 18 flushes the discrete keydown update synchronously — the listbox (rendered behind `{open && ...}`, line 265) unmounts and the focused option is removed BEFORE the browser performs the default Tab traversal. Focus falls to <body>, so the default Tab then lands on the first focusable element in the document instead of the control after the trigger. On narrow/mobile widths this dropdown is the app's primary navigation, so Tab-to-leave silently teleports keyboard users to the top of the page. (Escape, by contrast, correctly returns focus to the trigger via close(true).)
- **Fix:** undefined
- **Verifier refinement:** Simplest fix: in the Tab case call close(true) WITHOUT preventDefault. Inside close(), triggerRef.current?.focus() runs synchronously before React flushes the setOpen(false) unmount, so focus is on the trigger when the browser performs the default Tab — focus then moves naturally to the element after the trigger in ONE keystroke (and Shift+Tab to the element before it), with no extra keypress the suggested preventDefault+close(true) variant would require.
- **Verification:** Lines 209-211 call close(false) with no preventDefault while real DOM focus sits on an option inside the conditionally-rendered listbox (line 265); under React 19's synchronous discrete-event flush the focused option unmounts before the browser's default Tab traversal, dropping focus to body. On narrow widths this dropdown is the primary navigation, so the 2.4.3 impact and moderate severity are fair. Escape's close(true) contrast claim also checks out (lines 205-207).

### F062 — Collapsed dropdown nav orphans every tabpanel (dangling aria-labelledby, no tablist)
- **Where:** `frontend/src/components/TabNav.tsx:298` · **WCAG:** 1.3.1 Info and Relationships A · **Found by:** audit:aria
- **Issue:** All panels in App.tsx use aria-labelledby="tab-{id}" (e.g. App.tsx:590). In the collapsed TabDropdown mode the id="tab-{id}" elements exist only on the listbox options while the menu is open (TabNav.tsx:298) — with the menu closed (the normal state) the visible role="tabpanel" references a nonexistent id and there is no tablist/tab anywhere in the tree, so the tab/tabpanel relationship ACCESSIBILITY.md claims ("Tabs and their panels are linked with the correct tab/tabpanel roles") is broken precisely for the zoom/narrow-viewport users who get the dropdown. The dropdown also replaces tab semantics with listbox semantics, leaving orphaned tabpanels.
- **Fix:** undefined
- **Verification:** All ten App.tsx panels (lines 588-1073) use role=tabpanel + aria-labelledby="tab-{id}", but in collapsed mode the only tab-{id} elements are dropdown options (TabNav.tsx:298) that exist only while the menu is open and carry role=option, not role=tab — so in the normal closed state every labelledby idref dangles and the tabpanels are orphaned with no tablist in the tree. ACCESSIBILITY.md line 19 explicitly claims 'Tabs and their panels are linked with the correct tab/tabpanel roles' and its only disclosed contrast/structure exception is chart-bar labels, so this contradicts a published statement rather than being disclosed. Moderate is fair: it breaks panel names and structure for the entire narrow-viewport/zoom audience.

### F063 — ToggleSwitch off-state track is nearly invisible in light theme (1.48:1)
- **Where:** `frontend/src/components/ui/ToggleSwitch.tsx:27` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** The off-state track uses --sr-gray-400 (#D4D4D8): 1.48:1 against the white button surface AND 1.48:1 against the white knob, so the knob position — the visual state indicator of every 'merge subspecies' / 'show spuh' / 'hide weather' switch — is barely discernible in light theme (the on state passes: accent track 4.52:1 vs surface). Dark theme: track vs surface 2.29:1 (also <3:1) though knob-vs-track passes (7.73:1). aria-checked is correct for AT, but the sighted off/on distinction fails non-text contrast.
- **Fix:** undefined
- **Verifier refinement:** The suggested hexes verify (#8E8E96 = 3.25:1 on white; #6E6E78 = 3.51:1 vs dark surface, 5.04:1 vs white knob), but '--sr-border-medium-darkened' does not exist and hardcoded hexes in the component would violate the --sr-* token rule. Since --sr-gray-400 has no other consumers, either retune --sr-gray-400 to #8E8E96 (:root) / #6E6E78 ([data-theme="dark"]) in frontend/src/globals.css, or — cleaner, since #8E8E96 would be oddly dark for a 'gray-400' slot — add a dedicated --sr-toggle-off-track token in BOTH themes and reference it at ToggleSwitch.tsx:27. Also update the ACCESSIBILITY.md contrast claim if the fix doesn't ship immediately.
- **Verification:** Independently recomputed all ratios from globals.css tokens and they match exactly: light off-track #D4D4D8 is 1.48:1 vs both the white surface and the white knob (knob position imperceptible), on-track 4.52:1, dark track 2.29:1 vs surface with knob-vs-track 7.73:1. ACCESSIBILITY.md claims form controls meet AA contrast and discloses only chart-bar labels, so this is not a known exception. Moderate is right: a genuine 1.4.11 failure on a widely-used control, but state remains inferable from green-present/absent (state-vs-state 3.06:1) and aria-checked is correct for AT.

### F064 — The same 'open this checklist on eBird' function is identified four different ways across tabs
- **Where:** `frontend/src/components/WeatherTidePanel.tsx:108` · **WCAG:** 3.2.4 Consistent Identification (AA) · **Found by:** gap:WCAG 3.2.1/3.2.2 (on-foc
- **Issue:** One repeated function — a link that opens a specific checklist on ebird.org — is identified with four different affordances and three different name vocabularies depending on the tab (this is the identification inconsistency itself, distinct from the already-reported missing new-window indication and the ↗ accessible-name weakness): (1) bare '↗' glyph with title="Open checklist" (BirdingStats.tsx:1332, 1564, 1589); (2) data value + lucide ExternalLink icon, no label at all (speciesDetail/ui.tsx:52-78 StatValueLink, Checklists.tsx:48-65 DateLink) — except NamedBirdRow.tsx:91-100, where the identical id+icon link carries aria-label="Open eBird checklist S… (opens in a new tab)"; (3) plain accent-colored text with no icon and no underline, indistinguishable from the adjacent non-link values (BirdingStats.tsx:500, 695, 726, 1448, 1525; SightingsMap.tsx:67 popup dates; map/TargetMarkers.tsx:104); (4) underlined id text with title="View this checklist on eBird" (WeatherTidePanel.tsx:108-111, ChecklistComparer.tsx:429-432). Where a name exists it is inconsistent F31-style: "Open checklist" vs "View this checklist on eBird" vs "Open eBird checklist S…". A screen-reader user who learns one identification on the Checklists tab cannot recognize the same function on Statistics or in a map popup, and sighted users get no stable visual signature for 'this opens the checklist' either.
- **Evidence:** WeatherTidePanel.tsx:108-111 `<a href={`https://ebird.org/checklist/${id}`} … title="View this checklist on eBird" style={{ …textDecoration: 'underline' }}>{id}</a>` vs BirdingStats.tsx:1332 `<a … title="Open checklist" …>↗</a>` vs Checklists.tsx:54-62 `{formatDate(date)}<ExternalLink size={10}/>` (no label) vs BirdingStats.tsx:500-505 date-only link with `textDecoration: 'none'` and no icon.
- **Fix:** Create one shared ChecklistLink component (extending the existing speciesDetail/ui.tsx StatValueLink, which already gates on SUBMISSION_ID_RE) with a single visual signature (ExternalLink icon) and one accessible-name formula (e.g. aria-label=`Open checklist ${id} on eBird` plus the new-tab suffix already used in NamedBirdRow). Replace all four variants — the ↗ glyphs, the unlabeled icon links, the plain-text links, and the title-only underlined ids — with it.
- **Verification:** All four variants verified at the cited lines: bare ↗ with title='Open checklist' (BirdingStats 1332/1564/1589), value+ExternalLink with no label (StatValueLink ui.tsx:52-78, DateLink Checklists.tsx:48-65) vs NamedBirdRow.tsx:90-100's full aria-label, plain accent text with textDecoration:'none' (BirdingStats 500/695/726/1448/1525, SightingsMap.tsx:62-75, TargetMarkers.tsx:104 — which is in fact a fifth vocabulary, 'View checklist S… →'), and underlined id with title='View this checklist on eBird' (WeatherTidePanel.tsx:108-111, ChecklistComparer.tsx:429-432). This is a genuine 3.2.4 inconsistent-identification issue whose root cause (no shared component or name formula) is distinct from confirmed #53 (↗ missing accessible name) and structure findings #4/#5 (new-window indication, title-attribute reliance), which the finding explicitly carves out. The suggested shared ChecklistLink keeps the SUBMISSION_ID_RE standing security check, consistent with conventions.

### F065 — WelcomeScreen modal has no focus trap; map fullscreen overlay leaves hidden chrome tabbable
- **Where:** `frontend/src/components/WelcomeScreen.tsx:21` · **WCAG:** 2.4.3 Focus Order A · **Found by:** audit:keyboard
- **Issue:** WelcomeScreen declares role="dialog" aria-modal="true" and handles only Escape (line 21) — there is no Tab containment and the app behind it is not inert, so tabbing past "Explore the app first" moves focus into the fully obscured app underneath (the overlay background is opaque var(--sr-bg)); a sighted keyboard user loses the focus indicator entirely while aria-modal simultaneously tells screen readers the background doesn't exist. The map fullscreen mode has the same background-tabbing issue: it is a CSS overlay (panel fixed inset 0, App.tsx:1031-1042) and only body scroll is locked (App.tsx:390-395), so the tab bar and footer remain in the tab order while visually covered. (Not a 2.1.2 trap — focus can always leave — but focus lands on invisible controls.)
- **Fix:** undefined
- **Verifier refinement:** The suggested fix is correct and convention-consistent — HelpDocs.tsx:258-274 has exactly the referenced Tab-wrap handler. Two refinements: WelcomeScreen should query focusables from a ref on its own root div (it has no DOM id, unlike HelpDocs' getElementById('sr-help-overlay')), and for map fullscreen the `inert` attribute is safe in both desktop targets (WKWebView/Safari 15.5+, WebView2) — apply it to the sibling tabpanels, TabNav, and footer while mapFullscreen && activeTab === 'map-explorer', clearing it in the same effect cleanup as the existing body scroll lock at App.tsx:390-395. Update ACCESSIBILITY.md's Focus Management section in the same change per CLAUDE.md.
- **Verification:** WelcomeScreen.tsx:19-24 handles only Escape with no Tab containment while declaring aria-modal="true", and App.tsx:1195-1201 mounts it inside the full app tree, so the obscured tab bar/footer remain tabbable behind the opaque var(--sr-bg) overlay. The map fullscreen claim also holds: App.tsx:1031-1033 is a fixed inset-0 z-1200 CSS overlay and App.tsx:389-395 locks only body scroll — no inert/aria-hidden/focus-trap exists anywhere in frontend/src. ACCESSIBILITY.md's Focus Management section discloses only the Map Explorer filter-panel trap, not this gap; moderate is the right severity (focus lands on invisible controls but is recoverable, not a 2.1.2 trap).

### F066 — Dark theme: map pin tokens are lightened 'for dark backgrounds' but draw on the always-light Positron basemap (1.69–2.25:1)
- **Where:** `frontend/src/globals.css:199` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** There is no dark basemap variant (lib/mapStyle.ts tunes Positron only; no theme branch), yet the dark-theme --sr-map-* tokens lighten the pin colors: visited/heat pins #34D399 vs Positron land (~#F2F0EB) = 1.69:1, personal #E0A060 = 1.97:1, unvisited #7BA7C9 = 2.25:1 (light theme passes: 3.66–3.97:1). The GL circle/teardrop sprites read these tokens at runtime (map/SightingMarkers.tsx:80, lib/mapPins.ts, HotspotMarkers sprites), so switching the app to dark mode makes the primary map markers fainter against the unchanged light tiles. White glyphs inside the dark-theme teardrops also drop below 3:1 (star on #E0A060 = 2.24:1, dots on #7BA7C9 = 2.56:1). Mitigation: the sidebar in-view lists duplicate marker info, which keeps this from serious.
- **Fix:** undefined
- **Verifier refinement:** Add dedicated basemap-anchored pin tokens (e.g. --sr-map-pin-*) defined with the SAME values in both :root and [data-theme="dark"] for the GL circle layer and sprite bakes, keeping the existing --sr-map-* for sidebar legend/list dots on app surfaces. Note 'keep the light-theme values' alone is insufficient: light --sr-map-personal #C9842A is itself 2.70:1 on the land tint, so pair the shared pin tokens with a dark stroke ring — replace the hardcoded #ffffff circle-stroke-color (SightingMarkers.tsx:160) and sprite strokeStyle '#fff' (mapPins.ts:133) with a dark outline to supply the 3:1 boundary against light tiles.
- **Verification:** Verified the mechanism end-to-end: lib/mapStyle.ts has no dark basemap variant (Positron only), SightingMarkers.tsx reads --sr-map-visited via useCssToken at runtime (theme-reactive per the data-theme MutationObserver contract), and my recomputation matches exactly — dark tokens #34D399/#E0A060/#7BA7C9 hit 1.69/1.97/2.25:1 on ~#F2F0EB land, with white teardrop glyphs at 2.24/2.56; the pin strokes are hardcoded white (SightingMarkers.tsx:160, mapPins.ts:133) so they add no edge contrast on light tiles. One inaccuracy: light theme does NOT fully pass — --sr-map-personal #C9842A is only 2.70:1 on the land tint, so the personal pin fails 1.4.11 in both themes. ACCESSIBILITY.md documents the sidebar keyboard path to markers but not marker contrast, so not disclosed; the sidebar mitigation keeps moderate the right severity.

## MINOR

### F067 — Document title is static; active view is never reflected in the page/window title
- **Where:** `frontend/index.html:7` · **WCAG:** 2.4.2 Page Titled (A) — best-practice delta · **Found by:** audit:structure
- **Issue:** The title is "SnowRaven" (index.html:7) and nothing in src/ ever writes document.title (grep finds no occurrences); the Tauri window title is likewise static (src-tauri/tauri.conf.json:14). Because the app is a true single page with an in-page tab widget and no routing (no pushState/hash/router anywhere), the static title technically satisfies 2.4.2. But with ten distinct views (Weather, Species Detail, Map Explorer, Statistics, Settings, …), updating the title on tab change would aid orientation for screen-reader users, browser-tab/window switchers, and history — standard SPA practice.
- **Evidence:** index.html:7 `<title>SnowRaven</title>`; `grep -rn "document.title" frontend/src` returns nothing.
- **Fix:** On tab change in App.tsx, set document.title = `${activeLabel} — SnowRaven` (a small useEffect keyed on activeTab); this also updates the desktop window title in Tauri.
- **Verifier refinement:** The useEffect-on-activeTab setting document.title is right for web, but the claim that it 'also updates the desktop window title in Tauri' is wrong — Tauri v2 does not sync document.title to the native window title. Behind isTauri() also call getCurrentWindow().setTitle(...) from @tauri-apps/api/window, and add core:window:allow-set-title to src-tauri/capabilities/default.json (core:default does not include set-title).
- **Verification:** Verified: index.html:7 title is static, no document.title writes anywhere in frontend/src, no routing, and tauri.conf.json:14 hardcodes the window title. Not a 2.4.2 conformance failure (single page, no navigations) but a fairly-flagged SPA best-practice item with real if small orientation benefit for screen-reader users and tab/window switchers across ten distinct views. Severity minor is correct; not disclosed in ACCESSIBILITY.md.

### F068 — role="status" loading indicators mount already containing their text (announcement unreliable)
- **Where:** `frontend/src/App.tsx:125` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** audit:aria
- **Issue:** Loading/status chips are conditionally rendered elements that carry role="status" AND their message in the same insertion: TabLoading (App.tsx:125), tide states (App.tsx:837-845), MapExplorer loading chip (:1437), BirdingStats (:464, :883), AtlasLayer (:230), WeatherTidePanel (:165-235). Live regions must exist before their content changes; regions inserted with initial text are announced by some AT/browser combos and skipped by others, so loading/unavailable states are inconsistently conveyed. Pattern issue — reported once for all locations.
- **Fix:** undefined
- **Verification:** All cited instances verified: TabLoading (App.tsx:125, also the Suspense fallbacks), tide chips (:837-845), MapExplorer:1437, BirdingStats:464/:883, AtlasLayer:230, WeatherTidePanel:165-235 — every one is a conditionally-mounted element carrying role="status" together with its message, which AT announces inconsistently since the live region doesn't pre-exist its content change. Real but low-impact (loading-state awareness only; the loaded content itself is perceivable), so minor is the right severity and it's fairly batched as one pattern finding. The persistent-container fix is the standard remedy; note that for the Suspense fallbacks (TabLoading) the persistent region must live outside the Suspense boundary.

### F069 — No skip link to bypass the header/tab bar before main content
- **Where:** `frontend/src/App.tsx:570` · **WCAG:** 2.4.1 Bypass Blocks (A) · **Found by:** audit:structure
- **Issue:** There is no skip-to-main-content link anywhere in the app (grep for skip-link patterns across src/, index.css, App.css, globals.css, and main.tsx returns nothing). The branding header (App.tsx:570) and TabNav (App.tsx:583) render before <main> (App.tsx:586). Impact is low — strictly, 2.4.1 is satisfiable via the existing <main> landmark (technique ARIA11), the header contains no focusable elements, and both TabNav variants are a single tab stop (desktop roving tabindex at TabNav.tsx:132; collapsed dropdown is one trigger button) — so a sighted keyboard user pays only one extra Tab per page load. Reported as a best-practice gap rather than a conformance failure.
- **Evidence:** App.tsx:570 header div and :583 <TabNav> precede <main> at :586; no element or CSS class implementing a skip link exists in the codebase.
- **Fix:** Add a visually-hidden-until-focused anchor as the first child of the root div: <a href="#sr-main" className="sr-skip-link">Skip to main content</a>, give <main> id="sr-main" and tabIndex={-1}, and style .sr-skip-link to appear on :focus (positioned fixed, z-index above 1200 since overlays use 1200).
- **Verifier refinement:** Suggested fix is sound; style .sr-skip-link with var(--sr-*) tokens added to both :root and [data-theme="dark"] in globals.css (per the theming convention), and z-index above 1200 as noted since the map fullscreen overlay and dropdown both sit at 1200.
- **Verification:** Verified: no skip link exists anywhere in src/ or index.html; header (App.tsx:570) has no focusable elements, both TabNav modes are one tab stop, and <main> (App.tsx:586) provides a landmark, so 2.4.1 is technically satisfied (ARIA11) and the cost is one extra Tab. The auditor honestly framed it as a best-practice gap and rated it minor, which is correct — landmarks help AT users but not sighted keyboard users, so a skip link is the standard remaining piece. Fairly-flagged polish item, not pedantry.

### F070 — Bare "↗" glyph checklist links rely on title attribute alone; footer "SnowRaven" link hides its GitHub destination
- **Where:** `frontend/src/components/BirdingStats.tsx:1332` · **WCAG:** 2.4.4 Link Purpose (In Context) (A) · **Found by:** audit:structure
- **Issue:** Two weak link-purpose patterns (merged): (1) BirdingStats Highlights/Records links render the single glyph "↗" as their only content with title="Open checklist" (BirdingStats.tsx:1332, 1564, 1589). The accessible name comes only from the title fallback — fragile across AT, invisible to sighted keyboard users (title tooltips don't appear on focus), unavailable on touch — and the repeated identical name "Open checklist" is distinguished only by surrounding card/row context. (2) The footer link whose text is "SnowRaven" (App.tsx:1101-1110) actually navigates to the GitHub repository; neither the link text nor its programmatic context ("SnowRaven · Self-hosted Birding Tools") reveals that destination.
- **Evidence:** BirdingStats.tsx:1332 `<a href={`https://ebird.org/checklist/${card.c.submissionId}`} target="_blank" rel="noreferrer" title="Open checklist" ...>↗</a>`; App.tsx:1101-1109 `<a href="https://github.com/dtgibson/snowraven" ...>SnowRaven</a>`.
- **Fix:** Give the ↗ links a real accessible name including their distinguishing context, e.g. aria-label={`Open ${card.label} checklist on eBird`} (keep title for sighted hover if desired); change the footer link text or aria-label to "SnowRaven on GitHub".
- **Verifier refinement:** Treat the ↗-naming portion as covered by confirmed #53 and fix only the footer here: give the footer anchor aria-label="SnowRaven on GitHub (opens in new tab)" (or change the visible text), matching the existing SnowRaven Mini pattern at App.tsx:898.
- **Verification:** This is a merged finding: part (1) — the bare ↗ checklist links in BirdingStats (verified at 1332, 1564, 1589, title="Open checklist" only) — is a duplicate of confirmed finding #53 (same elements, same root cause: inadequate accessible naming of the repeated ↗ links). Part (2) is real and distinct: App.tsx:1101-1110 footer link with text 'SnowRaven' navigates to the GitHub repo with nothing in the text or contentinfo context revealing the destination, while App.tsx:898 already demonstrates the correct pattern for the SnowRaven Mini link. Confirmed on the footer-link aspect only; minor severity fits a single ambiguous 2.4.4 link.

### F071 — Multi-series line charts differentiated by hue alone
- **Where:** `frontend/src/components/BirdingStats.tsx:1775` · **WCAG:** 1.4.1 Use of Color A · **Found by:** audit:contrast
- **Issue:** The media-over-time chart draws four lines (photo/audio/video/total) distinguished only by stroke color, with a color-keyed legend — for color-blind users the photo (blue) vs video (purple) pair in particular is hard to separate. Tooltips name each series on hover and the role="img" summary covers AT users, so the information is reachable, but not glanceable. Same pattern wherever SightingsGraph's media chart overlays multiple series.
- **Fix:** undefined
- **Verifier refinement:** Add per-series strokeDasharray to the four <Line>s in BirdingStats.tsx (e.g. photo solid, audio '6 3', video '2 3', total '8 3 2 3'); since dot={false} there, dashes are the practical cue. Apply the same patterns to the photo/audio/video overlay in speciesDetail/SightingsGraph.tsx (~lines 157-172). Note recharts' default Legend line icon does not render the dash pattern, so either pass a custom legend `content` or distinct `legendType` icons so the legend also differs by more than hue. Also note ACCESSIBILITY.md currently claims 'Color is never the only way information is conveyed' — update it (or fix the charts in the same change) per the CLAUDE.md rule that it must stay true.
- **Verification:** Lines 1775-1778 of BirdingStats.tsx render four <Line> series with dot={false}, no strokeDasharray, and only stroke color differentiating them; Vienot protan/deutan simulation collapses the photo #3B82F6 / video #8B5CF6 pair from RGB distance 88.6 to ~1-2 (same in dark theme #60A5FA/#A78BFA), confirming the confusion claim. SightingsGraph.tsx's media overlay (lines ~157-172) is likewise hue-only (its dots are same-shape circles in the series color). Hover tooltips and the role=img label keep the info reachable, so minor severity is correctly stated.

### F072 — Chart non-text elements below 3:1: amber line in light theme, faint bar segments, stacked-segment boundaries
- **Where:** `frontend/src/components/BirdingStats.tsx:1776` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** Computed non-text chart parts under 3:1 — light theme: the Audio series line --sr-graph-audio #F59E0B vs the white card = 2.15:1 (media-over-time chart; SightingsGraph media line too), --sr-chart-blue-light bar segment vs surface = 1.80:1, tier-1 fill vs surface = 2.64:1; the hand-built stacked breeding bar's adjacent segments: tier-2 vs tier-1 = 2.04:1 light / 1.50:1 dark, tier-4 vs tier-2 = 2.79:1 light / 2.20:1 dark (BirdingStats.tsx:1677-1679). Mitigations keeping this minor: every chart has a data-bearing role="img" label, tooltips, and most values are duplicated as text (legend counts, BarRow numerals).
- **Fix:** undefined
- **Verifier refinement:** Darkening light --sr-graph-audio toward #B45309 (5.02:1, matches light --sr-age-immature) is verified and safe. But do NOT shift the shared --sr-tier-* palette to fix the stacked-bar boundaries — those tokens drive the map atlas overlay, hatch sprites (--sr-tier-N-rgb), legend parity, and the breeding-badge dark-text-on-light-tier pairing called out in ACCESSIBILITY.md; instead add 1px var(--sr-surface) gaps between the three stacked segments at BirdingStats.tsx:1677-1679, which fixes segment distinguishability with zero blast radius.
- **Verification:** Recomputed all cited non-text ratios and they match: light --sr-graph-audio #F59E0B vs white 2.15:1 (media LineChart at :1776), --sr-chart-blue-light vs white 1.80:1, tier-1 vs white 2.64:1, and the hand-built stacked breeding bar's adjacent segments at :1677-1679 (tier-2/tier-1 2.04 light / 1.50 dark; tier-4/tier-2 2.79 / 2.20). The stated mitigations are real (role="img" summaries at :557/:1266/:1755, legends, BarRow prints value+pct as text), which correctly keeps this at minor under 1.4.11's redundant-presentation allowance — the amber series line is the strongest genuine instance since the lines themselves carry the trend.

### F073 — Breeding-code meanings are hover-only: full code labels live solely in title attributes on the Breeding Codes tab and comparer badges
- **Where:** `frontend/src/components/BreedingCodeTable.tsx:148` · **WCAG:** 1.4.13 Content on Hover or Focus AA (UA-tooltip exemption applies — flagged as the only route to the information) · **Found by:** gap:WCAG 1.4.13 Content on H
- **Issue:** Title-attribute tooltips are user-agent controlled and therefore exempt from 1.4.13 itself, but three locations make the title the ONLY route to what an eBird breeding code abbreviation means, and UA title tooltips never appear on keyboard focus or touch: (1) BreedingCodeTable.tsx:148 — the matrix column headers show only the code (e.g. "NY") with `title={def.label}`; the per-cell sr-only text (line 228) adds only the tier category ("Confirmed"), not the label. (2) BreedingCodeList.tsx:324 — the per-code filter pills, same pattern (`title={def.label}` with the bare code as content). (3) ChecklistComparer.tsx:31-40 — BreedingBadge renders only `def.code` with `title={`${def.code} — ${def.label}`}` and no aria-label. There is no visible legend on the Breeding Codes tab, and docs/HELP.md's Breeding Codes section (line 273+) describes the matrix without listing the 23 codes; the full labels are visible only in Species Detail's per-species breeding section (SpeciesDetail.tsx:~1003), which covers just the codes that one selected species has. Mouse users get the UA tooltip and screen readers get the title as accessible description (name-from-content is the code, title falls back to description), but a sighted keyboard or touch user — including anyone on the self-hosted/tablet path — has no way on these views to learn that "FL" means recently fledged young.
- **Evidence:** BreedingCodeTable.tsx:148 `title={def.label}` on a <th> whose visible content is `{code}{sortIndicator(code)}`; BreedingCodeList.tsx:324 `title={def.label}` on the code pill whose content is a color dot + `{code}`; ChecklistComparer.tsx:31 `title={`${def.code} — ${def.label}`}` on a span containing only `{def.code}`; grep finds no other rendering of def.label in those three files and no code legend in docs/HELP.md.
- **Fix:** Add a non-hover route on the Breeding Codes tab: a collapsible "Code legend" disclosure above the matrix listing code → label (one shared component could also serve the comparer), or render the label in each column header at reduced size. Minimal alternative: append the label to each control's accessible name (aria-label={`${code} — ${def.label}`} on the pills/headers, keeping the visible code) and give ChecklistComparer's BreedingBadge the same aria-label — that fixes AT consistency, though a visible legend is what helps sighted touch/keyboard users.
- **Verification:** Verified at all three locations: BreedingCodeTable.tsx:148 (th content is just the code, title={def.label}; the per-cell sr-only at :228 adds only the tier category), BreedingCodeList.tsx:324 (pill with title only), and ChecklistComparer.tsx:31 (BreedingBadge, title only — on a non-interactive span, so even AT exposure is unreliable). grep confirms def.label renders nowhere else in those views, docs/HELP.md's Breeding Codes section lists no codes and its comparer section literally says 'Hover for the full code name', and full labels appear only in Species Detail's per-species section. ACCESSIBILITY.md's 'hidden text label' claim covers tier color, not code meaning, so nothing is disclosed; no existing finding overlaps (the confirmed breeding-code items are contrast-only).

### F074 — Comparer media/side-header cells fixed at 132px
- **Where:** `frontend/src/components/ChecklistComparer.tsx:74` · **WCAG:** 1.4.4 Resize Text AA · **Found by:** audit:resize
- **Issue:** The per-row media/count cell (`width: 132, flexShrink: 0`, ChecklistComparer.tsx:74) and its matching SideHeader (`width: 132`, :506) are fixed pixel widths holding 0.75rem text and badges. At 200% the cell contents (count + breeding-code badge + media badges) exceed 132px and push into the species-name column. Same family as the Statistics fixed-px finding but a separate file/owner, listed for completeness.
- **Fix:** undefined
- **Verifier refinement:** Prefer a shared rem-width constant (e.g. const SIDE_CELL_WIDTH = '9rem') used by both SideCell and SideHeader over minWidth — minWidth would let per-row cell widths diverge and break the A/B count alignment the fixed width exists for (per the code comment 'Fixed width so counts line up'). Also convert the inner count box at line 88 from width: 34 to a rem value (e.g. '2.25rem'), since it clips 3-digit counts at 200% independently of the cell width.
- **Verification:** Verified width: 132/flexShrink: 0 at line 74, the matching SideHeader 132 at line 506, plus a fixed 34px count box at line 88; lib/textScale.ts scales only rem text (--sr-text-scale up to 2x), so at 200% the rem-sized badge text outgrows the px boxes and a badge-dense row overflows or clips. Cannot be refuted via browser zoom: ACCESSIBILITY.md explicitly claims SC 1.4.4 conformance through the in-app control because the desktop app has no browser zoom, and its only zoom disclosure (horizontal scroll for tables/maps under 1.4.10) does not cover this. Minor is the right severity — localized overlap on the densest rows of one tab.

### F075 — Comment-count live regions announce on every keystroke
- **Where:** `frontend/src/components/Checklists.tsx:177` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** audit:aria
- **Issue:** CommentSearchBox's match count (<span aria-live="polite">N comments</span>, used by both the Checklist Comments and Species Comments sections) recomputes from the filter input on every keystroke, so a screen reader queues an "N comments" announcement per character typed, competing with character echo. The count announcement itself is good 4.1.3 practice — it just needs to settle.
- **Fix:** undefined
- **Verifier refinement:** The visible count and the live region are the same span, so a naive debounce would also lag the visible count. Split them: keep the existing visible span immediate but add aria-hidden, and render a visually-hidden sibling with aria-live="polite" fed by a ~400-500 ms debounced copy of the count text (debounced state via useEffect + setTimeout keyed on matches.length). Apply the same pattern to the line 668 checklist-count region, which has the same shape (though it is mostly click-driven, the date inputs can also type-churn it).
- **Verification:** Checklists.tsx:177 puts aria-live="polite" on the count span, and matches recomputes synchronously from the filter input (useMemo on filter, set in onChange), so every count-changing keystroke mutates the live region and queues an announcement over character echo — used by both comment sections. Slight overstatement ('every keystroke' is really 'every keystroke that changes the count') but substantively correct, and minor is the right severity. ACCESSIBILITY.md mentions polite count regions as a feature, not this chattiness as a known issue, so not already disclosed.

### F076 — The 'loading-saved' phase on four data tabs is a bare unlabeled spinner — nothing is announced and nothing names the state
- **Where:** `frontend/src/components/Checklists.tsx:469` · **WCAG:** 4.1.3 Status Messages AA (also 1.1.1 Non-text Content A) · **Found by:** gap:App states never exercis
- **Issue:** While a tab parses the stored CSV (the phase every visit passes through before the data or the SetupRequired panel appears), four tabs replace their entire panel with a centered Loader2 SVG and nothing else: Checklists.tsx:468-470 (the icon is even explicitly aria-hidden, leaving zero accessible content in the panel), LifeList.tsx:362-364, SpeciesDetail.tsx:408-410, and BreedingCodeList.tsx:174-176. There is no role="status", no aria-label, and no visible text, so a screen-reader user lands in an empty tabpanel with no indication anything is happening. Every other loading state in the app does this correctly and was recorded as a pass (boot skeleton role=status 'Loading SnowRaven', App.tsx TabLoading role=status with text, BirdingStats.tsx:464 computing status, SnowMap style placeholder) — these four bare spinners are the stragglers. Distinct from prior finding i=74 (which covers role=status nodes that mount with text already present — these have no status semantics at all).
- **Evidence:** Checklists.tsx:469 `<Loader2 size={24} strokeWidth={2} className="spin" style={{ color: 'var(--sr-accent)' }} aria-hidden />` as the sole child of the phase's wrapper div; identical Loader2-only blocks at LifeList.tsx:363, SpeciesDetail.tsx:409, BreedingCodeList.tsx:~176 (those three without even aria-hidden, but with no text either).
- **Fix:** Give each wrapper div role="status" and an aria-label (e.g. 'Loading saved eBird data') — or add visible text next to the spinner as WeatherTidePanel does ('Loading weather…') — and keep the SVG aria-hidden, matching the App.tsx TabLoading pattern.
- **Verifier refinement:** Matching the App.tsx TabLoading pattern (role='status' + label on the wrapper, SVG aria-hidden) is right; note per confirmed finding i=74 that a status node mounting with its label may still not be announced by all SRs, but it at least names the state for users who land in the panel — fixing both together would mean mounting the empty status container first and inserting the label.
- **Verification:** All four cited blocks verified: Checklists.tsx:466-472 (Loader2 with aria-hidden as the panel's sole content), LifeList.tsx:360-366, SpeciesDetail.tsx:406-412, BreedingCodeList.tsx:172-178 — no role='status', no aria-label, no text, leaving an empty tabpanel during the loading-saved phase every visit passes through. Genuinely distinct from confirmed i=74, which covers nodes that already have role='status' but mount with text present; these have no status semantics at all. Not disclosed in ACCESSIBILITY.md and not present in either findings file.

### F077 — Light theme: error text on error background is just below AA (4.41:1)
- **Where:** `frontend/src/components/Checklists.tsx:488` · **WCAG:** 1.4.3 Contrast (Minimum) AA · **Found by:** audit:contrast
- **Issue:** --sr-error #DC2626 on --sr-error-bg #FEF2F2 computes to 4.41:1 — marginally under 4.5:1 — and this pair styles every error banner and the 'negative' filter pill state: Checklists.tsx:236/488, LifeList.tsx:108/384, BreedingCodeList.tsx:196, Settings.tsx:304/480, ChecklistComparer.tsx:374, NamedBirds.tsx:108, WeatherTidePanel.tsx:186, SpeciesDetail.tsx:430, DropZone.tsx error text. Dark theme passes (7.07:1). Error on plain surface also passes (4.83:1), so only the tinted-banner pair needs a nudge.
- **Fix:** undefined
- **Verification:** Verified the pair co-occurs (Checklists.tsx:236 TriPill 'no' state and :488 error banner, plus the other listed files) and re-computed the ratio: #DC2626 on #FEF2F2 = 4.415:1, a genuine but marginal AA miss at 13px text; dark theme (7.07:1) and plain-surface (4.83:1) pass as the finding states. The 0.085 shortfall is perceptually negligible and light-theme-only, so 'moderate' overstates user impact — this is a compliance nudge, not a barrier; downgrading to minor. The fix is validated (#D31F1F = 4.82:1 on #FEF2F2, 5.27:1 on white) and convention-compliant as a single light-theme token edit in globals.css.

### F078 — External links open new windows without any indication (inconsistent across the app)
- **Where:** `frontend/src/components/HelpDocs.tsx:61` · **WCAG:** 3.2.5 / 2.4.4 advisory (G201) · **Found by:** audit:structure
- **Issue:** 43 target="_blank" anchors exist across ~20 component files; only two indicate the behavior — App.tsx:748 and App.tsx:898 use "(opens in new tab)" in aria-label. All others give no warning, including every link in the Help documentation (HelpDocs.tsx:61 renders all HELP.md links with target="_blank" rel="noreferrer" and no indication), the footer GitHub link (App.tsx:1101-1110), SpeciesLinks.tsx:20/45 (whose aria-labels "View X on eBird" omit it), LifeListTable.tsx media-count links, BirdingStats.tsx checklist/region links (15 instances), Checklists.tsx, MapExplorer.tsx, SpeciesDetail.tsx, NamedBirdRow.tsx, Settings.tsx, WeatherTidePanel.tsx, MediaCommentsSection.tsx, MediaStatsSections.tsx, ChecklistComparer.tsx, SightingsMap.tsx, map/HotspotMarkers.tsx, map/TargetMarkers.tsx, AtlasLayer.tsx, speciesDetail/ui.tsx, CommentText.tsx. Unexpected context changes disorient screen-reader and cognitive-disability users; the app already has the right pattern in two places — it just isn't applied consistently.
- **Evidence:** HelpDocs.tsx:61 `<a key={ki++} href={lm[2]} target="_blank" rel="noreferrer"` with no new-window indication; grep shows 43 target="_blank" instances, only App.tsx:748 and App.tsx:898 carry "(opens in new tab)".
- **Fix:** Adopt one shared ExternalA component (or extend SpeciesLinks' pattern) that appends "(opens in new tab)" to the accessible name (visually-hidden span or aria-label suffix) and use it for all target="_blank" anchors, including HelpDocs' renderInline link branch.
- **Verifier refinement:** A shared external-link component is sound; prefer a visually-hidden span suffix over aria-label so existing link text is preserved, and where an aria-label already exists (SpeciesLinks, icon-only links) append to the label, matching the wording already used at App.tsx:748/898. For icon-only favicon links keep title in sync with the label.
- **Verification:** Verified by grep: exactly 43 target="_blank" anchors across 20 component files, with only App.tsx:748 and App.tsx:898 announcing '(opens in new tab)'; HelpDocs.tsx renders every documentation link target="_blank" with no indication and SpeciesLinks.tsx aria-labels omit it. This is the new-window-indication aspect, explicitly distinct from confirmed #53 (which covers the ↗ links' accessible names), so not a duplicate. Correctly scoped as advisory (G201; 3.2.5 is AAA), so minor severity is right; not disclosed in ACCESSIBILITY.md.

### F079 — Multimedia (Life List) filter-count live region announces a wrong total when a county/date filter is active — '0 of 0 species' on a zero-result filter
- **Where:** `frontend/src/components/LifeList.tsx:437` · **WCAG:** 4.1.3 Status Messages AA · **Found by:** gap:App states never exercis
- **Issue:** Runtime-verified (Playwright, live app, data loaded): setting the From date to 2099-01-01 made the aria-live="polite" count region (LifeList.tsx:675) announce '0 of 0 species', telling a screen-reader (and sighted) user the dataset is empty rather than that the active filter matched nothing. Root cause: `const totalSpecies = displayEntries.length` (line 436) — but the `displayEntries` memo (lines 276-300) ALREADY applies the county/date location filter, so the denominator collapses with the numerator whenever a location/date filter is active (any nonzero case is equally wrong, e.g. '12 of 12 species' while hundreds exist). BreedingCodeList does this correctly — its denominator is the unfiltered `phaseData!.entries.length` (BreedingCodeList.tsx:243), which announced '0 of 92 species' in the same test.
- **Evidence:** Observed live-region text under From=2099-01-01: `{"live":"polite","text":"0 of 0 species"}` (Multimedia) vs `"0 of 92 species"` (Breeding Codes). Code: LifeList.tsx:436-439 `const totalSpecies = displayEntries.length; const countLabel = … : `${filteredCount} of ${totalSpecies} species``, where displayEntries is built from `rawEbirdObs.filter(o => { …countyFilter… dateRange… })` at lines 281-289.
- **Fix:** Compute the denominator from the unfiltered entry set (e.g. `phaseEntries.length`, or the pre-location-filter base list built with the same showSpuh/showNonBird/mergeSubspecies display options), mirroring BreedingCodeList.tsx:243, so the count label always reads 'X of <true total> species'.
- **Verifier refinement:** When an eBird backbone is loaded, the correct unfiltered denominator is the comprehensive base (buildComprehensiveEntries(rawEbirdObs, rawRows, mergeSubspecies)) with the same showSpuh/showNonBird filtering applied — phaseEntries.length alone is the ML-only entry count and undercounts in backbone mode. The suggestedFix's second option is the right one; also reuse it in filterStripText (line 447), which has the same collapsed denominator.
- **Verification:** Code-verified: LifeList.tsx:436 sets totalSpecies = displayEntries.length, but the displayEntries memo (lines 276-304) already applies the county/date filter, so the aria-live count at line 675 reads '0 of 0 species' (or 'N of N') whenever a location/date filter is active. BreedingCodeList.tsx:243 uses the unfiltered phaseData!.entries.length, confirming the in-house correct pattern. Not present in verified-findings.json or structure-findings.json (i=70 is a different live-region issue).

### F080 — Zero-result filter state in Multimedia and Breeding Codes renders a header-only empty table with no 'no results' message (Checklists has one)
- **Where:** `frontend/src/components/LifeListTable.tsx:253` · **WCAG:** 4.1.3 Status Messages AA (best-practice gap; the only signal is the count label) · **Found by:** gap:App states never exercis
- **Issue:** Runtime-verified: with a filter that matches nothing (From=2099-01-01), the Multimedia and Breeding Codes tabs render their full column-header row above a zero-row tbody with no visible or accessible 'no results match these filters' message anywhere in the panel (probe found 0 tbody rows and zero empty-state text nodes on both tabs). The only indication is the small count label ('0 of N species' — and on Multimedia that label is currently wrong, see the companion finding). The Checklists tab is the in-house model: it renders the explicit text 'No checklists match these filters.' (Checklists.tsx:705) distinct from its empty-data message, and MediaCommentsSection has emptyAll/emptyFiltered props. Locations: LifeListTable.tsx:253-254 (`<tbody>{sorted.map(…)}` with no empty branch) and BreedingCodeTable.tsx:253-area tbody (same pattern; its filter at BreedingCodeTable.tsx:29 can yield an empty `filtered`).
- **Evidence:** Runtime probe on both tabs with From=2099-01-01: `{"tableRows":0, "tablePresent":true, "emptyMsg":[]}`; Checklists in the same state showed `"emptyMsg":["No checklists match these filters."]` plus the announced count '0 of 3,163 checklists'.
- **Fix:** When the sorted/filtered row set is empty, render a single full-width cell (or a div below the table) with the Checklists wording — 'No species match these filters.' vs 'No species found in the stored file.' — in --sr-text-muted, in both LifeListTable and BreedingCodeTable. The existing aria-live count label then announces the change while the visible message explains it.
- **Verifier refinement:** Minor line correction: BreedingCodeTable's tbody is at lines 164-165, not ~253. Suggested fix is convention-compliant (--sr-text-muted exists in both themes); a full-width <td colSpan={...}> inside the tbody keeps the table structure valid.
- **Verification:** Code-verified: LifeListTable.tsx:253 (<tbody>{sorted.map(...)}) and BreedingCodeTable.tsx:164 (same pattern; its filter at line 29 can empty 'filtered') have no empty-row branch, and BreedingCodeList's only empty message (line 220) is gated on !hasLocationFilter, so a filtered-to-zero state renders a header-only table with no explanation. Checklists.tsx:705 renders the explicit 'No checklists match these filters.' message, so the inconsistency is real. Fairly flagged as a minor best-practice gap; no duplicate exists in either findings file.

### F081 — Life List species column is <td>, not a row header, unlike the Breeding Codes table
- **Where:** `frontend/src/components/LifeListTable.tsx:267` · **WCAG:** 1.3.1 Info and Relationships A · **Found by:** audit:sr-content
- **Issue:** BreedingCodeTable.tsx:174 correctly marks the species name cell <th scope="row">, but LifeListTable.tsx:267 uses a plain <td>. The Photo/Audio/Video count cells contain links whose only content is a bare number (e.g. "3"), so when navigating by cell or link, SRs that announce row headers would give the species context automatically in the Breeding table but not here. Column-header context still works, so impact is small.
- **Fix:** undefined
- **Verifier refinement:** Match BreedingCodeTable.tsx:174-187 fully: <th scope="row" style={{ padding: '9px 14px', verticalAlign: 'middle', textAlign: 'left', fontWeight: 'normal' }}>. The fontWeight: 'normal' reset (present in BreedingCodeTable at line 186 but omitted from the finding's snippet) matters because .sr-birdname-sci sets no font-weight, so the italic scientific-name line would otherwise inherit the th bold default.
- **Verification:** Verified both halves: LifeListTable.tsx:267 is a plain <td> while BreedingCodeTable.tsx:174 uses <th scope="row"> (with a comment explaining the textAlign reset), so the inconsistency is real and the bare-number link cells (e.g. "3") genuinely benefit from row-header semantics. Column headers still provide context, so this sits at the best-practice edge of 1.3.1; minor is the correct severity and the framing is honest, not pedantic.

### F082 — 'No media' minus icon nearly invisible (1.47:1 light / 1.70:1 dark)
- **Where:** `frontend/src/components/LifeListTable.tsx:288` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** Life List media columns render a Minus icon in --sr-gray-300 (#D1D5DB) on the white row surface = 1.47:1 (dark: #3F3F46 on #18181B = 1.70:1) to mean 'no media of this type' (repeated at lines 288, 302, 316, 331). The meaning ('zero') is inferable from the absence of a count link, which is why this is minor rather than moderate, but the chosen glyph as drawn fails non-text contrast in both themes.
- **Fix:** undefined
- **Verifier refinement:** Two of the finding's three suggested tokens fail its own 3:1 target: --sr-text-disabled computes to 2.56:1 light / 2.29:1 dark, and --sr-border-medium to 1.73:1 / 1.70:1 (its dark value IS #3F3F46, the currently-failing color). Use the third option only: color the glyph var(--sr-text-muted) (#71717A light = 4.83:1, #A1A1AA dark = 6.91:1; token already defined in both themes), either keeping the <Minus> icon or rendering a text em dash — the text variant additionally gives screen readers something to announce.
- **Verification:** Recomputed independently: --sr-gray-300 on --sr-surface is #D1D5DB/#FFFFFF = 1.47:1 light and #3F3F46/#18181B = 1.70:1 dark — exactly as reported, at lines 288/302/316/331, well under the 3:1 of SC 1.4.11. Minor is the right severity since the absent count link redundantly conveys 'zero'. Not covered by ACCESSIBILITY.md's lone known exception (chart-bar percentage labels).

### F083 — Comparison-mode switch uses tablist/tab roles without panels or arrow-key support
- **Where:** `frontend/src/components/ListComparer.tsx:150` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The Life Lists / Checklists mode switch is role="tablist" + role="tab" aria-selected, but there is no aria-controls, no role="tabpanel" for the swapped content, no roving tabindex, and no arrow-key handling — SR users are told they're in a tab widget whose conventions (arrows move, panel follows) don't hold. Inside the app shell's real tab system, this also nests tab semantics within a tabpanel.
- **Fix:** undefined
- **Verification:** Verified at ListComparer.tsx:150-165: role=tablist/tab with aria-selected but no aria-controls, no tabpanel on the swapped content (line 168), both tabs tabIndex={0} (no roving), and no onKeyDown anywhere in the file. The nesting claim also holds — App.tsx mounts ListComparer inside the id=panel-comparer role=tabpanel. Keyboard operation still works via Tab+Enter on the real buttons, so minor is the correct severity; ACCESSIBILITY.md does not disclose this (it asserts the main tab bar follows the standard arrow-key pattern), and the suggested SortSeg-style role=group/aria-pressed fix matches the existing convention in Checklists.tsx.

### F084 — Mobile filter-panel focus trap snapshots focusables once at open — focus can escape after content inside the panel changes
- **Where:** `frontend/src/components/MapExplorer.tsx:192` · **WCAG:** 2.4.3 Focus Order A (published claim: 'keeps it there while open') · **Found by:** audit:claims
- **Issue:** The statement says opening the Map Explorer filter panel 'moves focus into the panel and keeps it there while open' — and the trap (MapExplorer.tsx:187-221) does focus the first element, wrap Tab/Shift+Tab, and return focus on Escape. However `focusable` is queried once when the panel opens; the wrap comparison uses the stale `first`/`last` references. The panel's content is dynamic (the Filters accordion expands, async hotspot/target fetches append in-view list rows after the snapshot's last element), so after such a change, Tab from the true last element no longer matches `last` and focus walks out of the overlay onto the map content behind the backdrop. Edge case, but it makes the 'keeps it there' claim conditionally false.
- **Fix:** undefined
- **Verifier refinement:** Re-query `sidebar.querySelectorAll(...)` inside handleKeyDown on each Tab press and derive first/last from the live list. While there, filter to visible elements (e.g. `el.offsetParent !== null`) so the controls clipped inside the collapsed Filters accordion (maxHeight:0, overflow:hidden — still tabbable today) stop being invisible tab stops.
- **Verification:** Verified: the trap (lines 187-221, deps [sidebarOpen]) snapshots focusables once and wraps against captured first/last, while the sidebar's content is dynamic — the in-view marker lists and the async 'Find hotspots'/targets search results all render inside sidebarRef, so Tab from a newly-appended true-last element escapes the overlay, contradicting the ACCESSIBILITY.md 'keeps it there while open' claim. One correction: the Filters accordion expansion does NOT change the focusable set (its collapsed content stays in the DOM, merely clipped by maxHeight:0, and is already in the snapshot); the async list mutations are the real escape path. Edge case on mobile only, so minor is right; ACCESSIBILITY.md states the opposite, which is a claim, not a disclosure.

### F085 — Four inline `animation: 'spin …'` styles in MapExplorer bypass the .spin class convention
- **Where:** `frontend/src/components/MapExplorer.tsx:657` · **WCAG:** 2.3.3 Animation from Interactions (policy per ACCESSIBILITY.md) — regression-risk only, no current failure · **Found by:** audit:motion
- **Issue:** MapExplorer.tsx applies the spinner keyframes via inline style objects instead of the shared `.spin` class at four sites: line 657 (map loading chip, 'spin 1s linear infinite'), line 682 ('Locating…' spinner, 'spin 0.7s linear infinite'), line 1003 and line 1224 (hotspots / media-targets 'Finding…' spinners, 'spin 1s linear infinite'). There is NO current user impact: the reduced-motion kill in globals.css:588-594 uses a universal `*` selector with `!important` longhands, and author !important declarations override inline styles, so these spinners are correctly frozen for reduced-motion users today. The risk is silent regression: these are the only animations in the codebase that escape the `.spin` class, so any future refactor that scopes the RM block to classes (e.g. `.spin { animation: none }`) instead of `*` would leave these four spinning for reduced-motion users with nothing failing visibly. Reported once; all four locations listed here.
- **Fix:** undefined
- **Verifier refinement:** Suggested fix is correct and convention-aligned. One caveat: .spin runs at 0.7s while lines 657/1003/1224 use 1s, so adopting the class slightly speeds those three spinners — cosmetic and consistent with the rest of the app; line 682 already matches 0.7s.
- **Verification:** All four inline animation:'spin' sites exist as cited and a repo-wide grep confirms they are the only spinners bypassing the .spin class (line 1438 of the same file uses it). The reduced-motion block (globals.css:588-594) is `*` + !important longhands, which does override inline styles, so the finding's no-current-failure, regression-risk-only framing is accurate and fairly flagged against the published reduced-motion commitment in ACCESSIBILITY.md.

### F086 — WCAG 2.2 forward-looking: several form controls are under the 24px minimum target size
- **Where:** `frontend/src/components/MapExplorer.tsx:726` · **WCAG:** 2.5.8 Target Size (Minimum) — WCAG 2.2 AA · **Found by:** audit:forms
- **Issue:** Forward-looking 2.2 delta only. The atlas overlay role="switch" buttons are 38x22px (MapExplorer.tsx:726, :759, :788 — 22px tall, under the 24px minimum), and several zero-padding text buttons are ~16-18px tall: the API-key Show/Hide button (Settings.tsx:363-373, padding 0, 0.75rem text), the 'Clear filter' underline buttons (LifeList.tsx:694-703, Checklists.tsx:685-690, SpeciesDetail.tsx:658-667), and the 'Go to Settings →' banner buttons (App.tsx:611-619, padding 0). Most currently escape 2.5.8 via the spacing exception (no adjacent target within 24px), but that is fragile under layout changes and the small targets are already hard for motor-impaired users under 2.1 best practice.
- **Fix:** undefined
- **Verifier refinement:** The codebase already has a shared ui/ToggleSwitch.tsx (a 30px-tall role=switch button with the label inside) — prefer migrating the three inline atlas switches to it, or if the label-left/switch-right layout must stay, bump the bare track to >=24px tall (e.g. 44x24). For the zero-padding text buttons, padding (or min-height 24 with negative-margin compensation) as originally suggested is fine.
- **Verification:** All cited controls verified: the three hand-rolled role=switch buttons are 38x22 (MapExplorer.tsx:726, :759, :788 — 22px tall, under 24), and the padding-0 text buttons exist as described in Settings.tsx:363-373 (Show/Hide), LifeList.tsx:694-703 / Checklists.tsx:685-690 / SpeciesDetail.tsx:658-667 (Clear filter), and App.tsx:611-619 (Go to Settings), all at 0.75rem with no padding (~17px hit height). The finding honestly frames itself as a forward-looking WCAG 2.2 delta and acknowledges the spacing exception currently covers most of them, so minor is the right severity.

### F087 — Map Explorer Filters accordion button lacks aria-expanded
- **Where:** `frontend/src/components/MapExplorer.tsx:836` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The collapsible filter panel toggle in the sightings sidebar (button at :836, panel collapsed via max-height 0) exposes no expanded/collapsed state — the only cue is the rotated chevron. Every other disclosure in the app does carry aria-expanded (NamedBirdRow.tsx:36, ChecklistComparer.tsx:76/:440, TabNav.tsx:234).
- **Fix:** undefined
- **Verification:** The Filters disclosure button (MapExplorer.tsx:835-847) carries no aria-expanded; the only state cue is the rotated ChevronDown, and the panel collapses via maxHeight 0. Verified the cited convention claim: NamedBirdRow.tsx:36, ChecklistComparer.tsx:76 and :440, and TabNav.tsx:234 all set aria-expanded on their disclosures, so this is an isolated omission. Minor is right and the suggested one-attribute fix (aria-expanded={filterOpen}, optionally aria-controls) is correct and conventional.

### F088 — Map Explorer filters accordion clamps to maxHeight 600px with overflow:hidden — fails closed if content ever exceeds it
- **Where:** `frontend/src/components/MapExplorer.tsx:848` · **WCAG:** 1.4.4 Resize Text AA (forward-looking) · **Found by:** audit:resize
- **Issue:** The collapsible filter panel animates via `maxHeight: filterOpen ? 600 : 0` with `overflow: 'hidden'`. Measuring the current contents (species select, date range, county, breeding seg-control, media select ≈300px at 100%; ≈450-500px at 200% with seg-control wrapping), there is headroom today, so this is not a present-day failure. But the construction fails silently: any future filter addition, or further text growth, pushes the bottom controls (the Media select) past 600px where they are clipped and unreachable with no scrollbar — the classic animated-max-height trap. Flagged so it isn't extended blindly.
- **Fix:** undefined
- **Verifier refinement:** Both suggested options work; the grid-template-rows 0fr/1fr technique requires `min-height: 0` plus `overflow: hidden` on the inner row and is supported in WKWebView (Safari 16+) and WebView2, so it is safe for the desktop targets. The transitionend→maxHeight:'none' variant is the smaller diff for this inline-styled component.
- **Verification:** Line 848 is exactly `maxHeight: filterOpen ? 600 : 0` with overflow:hidden and no scrollbar path; clipped content would be unreachable since the clamped div sits inside the sidebar scroll container. I re-measured at 200% text scale: selects/inputs/SegControl are fixed-height px and only rem labels grow, so contents land ~420-500px — under the clamp, confirming no present-day 1.4.4 failure exactly as the finding states. Fairly-flagged fail-closed construction on a panel that conditionally gains filters (County, Media), kept at minor as forward-looking only.

### F089 — Date-range filter is a native date picker on four tabs but free-text 'YYYY-MM-DD' fields on Map Explorer
- **Where:** `frontend/src/components/MapExplorer.tsx:862` · **WCAG:** 3.2.4 Consistent Identification (AA) — advisory; primarily a consistency/usability gap · **Found by:** gap:WCAG 3.2.1/3.2.2 (on-foc
- **Issue:** The identical 'filter sightings by date range' function is presented and identified two different ways. Life List (LifeList.tsx:644-669), Breeding Codes (BreedingCodeList.tsx:408-419), Species Detail (SpeciesDetail.tsx:629-654), and Checklists (Checklists.tsx:653-667) all use paired <input type="date"> controls — native picker UI, a Calendar icon, an accent-fill active state, and a '→' separator. Map Explorer alone implements the same function as two <input type="text"> fields whose only identification is the placeholder 'YYYY-MM-DD' (MapExplorer.tsx:862-865), under a 'Date Range' sidebar label. A user who has learned the app-wide date-picker idiom reaches the map and must instead hand-type ISO dates with no picker, no format validation affordance, and no visible from/to distinction (the indistinguishable naming is already reported; this finding is the cross-tab inconsistency of widget and identification). The interaction divergence is not a strict 3.2.4 failure (3.2.4 governs identification, and the labels that do exist don't conflict), so this is recorded as an advisory consistency defect with real cognitive-accessibility cost.
- **Evidence:** MapExplorer.tsx:862-865 `<input type="text" placeholder="YYYY-MM-DD" value={dateFrom} …/><input type="text" placeholder="YYYY-MM-DD" value={dateTo} …/>` vs LifeList.tsx:644-647 `<input type="date" value={dateRange.from} …/>` (same pattern on BreedingCodeList.tsx:408, SpeciesDetail.tsx:629, Checklists.tsx:653).
- **Fix:** Switch the Map Explorer sidebar date fields to the same paired <input type="date"> pattern used everywhere else (with the From/To accessible names the prior audit already calls for); keep the visible 'Date Range' SidebarLabel, which the other tabs could in fact adopt.
- **Verifier refinement:** Switching to type="date" also moots the 'YYYY-MM-DD' placeholder-as-name issue from #56/#59 — apply the From/To aria-labels (as Checklists.tsx:655/663 already does) in the same change.
- **Verification:** Verified: MapExplorer.tsx:862-865 uses two <input type="text"> with only a 'YYYY-MM-DD' placeholder under one 'Date Range' label, while LifeList.tsx:644, BreedingCodeList.tsx:408, SpeciesDetail.tsx:629, and Checklists.tsx:653 all use paired <input type="date"> (one evidence overstatement: Checklists' pair has no Calendar icon or accent-fill active state — those three trimmings are on the other three tabs only, but the core text-vs-native-picker divergence holds). The advisory framing is fair — same function, different widget and identification, with real cognitive/motor cost from hand-typing ISO dates. Distinct aspect and fix from confirmed #56/#59, which cover the accessible-name/placeholder problem on the same inputs.

### F090 — WCAG 2.2: 12px icon-only eBird link in Nearest Unvisited Hotspots rows is below Target Size minimum
- **Where:** `frontend/src/components/MapExplorer.tsx:1108` · **WCAG:** 2.5.8 Target Size (Minimum) AA (WCAG 2.2, forward-looking) · **Found by:** audit:maps
- **Issue:** The external eBird link in each 'Nearest Unvisited Hotspots' row is an inline-flex anchor whose only content is a 12px ExternalLink icon with no padding, giving a ~12-14px hit target packed between the location-name button and the distance text — below the 24x24 CSS px minimum of WCAG 2.2 SC 2.5.8 and a genuinely hard touch/low-motor target. (For comparison, the equivalent crosshair locate buttons at :1326-1330 are 26x26 and pass.)
- **Fix:** undefined
- **Verification:** Verified at MapExplorer.tsx:1108-1118: the anchor is inline-flex with no padding around an ExternalLink size={12} icon, giving a ~12px hit target sitting 6px from the flex:1 location-name button on one side and the distance text on the other — under 2.5.8's 24px minimum, and the 6px gap means the spacing exception does not rescue it (the 24px circles of the anchor and the adjacent ~15px-tall name button intersect). The 26x26 crosshair comparison at :1326-1330 checks out. Fairly flagged as a forward-looking 2.2 item; minor is right and the suggested fixed 24x24/26x26 centered box matches the existing crosshair-button pattern.

### F091 — Recency tier is color-alone at a glance (sighted users); the faint 'old' dot compounds it in light theme (1.64:1)
- **Where:** `frontend/src/components/MapExplorer.tsx:1310` · **WCAG:** 1.4.1 Use of Color A · **Found by:** audit:contrast
- **Issue:** The fresh/mid/old recency tiers on the Nearest Targets sidebar dots and the on-map chip backgrounds are differentiated only by color for sighted users; the sr-only labels (MapExplorer.tsx:1311-1313) cover screen readers — so ACCESSIBILITY.md's specific claim about hidden labels on recency dots is TRUE — and the popup shows the tier as text ('≤7 days') plus the date, so the information is reachable by interaction. But a color-blind user scanning the list/map cannot rank targets without clicking each one, and the light-theme 'old' dot (#A8D4BB) is also only 1.64:1 against the sidebar surface (its compensating border is --sr-border at 1.27:1).
- **Fix:** undefined
- **Verification:** Re-ran the WCAG math: light-theme --sr-map-target-old #A8D4BB vs the sidebar's var(--sr-surface) #FFFFFF is 1.64:1 and the dot's --sr-border #E4E4E7 border is 1.27:1 — the reported ratios are exact (the 'old' tier takes the var(--sr-border) border branch at :1310 since its text token isn't 'white'). The sidebar row (:1310-1319) shows name/location/distance but no date or tier text, and the on-map chips (TargetMarkers.tsx) are colored by tier only, so the tier IS color-alone at a glance for sighted users; the sr-only labels (:1311-1313) and the popup's '≤7 days' text + date exist as the finding accurately concedes. Minor is fair given the interaction path, and the fix is valid: --sr-accent-strong exists in both themes (globals.css:30/:131) and is 7.98:1 against the light surface.

### F092 — Settings radiogroups use role=radio without the expected arrow-key behavior
- **Where:** `frontend/src/components/Settings.tsx:519` · **WCAG:** 4.1.2 Name, Role, Value (ARIA radiogroup pattern) AA · **Found by:** audit:forms
- **Issue:** The Color theme (:119), Text size (:519), and Date format (:586) groups are role="radiogroup" containing role="radio" buttons that are all tabIndex={0}. Screen readers announce them as radio buttons (e.g. "radio, 1 of 4") and instruct arrow-key navigation, but arrow keys do nothing and unchecked radios remain in the Tab order — behavior that contradicts the announced role. Users can still operate everything with Tab+Enter, so impact is confusion rather than blockage.
- **Fix:** undefined
- **Verification:** Same verified facts as index 37: three role=radiogroup/role=radio groups (119, 519, 586) with all-tabIndex={0} buttons, no arrow-key handling, no roving tabindex, so SR-announced radio semantics contradict actual behavior while Tab+Enter still works. The finding correctly scopes impact to confusion rather than blockage, so minor is appropriate (note 4.1.2 is Level A, not AA as labeled — immaterial). The suggested downgrade to the house aria-pressed pattern is validated (SortSeg at Checklists.tsx:67, aria-pressed used across BreedingCodeList and others).

### F093 — axe aria-prohibited-attr: aria-label on non-interactive drag-handle <div>s in Tab Layout settings
- **Where:** `frontend/src/components/Settings.tsx:722` · **WCAG:** 4.1.2 Name, Role, Value A · **Found by:** audit:axe
- **Issue:** axe-core rule `aria-prohibited-attr` (impact: serious), 9 instances (one per tab row) on the Settings tab. aria-label is prohibited on an element with an implicit generic role, so the "Drag to reorder X tab" name is ignored or inconsistently exposed by AT — and since the handle is a plain div, it is not focusable or operable by keyboard anyway (the sr-only caveat note at Settings.tsx:699 acknowledges mouse-only reorder).
- **Fix:** undefined
- **Verification:** Verified: aria-label sits on a plain non-focusable div with implicit generic role (Settings.tsx:721-723), 9 instances (DEFAULT_TAB_ORDER has 9 tabs), and ARIA 1.2 prohibits naming generic elements — a real axe aria-prohibited-attr hit. But the handle is not interactive or in the tab order, so the ignored/inconsistent name costs AT users nothing functional on its own; worst case is a stray confusing announcement in browse mode, and the actual operability failure is separately reported (findings 3/64). Downgrading from axe's mechanical 'serious' to minor; the suggested fix (aria-hidden short-term, real <button> with keyboard reorder long-term) is correct and consistent with the codebase's aria-label-on-button pattern.

### F094 — MapLibre attribution toggle is partially obscured to a 24×10 px target on every map (WCAG 2.2 target-size)
- **Where:** `frontend/src/components/SnowMap.tsx:121` · **WCAG:** 2.5.8 Target Size (Minimum) AA — WCAG 2.2 delta · **Found by:** gap:App states never exercis
- **Issue:** axe-core's target-size rule flagged the compact attribution control's <summary class="maplibregl-ctrl-attrib-button" aria-label="Toggle attribution"> in every runtime map state scanned (Map Explorer light and dark, with a popup open, in fullscreen, and behind the Help overlay): the button is nominally 24×24 but is partially obscured so its smallest unobstructed space is 24×10 px, below the 24×24 minimum. This is the keyboard/pointer target that expands the legally-required tile-provider credits. The element was present in the prior light scan's DOM but appears in no recorded finding (the prior 2.2 target-size findings i=67/75/85/86 cover favicon links, the combobox chevron, sidebar form controls, and a hotspot eBird link; raw finding notes mention the attribution font size and the AtlasLayer close button only).
- **Evidence:** axe target-size node, repeated across map states: html `<summary class="maplibregl-ctrl-attrib-button" title="Toggle attribution" aria-label="Toggle attribution"></summary>`, data `{"messageKey":"partiallyObscured","minSize":24,"width":24,"height":10}` — 'Target has insufficient size because it is partially obscured (smallest space is 24px by 10px, should be at least 24px by 24px)'. SnowMap.tsx:121 `attributionControl={{ compact: true }}`.
- **Fix:** Find what overlaps the bottom-right ctrl corner (the map container's bottom edge/rounding or the adjacent control margin) and add bottom clearance via CSS — e.g. a `.maplibregl-ctrl-bottom-right { margin-bottom: ... }` rule or padding on the map container — so the full 24×24 summary is exposed; verify with axe target-size on the Map Explorer afterward.
- **Verifier refinement:** The obscuring element is the .sr-map-fab-cluster (fullscreen/Filters FAB), not the map edge. Scope the fix to the Map Explorer: either move the attribution clear of the cluster (e.g. a Map Explorer-scoped `.maplibregl-ctrl-bottom-right { margin-right: 60px }` so the 24x24 summary clears the 36px FAB at right:16px) or render the AttributionControl bottom-left there; the other maps need no change.
- **Verification:** The axe geometry checks out structurally: .sr-map-fab-cluster (globals.css:470 — bottom:20px, right:16px, z-index:1050, 36px-tall buttons) overlaps the compact attribution <summary> (24x24 at bottom-right with maplibre's 10px margin, occupying 10-34px from the bottom edge), leaving exactly the reported 24x10 unobscured strip. Scope correction: the fab cluster renders only in MapExplorer.tsx:1447, so this affects the Map Explorer map only, not 'every map' — all scanned states in the evidence were Map Explorer states. Real WCAG 2.2 target-size issue, distinct from the i=67/75/85/86 target-size findings; minor severity is right.

### F095 — Species combobox erases the in-progress typed query (and displayed value) on every refocus
- **Where:** `frontend/src/components/SpeciesDetail.tsx:481` · **WCAG:** 3.2.1 On Focus (A) — advisory; no conformance failure (usability/data-loss) · **Found by:** gap:WCAG 3.2.1/3.2.2 (on-foc
- **Issue:** The species selector's onFocus handler (SpeciesDetail.tsx:481-485) runs setSelectorQuery(''), setActiveOptionIdx(-1), setDropdownOpen(true). Judged against 3.2.1: this is NOT a change of context — focus stays in the input, nothing navigates, no focus is moved elsewhere, and opening the component's own listbox below the input is an accepted combobox auto-expand pattern (the persistent species selection and the page content beneath are untouched, since selectorDisplayValue at :400-402 only swaps between query and selected name). The conformance verdict is therefore PASS, but the query wipe is a real usability defect that lands hardest on keyboard, screen-reader, and screen-magnifier users: type a partial search (e.g. 'warb'), Tab or Shift+Tab away to check a filter, return to the input — the typed text is silently discarded, the input goes blank, and the listbox resets to the full unfiltered species list. Pointer users who click back into the field mid-edit lose their query the same way. There is no announcement that the value changed. Tab already closes the dropdown (:488), so the refocus wipe is not needed for that flow.
- **Evidence:** SpeciesDetail.tsx:481-485 `onFocus={() => { setSelectorQuery(''); setActiveOptionIdx(-1); setDropdownOpen(true) }}`; display value at :400-402 `const selectorDisplayValue = dropdownOpen ? selectorQuery : (selectedSpecies ?? '')`.
- **Fix:** Stop clearing the query on focus: keep onFocus={() => { setDropdownOpen(true) }} so the prior query (and its filtered list) is restored on refocus, and select the input's text instead (e.currentTarget.select()) so a user who wants a fresh search can just start typing to replace it. Reset the query only on explicit selection or Escape, where it already happens (:487, :501).
- **Verification:** Code matches exactly: SpeciesDetail.tsx:481-485 onFocus runs setSelectorQuery('') before opening the dropdown, and the display value at :400-402 swaps to the now-empty query, so a partial search typed before tabbing away (Tab keeps selectorQuery intact at :488) is silently wiped on refocus. The 3.2.1-passes analysis is correct and the advisory framing is honest, but the silent data loss is real and lands hardest on keyboard/SR/magnifier users, so it is not pedantry. Distinct from confirmed #29 (Escape strands focus on body) — different handler, different defect.

### F096 — Species selector chevron is a click-only unnamed <span>
- **Where:** `frontend/src/components/SpeciesDetail.tsx:521` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The open/close chevron beside the species combobox input is a <span onClick> with no role, no accessible name, and no keyboard access. The function is redundantly available (focusing the input opens the list, Escape closes), so impact is low, but it's an interactive element invisible to AT and unreachable by keyboard.
- **Fix:** undefined
- **Verification:** Verified at SpeciesDetail.tsx:521-531 — a <span onClick={() => setDropdownOpen(o => !o)}> with no role, no accessible name, no tabIndex, no key handler, and notably no aria-hidden either, so AT perceives a mystery element while keyboard users cannot reach it. Minor is correct since the function is fully redundant (input focus opens, Escape/Tab closes). The proposed fix (aria-hidden decorative, or a tabIndex={-1} button with aria-label per the APG combobox pattern) is right; note this duplicates finding 84.

### F097 — Species selector dropdown toggle chevron is a mouse-only <span>
- **Where:** `frontend/src/components/SpeciesDetail.tsx:521` · **WCAG:** 2.1.1 Keyboard / 4.1.2 Name, Role, Value A/AA · **Found by:** audit:forms
- **Issue:** The chevron that opens/closes the species combobox list is a <span onClick> with no role, name, or tab stop. Keyboard users are not blocked (the input opens the listbox on focus and Escape closes it), but a pointer-AT or switch user targeting the visible affordance gets a dead control, and it is invisible to screen readers.
- **Fix:** undefined
- **Verification:** Same element as finding 73 (SpeciesDetail.tsx:521-531) viewed through 2.1.1: the visible toggle affordance is a mouse-only <span> — dead for switch/voice users targeting it, invisible to SR — though a full keyboard path exists through the input itself, so minor is the right severity. Confirmed in current code; no aria-hidden present. This is a duplicate of finding 73 and should be merged with it; the fix options given are both acceptable, with the tabIndex={-1} named button being the APG-preferred one.

### F098 — WCAG 2.2 forward-looking: sub-24px targets (species favicon links, combobox chevron)
- **Where:** `frontend/src/components/SpeciesLinks.tsx:18` · **WCAG:** 2.5.8 Target Size (Minimum) AA (WCAG 2.2) · **Found by:** audit:aria
- **Issue:** The eBird and Birds of the World favicon links rendered next to every bird name are 14×14 px targets with a 5 px gap — under the 24×24 minimum, packed in dense table rows where the spacing exception doesn't obviously hold, and they appear hundreds of times (Life List, Breeding Codes, Checklists, Stats). The SpeciesDetail chevron (~16 px, SpeciesDetail.tsx:521) is similar. Not a WCAG 2.1 AA failure; flagged as a 2.2 delta.
- **Fix:** undefined
- **Verifier refinement:** Padding on .sr-favicon-slot works (the anchor wraps the slot, so the anchor's hit area grows); pair it with a matching negative margin so dense table rows don't widen, and keep the img at 14px (the slot's fixed-size layout-shift guard comment in globals.css still holds if width/height become the padded total). The chevron 24px hit box is optional — it is arguably exempt under the 2.5.8 Equivalent exception since the 40px-tall input toggles the same dropdown.
- **Verification:** Same favicon facts verified independently (largely a duplicate of finding 67's part (a)): 14x14 anchor hit areas with a 5px gap (centers 19px apart < 24px, so the spacing exception fails), rendered via BirdName across Life List, Breeding Codes, Checklists, and Stats. Correctly framed as a 2.2 delta, not a 2.1 AA failure; minor stands and ACCESSIBILITY.md does not disclose it. The chevron half is weaker: the full-width 40px combobox input opens the dropdown on focus/click (SpeciesDetail.tsx onFocus handler), so the ~16px chevron plausibly meets 2.5.8's Equivalent exception.

### F099 — WCAG 2.2 deltas: 14×14 favicon link targets and sticky-header focus obscuring
- **Where:** `frontend/src/components/SpeciesLinks.tsx:36` · **WCAG:** 2.5.8 Target Size (Minimum) AA (WCAG 2.2) · **Found by:** audit:keyboard
- **Issue:** Forward-looking 2.2 items. (a) Target size: the eBird / Birds of the World favicon links rendered beside nearly every bird name are 14×14px, placed directly adjacent to each other — below the 24×24 minimum with no qualifying spacing exception; they are the most frequently repeated small target in the app. The Species Detail combobox chevron (SpeciesDetail.tsx:521, ~16px <span>) is also undersized but pointer-redundant. Most other small controls measured are compliant (close 26×26, eye 28×28, locate 26×26, FABs 36×36). (b) 2.4.11 Focus Not Obscured: LifeListTable's sticky header row (position: sticky, top: 0 — LifeListTable.tsx:182-187) can fully cover row links (BirdName, ML catalog) when Shift+Tab scrolls a focused element up under it; same risk for BreedingCodeTable's sticky first column (line 128).
- **Fix:** undefined
- **Verifier refinement:** Favicons: add padding (e.g. 5px) on the anchors or .sr-favicon-slot, offset with negative margin to preserve dense-row layout, keeping the img at 14px. Sticky tables: scroll-padding-top must go on the ACTUAL scrolling element — for LifeListTable that is the document (html), not the table wrapper (the wrapper is only a horizontal scrollport, and in wideMode the header sticks to the viewport; scroll-padding-top on the wrapper would be a no-op). For BreedingCodeTable's sticky column the analog is scroll-padding-left: 220px on the overflowX:auto wrapper div.
- **Verification:** Verified: each favicon anchor's hit area is the 14x14 .sr-favicon-slot (globals.css:285) with no padding and a 5px gap, so centers are 19px apart and the 2.5.8 spacing exception fails; SpeciesLinks is composed into the app-wide BirdName, so the targets repeat everywhere. The 2.4.11 half is real but narrower than stated: LifeListTable's sticky header only engages in wideMode (the default-mode overflowX:auto wrapper becomes the scrollport and never scrolls vertically, so the header never sticks), while BreedingCodeTable's sticky 220px first column does engage and can cover the tabIndex=0 sortable column headers. Fairly flagged as forward-looking 2.2 deltas at minor; ACCESSIBILITY.md discloses neither.

### F100 — Statistics map rank-pin numerals are fixed 10px SVG text — only text in the app that ignores the Text Size control
- **Where:** `frontend/src/components/statsPrimitives.tsx:98` · **WCAG:** 1.4.4 Resize Text AA · **Found by:** audit:resize
- **Issue:** RankIcon renders a fixed 24×24 SVG with `fontSize="10"` for the rank numbers on the Statistics geographic map pins (BirdingStats.tsx:865, 871). The numeral does not scale with the Text Size control or OS text size. Mitigations: it is map content (markers on a map get latitude under 1.4.10/1.4.4), and the same ranking is available as scalable text in the "Top locations by checklists/species" lists directly below (BirdingStats.tsx:906, 921). Reported as polish, plus a note: the icon fills are hardcoded hex (`#2D8653`, `#3B82F6`, echoed in the legend swatches at BirdingStats.tsx:888/892 and the popup gray `#71717A` at :876), which violates the project's own --sr-* token rule.
- **Fix:** undefined
- **Verifier refinement:** Em-sizing the SVG works (DOM markers inherit the scaled root font-size; note pins double in footprint at 200% and overlap more — acceptable). But do NOT blind-swap the pin fills to var(--sr-accent)/var(--sr-graph-photo): those tokens lighten in dark theme (#34D399/#60A5FA) and the white numeral would drop well below AA, while the pins sit on theme-independent map tiles where the current dark fills are correct in both themes — mint dedicated --sr-rank-pin-circle/--sr-rank-pin-square tokens in BOTH :root and [data-theme=dark] (same dark values in each is fine) and use them for both RankIcon and the legend swatches at BirdingStats.tsx:888/892. The popup gray at :876 should become var(--sr-text-muted) — that one is a real dark-theme AA fix (3.67:1 today), not just hygiene.
- **Verification:** RankIcon (statsPrimitives.tsx:92-101) is exactly as described — fixed 24x24 SVG with fontSize="10" numerals used as map Markers at BirdingStats.tsx:865/871, and a grep confirms it is the only SVG <text> in the app, so the numerals are the one text surface that ignores the Text Size control. The stated mitigations hold (the identical ranks render as scalable HTML text in the lists at BirdingStats.tsx:904-925, and map markers get 1.4.10/1.4.4 latitude), so minor/polish is the right severity; the bundled token complaint is also real — #2D8653/#3B82F6/#71717A are hardcoded against CLAUDE.md's explicit 'no hardcoded hex in component files' rule. The popup gray is worse than stylistic: the MapLibre popup background is themed (var(--sr-surface) = #18181B dark), and I computed #71717A on it at 3.67:1, below AA, while var(--sr-text-muted) is #A1A1AA in dark theme precisely for this.

### F101 — Tablist lacks Home/End keys (APG completeness)
- **Where:** `frontend/src/components/TabNav.tsx:109` · **WCAG:** 2.1.1 Keyboard A (advisory — APG tabs pattern; no AA failure) · **Found by:** audit:focus
- **Issue:** The desktop tab bar implements roving tabindex with ArrowLeft/ArrowRight + wrap (TabNav.tsx:109-123) but not Home/End to jump to the first/last tab, which the WAI-ARIA APG tabs pattern recommends (the collapsed dropdown DOES implement Home/End at lines 197-204, so the two modes are inconsistent). With 10+ tabs this is several extra keystrokes, not a barrier — forward-looking polish only.
- **Fix:** undefined
- **Verification:** TabBar's tablist onKeyDown (TabNav.tsx:109-123) handles only ArrowLeft/ArrowRight while the collapsed dropdown implements Home/End (lines 197-204), confirming both the gap and the cross-mode inconsistency. The finding correctly self-classifies as advisory APG polish with no AA failure (Home/End are optional in the APG tabs pattern, and arrow wrapping caps traversal at ~half the tab count), so minor is the right severity. The suggested fix mirrors an existing in-file pattern and is sound.

### F102 — TabDropdown listbox misuses aria-activedescendant and trigger lacks aria-controls
- **Where:** `frontend/src/components/TabNav.tsx:269` · **WCAG:** 4.1.2 Name Role Value A · **Found by:** audit:aria
- **Issue:** The dropdown menu manages focus by moving DOM focus to the option buttons (roving), yet the listbox container also sets aria-activedescendant — on an element that itself never receives focus (no tabindex), where the attribute has no effect, and pointing at the SELECTED tab rather than the focused option. All options are also tabIndex={0} (the pattern expects -1 with the container or roving single stop), and the trigger (:233) has aria-haspopup/aria-expanded but no aria-controls referencing the listbox. Functionally usable (DOM focus carries the announcements) but the ARIA contradicts the actual focus model.
- **Fix:** undefined
- **Verification:** All claims verify in current code: line 269 sets aria-activedescendant={`tab-${activeTab}`} on a listbox div that never receives focus (no tabindex) and points at the selected rather than focused option, options are tabIndex={0} (line 300) despite roving DOM focus, and the trigger (lines 229-237) lacks aria-controls (the listbox also has no id to reference). The component is functionally usable since DOM focus carries announcements, so minor is the correct severity for this ARIA-contradicts-focus-model issue. The suggested fix (drop activedescendant, tabIndex={-1} on options, listbox id + trigger aria-controls) is correct — programmatic .focus() works on tabindex=-1 elements, so the existing focusItem logic is unaffected.

### F103 — Bird-name links replace the standard focus ring with only a color change + underline
- **Where:** `frontend/src/globals.css:306` · **WCAG:** 2.4.7 Focus Visible AA (weak indicator; 2.4.13 Focus Appearance fail forward-looking) · **Found by:** audit:focus
- **Issue:** `.sr-birdname-link:hover, .sr-birdname-link:focus-visible { color: var(--sr-accent); text-decoration: underline; outline: none; }` — the class rule out-specifies the global `button:focus-visible` outline, so the most common interactive element in the app (every species name in Life List, Breeding Codes, Checklists, Map Explorer, Statistics, comparer rows) shows only a green tint + underline on keyboard focus, identical to the mouse hover style. In a dense table of hundreds of rows this is materially harder to track than the 3px ring used everywhere else, and it weakens the ACCESSIBILITY.md claim of a 'clear green outline' wherever focus lands. The global rule's faint 12% box-shadow still applies but contributes almost nothing.
- **Fix:** undefined
- **Verifier refinement:** No selector split needed: just delete `outline: none` from the combined rule — `.sr-birdname-link:hover, .sr-birdname-link:focus-visible { color: var(--sr-accent); text-decoration: underline; }`. Hover never paints an outline on a non-focused element, and the suggested split's `:hover { outline: none }` would suppress the ring whenever a focused name is also hovered; with the override gone, the global `button:focus-visible` 3px ring applies on keyboard focus only.
- **Verification:** Rule verified verbatim at globals.css:306; BirdName renders a <button class="sr-birdname-link"> with no other focus styling, and the (0,2,0) class selector beats the (0,1,1) global button:focus-visible ring, leaving only color+underline plus the near-invisible 12%-alpha box-shadow. However keyboard focus still produces a clearly visible change (4.18:1 color shift plus an underline appearing), so WCAG 2.1 SC 2.4.7 technically passes and 2.4.13 Focus Appearance is AAA in WCAG 2.2 — this is a real consistency issue that also contradicts ACCESSIBILITY.md's 'clear green outline' claim, but not an AA failure, so moderate overstates it.

### F104 — Form-control borders are far below non-text contrast (1.19–1.34:1)
- **Where:** `frontend/src/lib/mapExplorerFormat.ts:23` · **WCAG:** 1.4.11 Non-text Contrast AA · **Found by:** audit:contrast
- **Issue:** --sr-border (#E4E4E7 light / #27272A dark) is the only boundary cue on text inputs, selects, date inputs and unselected pills throughout the app (SELECT_STYLE here; the same '1px/1.5px solid var(--sr-border)' pattern on inputs in MapExplorer, Checklists, LifeList, Settings, SpeciesDetail). Computed: 1.27:1 light / 1.19:1 dark vs surface, 1.21:1 / 1.34:1 vs page bg; even --sr-border-medium is 1.73:1 light / 1.70:1 dark. Inputs whose extent is identified only by this border fail 1.4.11; the focus ring fixes it only once focused.
- **Fix:** undefined
- **Verifier refinement:** The --sr-border-input token approach is right (define in both :root and [data-theme="dark"]), but the suggested dark value #5F5F66 is only 2.80:1 vs --sr-surface #18181B — below the fix's own 3:1 target. Use ~#6B6B73 (3.35:1) or #707078 (3.61:1) for dark; light #949499 passes at 3.02:1 but only barely, so a slightly darker value gives margin.
- **Verification:** Re-computed all ratios and they match: --sr-border vs surface is 1.27:1 light / 1.19:1 dark (1.21/1.34 vs page bg), --sr-border-medium 1.73/1.70 — the borders are near-invisible. But the W3C Understanding of 1.4.11 does not require a visible boundary when the control is otherwise identifiable, and every cited control carries a placeholder ('Search by place name', 'Paste your API key', 'e.g. 37.8275') and/or a visible label, and SELECT_STYLE selects show a ~4.7:1 chevron glyph — so this is a genuine low-vision usability weakness, not a clear normative AA failure. Confirmed as a worthwhile polish fix at minor severity rather than moderate.

### F105 — Inline `outline: 'none'` on ~20 inputs/selects suppresses the designed focus ring app-wide
- **Where:** `frontend/src/lib/mapExplorerFormat.ts:27` · **WCAG:** 2.4.7 Focus Visible AA (1.4.11 degradation) · **Found by:** audit:focus
- **Issue:** One repeated pattern: form controls styled with inline `outline: 'none'`, which overrides the global input ring (globals.css:556-563, `outline: 2px solid var(--sr-accent)`). The only surviving indicator is the rule's `border-color: var(--sr-accent) !important` (stylesheet !important still beats inline) plus a 12%-alpha shadow — a 1.5px gray→green border tint instead of the designed 2px ring, easy to miss in dense filter sidebars, and weaker than ACCESSIBILITY.md's published 'border-hugging ring on inputs'. Locations: lib/mapExplorerFormat.ts:27 (SELECT_STYLE shared by all Map Explorer selects); MapExplorer.tsx:88,690,692,863,865,1184; LifeList.tsx:627,653,667; BreedingCodeList.tsx:388,419,435; SpeciesDetail.tsx:518,613,638,652,1273; Checklists.tsx:170,265; MediaCommentsSection.tsx:75; Settings.tsx:435,1219,1231,1243. Technically still a visible state change (so not a hard 2.4.7 fail), but a significant app-wide degradation versus the global design.
- **Fix:** undefined
- **Verification:** Verified all claims: 26 inline outline:'none' occurrences match the listed locations, inline style beats the non-!important outline in globals.css:556-563, and only the !important border-color plus the stylesheet box-shadow survive — the designed 2px ring never renders on these controls. However the surviving indicator (1.5px border flipping gray→accent at 4.52:1 light / 9.22:1 dark vs surface, plus a 4px glow, with explicit onFocus borderColor handlers in Settings) is a perceivable, conventional focus treatment, so 2.4.7 AA is not actually failed — the finding itself concedes this. Real as an app-wide design-consistency issue that also makes ACCESSIBILITY.md's published 'border-hugging ring on inputs' claim inaccurate, but a non-failing degradation grades as minor, not moderate.

### F106 — Southern-hemisphere moon emoji mirroring makes screen readers announce the wrong phase
- **Where:** `frontend/src/lib/weatherFormatter.ts:94` · **WCAG:** 1.1.1 Non-text Content A · **Found by:** audit:sr-content
- **Issue:** The moon-phase emoji is the ONLY carrier of moon-phase information in night weather blocks (no adjacent text line — the condition emoji, by contrast, is followed by the textual condition on the next line). For northern latitudes this is acceptable: SRs read Unicode emoji names ("waxing gibbous moon"), which match the phase. But MOON_SOUTH deliberately swaps to the visually-mirrored emoji for lat < 0, so a southern-hemisphere waxing crescent is rendered as 🌘 whose Unicode name is "waning crescent moon" — a SR (and anyone hovering for the character name) hears the opposite phase. Constrained by the paste-into-eBird output format, so an in-app-only fix is appropriate.
- **Fix:** undefined
- **Verifier refinement:** In the in-app renderings only (App.tsx Weather-tab <pre> ~line 811 and WeatherTidePanel's MonoBlock), split off the header line and wrap the emoji run in <span role="img" aria-label="..."> — a bare span aria-label is not reliably honored by SRs without role="img". Derive the true phase name by exporting it from weatherFormatter.ts (e.g. a moonPhaseName(unixTs) beside moonPhaseEmoji, or reverse-lookup the rendered emoji's index in MOON_SOUTH when lat < 0; northern emoji names are already correct). Do NOT change formatWeatherBody's output string — it is byte-locked with backend/formatters/weather.py and the commentBlocks.ts stripper anchors on the unspaced contiguous emoji run.
- **Verification:** weatherFormatter.ts:93-102 swaps to mirrored emoji for lat<0, so all six asymmetric phases carry the opposite phase's Unicode name (verified: southern waxing crescent emits 🌘 'waning crescent moon'); the emoji is the block's only phase carrier (formatWeatherBody emits `${emoji}${moon}` with no phase text line) and renders as raw text in the Weather tab <pre> (App.tsx:827) and WeatherTidePanel's MonoBlock, where SRs announce it by character name. ACCESSIBILITY.md does not disclose this — it actually claims icon-only info carries hidden text labels. Minor is fair: narrow trigger (southern hemisphere + night checklist + SR) but the announced info is actively wrong, a genuine 1.1.1 failure.

### F107 — Desktop: no page-zoom path exists — Ctrl/Cmd+= is a no-op and the window floor is 800 CSS px, so the 1.4.10 reflow condition is unreachable and ACCESSIBILITY.md's zoom sentence is false on desktop
- **Where:** `src-tauri/tauri.conf.json:17` · **WCAG:** 1.4.10 Reflow AA (WCAG2ICT) / 1.4.4 Resize Text AA — plus published-claim accuracy · **Found by:** gap:Tauri desktop shell acce
- **Issue:** The main window sets minWidth: 800 (tauri.conf.json:17) and does NOT set zoomHotkeysEnabled, which defaults to false (verified in the repo's own @tauri-apps/cli config.schema.json: WindowConfig.zoomHotkeysEnabled, "default": false — on Windows it controls WebView2's IsZoomControlEnabled, i.e. Ctrl+hotkey AND Ctrl+wheel zoom; on macOS/Linux it injects a JS polyfill). No Rust code wires any menu, accelerator, or zoom command (src-tauri/src/lib.rs:48-68 registers only plugins and 5 commands; main.rs is 6 lines), and capabilities/default.json lacks the webview:allow-set-webview-zoom permission the macOS/Linux polyfill would require even if enabled. Net effect, derived statically: in the desktop app, Ctrl/Cmd +/−/wheel zoom does nothing on any platform, and the window can never present narrower than 800 CSS px. Consequences under WCAG2ICT reasoning: (1) the web audit's 1.4.10 verification at 320 CSS px and its 1.4.4 verification via browser zoom are both unreachable on desktop — the only enlargement mechanism is the in-app Text Size control capped at 200% (lib/textScale.ts:11, TEXT_SCALES max 2). That satisfies SC 1.4.4's 200% requirement, but the 400%-equivalent enlarged view SC 1.4.10 presupposes (1280px viewport at 400% = 320 CSS px) can never be produced, so the desktop product conforms to 1.4.10 only vacuously, and a desktop low-vision user who needs more than 2x enlargement has no in-app path at all (a web user has browser zoom to 400%+, stackable with Text Size). OS display scaling enlarges physical pixels but cannot push the window below 800 CSS px either. (2) ACCESSIBILITY.md:39 states "Browser/page zoom (Ctrl/Cmd +/−) works too" within a statement that explicitly covers the desktop app (line 3: "It runs in the browser and as a desktop app") — that sentence is false on desktop, and it sits two lines below line 37's own correct disclosure that the desktop app "has no browser zoom of its own", an internal contradiction. Status: the config default is documented and there is zero zoom wiring anywhere in src-tauri, so this is asserted statically; a live desktop run to confirm the hotkeys are inert is advisable (needs-runtime-verification as a formality only).
- **Evidence:** tauri.conf.json:12-19 window config contains only title/width/height/minWidth/minHeight; node_modules/@tauri-apps/cli/config.schema.json WindowConfig.zoomHotkeysEnabled → "default": false, macOS/Linux polyfill "Requires `webview:allow-set-webview-zoom` permission"; src-tauri/capabilities/default.json has no webview:* permission; ACCESSIBILITY.md:39 "Browser/page zoom (Ctrl/Cmd +/−) works too." vs :37 "the desktop app, which has no browser zoom of its own".
- **Fix:** Set "zoomHotkeysEnabled": true on the main window in tauri.conf.json and add "webview:allow-set-webview-zoom" to capabilities/default.json — Tauri then provides Ctrl/Cmd +/− page zoom from 20% to 1000% on all three desktop platforms, giving the desktop product a real 1.4.10/1.4.4 path (verify on a live build, especially the macOS/Linux polyfill). Independently and immediately, reword ACCESSIBILITY.md line 39 to scope the page-zoom sentence to the browser build (e.g. "In the browser, page zoom (Ctrl/Cmd +/−) works too") so the published statement is true for desktop, and update it again when the hotkeys ship.
- **Verifier refinement:** Suggested fix is sound: add "zoomHotkeysEnabled": true to the window in tauri.conf.json and "webview:allow-set-webview-zoom" to src-tauri/capabilities/default.json, verifying on a live build per platform. Independently reword ACCESSIBILITY.md:39 to scope page zoom to the browser build, and harmonize with line 37 in the same edit (per CLAUDE.md, update ACCESSIBILITY.md in the same change as the behavior change once hotkeys ship).
- **Verification:** Every static claim verified: tauri.conf.json:17 sets minWidth 800 with no zoomHotkeysEnabled (schema default false, macOS/Linux polyfill needs webview:allow-set-webview-zoom which capabilities/default.json lacks), src-tauri/src/lib.rs wires no zoom, and TEXT_SCALES caps at 2 — so the desktop app genuinely offers no enlargement beyond 200% and no path to a 320-CSS-px-equivalent view. But no WCAG 2.1 AA SC is actually failed: 1.4.4 is satisfied by the 200% Text Size control and 1.4.10 is vacuously met (the condition is unreachable, not violated), and ACCESSIBILITY.md:37 itself already discloses the desktop app 'has no browser zoom of its own', leaving only line 39's ambiguous sentence as the doc defect. Real and worth fixing (cheap config change, genuine benefit to low-vision desktop users needing >2x), but moderate overstates a beyond-AA gap; minor.

# Appendix B — Refuted findings (7)

Reported by an auditor, struck by adversarial verification. Listed for transparency.

- **Map popups have no close button and never manage focus for the links inside them** (`frontend/src/components/map/HotspotMarkers.tsx`) — The core claim ('never manage focus', 'focus stays on the sidebar row, links only reachable by tabbing through map chrome') is false: none of the five Popup usages override maplibre's focusAfterOpen, which defaults to true (maplibre-gl/src/ui/popup.ts:19), and @vis.gl/react-maplibre spreads props into new mapLib.Popup() then calls setDOMContent(container).addTo(map) in its mount effect — after the portal children are committed — so addTo→_update→_focusFirstElement() focuses the popup's first 'View on eBird →' link on open, i.e. the first half of the finding's own fix already ships by default. The only residue (closeButton={false}, remove() not restoring focus) drops focus to body solely when a pointer click/pan closes a popup whose link holds focus — a mixed-input edge case the finding itself concedes is non-serious given the redundant keyboard path (InViewMarkerList rows plus the Nearest Unvisited rows' own aria-labeled eBird links). That residue is polish, not a 2.4.3 AA failure.
- **Marker-kind sr-only labels are nested inside aria-hidden and never reach AT** (`frontend/src/components/map/MapSidebarUI.tsx`) — The nesting is real (MapSidebarUI.tsx:101-102: sr-only inside aria-hidden never reaches AT), but no information is lost: the only caller that passes getDotLabel (hotspots in view, MapExplorer.tsx:1068-1072) duplicates the exact kind in getSecondary's visible text ('Visited · N species' / 'Personal location · …' / 'Unvisited hotspot'), which is part of the same button's accessible name, and the sightings list's dot is one uniform color with no getDotLabel. ACCESSIBILITY.md's 'map recency dots' claim refers to the Nearest Targets dot (MapExplorer.tsx:1310-1314), which is NOT aria-hidden and does reach screen readers, so the doc claim is not made false. This is dead code, not a 1.3.1 failure — technically-true with no user impact.
- **No active color-scheme declaration; unstyled placeholders fall below AA in dark theme (~3.85:1)** (`frontend/src/globals.css`) — The premise 'no ::placeholder rule exists anywhere' is false: globals.css line 1 imports Tailwind v4 (4.2.4), whose preflight sets `::placeholder { color: color-mix(in oklab, currentcolor 50%, transparent) }` (node_modules/tailwindcss/preflight.css:293), applying in Chrome/Firefox/Safari 17+ and overriding the Chrome #757575 UA default the finding's 3.85:1 math depends on. Placeholder-bearing inputs use color: var(--sr-text) on var(--sr-surface), so the actual rendered dark-theme placeholder is ~#868688 on #18181B = 4.88:1 — it PASSES AA; the genuinely marginal pair is the LIGHT theme (50% of #0F1117 on white ≈ 3.54:1), the opposite of what was filed. The missing color-scheme declaration (index.css is indeed dead code — main.tsx imports only globals.css) affects native scrollbars/select chrome only, which the finding itself concedes is cosmetic.
- **Boot spinner's reduced-motion fallback slows the spin to 2s instead of stopping it** (`frontend/index.html`) — Code claims verify (index.html:23 slows to 2s; globals.css:588-594 has the !important kill), but I confirmed the built dist/index.html loads the bundled CSS as render-blocking <link>s in <head>, so first paint cannot occur before the document-wide reduced-motion kill applies — no RM user of any shipped build (Tauri or web/Pi) ever sees the 2s spin. The ACCESSIBILITY.md 'near-instant' claim is therefore exactly true as experienced, and the residual behavior exists only in dev mode or total-CSS-failure scenarios, neither user-facing. Technically-true but zero real user impact; the finding itself documents its own neutralization.
- **Collapsed-mode tabpanels reference nonexistent tab ids (dangling aria-labelledby)** (`frontend/src/App.tsx`) — Duplicate of confirmed finding #33 (TabNav.tsx collapsed dropdown orphans every tabpanel / dangling aria-labelledby). Code verified — App.tsx:588-590 panels reference tab-* ids that exist only while the collapsed dropdown is open (TabNav.tsx:69-73, 297-298) — but this is the same issue at the same root cause; the ancillary aria-activedescendant note is part of the same collapsed-dropdown ARIA wiring, not a separate user-facing defect.
- **prefers-reduced-motion propagation into the desktop webviews is platform-dependent and unverified, with no in-app fallback — ACCESSIBILITY.md's unconditional reduced-motion promise rests entirely on it (Linux/WebKitGTK explicitly unconfirmed)** (`frontend/src/lib/scroll.ts`) — The verifiable facts (media-query-only motion suppression in globals.css:588-594/scroll.ts, no in-app reduce-motion setting) check out, but the finding locates no actual defect: relying on prefers-reduced-motion is the standard, recommended implementation, all three webview engines implement the query (WKWebView, Chromium/WebView2, WebKitGTK via gtk-enable-animations), the only verified platform (macOS, the primary shipped target) is conceded reliable, and Linux is not a released artifact. The alleged ACCESSIBILITY.md:45 inaccuracy rests entirely on a hypothetical propagation failure for which no evidence is presented, and reduced motion is AAA (2.3.3) — no WCAG 2.1 AA impact exists. The proposed in-app override is a reasonable enhancement, not the fix for a demonstrated barrier; this is speculative platform-caveat material, not a code finding.
- **WeatherTideSection 'Load weather & tide' button unmounts itself on activation — keyboard focus drops to <body> and its Loading/disabled rendering is dead code** (`frontend/src/components/WeatherTideSection.tsx`) — Duplicate of confirmed finding i=28 in verified-findings.json ('One-shot buttons unmount on activation, dropping keyboard focus to <body>') — the code claim is fully accurate (setLoaded(true) at WeatherTideSection.tsx:95 unmounts the button gated by !loaded at :116, making the loading/disabled rendering at :124-125 dead), but it is the identical defect at the identical root cause, just a location i=28's batch omitted, and i=28 explicitly frames itself as the repeated pattern reported once. The only other consequence (loading announced via WeatherTidePanel's mount-with-text role="status" nodes) is likewise already recorded as confirmed pattern finding i=74, which the finding itself acknowledges. Nothing here is a different aspect; it is a location amendment to i=28.
