[
 {
  "severity": "critical",
  "title": "axe select-name: six filter <select> elements have no accessible name",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 853,
  "wcag": "4.1.2 Name, Role, Value A",
  "description": "axe-core rule `select-name` (impact: critical), 6 instances across 4 tabs at runtime. Screen-reader users hear only \"combo box\" with no indication of what each filter does. Locations: MapExplorer.tsx:853 (species filter), :872 (county filter), :896 (media filter); SpeciesDetail.tsx:604 (county filter); LifeList.tsx:616 (county filter, the Multimedia tab); BreedingCodeList.tsx:376 (county filter). The visual label is a decorative MapPin icon with pointerEvents:'none', so nothing names the control. Checklists.tsx:629/:644 does this correctly with aria-label=\"Protocol\"/\"County\" — that tab had zero select-name violations, proving the in-house fix pattern.",
  "fix": "Add aria-label to each select, matching the Checklists.tsx pattern: aria-label=\"Species filter\" / \"County\" / \"Media type\" on the MapExplorer selects, and aria-label=\"County\" on the SpeciesDetail, LifeList, and BreedingCodeList county selects.",
  "dim": "axe",
  "verifierNote": "verifier unavailable",
  "i": 0,
  "verdict": {
   "i": 0,
   "verdict": "confirmed",
   "correctedSeverity": "critical",
   "reasoning": "All six selects verified bare: MapExplorer 853/872/896 (SidebarLabel is an unassociated div), SpeciesDetail 604, LifeList 616, BreedingCodeList 376 (decorative MapPin only); grep confirms no aria-label/title on any of them, while Checklists.tsx 629/644 has the in-house aria-label fix. Accessible name computes to empty, so SR users hear only the current value with no control purpose — axe select-name at critical impact is fairly rated for six primary filter controls across four tabs. Minor imprecision only: for the three MapExplorer selects the visual label is the SidebarLabel div, not a MapPin icon.",
   "alreadyDisclosed": false
  },
  "src": "audit:axe",
  "id": "F001",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "Breeding-tier colored text on 8–15% tier tints fails in both themes (1.97–4.12:1)",
  "file": "frontend/src/components/BreedingCodeList.tsx",
  "line": 45,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "Active breeding pills set color: var(--sr-tier-N) over rgba(var(--sr-tier-N-rgb), 0.08–0.15) composited on the card surface. Computed over --sr-surface: LIGHT — tier-1 text on tier-1@0.15 = 2.32:1 (any active tier-1 code pill, and the 'Possible' filter pill in BirdingStats); DARK — tier-4 text 1.97:1 (the active 'Confirmed' category pill), tier-3 text 2.94:1, tier-2 text 4.12:1, tier-2-on-tier-1-tint 3.53:1. Same defect at BreedingCodeList.tsx:58-60 (category pills), SpeciesDetail.tsx:786-788 (breeding category pill), BirdingStats.tsx:1637-1638 (Confirmed/Probable/Possible filter pills). Related same-root-cause case: the white badge count on the tier-2 circle (BreedingCodeTable.tsx:219) is 3.96:1 in dark (tier-2 lightened to #A855F7) while passing in light (5.38:1).",
  "fix": "Introduce --sr-tier-N-fg tokens per theme for text-on-tint (dark theme needs the text far lighter than the fill tokens; light theme tier-1 needs the existing --sr-tier-1-text dark purple), and extend the tier-1-text pattern to the dark-mode tier-2 badge circle (dark text on #A855F7 would pass).",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 8,
  "verdict": {
   "i": 8,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Re-ran all contrast math independently (alpha-compositing tints over --sr-surface with globals.css token values): every claimed ratio reproduces exactly (light tier-1 2.32:1; dark tier-4 1.97, tier-3 2.94, tier-2 4.12, tier-2-on-tier-1 3.53; badge 3.96 dark / 5.38 light), and pill text is 12px/500 so the 4.5:1 normal-text threshold applies. The pairs genuinely co-occur in codePillStyle (line 45) and categoryPillStyle (lines 58-60) active states, plus the cited BirdingStats:1626/1637-1638, SpeciesDetail:786-788, and BreedingCodeTable:219 locations, all verified in current code. ACCESSIBILITY.md does not disclose this — it affirmatively claims filter pills meet 4.5:1 in both themes, so 'serious' is the right severity; the suggested --sr-tier-N-fg fix matches the existing --sr-tier-1-text both-themes token precedent and I verified dark #1A0030 on dark tier-2 #A855F7 = 4.88:1 passes.",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F003",
  "primaryHere": true
 },
 {
  "severity": "serious",
  "title": "Breeding-code badge contrast fails — in the exact category ACCESSIBILITY.md cites as exemplary (down to 1.71:1 in dark theme)",
  "file": "frontend/src/components/ChecklistComparer.tsx",
  "line": 35,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "The statement says interface colors meet AA, 'for example, the top breeding-code badge uses dark text on its light tier color.' That is true only for the Breeding Codes table's tier-1 badge (BreedingCodeTable.tsx:219, #3B0764 on #C084FC = 5.68:1 light / 7.31:1 dark). Elsewhere the same badge concept fails: (1) ChecklistComparer's BreedingBadge uses `color: var(--sr-on-accent)` on `background: TIER_COLORS[def.tier]` — light theme: white on tier-1 #C084FC = 2.64:1; dark theme: #052E16 on tier-2 #A855F7 = 3.77:1, tier-3 #7C3AED = 2.62:1, tier-4 #6B21A8 = 1.71:1 (the badge text is essentially illegible in dark mode). (2) BreedingCodeTable.tsx:218-219 badge in dark theme: white on tier-2 #A855F7 = 3.96:1 (0.6875rem bold = small text, needs 4.5). (3) SpeciesDetail.tsx:788 breeding pill renders its label in TIER_COLORS[tier]; for tier 1 in light theme that is #C084FC on a near-white 8%-tinted surface ≈ 2.6:1. The published example claim is materially false outside the one table.",
  "fix": "In ChecklistComparer, reuse the BreedingCodeTable pattern: per-tier text color (`--sr-tier-1-text` on tier 1, white on tiers 3-4) instead of --sr-on-accent, and add a dark-theme tier-2 text token (white on #A855F7 fails; #1A0030-style dark text passes at ~4.97:1); for the SpeciesDetail pill, use the darker tier-text token rather than the raw tier color for tier 1.",
  "dim": "claims",
  "verifierNote": "verifier unavailable",
  "i": 15,
  "verdict": {
   "i": 15,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Recomputed all ratios with WCAG luminance: ChecklistComparer's BreedingBadge fails in both themes (light tier-1 white-on-#C084FC = 2.64:1; dark #052E16 on tier-2/3/4 = 3.77/2.62/1.71) at 0.625rem bold small text, and the BreedingCodeTable dark tier-2 (3.96) and SpeciesDetail tier-1 pill (2.46 on the composited 8% surface) sub-claims also verify. ACCESSIBILITY.md line 31 holds the breeding badge up as the AA example and its only disclosed exception is chart-bar labels, so this is not disclosed. Serious is correct: 1.71:1 on confirmed-breeding codes in dark mode is effectively illegible and contradicts the published statement.",
   "fixNote": "Per the tokens-in-both-themes convention, define per-tier text tokens rather than branching inline: keep --sr-tier-1-text (exists in both themes), add --sr-tier-2-text (#FFFFFF in :root = 5.38:1 on #9333EA; #1A0030 in dark = 4.88:1 on #A855F7) and --sr-tier-3-text/--sr-tier-4-text (#FFFFFF both themes, 5.70–15.0:1). Export a TIER_TEXT_COLORS map alongside TIER_COLORS in lib/breedingCodes.ts and use it in ChecklistComparer's BreedingBadge AND BreedingCodeTable.tsx:219 (whose hardcoded '#fff' also violates the no-raw-hex rule). For the SpeciesDetail.tsx:789 pill, set the tier-1 label color to var(--sr-tier-1-text) instead of the raw tier color.",
   "alreadyDisclosed": false
  },
  "src": "audit:claims",
  "id": "F004",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "Filter form controls have no programmatic label across most data tabs",
  "file": "frontend/src/components/LifeList.tsx",
  "line": 616,
  "wcag": "1.3.1 Info and Relationships / 4.1.2 Name, Role, Value (also 2.4.6) AA",
  "description": "The county <select> and From/To date inputs are repeated across tabs with no aria-label, no <label for>, and no association to any visible text — a screen-reader user hears an unnamed combobox and two unnamed, indistinguishable date fields. Locations: LifeList.tsx:616 (county select), :644 and :658 (date from/to); BreedingCodeList.tsx:376, :408, :424 (same trio); SpeciesDetail.tsx:604, :629, :643 (same trio). Related instances where a VISIBLE label exists but is not programmatically associated: MapExplorer.tsx:853 (species select), :872 (county select), :896 (media select) and :862/:864 (date text inputs) — their 'Species'/'County'/'Media'/'Date Range' headings are SidebarLabel <div>s (map/MapSidebarUI.tsx:35-43), so the selects have no accessible name and the two free-text date inputs are distinguishable only by the placeholder 'YYYY-MM-DD' (format instruction also vanishes once filled — 3.3.2); Settings.tsx:1214, :1226, :1238 (Latitude/Longitude/Radius number inputs whose labels are plain <div>s at :1213/:1225/:1237); ChecklistComparer.tsx:350-353 ('Checklist A/B' <label> elements with no htmlFor, so the inputs' name falls back to the example-URL placeholder). Checklists.tsx:629-663 does this correctly (aria-label=\"Protocol\"/\"County\"/\"From date\"/\"To date\"), proving the house pattern exists.",
  "fix": "Match the Checklists.tsx pattern: add aria-label (\"County\", \"From date\", \"To date\") to the bare selects/date inputs in LifeList, BreedingCodeList, SpeciesDetail. In MapExplorer, render SidebarLabel as a <label htmlFor> (or add aria-label to each control), and give the two date inputs distinct names (\"Date from (YYYY-MM-DD)\" / \"Date to (YYYY-MM-DD)\"). In Settings, convert the Latitude/Longitude/Radius caption <div>s to <label htmlFor> with input ids. In ChecklistComparer, add htmlFor/id to the existing <label> elements.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 10,
  "verdict": {
   "i": 10,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Verified every cited location in current code: LifeList/BreedingCodeList/SpeciesDetail county selects and date inputs have no aria-label/label/title; MapExplorer's SidebarLabel is a bare <div> (MapSidebarUI.tsx:35-43) so its selects are unnamed and the two text date inputs share the identical 'YYYY-MM-DD' placeholder; Settings lat/lon/radius captions are plain <div>s; ChecklistComparer's <label> has no htmlFor and doesn't wrap the input. Checklists.tsx really does carry the aria-label house pattern the fix cites, and ACCESSIBILITY.md discloses only chart-label contrast as a known exception, not this. Serious is the right severity — repeated unnamed form controls across most data tabs is a genuine 4.1.2/1.3.1 SR barrier, though the controls remain operable.",
   "fixNote": "The fix is correct and convention-consistent (mirror Checklists.tsx's aria-label=\"County\"/\"From date\"/\"To date\"). One caveat: do not unconditionally convert SidebarLabel to <label htmlFor> — it also captions the Breeding Code SegControl, a button group with no labelable element. Either add aria-label per control in MapExplorer (including format-bearing names like 'Date from (YYYY-MM-DD)' on the free-text date inputs), or give SidebarLabel an optional htmlFor prop that renders <label> only when an id is passed.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F007",
  "primaryHere": true
 },
 {
  "severity": "serious",
  "title": "--sr-text-disabled used for real content text (2.29–2.56:1)",
  "file": "frontend/src/components/Settings.tsx",
  "line": 240,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "The disabled-text token (#A1A1AA light = 2.56:1 on surface, 2.45:1 on bg; #52525B dark = 2.29:1 on surface, 1.93:1 on surface-subtle) is widely used for meaningful, non-disabled content: Settings file sublabels (Settings.tsx:240) and full explanatory paragraphs (Settings.tsx:1143, 1174), file dates (:235), tab-layout 'HIDDEN' labels (:739), Species Detail counters and zero values (SpeciesDetail.tsx:461, 584, 690, 830, 926), comparer '—' and italic placeholders that convey 'no value' (ChecklistComparer.tsx:100, 534), the List Comparer section label (ListComparer.tsx:203), and the LifeList 'no filter' chip (LifeList.tsx:606). The WCAG inactive-control exemption does not apply to any of these — they are informative text.",
  "fix": "Replace --sr-text-disabled with --sr-text-muted (4.83:1 light / 6.91:1 dark on surface) everywhere the text is content rather than a label inside a genuinely disabled control; reserve the disabled token for disabled buttons/inputs only.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 9,
  "verdict": {
   "i": 9,
   "verdict": "confirmed",
   "reasoning": "Re-ran the WCAG math independently: every reported ratio is exact (light #A1A1AA on #FFFFFF = 2.56:1, on #F9FAFB = 2.45:1; dark #52525B on #18181B = 2.29:1, on #27272A = 1.93:1; muted = 4.83/6.91 on surface). The cited Settings.tsx sites (235, 240, 250, 388, 739, 1143, 1174) are informative content — file dates, sublabels, status chips, explanatory paragraphs — none inside a disabled control, so the 1.4.3 inactive-control exemption does not apply. ACCESSIBILITY.md's contrast section claims AA for body text and discloses only chart-bar labels, not this; serious is the right severity for sub-2.6:1 body-size content text.",
   "fixNote": "Swapping to --sr-text-muted is right for most sites, but light-theme muted (#71717A) on --sr-surface-subtle (#F4F4F5) is only 4.40:1 — just under AA. The 'No file saved' / 'No key saved' chips (Settings.tsx:250, 388) sit on surface-subtle, so for those either use --sr-text (or a new AA-checked token added to BOTH :root and [data-theme=\"dark\"] in globals.css) or lighten the chip background; all other cited Settings sites are on --sr-surface or --sr-bg where muted passes (4.83:1 / 4.62:1 light, 6.91:1+ dark).",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F012",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "Filter selects and date inputs have no accessible name on four tabs",
  "file": "frontend/src/components/SpeciesDetail.tsx",
  "line": 604,
  "wcag": "4.1.2 Name Role Value A",
  "description": "The county <select> and the From/To <input type=\"date\"> controls have no aria-label, no <label>, and no title — a screen reader announces an unnamed combo box / date field. Same defect repeats: LifeList.tsx:616 (select), :645, :659 (dates); BreedingCodeList.tsx:376 (select), :409, :425 (dates); MapExplorer.tsx:853 (Species select), :872 (County select), :896 (Media select) — the visual SidebarLabel is a styled <div> with no htmlFor association — and MapExplorer.tsx:862/:864 (two date text inputs distinguished only by placeholder \"YYYY-MM-DD\"). Checklists.tsx:629/:644/:655/:663 does this correctly (aria-label on each), proving the intended pattern exists in-repo.",
  "fix": "Add aria-label to each control matching the Checklists tab pattern (aria-label=\"County\", \"From date\", \"To date\", \"Species\", \"Media\"), or convert SidebarLabel to a <label htmlFor> association in MapExplorer.",
  "dim": "aria",
  "verifierNote": "verifier unavailable",
  "i": 5,
  "verdict": {
   "i": 5,
   "verdict": "confirmed",
   "reasoning": "Every cited location verified: unnamed selects at SpeciesDetail.tsx:604, LifeList.tsx:616, BreedingCodeList.tsx:376, and MapExplorer.tsx:853/:872/:896 where SidebarLabel (map/MapSidebarUI.tsx:35) is a plain styled <div> with no htmlFor; MapExplorer.tsx:862/:864 are text inputs distinguished only by identical 'YYYY-MM-DD' placeholders. Minor nit: HTML-AAM makes the placeholder a fallback accessible name on the two text inputs, but both compute to the same string so the fields remain indistinguishable, and the selects have no name at all. Serious is fair and the aria-label fix matches the existing Checklists.tsx pattern.",
   "alreadyDisclosed": false
  },
  "src": "audit:aria",
  "id": "F015",
  "primaryHere": false
 },
 {
  "severity": "critical",
  "title": "axe label: From/To date inputs on three tabs have no accessible name",
  "file": "frontend/src/components/SpeciesDetail.tsx",
  "line": 630,
  "wcag": "4.1.2 Name, Role, Value A (also 3.3.2 Labels or Instructions A)",
  "description": "axe-core rule `label` (impact: critical), 6 instances across 3 tabs at runtime. The date-range filter inputs are announced as unlabeled date fields, and the only visual relationship cue is a decorative Calendar icon and a \"→\" span. Locations: SpeciesDetail.tsx:630 and :644, LifeList.tsx:645 and :659 (Multimedia tab), BreedingCodeList.tsx:409 and :425. The identical control in Checklists.tsx:655/:663 carries aria-label=\"From date\"/\"To date\" and passed — the fix pattern already exists in the codebase.",
  "fix": "Copy the Checklists.tsx pattern: add aria-label=\"From date\" / aria-label=\"To date\" to both date inputs in SpeciesDetail.tsx, LifeList.tsx, and BreedingCodeList.tsx.",
  "dim": "axe",
  "verifierNote": "verifier unavailable",
  "i": 1,
  "verdict": {
   "i": 1,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Verified: both date inputs at SpeciesDetail.tsx:629-654 and the LifeList.tsx/BreedingCodeList.tsx duplicates have no aria-label, <label>, or title, while Checklists.tsx:655/:663 carries the exact labels claimed. Real 4.1.2/3.3.2 failure — a SR user gets two identical unnamed date fields and cannot tell From from To. Downgraded critical→serious: 'critical' is axe's rule-impact taxonomy, but contextually these are secondary filter controls that stay operable, the same defect class finding 5 rates serious; the suggested fix is correct and matches the in-repo convention.",
   "alreadyDisclosed": false
  },
  "src": "audit:axe",
  "id": "F016",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "In-page jump links scroll the view but never move keyboard focus",
  "file": "frontend/src/components/BirdingStats.tsx",
  "line": 449,
  "wcag": "2.4.3 Focus Order A",
  "description": "The Statistics \"Jump to section\" nav (9 anchors) calls e.preventDefault() + smoothScrollIntoView, and LifeList's \"media comments\" anchor does the same (LifeList.tsx:505). preventDefault suppresses the browser's native sequential-focus relocation for fragment navigation, and the scroll targets are plain divs without tabindex=\"-1\" (e.g. MediaCommentsSection.tsx:40), so after \"jumping\" a keyboard user's focus is still on the link at the top — the next Tab continues from the nav and they must traverse every control between anyway. The jump feature is effectively visual-only for keyboard users.",
  "fix": "After scrolling, move focus to the destination: give each section container tabindex={-1} and call el.focus({ preventScroll: true }) alongside smoothScrollIntoView (wrap in the existing scroll helper so all jump links inherit it).",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 23,
  "verdict": {
   "i": 23,
   "verdict": "confirmed",
   "reasoning": "BirdingStats.tsx:449 (and LifeList.tsx:505) call e.preventDefault() + smoothScrollIntoView, and lib/scroll.ts only scrolls — it never moves focus; the targets are plain divs (SectionCard in statsPrimitives.tsx, MediaCommentsSection.tsx:40) with no tabindex. preventDefault suppresses the browser's native fragment-navigation focus relocation, so after a jump the next Tab resumes from the nav chip — the feature is visual-only for keyboard users. The suggested fix (tabindex=-1 on targets + focus({preventScroll:true}) folded into the shared scroll helper) is correct and convention-conforming.",
   "fixNote": "Put tabIndex={-1} on the SectionCard root div (statsPrimitives.tsx) so all nine Statistics targets inherit it, add the same to the #media-comments div in MediaCommentsSection.tsx, and add a jumpTo(el) wrapper in lib/scroll.ts that calls smoothScrollIntoView(el) then el.focus({ preventScroll: true }); switch the three preventDefault call sites (BirdingStats.tsx:449, LifeList.tsx:505) to it. Add :focus-visible-only outline styling (or outline: none on [tabindex=\"-1\"]) so the programmatic focus doesn't paint the green focus ring on the whole card for mouse users.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F029",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Sortable column headers are focusable <th> with no interactive role",
  "file": "frontend/src/components/LifeListTable.tsx",
  "line": 188,
  "wcag": "4.1.2 Name Role Value A",
  "description": "Sortable headers are <th tabIndex={0} onClick onKeyDown> with aria-sort (LifeListTable.tsx:188-250, BreedingCodeTable.tsx:120-160). aria-sort updates correctly, but the element exposes role columnheader only — nothing announces it as an activatable control, so a screen-reader user tabbing onto \"Entries\" or \"NB\" hears a plain column header and has no cue that Enter/Space re-sorts. The BreedingCodeTable code headers additionally expose only the terse code (e.g. \"NB\") as the name, with the expansion in title only.",
  "fix": "Put a real <button> inside each sortable <th> (th keeps aria-sort, button carries the label e.g. \"Sort by Entries\"); for breeding-code columns give the button an aria-label with the code's full label.",
  "dim": "aria",
  "verifierNote": "verifier unavailable",
  "i": 32,
  "verdict": {
   "i": 32,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Verified LifeListTable.tsx:188-250: four <th scope=\"col\" tabIndex={0} onClick onKeyDown aria-sort> headers expose only role columnheader, with no role/button conveying activatability, and the headers are the only column-sort mechanism (LifeList.tsx's toggle only switches nameSortMode). BreedingCodeTable.tsx:122-147 shares the pattern as claimed. ACCESSIBILITY.md describes sortable headers as working, not this gap, so not a disclosed exception; moderate severity is fair since keyboard operation and aria-sort do work — the failure is SR discoverability.",
   "fixNote": "The button-in-th fix is right (APG sortable-table pattern) but incomplete: also remove tabIndex/onClick/onKeyDown from the <th> and put them on the inner <button type=\"button\"> so there is one tab stop, keep aria-sort on the th, and give the button visible text plus the sort affordance (e.g. aria-label=\"Sort by Entries\" only where the visible label is an icon/terse code). Do NOT instead add role=\"button\" to the th — that would destroy the columnheader role and orphan aria-sort.",
   "alreadyDisclosed": false
  },
  "src": "audit:aria",
  "id": "F041",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "'Personal Location' popup badge fails in light theme (2.90:1)",
  "file": "frontend/src/components/map/HotspotMarkers.tsx",
  "line": 156,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "The hotspot popup's 'Personal Location' badge sets --sr-map-personal (#C9842A) text on --sr-is-target-bg (#FFF7ED): 2.90:1 in light theme at 11px/600 (dark passes, 8.38:1). The pin color token was designed as a map fill, not a text color.",
  "fix": "Use --sr-is-target-text (#C2410C, 4.88:1 on that bg) or --sr-warning for the badge text in light theme.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 49,
  "verdict": {
   "i": 49,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Independently recomputed: #C9842A (--sr-map-personal) on #FFF7ED (--sr-is-target-bg) = 2.90:1 in light theme at 11px/600 — small text, fails the 4.5:1 AA minimum exactly as reported; dark theme (#E0A060 on #1C0F00) = 8.38:1, passes. HotspotMarkers.tsx:156 matches the finding, and the badge is the popup's only textual indicator that the pin is a personal location, so it is meaningful text — moderate stands. Not disclosed: ACCESSIBILITY.md affirmatively claims map popups meet AA and lists only chart-bar percentage labels as the known exception.",
   "fixNote": "Swap color: 'var(--sr-map-personal)' for 'var(--sr-is-target-text)' unconditionally — no light-theme branch needed, since the token is theme-scoped (4.88:1 light, 8.30:1 dark, both pass) and --sr-is-target-bg + --sr-is-target-text is already the codebase's badge pairing (LifeList.tsx:529).",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F042",
  "primaryHere": false
 },
 {
  "title": "Breeding-code meanings are hover-only: full code labels live solely in title attributes on the Breeding Codes tab and comparer badges",
  "file": "frontend/src/components/BreedingCodeTable.tsx",
  "line": 148,
  "severity": "minor",
  "wcag": "1.4.13 Content on Hover or Focus AA (UA-tooltip exemption applies — flagged as the only route to the information)",
  "description": "Title-attribute tooltips are user-agent controlled and therefore exempt from 1.4.13 itself, but three locations make the title the ONLY route to what an eBird breeding code abbreviation means, and UA title tooltips never appear on keyboard focus or touch: (1) BreedingCodeTable.tsx:148 — the matrix column headers show only the code (e.g. \"NY\") with `title={def.label}`; the per-cell sr-only text (line 228) adds only the tier category (\"Confirmed\"), not the label. (2) BreedingCodeList.tsx:324 — the per-code filter pills, same pattern (`title={def.label}` with the bare code as content). (3) ChecklistComparer.tsx:31-40 — BreedingBadge renders only `def.code` with `title={`${def.code} — ${def.label}`}` and no aria-label. There is no visible legend on the Breeding Codes tab, and docs/HELP.md's Breeding Codes section (line 273+) describes the matrix without listing the 23 codes; the full labels are visible only in Species Detail's per-species breeding section (SpeciesDetail.tsx:~1003), which covers just the codes that one selected species has. Mouse users get the UA tooltip and screen readers get the title as accessible description (name-from-content is the code, title falls back to description), but a sighted keyboard or touch user — including anyone on the self-hosted/tablet path — has no way on these views to learn that \"FL\" means recently fledged young.",
  "evidence": "BreedingCodeTable.tsx:148 `title={def.label}` on a <th> whose visible content is `{code}{sortIndicator(code)}`; BreedingCodeList.tsx:324 `title={def.label}` on the code pill whose content is a color dot + `{code}`; ChecklistComparer.tsx:31 `title={`${def.code} — ${def.label}`}` on a span containing only `{def.code}`; grep finds no other rendering of def.label in those three files and no code legend in docs/HELP.md.",
  "suggestedFix": "Add a non-hover route on the Breeding Codes tab: a collapsible \"Code legend\" disclosure above the matrix listing code → label (one shared component could also serve the comparer), or render the label in each column header at reduced size. Minimal alternative: append the label to each control's accessible name (aria-label={`${code} — ${def.label}`} on the pills/headers, keeping the visible code) and give ChecklistComparer's BreedingBadge the same aria-label — that fixes AT consistency, though a visible legend is what helps sighted touch/keyboard users.",
  "dim": "gap:WCAG 1.4.13 Content on H",
  "j": 1,
  "verdict": {
   "i": 1,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "reasoning": "Verified at all three locations: BreedingCodeTable.tsx:148 (th content is just the code, title={def.label}; the per-cell sr-only at :228 adds only the tier category), BreedingCodeList.tsx:324 (pill with title only), and ChecklistComparer.tsx:31 (BreedingBadge, title only — on a non-interactive span, so even AT exposure is unreliable). grep confirms def.label renders nowhere else in those views, docs/HELP.md's Breeding Codes section lists no codes and its comparer section literally says 'Hover for the full code name', and full labels appear only in Species Detail's per-species section. ACCESSIBILITY.md's 'hidden text label' claim covers tier color, not code meaning, so nothing is disclosed; no existing finding overlaps (the confirmed breeding-code items are contrast-only)."
  },
  "src": "gap:WCAG 1.4.13 Content on H",
  "id": "F073",
  "primaryHere": true
 },
 {
  "title": "The 'loading-saved' phase on four data tabs is a bare unlabeled spinner — nothing is announced and nothing names the state",
  "file": "frontend/src/components/Checklists.tsx",
  "line": 469,
  "severity": "minor",
  "wcag": "4.1.3 Status Messages AA (also 1.1.1 Non-text Content A)",
  "description": "While a tab parses the stored CSV (the phase every visit passes through before the data or the SetupRequired panel appears), four tabs replace their entire panel with a centered Loader2 SVG and nothing else: Checklists.tsx:468-470 (the icon is even explicitly aria-hidden, leaving zero accessible content in the panel), LifeList.tsx:362-364, SpeciesDetail.tsx:408-410, and BreedingCodeList.tsx:174-176. There is no role=\"status\", no aria-label, and no visible text, so a screen-reader user lands in an empty tabpanel with no indication anything is happening. Every other loading state in the app does this correctly and was recorded as a pass (boot skeleton role=status 'Loading SnowRaven', App.tsx TabLoading role=status with text, BirdingStats.tsx:464 computing status, SnowMap style placeholder) — these four bare spinners are the stragglers. Distinct from prior finding i=74 (which covers role=status nodes that mount with text already present — these have no status semantics at all).",
  "evidence": "Checklists.tsx:469 `<Loader2 size={24} strokeWidth={2} className=\"spin\" style={{ color: 'var(--sr-accent)' }} aria-hidden />` as the sole child of the phase's wrapper div; identical Loader2-only blocks at LifeList.tsx:363, SpeciesDetail.tsx:409, BreedingCodeList.tsx:~176 (those three without even aria-hidden, but with no text either).",
  "suggestedFix": "Give each wrapper div role=\"status\" and an aria-label (e.g. 'Loading saved eBird data') — or add visible text next to the spinner as WeatherTidePanel does ('Loading weather…') — and keep the SVG aria-hidden, matching the App.tsx TabLoading pattern.",
  "dim": "gap:App states never exercis",
  "j": 12,
  "verdict": {
   "i": 12,
   "verdict": "confirmed",
   "reasoning": "All four cited blocks verified: Checklists.tsx:466-472 (Loader2 with aria-hidden as the panel's sole content), LifeList.tsx:360-366, SpeciesDetail.tsx:406-412, BreedingCodeList.tsx:172-178 — no role='status', no aria-label, no text, leaving an empty tabpanel during the loading-saved phase every visit passes through. Genuinely distinct from confirmed i=74, which covers nodes that already have role='status' but mount with text present; these have no status semantics at all. Not disclosed in ACCESSIBILITY.md and not present in either findings file.",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "fixNote": "Matching the App.tsx TabLoading pattern (role='status' + label on the wrapper, SVG aria-hidden) is right; note per confirmed finding i=74 that a status node mounting with its label may still not be announced by all SRs, but it at least names the state for users who land in the panel — fixing both together would mean mounting the empty status container first and inserting the label."
  },
  "src": "gap:App states never exercis",
  "id": "F076",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Light theme: error text on error background is just below AA (4.41:1)",
  "file": "frontend/src/components/Checklists.tsx",
  "line": 488,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "--sr-error #DC2626 on --sr-error-bg #FEF2F2 computes to 4.41:1 — marginally under 4.5:1 — and this pair styles every error banner and the 'negative' filter pill state: Checklists.tsx:236/488, LifeList.tsx:108/384, BreedingCodeList.tsx:196, Settings.tsx:304/480, ChecklistComparer.tsx:374, NamedBirds.tsx:108, WeatherTidePanel.tsx:186, SpeciesDetail.tsx:430, DropZone.tsx error text. Dark theme passes (7.07:1). Error on plain surface also passes (4.83:1), so only the tinted-banner pair needs a nudge.",
  "fix": "Darken light --sr-error slightly (e.g. #D31F1F ≈ 4.8:1 on #FEF2F2) or introduce an --sr-error-text token used on --sr-error-bg; one token edit in globals.css fixes all listed locations.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 44,
  "verdict": {
   "i": 44,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified the pair co-occurs (Checklists.tsx:236 TriPill 'no' state and :488 error banner, plus the other listed files) and re-computed the ratio: #DC2626 on #FEF2F2 = 4.415:1, a genuine but marginal AA miss at 13px text; dark theme (7.07:1) and plain-surface (4.83:1) pass as the finding states. The 0.085 shortfall is perceptually negligible and light-theme-only, so 'moderate' overstates user impact — this is a compliance nudge, not a barrier; downgrading to minor. The fix is validated (#D31F1F = 4.82:1 on #FEF2F2, 5.27:1 on white) and convention-compliant as a single light-theme token edit in globals.css.",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F077",
  "primaryHere": false
 },
 {
  "title": "External links open new windows without any indication (inconsistent across the app)",
  "file": "frontend/src/components/HelpDocs.tsx",
  "line": 61,
  "severity": "minor",
  "wcag": "3.2.5 / 2.4.4 advisory (G201)",
  "description": "43 target=\"_blank\" anchors exist across ~20 component files; only two indicate the behavior — App.tsx:748 and App.tsx:898 use \"(opens in new tab)\" in aria-label. All others give no warning, including every link in the Help documentation (HelpDocs.tsx:61 renders all HELP.md links with target=\"_blank\" rel=\"noreferrer\" and no indication), the footer GitHub link (App.tsx:1101-1110), SpeciesLinks.tsx:20/45 (whose aria-labels \"View X on eBird\" omit it), LifeListTable.tsx media-count links, BirdingStats.tsx checklist/region links (15 instances), Checklists.tsx, MapExplorer.tsx, SpeciesDetail.tsx, NamedBirdRow.tsx, Settings.tsx, WeatherTidePanel.tsx, MediaCommentsSection.tsx, MediaStatsSections.tsx, ChecklistComparer.tsx, SightingsMap.tsx, map/HotspotMarkers.tsx, map/TargetMarkers.tsx, AtlasLayer.tsx, speciesDetail/ui.tsx, CommentText.tsx. Unexpected context changes disorient screen-reader and cognitive-disability users; the app already has the right pattern in two places — it just isn't applied consistently.",
  "evidence": "HelpDocs.tsx:61 `<a key={ki++} href={lm[2]} target=\"_blank\" rel=\"noreferrer\"` with no new-window indication; grep shows 43 target=\"_blank\" instances, only App.tsx:748 and App.tsx:898 carry \"(opens in new tab)\".",
  "suggestedFix": "Adopt one shared ExternalA component (or extend SpeciesLinks' pattern) that appends \"(opens in new tab)\" to the accessible name (visually-hidden span or aria-label suffix) and use it for all target=\"_blank\" anchors, including HelpDocs' renderInline link branch.",
  "dim": "structure",
  "j": 4,
  "verdict": {
   "i": 4,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "reasoning": "Verified by grep: exactly 43 target=\"_blank\" anchors across 20 component files, with only App.tsx:748 and App.tsx:898 announcing '(opens in new tab)'; HelpDocs.tsx renders every documentation link target=\"_blank\" with no indication and SpeciesLinks.tsx aria-labels omit it. This is the new-window-indication aspect, explicitly distinct from confirmed #53 (which covers the ↗ links' accessible names), so not a duplicate. Correctly scoped as advisory (G201; 3.2.5 is AAA), so minor severity is right; not disclosed in ACCESSIBILITY.md.",
   "fixNote": "A shared external-link component is sound; prefer a visually-hidden span suffix over aria-label so existing link text is preserved, and where an aria-label already exists (SpeciesLinks, icon-only links) append to the label, matching the wording already used at App.tsx:748/898. For icon-only favicon links keep title in sync with the label."
  },
  "src": "audit:structure",
  "id": "F078",
  "primaryHere": false
 },
 {
  "title": "Multimedia (Life List) filter-count live region announces a wrong total when a county/date filter is active — '0 of 0 species' on a zero-result filter",
  "file": "frontend/src/components/LifeList.tsx",
  "line": 437,
  "severity": "minor",
  "wcag": "4.1.3 Status Messages AA",
  "description": "Runtime-verified (Playwright, live app, data loaded): setting the From date to 2099-01-01 made the aria-live=\"polite\" count region (LifeList.tsx:675) announce '0 of 0 species', telling a screen-reader (and sighted) user the dataset is empty rather than that the active filter matched nothing. Root cause: `const totalSpecies = displayEntries.length` (line 436) — but the `displayEntries` memo (lines 276-300) ALREADY applies the county/date location filter, so the denominator collapses with the numerator whenever a location/date filter is active (any nonzero case is equally wrong, e.g. '12 of 12 species' while hundreds exist). BreedingCodeList does this correctly — its denominator is the unfiltered `phaseData!.entries.length` (BreedingCodeList.tsx:243), which announced '0 of 92 species' in the same test.",
  "evidence": "Observed live-region text under From=2099-01-01: `{\"live\":\"polite\",\"text\":\"0 of 0 species\"}` (Multimedia) vs `\"0 of 92 species\"` (Breeding Codes). Code: LifeList.tsx:436-439 `const totalSpecies = displayEntries.length; const countLabel = … : `${filteredCount} of ${totalSpecies} species``, where displayEntries is built from `rawEbirdObs.filter(o => { …countyFilter… dateRange… })` at lines 281-289.",
  "suggestedFix": "Compute the denominator from the unfiltered entry set (e.g. `phaseEntries.length`, or the pre-location-filter base list built with the same showSpuh/showNonBird/mergeSubspecies display options), mirroring BreedingCodeList.tsx:243, so the count label always reads 'X of <true total> species'.",
  "dim": "gap:App states never exercis",
  "j": 10,
  "verdict": {
   "i": 10,
   "verdict": "confirmed",
   "reasoning": "Code-verified: LifeList.tsx:436 sets totalSpecies = displayEntries.length, but the displayEntries memo (lines 276-304) already applies the county/date filter, so the aria-live count at line 675 reads '0 of 0 species' (or 'N of N') whenever a location/date filter is active. BreedingCodeList.tsx:243 uses the unfiltered phaseData!.entries.length, confirming the in-house correct pattern. Not present in verified-findings.json or structure-findings.json (i=70 is a different live-region issue).",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "fixNote": "When an eBird backbone is loaded, the correct unfiltered denominator is the comprehensive base (buildComprehensiveEntries(rawEbirdObs, rawRows, mergeSubspecies)) with the same showSpuh/showNonBird filtering applied — phaseEntries.length alone is the ML-only entry count and undercounts in backbone mode. The suggestedFix's second option is the right one; also reuse it in filterStripText (line 447), which has the same collapsed denominator."
  },
  "src": "gap:App states never exercis",
  "id": "F079",
  "primaryHere": true
 },
 {
  "title": "Zero-result filter state in Multimedia and Breeding Codes renders a header-only empty table with no 'no results' message (Checklists has one)",
  "file": "frontend/src/components/LifeListTable.tsx",
  "line": 253,
  "severity": "minor",
  "wcag": "4.1.3 Status Messages AA (best-practice gap; the only signal is the count label)",
  "description": "Runtime-verified: with a filter that matches nothing (From=2099-01-01), the Multimedia and Breeding Codes tabs render their full column-header row above a zero-row tbody with no visible or accessible 'no results match these filters' message anywhere in the panel (probe found 0 tbody rows and zero empty-state text nodes on both tabs). The only indication is the small count label ('0 of N species' — and on Multimedia that label is currently wrong, see the companion finding). The Checklists tab is the in-house model: it renders the explicit text 'No checklists match these filters.' (Checklists.tsx:705) distinct from its empty-data message, and MediaCommentsSection has emptyAll/emptyFiltered props. Locations: LifeListTable.tsx:253-254 (`<tbody>{sorted.map(…)}` with no empty branch) and BreedingCodeTable.tsx:253-area tbody (same pattern; its filter at BreedingCodeTable.tsx:29 can yield an empty `filtered`).",
  "evidence": "Runtime probe on both tabs with From=2099-01-01: `{\"tableRows\":0, \"tablePresent\":true, \"emptyMsg\":[]}`; Checklists in the same state showed `\"emptyMsg\":[\"No checklists match these filters.\"]` plus the announced count '0 of 3,163 checklists'.",
  "suggestedFix": "When the sorted/filtered row set is empty, render a single full-width cell (or a div below the table) with the Checklists wording — 'No species match these filters.' vs 'No species found in the stored file.' — in --sr-text-muted, in both LifeListTable and BreedingCodeTable. The existing aria-live count label then announces the change while the visible message explains it.",
  "dim": "gap:App states never exercis",
  "j": 11,
  "verdict": {
   "i": 11,
   "verdict": "confirmed",
   "reasoning": "Code-verified: LifeListTable.tsx:253 (<tbody>{sorted.map(...)}) and BreedingCodeTable.tsx:164 (same pattern; its filter at line 29 can empty 'filtered') have no empty-row branch, and BreedingCodeList's only empty message (line 220) is gated on !hasLocationFilter, so a filtered-to-zero state renders a header-only table with no explanation. Checklists.tsx:705 renders the explicit 'No checklists match these filters.' message, so the inconsistency is real. Fairly flagged as a minor best-practice gap; no duplicate exists in either findings file.",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "fixNote": "Minor line correction: BreedingCodeTable's tbody is at lines 164-165, not ~253. Suggested fix is convention-compliant (--sr-text-muted exists in both themes); a full-width <td colSpan={...}> inside the tbody keeps the table structure valid."
  },
  "src": "gap:App states never exercis",
  "id": "F080",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "Life List species column is <td>, not a row header, unlike the Breeding Codes table",
  "file": "frontend/src/components/LifeListTable.tsx",
  "line": 267,
  "wcag": "1.3.1 Info and Relationships A",
  "description": "BreedingCodeTable.tsx:174 correctly marks the species name cell <th scope=\"row\">, but LifeListTable.tsx:267 uses a plain <td>. The Photo/Audio/Video count cells contain links whose only content is a bare number (e.g. \"3\"), so when navigating by cell or link, SRs that announce row headers would give the species context automatically in the Breeding table but not here. Column-header context still works, so impact is small.",
  "fix": "Change the species cell to <th scope=\"row\" style={{...textAlign:'left'}}> to match BreedingCodeTable.tsx:174.",
  "dim": "sr-content",
  "verifierNote": "verifier unavailable",
  "i": 81,
  "verdict": {
   "i": 81,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified both halves: LifeListTable.tsx:267 is a plain <td> while BreedingCodeTable.tsx:174 uses <th scope=\"row\"> (with a comment explaining the textAlign reset), so the inconsistency is real and the bare-number link cells (e.g. \"3\") genuinely benefit from row-header semantics. Column headers still provide context, so this sits at the best-practice edge of 1.3.1; minor is the correct severity and the framing is honest, not pedantic.",
   "fixNote": "Match BreedingCodeTable.tsx:174-187 fully: <th scope=\"row\" style={{ padding: '9px 14px', verticalAlign: 'middle', textAlign: 'left', fontWeight: 'normal' }}>. The fontWeight: 'normal' reset (present in BreedingCodeTable at line 186 but omitted from the finding's snippet) matters because .sr-birdname-sci sets no font-weight, so the italic scientific-name line would otherwise inherit the th bold default.",
   "alreadyDisclosed": false
  },
  "src": "audit:sr-content",
  "id": "F081",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "'No media' minus icon nearly invisible (1.47:1 light / 1.70:1 dark)",
  "file": "frontend/src/components/LifeListTable.tsx",
  "line": 288,
  "wcag": "1.4.11 Non-text Contrast AA",
  "description": "Life List media columns render a Minus icon in --sr-gray-300 (#D1D5DB) on the white row surface = 1.47:1 (dark: #3F3F46 on #18181B = 1.70:1) to mean 'no media of this type' (repeated at lines 288, 302, 316, 331). The meaning ('zero') is inferable from the absence of a count link, which is why this is minor rather than moderate, but the chosen glyph as drawn fails non-text contrast in both themes.",
  "fix": "Use --sr-text-disabled or --sr-border-medium darkened to ≥3:1, or render a plain '—' in --sr-text-muted (which also gives AT users something to announce).",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 80,
  "verdict": {
   "i": 80,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Recomputed independently: --sr-gray-300 on --sr-surface is #D1D5DB/#FFFFFF = 1.47:1 light and #3F3F46/#18181B = 1.70:1 dark — exactly as reported, at lines 288/302/316/331, well under the 3:1 of SC 1.4.11. Minor is the right severity since the absent count link redundantly conveys 'zero'. Not covered by ACCESSIBILITY.md's lone known exception (chart-bar percentage labels).",
   "fixNote": "Two of the finding's three suggested tokens fail its own 3:1 target: --sr-text-disabled computes to 2.56:1 light / 2.29:1 dark, and --sr-border-medium to 1.73:1 / 1.70:1 (its dark value IS #3F3F46, the currently-failing color). Use the third option only: color the glyph var(--sr-text-muted) (#71717A light = 4.83:1, #A1A1AA dark = 6.91:1; token already defined in both themes), either keeping the <Minus> icon or rendering a text em dash — the text variant additionally gives screen readers something to announce.",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F082",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "WCAG 2.2 forward-looking: several form controls are under the 24px minimum target size",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 726,
  "wcag": "2.5.8 Target Size (Minimum) — WCAG 2.2 AA",
  "description": "Forward-looking 2.2 delta only. The atlas overlay role=\"switch\" buttons are 38x22px (MapExplorer.tsx:726, :759, :788 — 22px tall, under the 24px minimum), and several zero-padding text buttons are ~16-18px tall: the API-key Show/Hide button (Settings.tsx:363-373, padding 0, 0.75rem text), the 'Clear filter' underline buttons (LifeList.tsx:694-703, Checklists.tsx:685-690, SpeciesDetail.tsx:658-667), and the 'Go to Settings →' banner buttons (App.tsx:611-619, padding 0). Most currently escape 2.5.8 via the spacing exception (no adjacent target within 24px), but that is fragile under layout changes and the small targets are already hard for motor-impaired users under 2.1 best practice.",
  "fix": "Bump the switch buttons to at least 24px tall (e.g. 44x24 track) and give the zero-padding text buttons enough padding (or min-height: 24px with negative-margin compensation) to reach a 24x24 hit area.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 85,
  "verdict": {
   "i": 85,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "All cited controls verified: the three hand-rolled role=switch buttons are 38x22 (MapExplorer.tsx:726, :759, :788 — 22px tall, under 24), and the padding-0 text buttons exist as described in Settings.tsx:363-373 (Show/Hide), LifeList.tsx:694-703 / Checklists.tsx:685-690 / SpeciesDetail.tsx:658-667 (Clear filter), and App.tsx:611-619 (Go to Settings), all at 0.75rem with no padding (~17px hit height). The finding honestly frames itself as a forward-looking WCAG 2.2 delta and acknowledges the spacing exception currently covers most of them, so minor is the right severity.",
   "fixNote": "The codebase already has a shared ui/ToggleSwitch.tsx (a 30px-tall role=switch button with the label inside) — prefer migrating the three inline atlas switches to it, or if the label-left/switch-right layout must stay, bump the bare track to >=24px tall (e.g. 44x24). For the zero-padding text buttons, padding (or min-height 24 with negative-margin compensation) as originally suggested is fine.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F086",
  "primaryHere": false
 },
 {
  "title": "Date-range filter is a native date picker on four tabs but free-text 'YYYY-MM-DD' fields on Map Explorer",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 862,
  "severity": "minor",
  "wcag": "3.2.4 Consistent Identification (AA) — advisory; primarily a consistency/usability gap",
  "description": "The identical 'filter sightings by date range' function is presented and identified two different ways. Life List (LifeList.tsx:644-669), Breeding Codes (BreedingCodeList.tsx:408-419), Species Detail (SpeciesDetail.tsx:629-654), and Checklists (Checklists.tsx:653-667) all use paired <input type=\"date\"> controls — native picker UI, a Calendar icon, an accent-fill active state, and a '→' separator. Map Explorer alone implements the same function as two <input type=\"text\"> fields whose only identification is the placeholder 'YYYY-MM-DD' (MapExplorer.tsx:862-865), under a 'Date Range' sidebar label. A user who has learned the app-wide date-picker idiom reaches the map and must instead hand-type ISO dates with no picker, no format validation affordance, and no visible from/to distinction (the indistinguishable naming is already reported; this finding is the cross-tab inconsistency of widget and identification). The interaction divergence is not a strict 3.2.4 failure (3.2.4 governs identification, and the labels that do exist don't conflict), so this is recorded as an advisory consistency defect with real cognitive-accessibility cost.",
  "evidence": "MapExplorer.tsx:862-865 `<input type=\"text\" placeholder=\"YYYY-MM-DD\" value={dateFrom} …/><input type=\"text\" placeholder=\"YYYY-MM-DD\" value={dateTo} …/>` vs LifeList.tsx:644-647 `<input type=\"date\" value={dateRange.from} …/>` (same pattern on BreedingCodeList.tsx:408, SpeciesDetail.tsx:629, Checklists.tsx:653).",
  "suggestedFix": "Switch the Map Explorer sidebar date fields to the same paired <input type=\"date\"> pattern used everywhere else (with the From/To accessible names the prior audit already calls for); keep the visible 'Date Range' SidebarLabel, which the other tabs could in fact adopt.",
  "dim": "gap:WCAG 3.2.1/3.2.2 (on-foc",
  "j": 4,
  "verdict": {
   "i": 4,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "reasoning": "Verified: MapExplorer.tsx:862-865 uses two <input type=\"text\"> with only a 'YYYY-MM-DD' placeholder under one 'Date Range' label, while LifeList.tsx:644, BreedingCodeList.tsx:408, SpeciesDetail.tsx:629, and Checklists.tsx:653 all use paired <input type=\"date\"> (one evidence overstatement: Checklists' pair has no Calendar icon or accent-fill active state — those three trimmings are on the other three tabs only, but the core text-vs-native-picker divergence holds). The advisory framing is fair — same function, different widget and identification, with real cognitive/motor cost from hand-typing ISO dates. Distinct aspect and fix from confirmed #56/#59, which cover the accessible-name/placeholder problem on the same inputs.",
   "fixNote": "Switching to type=\"date\" also moots the 'YYYY-MM-DD' placeholder-as-name issue from #56/#59 — apply the From/To aria-labels (as Checklists.tsx:655/663 already does) in the same change."
  },
  "src": "gap:WCAG 3.2.1/3.2.2 (on-foc",
  "id": "F089",
  "primaryHere": false
 },
 {
  "severity": "minor",
  "title": "WCAG 2.2 deltas: 14×14 favicon link targets and sticky-header focus obscuring",
  "file": "frontend/src/components/SpeciesLinks.tsx",
  "line": 36,
  "wcag": "2.5.8 Target Size (Minimum) AA (WCAG 2.2)",
  "description": "Forward-looking 2.2 items. (a) Target size: the eBird / Birds of the World favicon links rendered beside nearly every bird name are 14×14px, placed directly adjacent to each other — below the 24×24 minimum with no qualifying spacing exception; they are the most frequently repeated small target in the app. The Species Detail combobox chevron (SpeciesDetail.tsx:521, ~16px <span>) is also undersized but pointer-redundant. Most other small controls measured are compliant (close 26×26, eye 28×28, locate 26×26, FABs 36×36). (b) 2.4.11 Focus Not Obscured: LifeListTable's sticky header row (position: sticky, top: 0 — LifeListTable.tsx:182-187) can fully cover row links (BirdName, ML catalog) when Shift+Tab scrolls a focused element up under it; same risk for BreedingCodeTable's sticky first column (line 128).",
  "fix": "Give the favicon anchors padding to reach a ≥24×24 hit area (visual size can stay 14px). For sticky tables, add scroll-padding-top equal to the header height on the scroll container so focused elements land below the sticky header.",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 67,
  "verdict": {
   "i": 67,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified: each favicon anchor's hit area is the 14x14 .sr-favicon-slot (globals.css:285) with no padding and a 5px gap, so centers are 19px apart and the 2.5.8 spacing exception fails; SpeciesLinks is composed into the app-wide BirdName, so the targets repeat everywhere. The 2.4.11 half is real but narrower than stated: LifeListTable's sticky header only engages in wideMode (the default-mode overflowX:auto wrapper becomes the scrollport and never scrolls vertically, so the header never sticks), while BreedingCodeTable's sticky 220px first column does engage and can cover the tabIndex=0 sortable column headers. Fairly flagged as forward-looking 2.2 deltas at minor; ACCESSIBILITY.md discloses neither.",
   "fixNote": "Favicons: add padding (e.g. 5px) on the anchors or .sr-favicon-slot, offset with negative margin to preserve dense-row layout, keeping the img at 14px. Sticky tables: scroll-padding-top must go on the ACTUAL scrolling element — for LifeListTable that is the document (html), not the table wrapper (the wrapper is only a horizontal scrollport, and in wideMode the header sticks to the viewport; scroll-padding-top on the wrapper would be a no-op). For BreedingCodeTable's sticky column the analog is scroll-padding-left: 220px on the overflowX:auto wrapper div.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F099",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Inline `outline: 'none'` on ~20 inputs/selects suppresses the designed focus ring app-wide",
  "file": "frontend/src/lib/mapExplorerFormat.ts",
  "line": 27,
  "wcag": "2.4.7 Focus Visible AA (1.4.11 degradation)",
  "description": "One repeated pattern: form controls styled with inline `outline: 'none'`, which overrides the global input ring (globals.css:556-563, `outline: 2px solid var(--sr-accent)`). The only surviving indicator is the rule's `border-color: var(--sr-accent) !important` (stylesheet !important still beats inline) plus a 12%-alpha shadow — a 1.5px gray→green border tint instead of the designed 2px ring, easy to miss in dense filter sidebars, and weaker than ACCESSIBILITY.md's published 'border-hugging ring on inputs'. Locations: lib/mapExplorerFormat.ts:27 (SELECT_STYLE shared by all Map Explorer selects); MapExplorer.tsx:88,690,692,863,865,1184; LifeList.tsx:627,653,667; BreedingCodeList.tsx:388,419,435; SpeciesDetail.tsx:518,613,638,652,1273; Checklists.tsx:170,265; MediaCommentsSection.tsx:75; Settings.tsx:435,1219,1231,1243. Technically still a visible state change (so not a hard 2.4.7 fail), but a significant app-wide degradation versus the global design.",
  "fix": "Remove every inline `outline: 'none'` from inputs/selects — the global stylesheet already suppresses the default ring on non-focus-visible states and supplies the 2px accent ring on focus. The per-element onFocus borderColor handlers (e.g. Settings.tsx:437) become redundant and can stay or go.",
  "dim": "focus",
  "verifierNote": "verifier unavailable",
  "i": 24,
  "verdict": {
   "i": 24,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified all claims: 26 inline outline:'none' occurrences match the listed locations, inline style beats the non-!important outline in globals.css:556-563, and only the !important border-color plus the stylesheet box-shadow survive — the designed 2px ring never renders on these controls. However the surviving indicator (1.5px border flipping gray→accent at 4.52:1 light / 9.22:1 dark vs surface, plus a 4px glow, with explicit onFocus borderColor handlers in Settings) is a perceivable, conventional focus treatment, so 2.4.7 AA is not actually failed — the finding itself concedes this. Real as an app-wide design-consistency issue that also makes ACCESSIBILITY.md's published 'border-hugging ring on inputs' claim inaccurate, but a non-failing degradation grades as minor, not moderate.",
   "alreadyDisclosed": false
  },
  "src": "audit:focus",
  "id": "F105",
  "primaryHere": false
 }
]