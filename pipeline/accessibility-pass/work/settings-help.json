[
 {
  "severity": "serious",
  "title": "Contrast claim is overstated: informational text rendered in the --sr-text-disabled token sits at 2.3–2.6:1 in both themes",
  "file": "frontend/src/components/Checklists.tsx",
  "line": 177,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "ACCESSIBILITY.md claims body text and primary interface text meet 4.5:1 in both themes, with exactly one disclosed exception (percentage labels inside chart bars). In fact, real informational text — not disabled controls — is rendered in --sr-text-disabled, which computes to 2.56:1 light (#A1A1AA on #FFFFFF) / 2.45:1 on the page bg, and 2.29:1 dark (#52525B on #18181B). Locations: Checklists.tsx:177 and :668 (the result-count labels that are also the polite live regions), MediaCommentsSection.tsx:100 and NamedBirdsTable.tsx:77 (count labels), SpeciesDetail.tsx:461 ('{n} species' count) and :584 (scientific names inside the species-combobox options), ChecklistComparer.tsx:534 (italic placeholder labels), and the uppercase mini-headers in SetupRequired.tsx:56, ListComparer.tsx:203, HelpDocs.tsx:354. None of these are covered by the statement's disclosed chart-label exception, so the published claim is false as written, and low-vision users genuinely cannot read these labels.",
  "fix": "Use --sr-text-muted (4.83:1 light / 6.91:1 dark — already passing) for all informational text and reserve --sr-text-disabled strictly for genuinely disabled controls; or add a passing 'faint text' token. Then re-verify the ACCESSIBILITY.md contrast paragraph.",
  "dim": "claims",
  "verifierNote": "verifier unavailable",
  "i": 14,
  "verdict": {
   "i": 14,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Re-ran WCAG luminance math: --sr-text-disabled is 2.56:1 on white / 2.46:1 on --sr-surface-faint (the line 177/668 labels' actual backdrop) light, 2.29:1 / 2.20:1 dark — all well below 4.5:1 for 11-12px informational text. All ten cited locations exist and use the token for real content (count labels that are also the only result-count display, combobox sci names, mini-headers), and ACCESSIBILITY.md's sole disclosed exception (chart-bar percentage labels) covers none of them, so the published AA claim is false as written. Fix is correct and convention-compliant: --sr-text-muted passes in both themes (4.83/4.63 light, 6.91/6.63 dark) and is already defined in both.",
   "alreadyDisclosed": false
  },
  "src": "audit:claims",
  "id": "F005",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "Help overlay: fixed 200px TOC sidebar never collapses — content column collapses to ~30px at 320px width",
  "file": "frontend/src/components/HelpDocs.tsx",
  "line": 348,
  "wcag": "1.4.10 Reflow AA",
  "description": "The HelpDocs body lays the TOC and content side by side with inline styles only — no media query and no wrap. The TOC is `width: 200, flexShrink: 0`, the row adds `padding: '0 24px'` and `gap: 40`, so at a 320 CSS px viewport (e.g. 1280px window at 400% zoom, or a phone) the help content column gets roughly 320 − 48 − 200 − 40 ≈ 32px. Markdown text wraps at one or two characters per line (long words overflow), making the in-app documentation effectively unreadable exactly for the low-vision users most likely to need it. Every other layout in the app handles this (globals.css has a 640px media query; TabNav measures and collapses), so this is the one true reflow failure found. It also contradicts ACCESSIBILITY.md line 39 (\"The layout reflows for narrow windows and at large sizes … without clipping content\").",
  "fix": "Below a narrow-width breakpoint (~700px), stack the TOC above the content (flexDirection column) or move it behind a disclosure button. A `.sr-help-body` class plus a rule in the existing `@media (max-width: 640px)` block in globals.css is the smallest change; alternatively reuse the TabNav measurement pattern.",
  "dim": "resize",
  "verifierNote": "verifier unavailable",
  "i": 13,
  "verdict": {
   "i": 13,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "HelpDocs.tsx lines 341-351 lay out the TOC (inline width:200, flexShrink:0) and content (flex:1, minWidth:0) with gap 40 and 0 24px padding, all inline styles with no className, and globals.css's only width media block (640px) has no help-overlay rules — so at 320 CSS px the content column is ~32px as claimed. ACCESSIBILITY.md line 39 affirmatively claims reflow works (exempting only tables/maps), so this is contradicted rather than disclosed. Severity serious is fair: the in-app documentation becomes unreadable at 400% zoom for exactly the low-vision users it serves.",
   "fixNote": "The suggested .sr-help-body class + rule in the existing @media (max-width: 640px) block is right, but because the layout is inline styles the override needs !important (the existing block already does this pattern for .sr-header's inline padding) — e.g. add classes to the row and nav and rule `.sr-help-row { flex-direction: column !important; gap: 0 !important; } .sr-help-toc { width: 100% !important; position: static !important; max-height: none !important; }`. The existing 640px breakpoint is sufficient for SC 1.4.10 (failure threshold is 320 CSS px); no new ~700px breakpoint needed.",
   "alreadyDisclosed": false
  },
  "src": "audit:resize",
  "id": "F006",
  "primaryHere": true
 },
 {
  "severity": "serious",
  "title": "Filter form controls have no programmatic label across most data tabs",
  "file": "frontend/src/components/LifeList.tsx",
  "line": 616,
  "wcag": "1.3.1 Info and Relationships / 4.1.2 Name, Role, Value (also 2.4.6) AA",
  "description": "The county <select> and From/To date inputs are repeated across tabs with no aria-label, no <label for>, and no association to any visible text — a screen-reader user hears an unnamed combobox and two unnamed, indistinguishable date fields. Locations: LifeList.tsx:616 (county select), :644 and :658 (date from/to); BreedingCodeList.tsx:376, :408, :424 (same trio); SpeciesDetail.tsx:604, :629, :643 (same trio). Related instances where a VISIBLE label exists but is not programmatically associated: MapExplorer.tsx:853 (species select), :872 (county select), :896 (media select) and :862/:864 (date text inputs) — their 'Species'/'County'/'Media'/'Date Range' headings are SidebarLabel <div>s (map/MapSidebarUI.tsx:35-43), so the selects have no accessible name and the two free-text date inputs are distinguishable only by the placeholder 'YYYY-MM-DD' (format instruction also vanishes once filled — 3.3.2); Settings.tsx:1214, :1226, :1238 (Latitude/Longitude/Radius number inputs whose labels are plain <div>s at :1213/:1225/:1237); ChecklistComparer.tsx:350-353 ('Checklist A/B' <label> elements with no htmlFor, so the inputs' name falls back to the example-URL placeholder). Checklists.tsx:629-663 does this correctly (aria-label=\"Protocol\"/\"County\"/\"From date\"/\"To date\"), proving the house pattern exists.",
  "fix": "Match the Checklists.tsx pattern: add aria-label (\"County\", \"From date\", \"To date\") to the bare selects/date inputs in LifeList, BreedingCodeList, SpeciesDetail. In MapExplorer, render SidebarLabel as a <label htmlFor> (or add aria-label to each control), and give the two date inputs distinct names (\"Date from (YYYY-MM-DD)\" / \"Date to (YYYY-MM-DD)\"). In Settings, convert the Latitude/Longitude/Radius caption <div>s to <label htmlFor> with input ids. In ChecklistComparer, add htmlFor/id to the existing <label> elements.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 10,
  "verdict": {
   "i": 10,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Verified every cited location in current code: LifeList/BreedingCodeList/SpeciesDetail county selects and date inputs have no aria-label/label/title; MapExplorer's SidebarLabel is a bare <div> (MapSidebarUI.tsx:35-43) so its selects are unnamed and the two text date inputs share the identical 'YYYY-MM-DD' placeholder; Settings lat/lon/radius captions are plain <div>s; ChecklistComparer's <label> has no htmlFor and doesn't wrap the input. Checklists.tsx really does carry the aria-label house pattern the fix cites, and ACCESSIBILITY.md discloses only chart-label contrast as a known exception, not this. Serious is the right severity — repeated unnamed form controls across most data tabs is a genuine 4.1.2/1.3.1 SR barrier, though the controls remain operable.",
   "fixNote": "The fix is correct and convention-consistent (mirror Checklists.tsx's aria-label=\"County\"/\"From date\"/\"To date\"). One caveat: do not unconditionally convert SidebarLabel to <label htmlFor> — it also captions the Breeding Code SegControl, a button group with no labelable element. Either add aria-label per control in MapExplorer (including format-bearing names like 'Date from (YYYY-MM-DD)' on the free-text date inputs), or give SidebarLabel an optional htmlFor prop that renders <label> only when an id is passed.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F007",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "Inline error messages are silent to assistive tech (no role=alert/aria-live, no aria-describedby)",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 105,
  "wcag": "4.1.3 Status Messages / 3.3.1 Error Identification AA",
  "description": "Several async form-submission error paths render a plain styled <div>/<span>/<p>, so a screen-reader user who activates the control hears nothing and must hunt the page to discover the failure. Locations: MapExplorer.tsx:105 (place-name search failure), :687 (geolocation error), :1006-1011 (Find Hotspots error), :1227-1232 (Find Recent Sightings error); Settings.tsx:300-309 (malformed/failed CSV upload error in FileRow), :476-485 (API-key save error in KeyRow), :1210 (map-location detect error), :1281-1283 (map defaults save error — additionally the text 'Check your values and try again.' does not identify which of the three fields is invalid); ListComparer.tsx:276-280 (my-list load error). None are associated to their input via aria-describedby either. The codebase has correct reference implementations: App.tsx:668+717-736 (role=alert plus aria-describedby on the input), ChecklistComparer.tsx:372, DropZone.tsx:132, WeatherTidePanel.tsx:184.",
  "fix": "Give each error container role=\"alert\" (or aria-live=\"polite\" for non-urgent ones), and where the error relates to a specific input add id + aria-describedby on that input (the App.tsx checklist-input pattern). Make the map-defaults message name the offending field, e.g. \"Latitude must be between -90 and 90.\"",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 11,
  "verdict": {
   "i": 11,
   "verdict": "confirmed",
   "correctedSeverity": "serious",
   "reasoning": "Every cited location verified as a plain styled div/span/p (MapExplorer 105/687/1006/1227, Settings 300-309/476-485/1210/1281-1283 including the field-anonymous 'Check your values' message, ListComparer 276-280); grep shows zero role=alert or aria-live in those files. The cited correct reference implementations all exist (App.tsx role=alert + aria-describedby, ChecklistComparer 372, DropZone 132, WeatherTidePanel 184), so this is inconsistent application of an established house pattern. Serious is fair for 4.1.3/3.3.1: async failures on key flows (location search, CSV upload, API-key save) are completely silent to SR users.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F010",
  "primaryHere": false
 },
 {
  "severity": "serious",
  "title": "--sr-text-disabled used for real content text (2.29–2.56:1)",
  "file": "frontend/src/components/Settings.tsx",
  "line": 240,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "The disabled-text token (#A1A1AA light = 2.56:1 on surface, 2.45:1 on bg; #52525B dark = 2.29:1 on surface, 1.93:1 on surface-subtle) is widely used for meaningful, non-disabled content: Settings file sublabels (Settings.tsx:240) and full explanatory paragraphs (Settings.tsx:1143, 1174), file dates (:235), tab-layout 'HIDDEN' labels (:739), Species Detail counters and zero values (SpeciesDetail.tsx:461, 584, 690, 830, 926), comparer '—' and italic placeholders that convey 'no value' (ChecklistComparer.tsx:100, 534), the List Comparer section label (ListComparer.tsx:203), and the LifeList 'no filter' chip (LifeList.tsx:606). The WCAG inactive-control exemption does not apply to any of these — they are informative text.",
  "fix": "Replace --sr-text-disabled with --sr-text-muted (4.83:1 light / 6.91:1 dark on surface) everywhere the text is content rather than a label inside a genuinely disabled control; reserve the disabled token for disabled buttons/inputs only.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 9,
  "verdict": {
   "i": 9,
   "verdict": "confirmed",
   "reasoning": "Re-ran the WCAG math independently: every reported ratio is exact (light #A1A1AA on #FFFFFF = 2.56:1, on #F9FAFB = 2.45:1; dark #52525B on #18181B = 2.29:1, on #27272A = 1.93:1; muted = 4.83/6.91 on surface). The cited Settings.tsx sites (235, 240, 250, 388, 739, 1143, 1174) are informative content — file dates, sublabels, status chips, explanatory paragraphs — none inside a disabled control, so the 1.4.3 inactive-control exemption does not apply. ACCESSIBILITY.md's contrast section claims AA for body text and discloses only chart-bar labels, not this; serious is the right severity for sub-2.6:1 body-size content text.",
   "fixNote": "Swapping to --sr-text-muted is right for most sites, but light-theme muted (#71717A) on --sr-surface-subtle (#F4F4F5) is only 4.40:1 — just under AA. The 'No file saved' / 'No key saved' chips (Settings.tsx:250, 388) sit on surface-subtle, so for those either use --sr-text (or a new AA-checked token added to BOTH :root and [data-theme=\"dark\"] in globals.css) or lighten the chip background; all other cited Settings sites are on --sr-surface or --sr-bg where muted passes (4.83:1 / 4.62:1 light, 6.91:1+ dark).",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F012",
  "primaryHere": true
 },
 {
  "severity": "serious",
  "title": "Settings tab reorder is drag-and-drop only — no keyboard alternative",
  "file": "frontend/src/components/Settings.tsx",
  "line": 712,
  "wcag": "2.1.1 Keyboard A",
  "description": "Tab Layout rows are reordered exclusively via HTML5 drag-and-drop on the <li draggable> (onDragStart/onDragOver/onDrop, Settings.tsx:712-717). The drag handle is a non-focusable <div aria-label=\"Drag to reorder…\"> (line 721-723) — it isn't even in the tab order, and there are no move-up/move-down buttons. The component self-documents the failure with an sr-only note at line 699 (\"Keyboard reordering is not supported in this version\"), which is honest disclosure but still a WCAG 2.1.1 failure: the reorder function is entirely unavailable to keyboard (and screen-reader) users. The per-row show/hide eye toggles ARE proper buttons, so only ordering is blocked.",
  "fix": "Add visible Move up / Move down buttons per row (simplest, also helps touch+AT), or implement the APG keyboard drag pattern (focusable handle, Space to grab, arrows to move, Space to drop, with an aria-live announcement). Then delete the sr-only disclaimer.",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 3,
  "verdict": {
   "i": 3,
   "verdict": "confirmed",
   "reasoning": "Verified: reorder is exclusively HTML5 DnD on the <li draggable> (Settings.tsx:710-718); the handle div (721-730) is unfocusable, there are no move-up/down buttons, and the file's only onKeyDown is the API-key Enter handler — so ordering is unreachable by keyboard, a hard 2.1.1 Level A failure. The sr-only note at line 699 self-documents it; ACCESSIBILITY.md's keyboard section does not disclose this exception (its only caveat is map markers, which have a keyboard alternative). Severity serious is correct; the suggested fix (move buttons or APG keyboard-drag with aria-live) is sound and fits the existing onReorder/storage-seam plumbing.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F013",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "One-shot buttons unmount on activation, dropping keyboard focus to <body>",
  "file": "frontend/src/components/Checklists.tsx",
  "line": 205,
  "wcag": "2.4.3 Focus Order A",
  "description": "Repeated pattern: a focused control disappears when activated, resetting focus to the document body so the keyboard user's next Tab restarts from the top of the page. Locations: 'Show all N' one-shot buttons rendered behind `{!showAll && ...}` — Checklists.tsx:205 (comments) and :718 (checklist rows), MediaCommentsSection.tsx:159, SpeciesDetail.tsx:1347 (species comments); and Settings KeyRow Save/Cancel — Settings.tsx:457 (Cancel) and the post-save `setEditing(false)` at :973 unmount the editing row that contains the focused button. (The toggle-style variants that stay mounted — SpeciesDetail locations/co-occurrence, BirdingStats counties — are fine and are the model to copy.)",
  "fix": "Either keep the button mounted as a toggle ('Show fewer'), or on activation move focus explicitly — e.g. focus the first newly revealed row, or for KeyRow focus the Edit button that replaces the editor.",
  "dim": "focus",
  "verifierNote": "verifier unavailable",
  "i": 28,
  "verdict": {
   "i": 28,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "ShowAllButton at Checklists.tsx:205 and :718 renders behind {!showAll && ...} and its onClick flips that state, unmounting the focused button; the file contains no useRef or .focus() call, so focus falls to <body> and the next Tab restarts from the page top. The same pattern is real at MediaCommentsSection.tsx:159, SpeciesDetail.tsx:1347, and Settings KeyRow (Cancel :457, post-save setEditing(false) :973). The toggle variants cited as the model do exist in-repo, so the fix direction matches existing conventions.",
   "fixNote": "Prefer the stay-mounted toggle ('Show fewer'), matching the existing in-repo model (SpeciesDetail locations/co-occurrence, BirdingStats counties). If moving focus instead, note the revealed rows are plain divs: give the first newly revealed row container tabIndex={-1} and call .focus() on it after the state flip (e.g. via a ref + effect keyed on showAll); for Settings KeyRow, focus the Edit button that replaces the editor.",
   "alreadyDisclosed": false
  },
  "src": "audit:focus",
  "id": "F036",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "DropZone error state's border and icon are nearly invisible (1.74–1.90:1)",
  "file": "frontend/src/components/DropZone.tsx",
  "line": 40,
  "wcag": "1.4.11 Non-text Contrast AA",
  "description": "In the error state the drop zone's border and upload icon switch to --sr-error-muted (#FCA5A5 light): 1.74:1 against the --sr-error-bg fill and 1.90:1 against the surrounding surface, so the state-change boundary/icon fails non-text contrast (dark: icon vs surface 2.74:1; vs error-bg 3.02:1 passes). The role=alert error text is present, but it itself is the marginal 4.41:1 light error-on-error-bg pair reported separately.",
  "fix": "Use --sr-error (#DC2626) for the error-state border and icon — 4.41:1 vs error-bg and 4.83:1 vs surface, comfortably over the 3:1 non-text bar.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 50,
  "verdict": {
   "i": 50,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "DropZone.tsx lines 39-43 and 108 set the error-state border and icon to --sr-error-muted on --sr-error-bg; my own WCAG luminance math reproduces every reported ratio (light 1.74:1 vs error-bg, 1.90:1 vs white surface; dark 2.74:1 vs surface, 3.02:1 vs error-bg), confirming a 1.4.11 failure in light theme and a marginal dark theme. The role=alert text partially conveys the state, so moderate is the right severity. The suggested var(--sr-error) fix is verified in both themes (4.41/4.83:1 light, 7.07/6.40:1 dark) and follows the --sr-* token convention; note it must be applied at both line 40 (border) and line 108 (icon).",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F037",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "DropZone inline `outline: 'none'` defeats the global keyboard focus ring",
  "file": "frontend/src/components/DropZone.tsx",
  "line": 69,
  "wcag": "2.4.7 Focus Visible AA",
  "description": "The DropZone (role=\"button\", tabIndex=0 — keyboard activation itself works) sets outline: 'none' in its inline style. Inline styles override the stylesheet's [role=\"button\"]:focus-visible { outline: 3px solid var(--sr-accent) } rule (globals.css:546-551, no !important), leaving only the rule's faint 12%-alpha box-shadow glow as the focus indicator; the border/background highlight is driven by mouse-only isHovered state. Keyboard users on the List Comparer get an almost invisible cue on the primary upload control. (Reported here because it directly degrades keyboard operation of an otherwise-correct widget; full focus-visible sweep belongs to that dimension.)",
  "fix": "Delete the inline outline: 'none' (the global :focus-visible ring only shows for keyboard focus, so the mouse aesthetics are unaffected), or mirror the isDragging accent border on :focus-visible.",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 66,
  "verdict": {
   "i": 66,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Line 69's inline outline:'none' overrides globals.css:542-551's [role=\"button\"]:focus-visible / [tabindex]:focus-visible ring (no !important), leaving only the rule's rgba(45,134,83,0.12) box-shadow (~1.05:1 over the light surface, effectively invisible), and the component has no focus-driven styling of its own — isHovered is mouse-only. This directly contradicts ACCESSIBILITY.md's claim that every focused control shows a clear green outline, so it is not a disclosed exception. An effectively absent focus indicator on the List Comparer's two upload controls is a real 2.4.7 AA failure, understated at minor; the suggested fix (delete the inline outline:'none') is correct and safe since :focus-visible does not fire on mouse interaction.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F038",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "Help overlay closes without restoring focus to the control that opened it",
  "file": "frontend/src/components/HelpDocs.tsx",
  "line": 257,
  "wcag": "2.4.3 Focus Order A",
  "description": "HelpDocs correctly focuses its Close button on open and traps Tab (re-querying per keydown — the best trap in the codebase), but on close (Escape at line 257 or the Close button) the overlay unmounts with focus inside it and nothing restores focus: App.tsx:1205 is just `onClose={() => setHelpOpen(false)}`. Focus drops to <body>, so a keyboard user who opened Help from the footer Help button, Settings' 'Open help', or the Welcome screen must Tab from the top of the document to get back to where they were. Standard dialog behavior is to return focus to the invoking element.",
  "fix": "Capture `document.activeElement` when helpOpen becomes true (or pass an opener ref) and `.focus()` it after the overlay unmounts; same for the Welcome-screen-opened case.",
  "dim": "focus",
  "verifierNote": "verifier unavailable",
  "i": 27,
  "verdict": {
   "i": 27,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Verified App.tsx:1205 is bare `onClose={() => setHelpOpen(false)}`, HelpDocs has no unmount cleanup, and no activeElement capture/restore exists anywhere in src/ — focus is inside the unmounting overlay (Close button focused on mount) and drops to <body> on both Escape (line 257) and the Close button. All three openers (footer App.tsx:1113, Settings.tsx:1078, WelcomeScreen.tsx:100) are affected. ACCESSIBILITY.md's Focus Management section only covers the map filter panel (which does restore focus), so not disclosed; moderate is the right severity.",
   "fixNote": "The capture-activeElement-in-HelpDocs approach works because the lazy mount uses `fallback={null}` (focus is still on the opener at first effect time), but the capture effect must be declared BEFORE the existing `closeRef.current?.focus()` effect at line 251, with the restore in that effect's cleanup: `useEffect(() => { const opener = document.activeElement as HTMLElement | null; return () => opener?.focus() }, [])`. This covers Escape, the Close button, and all three openers with no prop plumbing.",
   "alreadyDisclosed": false
  },
  "src": "audit:focus",
  "id": "F039",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "Closing the Help overlay drops focus to <body> instead of returning to the Help button",
  "file": "frontend/src/components/HelpDocs.tsx",
  "line": 318,
  "wcag": "2.4.3 Focus Order A",
  "description": "HelpDocs correctly focuses its Close button on open and traps Tab, but onClose (Close button or Escape) simply unmounts the overlay (App.tsx:1203-1205 just sets helpOpen false) — focus falls to document.body and the keyboard user restarts from the top of the page. The Map Explorer filter sidebar (filtersButtonRef refocus) and the TabNav dropdown (close(true)) both restore focus correctly; HelpDocs is the inconsistent one. It can be opened from three places (footer, Welcome screen, Settings), so the opener should be remembered.",
  "fix": "Capture document.activeElement when the overlay mounts and restore focus to it in a cleanup effect (covers all three openers without prop plumbing).",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 65,
  "verdict": {
   "i": 65,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "This is the same defect as index 27 (exact duplicate, different cited line): App.tsx:1203-1205 just sets helpOpen false, no capture/restore code exists, and focus verifiably falls to <body>; the contrast with the map filter sidebar's correct refocus is accurate. Confirmed, but minor understates it — a keyboard user re-Tabs from the document top after every Help close, and the identical finding at index 27 is rated moderate, so aligned to moderate. Should be merged with index 27 as one finding.",
   "fixNote": "Same fix as index 27: a mount-time effect in HelpDocs declared before the closeRef-focus effect, capturing document.activeElement and refocusing it in the cleanup — covers all three openers without prop plumbing.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F040",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "Map Explorer mobile filter focus trap snapshots focusables once — focus can escape as content changes",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 192,
  "wcag": "2.1.2 No Keyboard Trap A",
  "description": "The mobile filter sidebar trap queries focusable elements once when the overlay opens and captures first/last in the closure (lines 192-196). The sidebar's content is highly dynamic — the Filters accordion expands, the hotspot legend and in-view marker lists appear after a fetch and update on every pan/zoom, Show-all reveals rows. Once elements exist after the stale `last`, Tab from the actual last element is not intercepted and focus walks out of the modal into the backdrop-covered page; elements past the stale `last` are also skipped by the wrap. Escape + focus return (lines 200-203) work correctly. HelpDocs already implements the correct pattern (re-query inside the keydown handler, HelpDocs.tsx:261-263).",
  "fix": "Move the querySelectorAll inside handleKeyDown so first/last reflect the live DOM on every Tab press, matching HelpDocs.tsx.",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 22,
  "verdict": {
   "i": 22,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Verified: lines 192-196 snapshot focusables once with effect deps [sidebarOpen], while the sidebar holds the collapsible Filters accordion, async hotspot/target lists, and viewport-scoped in-view marker lists that change on every pan/zoom — so the captured last goes stale and Tab from the real last element walks out of the non-inert overlay. HelpDocs.tsx 255-275 re-queries inside the keydown handler exactly as the fix says, so the fix matches house convention. One factual error in the description — 'Escape + focus return work correctly' is wrong (the restore is dead code, see finding 4) — but it doesn't affect this finding's core claim; moderate is right (it partially duplicates finding 4's defect #2, and 2.4.3 is arguably the better SC tag than 2.1.2).",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F046",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Placeholder-only (or format-only) accessible names on text inputs; Map Explorer From/To dates are indistinguishable",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 862,
  "wcag": "2.4.6 Headings and Labels AA / 1.3.1 Info and Relationships A",
  "description": "Several inputs have no label or aria-label, so the accessible name falls back to the placeholder. Worst case: the Map Explorer date-range inputs (MapExplorer.tsx:862,864) both have placeholder \"YYYY-MM-DD\" — a SR user hears the format string twice with nothing distinguishing From vs To (the visible \"Date Range\" SidebarLabel is not programmatically associated). Same pattern: MapExplorer.tsx:84 (place search), 689/691 (Latitude/Longitude), 1182 (target species search); Settings.tsx:1216/1228/1240 map-default inputs, whose visible \"Latitude\"/\"Longitude\"/\"Radius (mi)\" labels are unassociated <div>s so the computed name is \"e.g. 37.8275\"; SpeciesDetail.tsx:1268 and MediaCommentsSection.tsx:70 comment filters (placeholder is at least descriptive there, but disappears once text is typed for sighted users). Contrast: Checklists.tsx:655/663 date inputs do it right with aria-label=\"From date\"/\"To date\".",
  "fix": "Add aria-label to each (e.g. \"From date (YYYY-MM-DD)\" / \"To date (YYYY-MM-DD)\", \"Latitude\", \"Longitude\", \"Search by place name\"), or associate the existing visible labels via htmlFor/id.",
  "dim": "sr-content",
  "verifierNote": "verifier unavailable",
  "i": 56,
  "verdict": {
   "i": 56,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Verified every cited input: MapExplorer 862/864 are both placeholder=\"YYYY-MM-DD\" with the 'Date Range' SidebarLabel an unassociated div, so From and To compute identical accessible names; 84, 689/691, 1182, Settings 1216/1228/1240 (visible Latitude/Longitude/Radius labels are plain divs), SpeciesDetail 1268, and MediaCommentsSection 70 all have placeholder-only names. Checklists.tsx 655/663 confirmed as the in-house aria-label=\"From date\"/\"To date\" pattern the fix mirrors. Moderate is right — placeholders provide a partial name fallback, but the indistinguishable date pair is a genuine 2.4.6/1.3.1 failure.",
   "alreadyDisclosed": false
  },
  "src": "audit:sr-content",
  "id": "F048",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Map Explorer async error messages are silent to assistive tech",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 1006,
  "wcag": "4.1.3 Status Messages AA",
  "description": "The hotspot search error (:1006-1011) and media-targets search error (:1227-1232) render as plain styled divs after an async failure — no role=\"alert\" or aria-live, so a screen-reader user who pressed \"Find Hotspots\"/\"Find Recent Sightings\" gets no feedback that the request failed (the role=\"status\" loading chip just disappears). Other error paths in the app do this correctly (ChecklistComparer.tsx:372, App.tsx:720, DropZone.tsx:132).",
  "fix": "Add role=\"alert\" to both error divs (matching ChecklistComparer's pattern).",
  "dim": "aria",
  "verifierNote": "verifier unavailable",
  "i": 36,
  "verdict": {
   "i": 36,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Verified: hotspotsError (1006-1011) and targetsError (1227-1232) render as plain divs with an aria-less AlertCircle icon, no role/aria-live anywhere in MapExplorer per grep, and the role=status loading chip (1437) simply unmounts on failure — so an SR user gets no failure feedback. Cited reference implementations (ChecklistComparer 372, App 720, DropZone 132) all exist, and role=alert on these two divs is the correct house-pattern fix. Note this is a strict subset of finding 11 (same two MapExplorer locations); confirmed independently at moderate, but the two should be deduplicated when applying fixes.",
   "alreadyDisclosed": false
  },
  "src": "audit:aria",
  "id": "F050",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "Placeholder is the only label on search/filter, coordinate, and API-key text inputs",
  "file": "frontend/src/components/MediaCommentsSection.tsx",
  "line": 66,
  "wcag": "3.3.2 Labels or Instructions (also 2.4.6) AA",
  "description": "These text inputs have no label or aria-label; their only identification is the placeholder, which disappears as soon as the user types (and browsers' placeholder→accessible-name fallback is a last resort, not a reliable label). Locations: MediaCommentsSection.tsx:66-70 ('Filter media comments…'); Checklists.tsx:161-165 (shared CommentSearchBox, placeholders 'Filter checklist comments…' / 'Filter species comments…' at :544/:555); SpeciesDetail.tsx:1264-1268 ('Filter comments…'); MapExplorer.tsx:82-84 ('Search by place name'), :689/:691 (Latitude/Longitude number inputs — after entry a sighted user sees two bare numbers with nothing saying which is which), :1182-1184 ('Search species…'); Settings.tsx:424-428 (API key entry, 'Paste your API key' — the row label at :352 is an unassociated <div>). Impact is worst for low-vision and cognitive users mid-entry, and for the lat/lng pair where the only differentiator vanishes.",
  "fix": "Add aria-label to each input (e.g. aria-label=\"Filter media comments\", aria-label=\"Latitude\", aria-label=\"eBird API key\") while keeping the placeholder as a hint; for the Map Explorer lat/lng pair, prefer small visible labels like the Settings map-defaults section already has.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 57,
  "verdict": {
   "i": 57,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "Read every cited input: MediaCommentsSection.tsx:66-79, the shared CommentSearchBox in Checklists.tsx:161-175 (both instantiations), SpeciesDetail.tsx:1264-1277, MapExplorer.tsx:82-89/:689-692/:1182-1185, and Settings.tsx:424-440 — none has an aria-label, aria-labelledby, or associated <label>; the placeholder is the only identification, and the Settings row label is an unassociated <div>. ACCESSIBILITY.md's only disclosed exception is chart-bar contrast, so this is undisclosed. Moderate is correct: placeholder fallback usually yields *some* accessible name, but it vanishes on input (worst for the bare lat/lng number pair, which affects sighted users too) — a fair 3.3.2 AA failure, not serious, not pedantry.",
   "fixNote": "The suggested aria-labels are right and match the file-local convention (Checklists.tsx already puts aria-label on its Protocol/County selects and From/To date inputs; MapExplorer/Settings use aria-label heavily elsewhere). One refinement: CommentSearchBox is a shared component, so add a single aria-label derived from its existing title/placeholder prop (e.g. aria-label={placeholder.replace(/…$/, '')}) — one change covers both Checklists instances. For Settings, derive it from the row's label prop (aria-label={`${label} API key value`} or similar) rather than a hardcoded string, since the component renders multiple key rows.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F051",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "role=\"radio\" groups in Settings have no arrow-key behavior or roving tabindex",
  "file": "frontend/src/components/Settings.tsx",
  "line": 119,
  "wcag": "4.1.2 Name Role Value A",
  "description": "Theme (:119-128), Text size (:519-525), and Date format (:586-591) use role=\"radiogroup\"/role=\"radio\" with aria-checked, but every radio is a tabIndex={0} button and arrow keys do nothing. The radio role contract tells SR users \"radio button, 1 of 3 — use arrow keys\"; pressing arrows is a no-op, and Tab walks through each radio as a separate stop. Operable but misleading — the role advertises an interaction model the widget doesn't implement.",
  "fix": "Either implement the radiogroup pattern (roving tabindex + ArrowLeft/Right/Up/Down + Home/End), or swap to plain buttons with aria-pressed (matching SortSeg) which matches the actual Tab-based interaction.",
  "dim": "aria",
  "verifierNote": "verifier unavailable",
  "i": 37,
  "verdict": {
   "i": 37,
   "verdict": "confirmed",
   "reasoning": "Verified all three groups (Settings.tsx:119, 519, 586): role=radiogroup containing role=radio <button>s, every one tabIndex={0}, no roving tabindex, and no arrow-key handler anywhere in the file (only the key-input Enter handler at 439) — so the announced radio interaction model ('use arrow keys') is a no-op and unchecked radios stay in the Tab order. Everything remains operable via Tab+Enter, so moderate (misleading, not blocking) is the right severity. The fix is validated against the codebase: SortSeg exists (Checklists.tsx:67) and aria-pressed buttons are the established house pattern, so either implementing the APG radiogroup pattern or downgrading to aria-pressed is convention-correct.",
   "alreadyDisclosed": false
  },
  "src": "audit:aria",
  "id": "F053",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "Tab Layout reordering is mouse/touch-only with no keyboard alternative — the statement's keyboard section discloses no exceptions",
  "file": "frontend/src/components/Settings.tsx",
  "line": 721,
  "wcag": "2.1.1 Keyboard A (also WCAG 2.2 2.5.7 Dragging Movements AA)",
  "description": "Settings → Tab Layout reorders tabs exclusively via HTML5 drag-and-drop on the <li> rows; the 'drag handle' is a non-focusable plain <div> carrying an aria-label (which is also invalid ARIA on a generic div — it won't be announced), and there is no keyboard mechanism (no move-up/move-down buttons, no arrow-key handling). The app itself admits this in an sr-only note at Settings.tsx:699 ('Keyboard reordering is not supported in this version'), and ACCESSIBILITY.md's 'Nearly all of SnowRaven is reachable and operable with the keyboard alone' hedge technically covers it — but the Keyboard Navigation section enumerates zero exceptions, so a reader has no way to learn this feature is keyboard-inaccessible. Show/hide toggles are keyboard-accessible; ordering is not achievable at all without a pointer, which is a hard 2.1.1 failure for that function (and a 2.5.7 Dragging Movements failure under WCAG 2.2).",
  "fix": "Add keyboard reordering (focusable handle button with ArrowUp/ArrowDown + aria-live position announcements, or per-row Move up/Move down buttons), and until then name this exception explicitly in ACCESSIBILITY.md's keyboard section.",
  "dim": "claims",
  "verifierNote": "verifier unavailable",
  "i": 64,
  "verdict": {
   "i": 64,
   "verdict": "confirmed",
   "reasoning": "Both halves check out: (a) reordering is pointer-only — DnD on the <li>, unfocusable handle div with an invalid aria-label, no keyboard mechanism in the file — and (b) ACCESSIBILITY.md's Keyboard Navigation section enumerates only the map-marker caveat and never names this exception, so the 'Nearly all' hedge gives readers no way to learn ordering is keyboard-inaccessible. The sr-only in-app note (line 699) is disclosure to SR users but not in the published statement, so alreadyDisclosed stays false. Moderate is fair for the claims-dimension framing (the underlying 2.1.1 failure is index 3's serious finding); the fix — keyboard reorder, or naming the exception in ACCESSIBILITY.md until then — is correct.",
   "alreadyDisclosed": false
  },
  "src": "audit:claims",
  "id": "F054",
  "primaryHere": true
 },
 {
  "title": "Primary navigation and header have no landmark exposure; HelpDocs TOC nav is unlabeled",
  "file": "frontend/src/components/TabNav.tsx",
  "line": 105,
  "severity": "moderate",
  "wcag": "1.3.1 Info and Relationships (A)",
  "description": "Three related landmark-structure gaps (merged): (1) Desktop tab bar: <nav aria-label=\"Main navigation\" role=\"tablist\"> (TabNav.tsx:105-108) — the explicit role=\"tablist\" overrides <nav>'s implicit navigation role, so no navigation landmark exists anywhere on the page; screen-reader landmark navigation (rotor/landmark list) shows no way to find the primary navigation. (2) Collapsed mode: TabDropdown renders in a plain <div> (TabNav.tsx:228) with no nav landmark at all, so narrow/mobile users also get no navigation landmark. (3) The app header with branding and tagline is a plain styled div (App.tsx:570), not <header>/banner, leaving it outside any landmark. By contrast <main> (App.tsx:586) and the footer role=\"contentinfo\" (App.tsx:1100) are correct. Additionally the HelpDocs TOC <nav> (HelpDocs.tsx:347) has no accessible name, so it announces as an anonymous navigation region inside the documentation dialog.",
  "evidence": "TabNav.tsx:105-108 `<nav aria-label=\"Main navigation\" ... role=\"tablist\">`; TabNav.tsx:228 dropdown wrapper is `<div style={{ padding: '12px 16px', position: 'relative' }} ref={wrapRef}>`; App.tsx:570 `<div className=\"sr-header\" ...>`; HelpDocs.tsx:347 `<nav style={{...}}>` with no aria-label.",
  "suggestedFix": "Wrap the tablist in a labeled nav instead of putting role=\"tablist\" on the nav itself: <nav aria-label=\"Main\"><div role=\"tablist\">…</div></nav>; wrap TabDropdown's container in the same <nav aria-label=\"Main\">; change the App header div to <header> (banner); add aria-label=\"Documentation contents\" to the HelpDocs TOC nav.",
  "dim": "structure",
  "j": 1,
  "verdict": {
   "i": 1,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "alreadyDisclosed": false,
   "reasoning": "All four citations verified: role=\"tablist\" on the <nav> (TabNav.tsx:105-108) overrides its implicit navigation role, so desktop mode exposes no navigation landmark; collapsed mode's wrapper (TabNav.tsx:228) is a bare div with none either; the branding header (App.tsx:570) is a plain div outside any landmark; HelpDocs.tsx:347 is an unlabeled <nav>. Screen-reader landmark navigation therefore cannot locate the primary navigation in either mode — a genuine 1.3.1-adjacent structural gap, distinct from confirmed #33 (which is about dangling tabpanel aria-labelledby, not landmark exposure). Moderate is fair given main and contentinfo exist but the primary nav is unfindable by landmark.",
   "fixNote": "Fix is correct (nav wrapping a div[role=tablist] is the canonical pattern). Use the same accessible name (e.g. aria-label=\"Main navigation\") on both the desktop and collapsed-mode <nav> wrappers so the landmark announces identically across modes; keep the sr-header className when converting the header div to <header>."
  },
  "src": "audit:structure",
  "id": "F060",
  "primaryHere": false
 },
 {
  "severity": "moderate",
  "title": "WelcomeScreen modal has no focus trap; map fullscreen overlay leaves hidden chrome tabbable",
  "file": "frontend/src/components/WelcomeScreen.tsx",
  "line": 21,
  "wcag": "2.4.3 Focus Order A",
  "description": "WelcomeScreen declares role=\"dialog\" aria-modal=\"true\" and handles only Escape (line 21) — there is no Tab containment and the app behind it is not inert, so tabbing past \"Explore the app first\" moves focus into the fully obscured app underneath (the overlay background is opaque var(--sr-bg)); a sighted keyboard user loses the focus indicator entirely while aria-modal simultaneously tells screen readers the background doesn't exist. The map fullscreen mode has the same background-tabbing issue: it is a CSS overlay (panel fixed inset 0, App.tsx:1031-1042) and only body scroll is locked (App.tsx:390-395), so the tab bar and footer remain in the tab order while visually covered. (Not a 2.1.2 trap — focus can always leave — but focus lands on invisible controls.)",
  "fix": "Give WelcomeScreen the same Tab-wrap handler HelpDocs uses (HelpDocs.tsx:256-274). For map fullscreen, set inert (or aria-hidden + tabindex management) on the sibling app chrome while active, and consider Escape-to-exit-fullscreen as polish.",
  "dim": "keyboard",
  "verifierNote": "verifier unavailable",
  "i": 21,
  "verdict": {
   "i": 21,
   "verdict": "confirmed",
   "correctedSeverity": "moderate",
   "reasoning": "WelcomeScreen.tsx:19-24 handles only Escape with no Tab containment while declaring aria-modal=\"true\", and App.tsx:1195-1201 mounts it inside the full app tree, so the obscured tab bar/footer remain tabbable behind the opaque var(--sr-bg) overlay. The map fullscreen claim also holds: App.tsx:1031-1033 is a fixed inset-0 z-1200 CSS overlay and App.tsx:389-395 locks only body scroll — no inert/aria-hidden/focus-trap exists anywhere in frontend/src. ACCESSIBILITY.md's Focus Management section discloses only the Map Explorer filter-panel trap, not this gap; moderate is the right severity (focus lands on invisible controls but is recoverable, not a 2.1.2 trap).",
   "fixNote": "The suggested fix is correct and convention-consistent — HelpDocs.tsx:258-274 has exactly the referenced Tab-wrap handler. Two refinements: WelcomeScreen should query focusables from a ref on its own root div (it has no DOM id, unlike HelpDocs' getElementById('sr-help-overlay')), and for map fullscreen the `inert` attribute is safe in both desktop targets (WKWebView/Safari 15.5+, WebView2) — apply it to the sibling tabpanels, TabNav, and footer while mapFullscreen && activeTab === 'map-explorer', clearing it in the same effect cleanup as the existing body scroll lock at App.tsx:390-395. Update ACCESSIBILITY.md's Focus Management section in the same change per CLAUDE.md.",
   "alreadyDisclosed": false
  },
  "src": "audit:keyboard",
  "id": "F065",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "Light theme: error text on error background is just below AA (4.41:1)",
  "file": "frontend/src/components/Checklists.tsx",
  "line": 488,
  "wcag": "1.4.3 Contrast (Minimum) AA",
  "description": "--sr-error #DC2626 on --sr-error-bg #FEF2F2 computes to 4.41:1 — marginally under 4.5:1 — and this pair styles every error banner and the 'negative' filter pill state: Checklists.tsx:236/488, LifeList.tsx:108/384, BreedingCodeList.tsx:196, Settings.tsx:304/480, ChecklistComparer.tsx:374, NamedBirds.tsx:108, WeatherTidePanel.tsx:186, SpeciesDetail.tsx:430, DropZone.tsx error text. Dark theme passes (7.07:1). Error on plain surface also passes (4.83:1), so only the tinted-banner pair needs a nudge.",
  "fix": "Darken light --sr-error slightly (e.g. #D31F1F ≈ 4.8:1 on #FEF2F2) or introduce an --sr-error-text token used on --sr-error-bg; one token edit in globals.css fixes all listed locations.",
  "dim": "contrast",
  "verifierNote": "verifier unavailable",
  "i": 44,
  "verdict": {
   "i": 44,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified the pair co-occurs (Checklists.tsx:236 TriPill 'no' state and :488 error banner, plus the other listed files) and re-computed the ratio: #DC2626 on #FEF2F2 = 4.415:1, a genuine but marginal AA miss at 13px text; dark theme (7.07:1) and plain-surface (4.83:1) pass as the finding states. The 0.085 shortfall is perceptually negligible and light-theme-only, so 'moderate' overstates user impact — this is a compliance nudge, not a barrier; downgrading to minor. The fix is validated (#D31F1F = 4.82:1 on #FEF2F2, 5.27:1 on white) and convention-compliant as a single light-theme token edit in globals.css.",
   "alreadyDisclosed": false
  },
  "src": "audit:contrast",
  "id": "F077",
  "primaryHere": false
 },
 {
  "title": "External links open new windows without any indication (inconsistent across the app)",
  "file": "frontend/src/components/HelpDocs.tsx",
  "line": 61,
  "severity": "minor",
  "wcag": "3.2.5 / 2.4.4 advisory (G201)",
  "description": "43 target=\"_blank\" anchors exist across ~20 component files; only two indicate the behavior — App.tsx:748 and App.tsx:898 use \"(opens in new tab)\" in aria-label. All others give no warning, including every link in the Help documentation (HelpDocs.tsx:61 renders all HELP.md links with target=\"_blank\" rel=\"noreferrer\" and no indication), the footer GitHub link (App.tsx:1101-1110), SpeciesLinks.tsx:20/45 (whose aria-labels \"View X on eBird\" omit it), LifeListTable.tsx media-count links, BirdingStats.tsx checklist/region links (15 instances), Checklists.tsx, MapExplorer.tsx, SpeciesDetail.tsx, NamedBirdRow.tsx, Settings.tsx, WeatherTidePanel.tsx, MediaCommentsSection.tsx, MediaStatsSections.tsx, ChecklistComparer.tsx, SightingsMap.tsx, map/HotspotMarkers.tsx, map/TargetMarkers.tsx, AtlasLayer.tsx, speciesDetail/ui.tsx, CommentText.tsx. Unexpected context changes disorient screen-reader and cognitive-disability users; the app already has the right pattern in two places — it just isn't applied consistently.",
  "evidence": "HelpDocs.tsx:61 `<a key={ki++} href={lm[2]} target=\"_blank\" rel=\"noreferrer\"` with no new-window indication; grep shows 43 target=\"_blank\" instances, only App.tsx:748 and App.tsx:898 carry \"(opens in new tab)\".",
  "suggestedFix": "Adopt one shared ExternalA component (or extend SpeciesLinks' pattern) that appends \"(opens in new tab)\" to the accessible name (visually-hidden span or aria-label suffix) and use it for all target=\"_blank\" anchors, including HelpDocs' renderInline link branch.",
  "dim": "structure",
  "j": 4,
  "verdict": {
   "i": 4,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "alreadyDisclosed": false,
   "reasoning": "Verified by grep: exactly 43 target=\"_blank\" anchors across 20 component files, with only App.tsx:748 and App.tsx:898 announcing '(opens in new tab)'; HelpDocs.tsx renders every documentation link target=\"_blank\" with no indication and SpeciesLinks.tsx aria-labels omit it. This is the new-window-indication aspect, explicitly distinct from confirmed #53 (which covers the ↗ links' accessible names), so not a duplicate. Correctly scoped as advisory (G201; 3.2.5 is AAA), so minor severity is right; not disclosed in ACCESSIBILITY.md.",
   "fixNote": "A shared external-link component is sound; prefer a visually-hidden span suffix over aria-label so existing link text is preserved, and where an aria-label already exists (SpeciesLinks, icon-only links) append to the label, matching the wording already used at App.tsx:748/898. For icon-only favicon links keep title in sync with the label."
  },
  "src": "audit:structure",
  "id": "F078",
  "primaryHere": true
 },
 {
  "severity": "minor",
  "title": "WCAG 2.2 forward-looking: several form controls are under the 24px minimum target size",
  "file": "frontend/src/components/MapExplorer.tsx",
  "line": 726,
  "wcag": "2.5.8 Target Size (Minimum) — WCAG 2.2 AA",
  "description": "Forward-looking 2.2 delta only. The atlas overlay role=\"switch\" buttons are 38x22px (MapExplorer.tsx:726, :759, :788 — 22px tall, under the 24px minimum), and several zero-padding text buttons are ~16-18px tall: the API-key Show/Hide button (Settings.tsx:363-373, padding 0, 0.75rem text), the 'Clear filter' underline buttons (LifeList.tsx:694-703, Checklists.tsx:685-690, SpeciesDetail.tsx:658-667), and the 'Go to Settings →' banner buttons (App.tsx:611-619, padding 0). Most currently escape 2.5.8 via the spacing exception (no adjacent target within 24px), but that is fragile under layout changes and the small targets are already hard for motor-impaired users under 2.1 best practice.",
  "fix": "Bump the switch buttons to at least 24px tall (e.g. 44x24 track) and give the zero-padding text buttons enough padding (or min-height: 24px with negative-margin compensation) to reach a 24x24 hit area.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 85,
  "verdict": {
   "i": 85,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "All cited controls verified: the three hand-rolled role=switch buttons are 38x22 (MapExplorer.tsx:726, :759, :788 — 22px tall, under 24), and the padding-0 text buttons exist as described in Settings.tsx:363-373 (Show/Hide), LifeList.tsx:694-703 / Checklists.tsx:685-690 / SpeciesDetail.tsx:658-667 (Clear filter), and App.tsx:611-619 (Go to Settings), all at 0.75rem with no padding (~17px hit height). The finding honestly frames itself as a forward-looking WCAG 2.2 delta and acknowledges the spacing exception currently covers most of them, so minor is the right severity.",
   "fixNote": "The codebase already has a shared ui/ToggleSwitch.tsx (a 30px-tall role=switch button with the label inside) — prefer migrating the three inline atlas switches to it, or if the label-left/switch-right layout must stay, bump the bare track to >=24px tall (e.g. 44x24). For the zero-padding text buttons, padding (or min-height 24 with negative-margin compensation) as originally suggested is fine.",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F086",
  "primaryHere": false
 },
 {
  "severity": "minor",
  "title": "Settings radiogroups use role=radio without the expected arrow-key behavior",
  "file": "frontend/src/components/Settings.tsx",
  "line": 519,
  "wcag": "4.1.2 Name, Role, Value (ARIA radiogroup pattern) AA",
  "description": "The Color theme (:119), Text size (:519), and Date format (:586) groups are role=\"radiogroup\" containing role=\"radio\" buttons that are all tabIndex={0}. Screen readers announce them as radio buttons (e.g. \"radio, 1 of 4\") and instruct arrow-key navigation, but arrow keys do nothing and unchecked radios remain in the Tab order — behavior that contradicts the announced role. Users can still operate everything with Tab+Enter, so impact is confusion rather than blockage.",
  "fix": "Either implement the radiogroup pattern (roving tabindex + ArrowLeft/Right to move checked state) or downgrade to the house pattern used elsewhere: plain buttons with aria-pressed inside role=\"group\" aria-label.",
  "dim": "forms",
  "verifierNote": "verifier unavailable",
  "i": 83,
  "verdict": {
   "i": 83,
   "verdict": "confirmed",
   "reasoning": "Same verified facts as index 37: three role=radiogroup/role=radio groups (119, 519, 586) with all-tabIndex={0} buttons, no arrow-key handling, no roving tabindex, so SR-announced radio semantics contradict actual behavior while Tab+Enter still works. The finding correctly scopes impact to confusion rather than blockage, so minor is appropriate (note 4.1.2 is Level A, not AA as labeled — immaterial). The suggested downgrade to the house aria-pressed pattern is validated (SortSeg at Checklists.tsx:67, aria-pressed used across BreedingCodeList and others).",
   "alreadyDisclosed": false
  },
  "src": "audit:forms",
  "id": "F092",
  "primaryHere": true
 },
 {
  "severity": "serious",
  "title": "axe aria-prohibited-attr: aria-label on non-interactive drag-handle <div>s in Tab Layout settings",
  "file": "frontend/src/components/Settings.tsx",
  "line": 722,
  "wcag": "4.1.2 Name, Role, Value A",
  "description": "axe-core rule `aria-prohibited-attr` (impact: serious), 9 instances (one per tab row) on the Settings tab. aria-label is prohibited on an element with an implicit generic role, so the \"Drag to reorder X tab\" name is ignored or inconsistently exposed by AT — and since the handle is a plain div, it is not focusable or operable by keyboard anyway (the sr-only caveat note at Settings.tsx:699 acknowledges mouse-only reorder).",
  "fix": "Short term: drop the aria-label (or change the div to aria-hidden=\"true\") so the prohibited attribute is gone and the handle is honestly decorative. Proper fix: make the handle a <button> with that aria-label plus keyboard reorder (ArrowUp/ArrowDown move the row), which would also retire the mouse-only caveat.",
  "dim": "axe",
  "verifierNote": "verifier unavailable",
  "i": 17,
  "verdict": {
   "i": 17,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified: aria-label sits on a plain non-focusable div with implicit generic role (Settings.tsx:721-723), 9 instances (DEFAULT_TAB_ORDER has 9 tabs), and ARIA 1.2 prohibits naming generic elements — a real axe aria-prohibited-attr hit. But the handle is not interactive or in the tab order, so the ignored/inconsistent name costs AT users nothing functional on its own; worst case is a stray confusing announcement in browse mode, and the actual operability failure is separately reported (findings 3/64). Downgrading from axe's mechanical 'serious' to minor; the suggested fix (aria-hidden short-term, real <button> with keyboard reorder long-term) is correct and consistent with the codebase's aria-label-on-button pattern.",
   "alreadyDisclosed": false
  },
  "src": "audit:axe",
  "id": "F093",
  "primaryHere": true
 },
 {
  "severity": "moderate",
  "title": "Inline `outline: 'none'` on ~20 inputs/selects suppresses the designed focus ring app-wide",
  "file": "frontend/src/lib/mapExplorerFormat.ts",
  "line": 27,
  "wcag": "2.4.7 Focus Visible AA (1.4.11 degradation)",
  "description": "One repeated pattern: form controls styled with inline `outline: 'none'`, which overrides the global input ring (globals.css:556-563, `outline: 2px solid var(--sr-accent)`). The only surviving indicator is the rule's `border-color: var(--sr-accent) !important` (stylesheet !important still beats inline) plus a 12%-alpha shadow — a 1.5px gray→green border tint instead of the designed 2px ring, easy to miss in dense filter sidebars, and weaker than ACCESSIBILITY.md's published 'border-hugging ring on inputs'. Locations: lib/mapExplorerFormat.ts:27 (SELECT_STYLE shared by all Map Explorer selects); MapExplorer.tsx:88,690,692,863,865,1184; LifeList.tsx:627,653,667; BreedingCodeList.tsx:388,419,435; SpeciesDetail.tsx:518,613,638,652,1273; Checklists.tsx:170,265; MediaCommentsSection.tsx:75; Settings.tsx:435,1219,1231,1243. Technically still a visible state change (so not a hard 2.4.7 fail), but a significant app-wide degradation versus the global design.",
  "fix": "Remove every inline `outline: 'none'` from inputs/selects — the global stylesheet already suppresses the default ring on non-focus-visible states and supplies the 2px accent ring on focus. The per-element onFocus borderColor handlers (e.g. Settings.tsx:437) become redundant and can stay or go.",
  "dim": "focus",
  "verifierNote": "verifier unavailable",
  "i": 24,
  "verdict": {
   "i": 24,
   "verdict": "confirmed",
   "correctedSeverity": "minor",
   "reasoning": "Verified all claims: 26 inline outline:'none' occurrences match the listed locations, inline style beats the non-!important outline in globals.css:556-563, and only the !important border-color plus the stylesheet box-shadow survive — the designed 2px ring never renders on these controls. However the surviving indicator (1.5px border flipping gray→accent at 4.52:1 light / 9.22:1 dark vs surface, plus a 4px glow, with explicit onFocus borderColor handlers in Settings) is a perceivable, conventional focus treatment, so 2.4.7 AA is not actually failed — the finding itself concedes this. Real as an app-wide design-consistency issue that also makes ACCESSIBILITY.md's published 'border-hugging ring on inputs' claim inaccurate, but a non-failing degradation grades as minor, not moderate.",
   "alreadyDisclosed": false
  },
  "src": "audit:focus",
  "id": "F105",
  "primaryHere": false
 }
]