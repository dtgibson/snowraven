{
  "activeFeature": null,
  "sessionNumber": 47,
  "sessionType": null,
  "currentStage": null,
  "lastCompletedStage": 6,
  "lastCheckpointStatus": "approved",
  "awaitingHuman": null,
  "confirmedArtifacts": [],
  "releasedVersion": "0.5.43",
  "lastFeature": "0.5.43 map-center-pin BUILT + VERIFIED + RECORDED + COMMITTED on the VM (session 47). FINAL DEPLOY (in progress): VM pushes main + tag v0.5.43 (triggers Windows CI), then the Mac runs `zsh -lc ./release.sh` to build/notarize macOS + fetch/sign the Windows installer + publish the GitHub release and latest.json. Improve run (re-scoped from the New-Feature boundary): a draggable center pin on the Map Explorer Hotspots/Nearby Lifers/Media Targets views -- right-click (desktop) or long-press (touch) drops the center and re-runs the active view's search; drag to fine-tune; session-only (does NOT touch saved map-defaults). New CenterPinDropper + CenterPin in components/map/MapControls.tsx (contextmenu + a hand-rolled long-press timer cancelled on pan/zoom/slop/2nd-touch/touchend, deduped against a synthesized post-long-press contextmenu via lastTouchFire); applyCenter wiring in MapExplorer.tsx; gestures distinct from left-click so no collision with existing pin/popup selection; the center pin is a DOM Marker superseding the detected-location dot via !centerPinShown. CI mirror green (lint+typecheck+978 tests+build, 8 new CenterPinDropper tests); adversarial regression + security reviews clean (2 minor findings fixed: website copy + the contextmenu dedup). The 0.5.42 load-opt is preserved (entry chunk unchanged ~218 KB). Version bumped (package.json + tauri.conf.json + package-lock 0.5.43); CHANGELOG/HELP/website updated. Chronicler: DECISIONS/PRODUCT_CONTEXT/ROADMAP (shipped 78) + a new CLAUDE.md rule (the contextmenu/long-press map-gesture pattern). RECONCILED onto the Mac's 0.5.42-live closeout (e5700f3) before stacking 0.5.43. Mac must confirm HEAD==tag commit + most-recent successful windows-build.yml headSha==tag commit (tag-re-push guard) before release.sh. Prior: 0.5.42 tab-order-and-load-optimization RELEASED (live, both platforms).",
  "remainingBacklog": "PENDING RELEASE: 0.5.43 is committed + pushed from the VM with tag v0.5.43 (Windows CI triggered); the Mac must run `zsh -lc ./release.sh` to build/notarize macOS, fetch+sign the Windows installer, and publish the GitHub release + latest.json. STANDING RELEASE NOTE: always run the release via `zsh -lc ./release.sh` (Apple signing creds are exported only in the Mac LOGIN profile; a bare ./release.sh fails preflight with APPLE_SIGNING_IDENTITY is not set). STANDING tag-re-push guard before release.sh after any tag re-push: confirm the most-recent successful windows-build.yml run headSha == tag commit (gh run list --workflow windows-build.yml --status success --limit 1 --json databaseId,headSha). Known minor deferred: computeLocationsSorted (speciesStats.ts) groups by location NAME keeping first-seen locId -- Set-gated, never a 404; align to locId (like computeGeo) when next touched. ROADMAP Up Next: mobile app (responsive groundwork done); Windows code signing. NOT-FOR-RELEASE: origin has an unmerged docs/snoa-3-accuracy-fixes branch -- left untouched.",
  "createdAt": "2026-06-18T04:57:59Z",
  "updatedAt": "2026-06-18T06:17:36Z"
}
